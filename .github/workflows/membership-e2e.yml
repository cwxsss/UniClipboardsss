name: 'Membership E2E'
run-name: "membership-e2e-${{ inputs.tier || (github.event_name == 'pull_request' && 'pr') || 'nightly' }}-${{ github.run_number }}"

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - 'crates/**'
      - 'apps/**'
      - 'tests/e2e/**'
      - 'vendor/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'
      - '.github/docker/build-bookworm.Dockerfile'
      - '.github/workflows/membership-e2e.yml'
      - 'scripts/e2e/run-membership-matrix.sh'
      - 'scripts/__tests__/membership-e2e-hardening.test.ts'
  schedule:
    - cron: '23 18 * * *'
  workflow_dispatch:
    inputs:
      tier:
        description: '测试层级'
        required: true
        type: choice
        options:
          - nightly
          - pr
        default: nightly
      ref:
        description: '要测试的分支、标签或提交'
        required: false
        type: string
        default: ''
permissions:
  contents: read
  packages: read

jobs:
  setup-matrix:
    name: Select membership platforms
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Select Linux for pull requests and all platforms otherwise
        id: set-matrix
        env:
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            echo 'matrix=[{"name":"Linux","runner":"ubuntu-22.04","container":"ghcr.io/uniclipboard/build-bookworm:latest"}]' >> "$GITHUB_OUTPUT"
          else
            echo 'matrix=[{"name":"macOS","runner":"macos-latest"},{"name":"Linux","runner":"ubuntu-22.04","container":"ghcr.io/uniclipboard/build-bookworm:latest"},{"name":"Windows","runner":"windows-latest"}]' >> "$GITHUB_OUTPUT"
          fi

  membership:
    needs: setup-matrix
    name: "${{ matrix.name }} / ${{ inputs.tier || (github.event_name == 'pull_request' && 'pr') || 'nightly' }}"
    runs-on: ${{ matrix.runner }}
    container: ${{ matrix.container || null }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup-matrix.outputs.matrix) }}

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || github.ref }}
          submodules: recursive

      - name: Install pinned Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.95.0

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: '. -> target'
          shared-key: membership-e2e-${{ runner.os }}
          save-if: ${{ github.event_name == 'schedule' }}

      - name: Build daemon and CLI
        run: cargo build -p uc-daemon -p uc-cli --features uc-daemon/e2e-rendezvous

      - name: Verify fixed release preparation
        run: cargo test --manifest-path tests/e2e/Cargo.toml --test release_assets

      - name: Verify local invitation service contract
        run: cargo test --manifest-path tests/e2e/Cargo.toml --test rendezvous_contract

      - name: Prepare fixed legacy release
        shell: bash
        run: >-
          cargo run --manifest-path tests/e2e/Cargo.toml
          --bin prepare-legacy-release --
          --output "$RUNNER_TEMP/legacy-release"

      - name: Run membership matrix
        shell: bash
        env:
          UC_E2E_ARTIFACT_DIR: ${{ runner.temp }}/membership-e2e
          UC_E2E_KEEP_PROFILES: '1'
          UC_E2E_LEGACY_RELEASE_DIR: ${{ runner.temp }}/legacy-release
          UC_E2E_RUST_LOG: info
          MEMBERSHIP_TIER: ${{ inputs.tier || (github.event_name == 'pull_request' && 'pr') || 'nightly' }}
        run: bash scripts/e2e/run-membership-matrix.sh "$MEMBERSHIP_TIER"

      - name: Upload matrix results and isolated profiles
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: membership-e2e-${{ matrix.name }}-${{ inputs.tier || (github.event_name == 'pull_request' && 'pr') || 'nightly' }}
          if-no-files-found: warn
          retention-days: 14
          path: |
            ${{ runner.temp }}/membership-e2e/**
            ~/Library/Application Support/app.uniclipboard.desktop-e2e-membership-*/**
            ~/Library/Logs/app.uniclipboard.desktop-e2e-membership-*/**
            ~/.local/share/app.uniclipboard.desktop-e2e-membership-*/**
            ~/.local/state/app.uniclipboard.desktop-e2e-membership-*/**
            ~/AppData/Local/app.uniclipboard.desktop-e2e-membership-*/**
