// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AcknowledgeUpgradeData, AcknowledgeUpgradeErrors, AcknowledgeUpgradeResponses, AuthConnectData, AuthConnectErrors, AuthConnectResponses, CancelClipboardTransferData, CancelClipboardTransferErrors, CancelClipboardTransferResponses, CaptureUiEventData, CaptureUiEventResponses, ClearClipboardHistoryData, ClearClipboardHistoryErrors, ClearClipboardHistoryResponses, ClearStorageCacheData, ClearStorageCacheErrors, ClearStorageCacheResponses, DeleteClipboardEntryData, DeleteClipboardEntryErrors, DeleteClipboardEntryResponses, DispatchClipboardTextData, DispatchClipboardTextErrors, DispatchClipboardTextResponses, ExportConfigData, ExportConfigErrors, ExportConfigResponses, ExportLogsData, ExportLogsErrors, ExportLogsResponses, FactoryResetSpaceData, FactoryResetSpaceErrors, FactoryResetSpaceResponses, GetClipboardBlobData, GetClipboardBlobErrors, GetClipboardBlobResponses, GetClipboardEntryData, GetClipboardEntryDeliveryData, GetClipboardEntryDeliveryErrors, GetClipboardEntryDeliveryResponses, GetClipboardEntryErrors, GetClipboardEntryFileData, GetClipboardEntryFileErrors, GetClipboardEntryFileResponses, GetClipboardEntryResourceData, GetClipboardEntryResourceErrors, GetClipboardEntryResourceResponses, GetClipboardEntryResponses, GetClipboardStatsData, GetClipboardStatsErrors, GetClipboardStatsResponses, GetClipboardThumbnailData, GetClipboardThumbnailErrors, GetClipboardThumbnailResponses, GetDebugStatusData, GetDebugStatusErrors, GetDebugStatusResponses, GetEncryptionStateData, GetEncryptionStateErrors, GetEncryptionStateResponses, GetHealthData, GetHealthResponses, GetLifecycleStatusData, GetLifecycleStatusErrors, GetLifecycleStatusResponses, GetLocalDeviceInfoData, GetLocalDeviceInfoErrors, GetLocalDeviceInfoResponses, GetMemberSyncPreferencesData, GetMemberSyncPreferencesErrors, GetMemberSyncPreferencesResponses, GetMobileSyncSettingsData, GetMobileSyncSettingsErrors, GetMobileSyncSettingsResponses, GetSearchStatusData, GetSearchStatusErrors, GetSearchStatusResponses, GetSettingsData, GetSettingsErrors, GetSettingsResponses, GetStatusData, GetStatusResponses, GetStorageStatsData, GetStorageStatsErrors, GetStorageStatsResponses, GetUpgradeStatusData, GetUpgradeStatusErrors, GetUpgradeStatusResponses, ImportConfigData, ImportConfigErrors, ImportConfigResponses, ListClipboardEntriesData, ListClipboardEntriesErrors, ListClipboardEntriesResponses, ListMobileDevicesData, ListMobileDevicesErrors, ListMobileDevicesResponses, ListMobileLanInterfacesData, ListMobileLanInterfacesErrors, ListMobileLanInterfacesResponses, ListPairedDevicesData, ListPairedDevicesErrors, ListPairedDevicesResponses, ListPeersData, ListPeersErrors, ListPeersResponses, LockEncryptionSessionData, LockEncryptionSessionErrors, LockEncryptionSessionResponses, PreviewConfigImportData, PreviewConfigImportErrors, PreviewConfigImportResponses, ProbeRelayUrlData, ProbeRelayUrlErrors, ProbeRelayUrlResponses, RebuildSearchIndexData, RebuildSearchIndexErrors, RebuildSearchIndexResponses, RefreshPresenceData, RefreshPresenceErrors, RefreshPresenceResponses, RegisterMobileDeviceData, RegisterMobileDeviceErrors, RegisterMobileDeviceResponses, RequestLifecycleRestartData, RequestLifecycleRestartErrors, RequestLifecycleRestartResponses, ResendClipboardEntryData, ResendClipboardEntryErrors, ResendClipboardEntryResponses, RestoreClipboardEntryData, RestoreClipboardEntryErrors, RestoreClipboardEntryResponses, RetryLifecycleData, RetryLifecycleErrors, RetryLifecycleResponses, RevokeMobileDeviceData, RevokeMobileDeviceErrors, RevokeMobileDeviceResponses, RotateMobilePasswordData, RotateMobilePasswordErrors, RotateMobilePasswordResponses, SearchQueryData, SearchQueryErrors, SearchQueryResponses, SetupV2CancelData, SetupV2CancelErrors, SetupV2CancelResponses, SetupV2GetMigrationProgressData, SetupV2GetMigrationProgressErrors, SetupV2GetMigrationProgressResponses, SetupV2GetStateData, SetupV2GetStateErrors, SetupV2GetStateResponses, SetupV2InitializeData, SetupV2InitializeErrors, SetupV2InitializeResponses, SetupV2IssueInvitationData, SetupV2IssueInvitationErrors, SetupV2IssueInvitationResponses, SetupV2RedeemData, SetupV2RedeemErrors, SetupV2RedeemResponses, SetupV2ResetData, SetupV2ResetErrors, SetupV2ResetResponses, SetupV2SwitchSpaceData, SetupV2SwitchSpaceErrors, SetupV2SwitchSpaceResponses, SignalLifecycleReadyData, SignalLifecycleReadyResponses, ToggleClipboardEntryFavoriteData, ToggleClipboardEntryFavoriteErrors, ToggleClipboardEntryFavoriteResponses, UnlockEncryptionSessionData, UnlockEncryptionSessionErrors, UnlockEncryptionSessionResponses, UnlockSpaceWithPassphraseData, UnlockSpaceWithPassphraseErrors, UnlockSpaceWithPassphraseResponses, UnpairDeviceData, UnpairDeviceErrors, UnpairDeviceResponses, UpdateDebugModeData, UpdateDebugModeErrors, UpdateDebugModeResponses, UpdateMemberSyncPreferencesData, UpdateMemberSyncPreferencesErrors, UpdateMemberSyncPreferencesResponses, UpdateMobileDeviceData, UpdateMobileDeviceErrors, UpdateMobileDeviceResponses, UpdateMobileSyncSettingsData, UpdateMobileSyncSettingsErrors, UpdateMobileSyncSettingsResponses, UpdateSettingsData, UpdateSettingsErrors, UpdateSettingsResponses, VerifyKeychainAccessData, VerifyKeychainAccessErrors, VerifyKeychainAccessResponses, WebsocketUpgradeData, WebsocketUpgradeErrors } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * POST /analytics/capture
 *
 * Decode a GUI UI-interaction event and hand it to the daemon's analytics sink.
 * Fire-and-forget: `accepted: true` only confirms the event was decoded and
 * dispatched, not that it reached PostHog.
 */
export const captureUiEvent = <ThrowOnError extends boolean = false>(options: Options<CaptureUiEventData, ThrowOnError>) => (options.client ?? client).post<CaptureUiEventResponses, unknown, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/analytics/capture',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /auth/connect.
 *
 * Validates the bearer token in the Authorization header, registers the client
 * PID, and returns a JWT session token wrapped in the canonical envelope.
 *
 * Rate limiting: This endpoint has no session token yet, so rate limiting
 * is applied by client IP address (from ConnectInfo). This is trustworthy
 * because it comes from the TCP stack, not caller-controlled input.
 *
 * NOTE on ConnectInfo: ConnectInfo<SocketAddr> reads the socket address from
 * the TCP connection metadata, NOT from HTTP headers. In test contexts (using
 * tower::ServiceExt::oneshot without a real TCP listener), the socket address
 * will be a default value (typically 127.0.0.1:0 or ::1:0). The unit tests
 * for SlidingWindowRateLimiter cover the rate limiting logic independently.
 * IP-based rate limiting for /auth/connect works correctly in production.
 *
 * IMPORTANT: ConnectInfo<SocketAddr> works ONLY when the server uses
 * `into_make_service_with_connect_info::<SocketAddr>()`.
 * In test contexts using tower::ServiceExt::oneshot, ConnectInfo may be absent.
 * The handler uses Option<ConnectInfo<SocketAddr>> so tests work correctly.
 * IP-based rate limiting is skipped when ConnectInfo is unavailable (test-only code path).
 */
export const authConnect = <ThrowOnError extends boolean = false>(options: Options<AuthConnectData, ThrowOnError>) => (options.client ?? client).post<AuthConnectResponses, AuthConnectErrors, ThrowOnError>({
    url: '/auth/connect',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /clipboard/blobs/:blob_id
 *
 * Serves the raw bytes of a stored blob. Binary endpoint: the response is
 * `application/octet-stream` (the resolved MIME type when known), NOT the
 * `{ data, ts }` JSON envelope (ADR-008 §0.2 keeps binary endpoints exempt).
 * Returns 404 if the blob is unknown, 500 on an internal resolution failure.
 */
export const getClipboardBlob = <ThrowOnError extends boolean = false>(options: Options<GetClipboardBlobData, ThrowOnError>) => (options.client ?? client).get<GetClipboardBlobResponses, GetClipboardBlobErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/blobs/{blob_id}',
    ...options
});

/**
 * POST /clipboard/cancel-transfer/:transfer_id
 *
 * Cancels an in-flight inbound file transfer. Returns the cancellation outcome.
 */
export const cancelClipboardTransfer = <ThrowOnError extends boolean = false>(options: Options<CancelClipboardTransferData, ThrowOnError>) => (options.client ?? client).post<CancelClipboardTransferResponses, CancelClipboardTransferErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/cancel-transfer/{transfer_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /clipboard/dispatch
 *
 * Wraps plaintext into a single `text/plain` snapshot and fans it out to
 * online peers. Returns the per-target delivery outcome.
 */
export const dispatchClipboardText = <ThrowOnError extends boolean = false>(options: Options<DispatchClipboardTextData, ThrowOnError>) => (options.client ?? client).post<DispatchClipboardTextResponses, DispatchClipboardTextErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/dispatch',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /clipboard/entries?limit=50&offset=0
 *
 * Lists clipboard entries with pagination. Returns camelCase entry projections.
 * Populates `linkDomains` from `linkUrls`. Limit is clamped to 1000.
 */
export const listClipboardEntries = <ThrowOnError extends boolean = false>(options?: Options<ListClipboardEntriesData, ThrowOnError>) => (options?.client ?? client).get<ListClipboardEntriesResponses, ListClipboardEntriesErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/entries',
    ...options
});

/**
 * POST /clipboard/entries/clear
 *
 * Clears all clipboard history via bulk deletion.
 * Returns the number of entries deleted and any failures.
 */
export const clearClipboardHistory = <ThrowOnError extends boolean = false>(options?: Options<ClearClipboardHistoryData, ThrowOnError>) => (options?.client ?? client).post<ClearClipboardHistoryResponses, ClearClipboardHistoryErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/entries/clear',
    ...options
});

/**
 * DELETE /clipboard/entries/:id
 *
 * Deletes an entry. Returns 204 on success, 404 if not found.
 */
export const deleteClipboardEntry = <ThrowOnError extends boolean = false>(options: Options<DeleteClipboardEntryData, ThrowOnError>) => (options.client ?? client).delete<DeleteClipboardEntryResponses, DeleteClipboardEntryErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/entries/{id}',
    ...options
});

/**
 * GET /clipboard/entries/:id
 *
 * Returns entry detail (full text content). Returns 404 if not found,
 * 422 if entry is not text content.
 */
export const getClipboardEntry = <ThrowOnError extends boolean = false>(options: Options<GetClipboardEntryData, ThrowOnError>) => (options.client ?? client).get<GetClipboardEntryResponses, GetClipboardEntryErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/entries/{id}',
    ...options
});

/**
 * GET /clipboard/entries/:id/delivery
 *
 * Returns the entry's origin + per-trusted-peer delivery status for the detail
 * panel (ADR-008 P3-1 / D15; formerly the GUI-only
 * `clipboard_entry_delivery_view` Tauri command). Entry-not-found is a normal
 * degraded-render case for the frontend, so it maps to a plain 404.
 */
export const getClipboardEntryDelivery = <ThrowOnError extends boolean = false>(options: Options<GetClipboardEntryDeliveryData, ThrowOnError>) => (options.client ?? client).get<GetClipboardEntryDeliveryResponses, GetClipboardEntryDeliveryErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/entries/{id}/delivery',
    ...options
});

/**
 * POST /clipboard/entries/:id/favorite
 *
 * Toggles favorite state for an entry. Returns 200 on success, 404 if not found.
 */
export const toggleClipboardEntryFavorite = <ThrowOnError extends boolean = false>(options: Options<ToggleClipboardEntryFavoriteData, ThrowOnError>) => (options.client ?? client).post<ToggleClipboardEntryFavoriteResponses, ToggleClipboardEntryFavoriteErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/entries/{id}/favorite',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /clipboard/entries/:id/file
 *
 * Serves the bytes of an entry's first materialized free-file (ADR-008
 * P5-1b). The daemon materializes inbound free-files into a controlled cache
 * and rewrites the entry's file-list representation; this endpoint reads that
 * cached file and streams it back. Binary endpoint: the response is
 * `application/octet-stream` (the representation MIME when known), NOT the
 * `{ data, ts }` JSON envelope (ADR-008 §0.2 keeps binary endpoints exempt).
 * A `Content-Disposition: attachment` header carries the cached filename so
 * CLI `recv` can name the local copy. Returns 404 when the entry is unknown
 * or carries no materialized free-file, 500 on a read failure.
 */
export const getClipboardEntryFile = <ThrowOnError extends boolean = false>(options: Options<GetClipboardEntryFileData, ThrowOnError>) => (options.client ?? client).get<GetClipboardEntryFileResponses, GetClipboardEntryFileErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/entries/{id}/file',
    ...options
});

/**
 * GET /clipboard/entries/:id/resource
 *
 * Returns resource metadata (blob URL or inline content).
 */
export const getClipboardEntryResource = <ThrowOnError extends boolean = false>(options: Options<GetClipboardEntryResourceData, ThrowOnError>) => (options.client ?? client).get<GetClipboardEntryResourceResponses, GetClipboardEntryResourceErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/entries/{id}/resource',
    ...options
});

/**
 * POST /clipboard/resend
 *
 * Re-dispatches a previously captured entry to (optionally filtered) peers.
 */
export const resendClipboardEntry = <ThrowOnError extends boolean = false>(options: Options<ResendClipboardEntryData, ThrowOnError>) => (options.client ?? client).post<ResendClipboardEntryResponses, ResendClipboardEntryErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/resend',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /clipboard/restore/{entry_id}
 *
 * Re-apply a stored clipboard entry to the local system clipboard. Wrapped in
 * the canonical `{ data, ts }` envelope (ADR-008 §0.1/§0.2); errors use the
 * canonical `ApiErrorResponse`. The `payload_unavailable` (410) error carries
 * `{ entry_id, rep_id, state }` in `details` (§0.3); the `code`/`message`
 * strings are LOAD-BEARING and preserved.
 */
export const restoreClipboardEntry = <ThrowOnError extends boolean = false>(options: Options<RestoreClipboardEntryData, ThrowOnError>) => (options.client ?? client).post<RestoreClipboardEntryResponses, RestoreClipboardEntryErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/restore/{entry_id}',
    ...options
});

/**
 * GET /clipboard/stats
 *
 * Returns aggregate clipboard statistics (total items and total size).
 */
export const getClipboardStats = <ThrowOnError extends boolean = false>(options?: Options<GetClipboardStatsData, ThrowOnError>) => (options?.client ?? client).get<GetClipboardStatsResponses, GetClipboardStatsErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/stats',
    ...options
});

/**
 * GET /clipboard/thumbnails/:rep_id
 *
 * Serves the raw bytes of a representation's thumbnail. Binary endpoint: the
 * response is `application/octet-stream` (the resolved MIME type when known),
 * NOT the `{ data, ts }` JSON envelope (ADR-008 §0.2 keeps binary endpoints
 * exempt). Returns 404 if the thumbnail is unknown, 500 on an internal
 * resolution failure.
 */
export const getClipboardThumbnail = <ThrowOnError extends boolean = false>(options: Options<GetClipboardThumbnailData, ThrowOnError>) => (options.client ?? client).get<GetClipboardThumbnailResponses, GetClipboardThumbnailErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/clipboard/thumbnails/{rep_id}',
    ...options
});

/**
 * POST /config/export
 *
 * Pack the current installation into an encrypted `.ucbundle` written to
 * `targetPath`, sealed with the installation's own key material (no export
 * password; opening it later requires the space passphrase). The facade
 * enforces the preconditions (initialized + unlocked) before any material is
 * read. D14: session-JWT gated; the handler MUST NOT log the request body (no
 * path on any span here).
 */
export const exportConfig = <ThrowOnError extends boolean = false>(options: Options<ExportConfigData, ThrowOnError>) => (options.client ?? client).post<ExportConfigResponses, ExportConfigErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/config/export',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /config/import
 *
 * Validate a bundle and stage it for the next restart to apply on boot.
 * Applying on the next boot replaces whatever configuration the target
 * currently holds — there is no uninitialized precondition. `confirmed` must be
 * `true` (the import is a device-identity move that overwrites in place); a
 * missing/invalid body or `confirmed != true` is a 400. D14: session-JWT
 * gated; the handler MUST NOT log the request body.
 */
export const importConfig = <ThrowOnError extends boolean = false>(options: Options<ImportConfigData, ThrowOnError>) => (options.client ?? client).post<ImportConfigResponses, ImportConfigErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/config/import',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /config/import/preview
 *
 * Decrypt a bundle's manifest and return its non-secret descriptive metadata
 * so the UI can confirm before staging. Read-only and ungated. D14:
 * session-JWT gated; the handler MUST NOT log the request body.
 */
export const previewConfigImport = <ThrowOnError extends boolean = false>(options: Options<PreviewConfigImportData, ThrowOnError>) => (options.client ?? client).post<PreviewConfigImportResponses, PreviewConfigImportErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/config/import/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /device/me
 *
 * Returns the local device's peer ID and resolved device name.
 */
export const getLocalDeviceInfo = <ThrowOnError extends boolean = false>(options?: Options<GetLocalDeviceInfoData, ThrowOnError>) => (options?.client ?? client).get<GetLocalDeviceInfoResponses, GetLocalDeviceInfoErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/device/me',
    ...options
});

export const getDebugStatus = <ThrowOnError extends boolean = false>(options?: Options<GetDebugStatusData, ThrowOnError>) => (options?.client ?? client).get<GetDebugStatusResponses, GetDebugStatusErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/diagnostics/debug',
    ...options
});

export const updateDebugMode = <ThrowOnError extends boolean = false>(options: Options<UpdateDebugModeData, ThrowOnError>) => (options.client ?? client).put<UpdateDebugModeResponses, UpdateDebugModeErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/diagnostics/debug',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const exportLogs = <ThrowOnError extends boolean = false>(options: Options<ExportLogsData, ThrowOnError>) => (options.client ?? client).post<ExportLogsResponses, ExportLogsErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/diagnostics/log-export',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /encryption/factory-reset
 *
 * Wipes key material + clears setup status + cancels pending invitations
 * (ADR-008 P3-1 / D15). Routes through `SpaceSetupFacade::factory_reset`,
 * mirroring the former in-process `factory_reset_space` Tauri command.
 */
export const factoryResetSpace = <ThrowOnError extends boolean = false>(options?: Options<FactoryResetSpaceData, ThrowOnError>) => (options?.client ?? client).post<FactoryResetSpaceResponses, FactoryResetSpaceErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/encryption/factory-reset',
    ...options
});

/**
 * GET /encryption/keychain-access
 *
 * Verifies macOS Keychain "Always Allow" permission for this app.
 * Returns `granted: true` if Keychain access succeeds silently, `false` if permission denied.
 */
export const verifyKeychainAccess = <ThrowOnError extends boolean = false>(options?: Options<VerifyKeychainAccessData, ThrowOnError>) => (options?.client ?? client).get<VerifyKeychainAccessResponses, VerifyKeychainAccessErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/encryption/keychain-access',
    ...options
});

/**
 * POST /encryption/lock
 *
 * Locks the encryption session by clearing the master key.
 */
export const lockEncryptionSession = <ThrowOnError extends boolean = false>(options?: Options<LockEncryptionSessionData, ThrowOnError>) => (options?.client ?? client).post<LockEncryptionSessionResponses, LockEncryptionSessionErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/encryption/lock',
    ...options
});

/**
 * GET /encryption/state
 *
 * Returns the current encryption state and session readiness.
 */
export const getEncryptionState = <ThrowOnError extends boolean = false>(options?: Options<GetEncryptionStateData, ThrowOnError>) => (options?.client ?? client).get<GetEncryptionStateResponses, GetEncryptionStateErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/encryption/state',
    ...options
});

/**
 * POST /encryption/unlock
 *
 * Attempts to auto-unlock the encryption session using keyring-stored KEK.
 * No passphrase is required — credentials are retrieved from the OS keychain.
 * On success, broadcasts the `encryption.session_ready` WebSocket event.
 */
export const unlockEncryptionSession = <ThrowOnError extends boolean = false>(options?: Options<UnlockEncryptionSessionData, ThrowOnError>) => (options?.client ?? client).post<UnlockEncryptionSessionResponses, UnlockEncryptionSessionErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/encryption/unlock',
    ...options
});

/**
 * POST /encryption/unlock-with-passphrase
 *
 * Unlocks the space with a user-supplied plaintext passphrase (ADR-008 D15).
 *
 * Routes through `SpaceSetupFacade::unlock_space`, which (unlike the bare
 * `encryption.unlock`) also runs the switch-space migration recovery hook —
 * the same reason the keyring `unlock_handler` above delegates to
 * `try_resume_session`. On success it broadcasts `encryption.session_ready`
 * so WS subscribers react identically regardless of which unlock path ran.
 *
 * D14: this endpoint is session-JWT gated (it is NOT in `PUBLIC_PATHS`) and
 * the handler MUST NOT log the request body — there is intentionally no
 * `?req` / passphrase field on any span or tracing event here.
 */
export const unlockSpaceWithPassphrase = <ThrowOnError extends boolean = false>(options: Options<UnlockSpaceWithPassphraseData, ThrowOnError>) => (options.client ?? client).post<UnlockSpaceWithPassphraseResponses, UnlockSpaceWithPassphraseErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/encryption/unlock-with-passphrase',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /health
 *
 * Public (L1) liveness probe. Returns the daemon status string plus version
 * metadata wrapped in the canonical `{ data, ts }` envelope (ADR-008 §0.2).
 * The previous bare `{ status, ... }` shape is retired.
 */
export const getHealth = <ThrowOnError extends boolean = false>(options?: Options<GetHealthData, ThrowOnError>) => (options?.client ?? client).get<GetHealthResponses, unknown, ThrowOnError>({ url: '/health', ...options });

/**
 * 通知 daemon：已解锁，可以开始采集剪贴板——打开 clipboard capture 门控。
 *
 * 锁定期 daemon 把剪贴板采集门控住(deferred services);解锁后用本端点
 * 放行。ADR-008 P3-3 起 daemon 永远是独立进程,GUI 作为纯客户端经 loopback
 * HTTP 调它(旧 `GuiInProcess` 同进程模式已删除)。
 */
export const signalLifecycleReady = <ThrowOnError extends boolean = false>(options?: Options<SignalLifecycleReadyData, ThrowOnError>) => (options?.client ?? client).post<SignalLifecycleReadyResponses, unknown, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/lifecycle/ready',
    ...options
});

/**
 * POST /lifecycle/restart — request a controlled restart/promotion of a
 *
 * transient (Oneshot) daemon (ADR-008 P5-L).
 *
 * REFUSES unless this daemon is an Oneshot residency AND the single-instance
 * lock is enabled AND the target is not itself Oneshot. The accepted path raises
 * the L8b `quiescing` flag (via the coordinator) so admission gates drain
 * in-flight work; the Oneshot supervisor then self-terminates and `app.rs`
 * persists the handover record. Production-neutral: no Oneshot daemon exists
 * until L8d, so the accept path is unreachable in production.
 */
export const requestLifecycleRestart = <ThrowOnError extends boolean = false>(options: Options<RequestLifecycleRestartData, ThrowOnError>) => (options.client ?? client).post<RequestLifecycleRestartResponses, RequestLifecycleRestartErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/lifecycle/restart',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /lifecycle/retry
 *
 * Slice4 P5c: libp2p `start_network` 已退役,iroh 路由由
 * `SpaceSetupAssembly` 启动时即装好,no longer 需要 retry 出动 network。
 * 这个 endpoint 现在只做 lifecycle 状态推进 + 触发 deferred 服务启动,
 * 等价于 GUI 端 `/lifecycle/ready` 的 idempotent 重试入口。
 */
export const retryLifecycle = <ThrowOnError extends boolean = false>(options?: Options<RetryLifecycleData, ThrowOnError>) => (options?.client ?? client).post<RetryLifecycleResponses, RetryLifecycleErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/lifecycle/retry',
    ...options
});

/**
 * GET /lifecycle/status
 *
 * Returns the current daemon lifecycle state wrapped in the canonical
 * `{ data, ts }` envelope (ADR-008). The bare `{ state }` shape is retired.
 */
export const getLifecycleStatus = <ThrowOnError extends boolean = false>(options?: Options<GetLifecycleStatusData, ThrowOnError>) => (options?.client ?? client).get<GetLifecycleStatusResponses, GetLifecycleStatusErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/lifecycle/status',
    ...options
});

/**
 * GET /member/:device_id/sync-preferences
 *
 * 返回已接纳成员的同步偏好（双向 send/receive + 双套 content_types）。
 */
export const getMemberSyncPreferences = <ThrowOnError extends boolean = false>(options: Options<GetMemberSyncPreferencesData, ThrowOnError>) => (options.client ?? client).get<GetMemberSyncPreferencesResponses, GetMemberSyncPreferencesErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/member/{device_id}/sync-preferences',
    ...options
});

/**
 * PATCH /member/:device_id/sync-preferences
 *
 * 部分更新成员的同步偏好；未提供的字段保留当前值。
 *
 * 内部 `get → merge → save`，保持与 `UpdateMemberSettingsUseCase` 的全量覆盖语义对齐。
 */
export const updateMemberSyncPreferences = <ThrowOnError extends boolean = false>(options: Options<UpdateMemberSyncPreferencesData, ThrowOnError>) => (options.client ?? client).patch<UpdateMemberSyncPreferencesResponses, UpdateMemberSyncPreferencesErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/member/{device_id}/sync-preferences',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /mobile-sync/devices
 */
export const listMobileDevices = <ThrowOnError extends boolean = false>(options?: Options<ListMobileDevicesData, ThrowOnError>) => (options?.client ?? client).get<ListMobileDevicesResponses, ListMobileDevicesErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/mobile-sync/devices',
    ...options
});

/**
 * POST /mobile-sync/devices
 */
export const registerMobileDevice = <ThrowOnError extends boolean = false>(options: Options<RegisterMobileDeviceData, ThrowOnError>) => (options.client ?? client).post<RegisterMobileDeviceResponses, RegisterMobileDeviceErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/mobile-sync/devices',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * DELETE /mobile-sync/devices/{device_id}
 */
export const revokeMobileDevice = <ThrowOnError extends boolean = false>(options: Options<RevokeMobileDeviceData, ThrowOnError>) => (options.client ?? client).delete<RevokeMobileDeviceResponses, RevokeMobileDeviceErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/mobile-sync/devices/{device_id}',
    ...options
});

/**
 * PATCH /mobile-sync/devices/{device_id}
 */
export const updateMobileDevice = <ThrowOnError extends boolean = false>(options: Options<UpdateMobileDeviceData, ThrowOnError>) => (options.client ?? client).patch<UpdateMobileDeviceResponses, UpdateMobileDeviceErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/mobile-sync/devices/{device_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /mobile-sync/devices/{device_id}/rotate-password
 */
export const rotateMobilePassword = <ThrowOnError extends boolean = false>(options: Options<RotateMobilePasswordData, ThrowOnError>) => (options.client ?? client).post<RotateMobilePasswordResponses, RotateMobilePasswordErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/mobile-sync/devices/{device_id}/rotate-password',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /mobile-sync/lan-interfaces
 */
export const listMobileLanInterfaces = <ThrowOnError extends boolean = false>(options?: Options<ListMobileLanInterfacesData, ThrowOnError>) => (options?.client ?? client).get<ListMobileLanInterfacesResponses, ListMobileLanInterfacesErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/mobile-sync/lan-interfaces',
    ...options
});

/**
 * GET /mobile-sync/settings
 */
export const getMobileSyncSettings = <ThrowOnError extends boolean = false>(options?: Options<GetMobileSyncSettingsData, ThrowOnError>) => (options?.client ?? client).get<GetMobileSyncSettingsResponses, GetMobileSyncSettingsErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/mobile-sync/settings',
    ...options
});

/**
 * PATCH /mobile-sync/settings
 */
export const updateMobileSyncSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateMobileSyncSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateMobileSyncSettingsResponses, UpdateMobileSyncSettingsErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/mobile-sync/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /paired-devices
 *
 * List paired space members with presence. Wrapped in the canonical
 * `{ data, ts }` envelope (ADR-008 §0.2); the previous bare top-level array
 * is retired.
 */
export const listPairedDevices = <ThrowOnError extends boolean = false>(options?: Options<ListPairedDevicesData, ThrowOnError>) => (options?.client ?? client).get<ListPairedDevicesResponses, ListPairedDevicesErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/paired-devices',
    ...options
});

/**
 * POST /pairing/unpair
 *
 * Revokes the local member record for the given peer. Success is signalled by
 * `204 No Content` with no body (ADR-008 §B Rule 3 — 204 endpoints are NOT
 * enveloped). Errors flow through the shared `ApiError` carrier and therefore
 * serialize to `ApiErrorResponse { code, message, details? }` on the wire —
 * the dedicated `PairingApiErrorResponse` contract only covers the retired
 * libp2p pairing routes, not this revoke path.
 */
export const unpairDevice = <ThrowOnError extends boolean = false>(options: Options<UnpairDeviceData, ThrowOnError>) => (options.client ?? client).post<UnpairDeviceResponses, UnpairDeviceErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/pairing/unpair',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /peers
 *
 * List discovered peer snapshots (topology view). Wrapped in the canonical
 * `{ data, ts }` envelope (ADR-008 §0.2); the previous bare top-level array
 * is retired.
 */
export const listPeers = <ThrowOnError extends boolean = false>(options?: Options<ListPeersData, ThrowOnError>) => (options?.client ?? client).get<ListPeersResponses, ListPeersErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/peers',
    ...options
});

/**
 * POST /presence/refresh
 *
 * Actively probe paired peers' reachability and return the round's counters.
 * Wrapped in the canonical `{ data, ts }` envelope (ADR-008 §0.2); the
 * previous bare object (counters at top level) is retired.
 */
export const refreshPresence = <ThrowOnError extends boolean = false>(options?: Options<RefreshPresenceData, ThrowOnError>) => (options?.client ?? client).post<RefreshPresenceResponses, RefreshPresenceErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/presence/refresh',
    ...options
});

/**
 * GET /search/query
 *
 * Execute a structured search query against the local encrypted search index.
 * Returns HTTP 423 if the encryption session is locked.
 *
 * ADR-008 wire change: `total`/`hasMore` are no longer top-level siblings of
 * the envelope — they are folded INTO the `data` payload alongside the renamed
 * `items` array (`SearchQueryResultDto`). The response is the canonical
 * `ApiEnvelope<SearchQueryResultDto>` (`{ data: { items, total, hasMore }, ts }`).
 */
export const searchQuery = <ThrowOnError extends boolean = false>(options: Options<SearchQueryData, ThrowOnError>) => (options.client ?? client).get<SearchQueryResponses, SearchQueryErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/search/query',
    ...options
});

/**
 * POST /search/rebuild
 *
 * Trigger a manual full rebuild of the search index.
 * Returns HTTP 202 on accept, HTTP 409 with `rebuild_already_running` when another rebuild is in progress.
 * Returns HTTP 423 if the encryption session is locked.
 *
 * Already on `{ data, ts }`; the bespoke wrapper is replaced by the canonical
 * `ApiEnvelope<SearchRebuildAcceptedData>` (identical JSON, not a wire change).
 */
export const rebuildSearchIndex = <ThrowOnError extends boolean = false>(options?: Options<RebuildSearchIndexData, ThrowOnError>) => (options?.client ?? client).post<RebuildSearchIndexResponses, RebuildSearchIndexErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/search/rebuild',
    ...options
});

/**
 * GET /search/status
 *
 * Returns the current search index availability snapshot (coordinator status + index meta timestamps).
 * Returns HTTP 423 if the encryption session is locked.
 *
 * Already on `{ data, ts }`; the bespoke wrapper is replaced by the canonical
 * `ApiEnvelope<SearchStatusData>` (identical JSON, not a wire change).
 */
export const getSearchStatus = <ThrowOnError extends boolean = false>(options?: Options<GetSearchStatusData, ThrowOnError>) => (options?.client ?? client).get<GetSearchStatusResponses, GetSearchStatusErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/search/status',
    ...options
});

/**
 * GET /settings
 *
 * Returns the current application settings as a typed Settings struct.
 */
export const getSettings = <ThrowOnError extends boolean = false>(options?: Options<GetSettingsData, ThrowOnError>) => (options?.client ?? client).get<GetSettingsResponses, GetSettingsErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/settings',
    ...options
});

/**
 * PUT /settings
 *
 * Updates application settings. Accepts a partial settings object and merges it
 * with the existing settings.
 *
 * NOTE: Unlike the Tauri command, this handler does NOT apply OS-level side
 * effects (no autostart registration, no keyboard shortcut updates). It only
 * persists the settings domain model.
 */
export const updateSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateSettingsData, ThrowOnError>) => (options.client ?? client).put<UpdateSettingsResponses, UpdateSettingsErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /settings/relay-probe
 *
 * Probes a candidate relay URL for reachability. Reads/writes no persisted
 * settings, so it is safe to call repeatedly ("test before save"). A probe
 * that fails to reach the relay is a NORMAL categorized outcome returned 200
 * (mirrors the Tauri command contract) — only a missing relay-diagnostic
 * adapter (server misconfiguration) becomes a 500 `ApiError`.
 */
export const probeRelayUrl = <ThrowOnError extends boolean = false>(options: Options<ProbeRelayUrlData, ThrowOnError>) => (options.client ?? client).post<ProbeRelayUrlResponses, ProbeRelayUrlErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/settings/relay-probe',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /status
 *
 * Diagnostic snapshot: version metadata, uptime, and worker health. Wrapped
 * in the canonical `{ data, ts }` envelope (ADR-008 §0.2); the previous bare
 * object shape is retired.
 */
export const getStatus = <ThrowOnError extends boolean = false>(options?: Options<GetStatusData, ThrowOnError>) => (options?.client ?? client).get<GetStatusResponses, unknown, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/status',
    ...options
});

/**
 * POST /storage/clear-cache
 *
 * Clears the cache directory contents. Requires `confirmed: true` in the request body.
 * Returns 400 if confirmation is missing or false.
 */
export const clearStorageCache = <ThrowOnError extends boolean = false>(options: Options<ClearStorageCacheData, ThrowOnError>) => (options.client ?? client).post<ClearStorageCacheResponses, ClearStorageCacheErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/storage/clear-cache',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /storage/stats
 *
 * Returns storage statistics across database, cache, and spool directories.
 * Includes blob_count derived from the total number of clipboard entries.
 */
export const getStorageStats = <ThrowOnError extends boolean = false>(options?: Options<GetStorageStatsData, ThrowOnError>) => (options?.client ?? client).get<GetStorageStatsResponses, GetStorageStatsErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/storage/stats',
    ...options
});

/**
 * POST /upgrade/ack
 *
 * Advance the stored version cursor to the running build, clearing the
 * "re-pair after upgrade" notice.
 */
export const acknowledgeUpgrade = <ThrowOnError extends boolean = false>(options?: Options<AcknowledgeUpgradeData, ThrowOnError>) => (options?.client ?? client).post<AcknowledgeUpgradeResponses, AcknowledgeUpgradeErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/upgrade/ack',
    ...options
});

/**
 * GET /upgrade/status
 *
 * Detect whether the running build is a fresh install / unchanged / upgraded
 * / downgraded relative to the stored version cursor.
 */
export const getUpgradeStatus = <ThrowOnError extends boolean = false>(options?: Options<GetUpgradeStatusData, ThrowOnError>) => (options?.client ?? client).get<GetUpgradeStatusResponses, GetUpgradeStatusErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/upgrade/status',
    ...options
});

export const setupV2Cancel = <ThrowOnError extends boolean = false>(options?: Options<SetupV2CancelData, ThrowOnError>) => (options?.client ?? client).post<SetupV2CancelResponses, SetupV2CancelErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/v2/setup/cancel',
    ...options
});

export const setupV2Initialize = <ThrowOnError extends boolean = false>(options: Options<SetupV2InitializeData, ThrowOnError>) => (options.client ?? client).post<SetupV2InitializeResponses, SetupV2InitializeErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/v2/setup/initialize',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const setupV2IssueInvitation = <ThrowOnError extends boolean = false>(options?: Options<SetupV2IssueInvitationData, ThrowOnError>) => (options?.client ?? client).post<SetupV2IssueInvitationResponses, SetupV2IssueInvitationErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/v2/setup/issue-invitation',
    ...options
});

export const setupV2GetMigrationProgress = <ThrowOnError extends boolean = false>(options?: Options<SetupV2GetMigrationProgressData, ThrowOnError>) => (options?.client ?? client).get<SetupV2GetMigrationProgressResponses, SetupV2GetMigrationProgressErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/v2/setup/migration-progress',
    ...options
});

export const setupV2Redeem = <ThrowOnError extends boolean = false>(options: Options<SetupV2RedeemData, ThrowOnError>) => (options.client ?? client).post<SetupV2RedeemResponses, SetupV2RedeemErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/v2/setup/redeem',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const setupV2Reset = <ThrowOnError extends boolean = false>(options?: Options<SetupV2ResetData, ThrowOnError>) => (options?.client ?? client).post<SetupV2ResetResponses, SetupV2ResetErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/v2/setup/reset',
    ...options
});

export const setupV2GetState = <ThrowOnError extends boolean = false>(options?: Options<SetupV2GetStateData, ThrowOnError>) => (options?.client ?? client).get<SetupV2GetStateResponses, SetupV2GetStateErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/v2/setup/state',
    ...options
});

export const setupV2SwitchSpace = <ThrowOnError extends boolean = false>(options: Options<SetupV2SwitchSpaceData, ThrowOnError>) => (options.client ?? client).post<SetupV2SwitchSpaceResponses, SetupV2SwitchSpaceErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/v2/setup/switch-space',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Returns the WebSocket router.
 *
 * `GET /ws` is a protocol upgrade, NOT a `{ data, ts }` envelope endpoint. On
 * success it returns `101 Switching Protocols` and the connection then speaks
 * the daemon WebSocket protocol: the client sends `WsSubscribeRequest` frames
 * and the server streams `DaemonWsEvent` frames (one per subscribed topic
 * event). These frame schemas are documented here for reference only; the live
 * stream is not modelled by the OpenAPI response body.
 */
export const websocketUpgrade = <ThrowOnError extends boolean = false>(options: Options<WebsocketUpgradeData, ThrowOnError>) => (options.client ?? client).get<unknown, WebsocketUpgradeErrors, ThrowOnError>({
    security: [{
            in: 'query',
            name: 'auth',
            type: 'apiKey'
        }, { name: 'Authorization', type: 'apiKey' }],
    url: '/ws',
    ...options
});
