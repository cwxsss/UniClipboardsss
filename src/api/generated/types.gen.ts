// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: `${string}://${string}` | (string & {});
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type AckUpgradeEnvelope = {
    data: AckUpgradePayload;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Payload for `POST /upgrade/ack`, wrapped by `AckUpgradeEnvelope`
 * (`ApiEnvelope<AckUpgradePayload>`).
 */
export type AckUpgradePayload = {
    acknowledged: string;
};

/**
 * Canonical daemon HTTP error body.
 *
 * Wire shape: `{ "code": "<machine_code>", "message": "<human text>" }` plus an
 * optional `details` object. `code` is a stable snake_case token (e.g.
 * `not_found`, `bad_request`, `runtime_unavailable`, `conflict`,
 * `internal_error`, `payload_unavailable`). `message` is human-readable
 * English; setup-v2 error classifiers and the clipboard restore-410 handler
 * substring-match it, so the strings are LOAD-BEARING — do not silently reword.
 *
 * `Deserialize` is added (the original webserver struct was Serialize-only) so
 * the Rust `uc-daemon-client` and tests can decode error bodies.
 */
export type ApiErrorResponse = {
    code: string;
    /**
     * Optional structured context (per §0.3). E.g. the restore-410
     * `payload_unavailable` error carries `{ entry_id, rep_id, state }`.
     * Omitted from the wire when `None`.
     */
    details?: unknown;
    message: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type CancelTransferEnvelope = {
    data: CancelTransferResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /clipboard/cancel-transfer/:transfer_id`.
 */
export type CancelTransferRequest = {
    /**
     * Cancellation reason: `"local_user"` | `"timeout"` etc.
     */
    reason: string;
};

/**
 * Response body for `POST /clipboard/cancel-transfer/:transfer_id`.
 */
export type CancelTransferResponse = {
    /**
     * `"cancelled"` | `"not_inflight"`.
     */
    outcome: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type CaptureUiEventEnvelope = {
    data: CaptureUiEventResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Tagged union of GUI-originated UI analytics events (discriminated by `kind`).
 *
 * Wire-compatible with the retired `capture_update_ui_event` Tauri command's
 * `UpdateUiEvent` (same discriminators / field names), except `DialogOpened`
 * now carries `install_kind` explicitly (see [`UiInstallKind`]).
 */
export type CaptureUiEventRequest = {
    install_kind: UiInstallKind;
    kind: 'dialog_opened';
    phase: UiUpdatePhase;
    source: UiDialogOpenSource;
} | {
    kind: 'dismissed';
    phase: UiUpdatePhase;
    source: UiDismissSource;
} | {
    action: UiUpdateAction;
    error_kind?: string | null;
    kind: 'action_invoked';
    outcome: UiUpdateActionOutcome;
} | {
    failure_kind?: UiUpdateFailureKind | null;
    install_kind: UiInstallKind;
    kind: 'check_performed';
    outcome: UiUpdateCheckOutcome;
    source: UiUpdateCheckSource;
} | {
    delivery_status: UiNotificationDeliveryStatus;
    install_kind: UiInstallKind;
    kind: 'notification_shown';
    version: string;
};

/**
 * Response for `POST /analytics/capture`. `capture` is fire-and-forget, so
 * `accepted` only confirms the daemon decoded the event and handed it to the
 * sink — not that it reached PostHog.
 */
export type CaptureUiEventResponse = {
    accepted: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type ClearCacheEnvelope = {
    data: ClearCacheResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request payload for `POST /storage/clear-cache`.
 */
export type ClearCacheRequest = {
    confirmed: boolean;
};

/**
 * Response payload for `POST /storage/clear-cache` on success.
 */
export type ClearCacheResponse = {
    freedBytes: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type ClearHistoryEnvelope = {
    data: ClearHistoryResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Result of clearing clipboard history.
 * Matches the frontend `ClearHistoryResult` interface.
 */
export type ClearHistoryResultDto = {
    deletedCount: number;
    failedEntries: Array<Array<string>>;
};

/**
 * Aggregate clipboard statistics.
 * Matches the frontend `ClipboardStats` interface.
 */
export type ClipboardStatsDto = {
    totalItems: number;
    totalSize: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type ClipboardStatsEnvelope = {
    data: ClipboardStatsDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /auth/connect`.
 */
export type ConnectRequest = {
    /**
     * Client type: `"gui"`, `"cli"`, or `"other"`.
     */
    clientType: string;
    /**
     * Client process ID. Used for PID whitelist verification in JWT middleware.
     */
    pid: number;
};

export type ContentTypesDto = {
    codeSnippet: boolean;
    file: boolean;
    image: boolean;
    link: boolean;
    richText: boolean;
    text: boolean;
};

export type ContentTypesPatchDto = {
    codeSnippet?: boolean | null;
    file?: boolean | null;
    image?: boolean | null;
    link?: boolean | null;
    richText?: boolean | null;
    text?: boolean | null;
};

/**
 * Companion to [`SetupStateResponse::current_invitation`].
 */
export type CurrentInvitation = {
    code: string;
    expiresAtMs: number;
};

/**
 * Daemon residency mode reported in the health/status handshake (ADR-008 P5-L L1).
 *
 * Wire values (camelCase, to match the `HealthResponse`/`StatusResponse` field
 * naming these enums travel inside): `"standalone" | "serverHeadless" |
 * "oneshot"`. The wire enum is defined HERE in the contract — it deliberately
 * does NOT depend on `uc-daemon`'s internal `DaemonRunMode`; the producer maps
 * `DaemonRunMode -> DaemonResidency` at the daemon/webserver boundary.
 *
 * Consumers (CLI L2 version-check, future R8-F2 takeover) read this to learn
 * whether the daemon they are talking to is a persistent member node
 * (`Standalone`/`ServerHeadless`) or a transient `Oneshot` that a persistent
 * client may later take over. As of L1 the CLI/GUI do NOT act on this field.
 *
 * Backward-tolerant: the field carries `#[serde(default)]`, so an OLDER
 * daemon body that omits `residency` decodes to [`Self::Standalone`], and a
 * NEWER body's `residency` is simply ignored by an older client. New variants
 * must be added at the END so existing clients keep deserializing known values.
 */
export type DaemonResidency = 'standalone' | 'serverHeadless' | 'oneshot';

export type DaemonWsEvent = {
    payload: {
        [key: string]: unknown;
    };
    sessionId?: string | null;
    topic: string;
    ts: number;
    type: string;
};

export type DebugStatusDto = {
    debugMode: boolean;
    effectiveLogProfile: string;
    restartRequired: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type DebugStatusEnvelope = {
    data: DebugStatusDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Failure reason. i18n key convention: `delivery.failureReason.<variant>`.
 */
export type DeliveryFailureReasonDto = 'offline' | 'localPolicy' | 'peerRejected' | 'io' | 'internal';

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type DispatchOutcomeEnvelope = {
    data: DispatchOutcomeResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Response body for `POST /clipboard/dispatch`.
 */
export type DispatchOutcomeResponse = {
    atMs: number;
    contentHash: string;
    perTarget: Array<PerTargetOutcomeDto>;
    totalAccepted: number;
    totalDuplicate: number;
    totalErrored: number;
    totalOffline: number;
};

/**
 * Request body for `POST /clipboard/dispatch`.
 *
 * The daemon wraps the text into a single `text/plain`
 * `SystemClipboardSnapshot` and fans it out to online peers.
 */
export type DispatchTextRequest = {
    /**
     * Optional target device IDs. Empty or absent = full fan-out.
     */
    peers?: Array<string> | null;
    /**
     * Plaintext to dispatch.
     */
    text: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type EncryptionActionEnvelope = {
    data: EncryptionActionResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Shared response payload for `POST /encryption/unlock` and
 * `POST /encryption/lock`.
 */
export type EncryptionActionResponse = {
    success: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type EncryptionStateEnvelope = {
    data: EncryptionStateResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Response payload for GET /encryption/state.
 */
export type EncryptionStateResponse = {
    initialized: boolean;
    sessionReady: boolean;
};

/**
 * Per-target status: `tag` + (on failure) a `reason` sub-discriminator.
 */
export type EntryDeliveryStatusDto = {
    tag: 'pending';
} | {
    tag: 'delivered';
} | {
    tag: 'duplicate';
} | {
    reason: DeliveryFailureReasonDto;
    tag: 'failed';
};

/**
 * One trusted peer's current sync status for the entry.
 */
export type EntryDeliveryTargetDto = {
    /**
     * Wire-level failure detail for UI tooltips; `null` on success / pending.
     */
    reasonDetail?: string | null;
    status: EntryDeliveryStatusDto;
    targetDeviceId: string;
    /**
     * Human-readable name from the member directory; `null` when unresolved.
     */
    targetDeviceName?: string | null;
    /**
     * `null` when `Pending` (never attempted). Epoch milliseconds.
     */
    updatedAtMs?: number | null;
};

/**
 * Mirror of the domain `EntryDeliveryView`: origin + every trusted peer's
 * latest delivery status.
 */
export type EntryDeliveryViewDto = {
    deliveries: Array<EntryDeliveryTargetDto>;
    entryId: string;
    source: EntrySourceDto;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type EntryDeliveryViewEnvelope = {
    data: EntryDeliveryViewDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Full entry detail (text content).
 * Matches the frontend `EntryDetail` interface.
 */
export type EntryDetailDto = {
    activeTimeMs: number;
    content: string;
    createdAtMs: number;
    id: string;
    mimeType?: string | null;
    sizeBytes: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type EntryDetailEnvelope = {
    data: EntryDetailDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Clipboard entry projection — lightweight summary for list views.
 * Matches the frontend `ClipboardEntryDto` interface.
 */
export type EntryProjectionResponseDto = {
    activeTime: number;
    capturedAt: number;
    contentType: string;
    fileSizes?: Array<number> | null;
    fileTransferReason?: string | null;
    fileTransferStatus?: string | null;
    hasDetail: boolean;
    id: string;
    /**
     * Original image height in pixels (only for image entries).
     */
    imageHeight?: number | null;
    /**
     * Original image width in pixels (only for image entries).
     */
    imageWidth?: number | null;
    isEncrypted: boolean;
    isFavorited: boolean;
    linkDomains?: Array<string> | null;
    linkUrls?: Array<string> | null;
    /**
     * `paste_rep` 的 payload_state, 仅在 `Lost` 时输出。前端用此把"内容已
     * 丢失"的 entry 灰显, 让用户在点击粘贴前就知道这条记录已不可用。
     */
    payloadState?: string | null;
    preview: string;
    sizeBytes: number;
    thumbnailUrl?: string | null;
    updatedAt: number;
};

/**
 * Resource metadata (blob URL or inline data).
 * Matches the frontend `ClipboardEntryResource` interface.
 */
export type EntryResourceDto = {
    blobId?: string | null;
    /**
     * Base64-encoded inline data (when content is stored inline, not in blob).
     */
    inlineData?: string | null;
    mimeType?: string | null;
    sizeBytes: number;
    url?: string | null;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type EntryResourceEnvelope = {
    data: EntryResourceDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Entry origin. `tag` drives the frontend discriminated union.
 */
export type EntrySourceDto = {
    tag: 'local';
} | {
    device_id: string;
    device_name?: string | null;
    tag: 'remote';
} | {
    tag: 'historical';
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type ExportConfigEnvelope = {
    data: ExportConfigResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /config/export`.
 *
 * No export password is taken: the bundle is sealed with the installation's own
 * key material, so opening it later requires the space passphrase. `target_path`
 * is the absolute destination the daemon writes the `.ucbundle` file to.
 */
export type ExportConfigRequest = {
    targetPath: string;
};

/**
 * Response payload for `POST /config/export` on success.
 */
export type ExportConfigResponse = {
    /**
     * Absolute path the bundle was written to.
     */
    path: string;
};

export type FileSyncSettingsDto = {
    fileAutoCleanup: boolean;
    fileCacheQuotaPerDevice: number;
    fileRetentionHours: number;
    fileSyncEnabled: boolean;
    maxFileSize: number;
    smallFileThreshold: number;
};

export type FileSyncSettingsPatchDto = {
    fileAutoCleanup?: boolean | null;
    fileCacheQuotaPerDevice?: number | null;
    fileRetentionHours?: number | null;
    fileSyncEnabled?: boolean | null;
    maxFileSize?: number | null;
    smallFileThreshold?: number | null;
};

export type GeneralSettingsDto = {
    autoCheckUpdate: boolean;
    /**
     * Whether to download the next available update in the background.
     * Persisted alongside `auto_check_update`; consumed by the frontend's
     * `UpdateContext` after a successful `check_for_update` to decide
     * whether to start a silent download.
     */
    autoDownloadUpdate?: boolean;
    autoStart: boolean;
    /**
     * Persistent local diagnostic logging mode. Takes effect after restart.
     */
    debugMode?: boolean;
    deviceName?: string | null;
    language?: string | null;
    silentStart: boolean;
    /**
     * Whether anonymous diagnostic telemetry is enabled.
     */
    telemetryEnabled: boolean;
    theme: ThemeDto;
    /**
     * 旧版"统一主题预设"字段（v0.7 之前唯一字段）。新前端不再写入,
     * 但 wire 仍透传以便老 daemon ↔ 新前端 / 新 daemon ↔ 老前端兼容。
     * 删除计划见 `uc_core::settings::model::GeneralSettings::theme_color`。
     */
    themeColor?: string | null;
    /**
     * Dark 模式下的主题预设名（如 `"zinc"`）；为 `None` 时 daemon 端
     * 将回退到 `theme_color`。wire 字段名 `themeColorDark`（camelCase）。
     */
    themeColorDark?: string | null;
    /**
     * Light 模式下的主题预设名（如 `"zinc"`）；为 `None` 时 daemon 端
     * 将回退到 `theme_color`。wire 字段名 `themeColorLight`（camelCase）。
     */
    themeColorLight?: string | null;
    /**
     * Dark 模式下用户对预设 token 的自定义覆盖（语义同 light）。wire 字段名 `themeOverridesDark`。
     */
    themeOverridesDark?: {
        [key: string]: string;
    };
    /**
     * Light 模式下用户对预设 token 的自定义覆盖（`{ tokenName: oklchString }`）。
     * 为空 map 表示完全跟随 preset。wire 字段名 `themeOverridesLight`。
     */
    themeOverridesLight?: {
        [key: string]: string;
    };
    updateChannel?: UpdateChannelDto | null;
    /**
     * Whether anonymous product usage analytics is enabled.
     * 与 `telemetry_enabled` 拆开（schema doc §6.4）：前者控制 Sentry 错误
     * 上报，本字段控制产品 telemetry（漏斗 / 留存 / 同步可靠性事件）。
     */
    usageAnalyticsEnabled?: boolean;
};

/**
 * All fields are optional — only provided fields are updated.
 */
export type GeneralSettingsPatchDto = {
    autoCheckUpdate?: boolean | null;
    autoDownloadUpdate?: boolean | null;
    autoStart?: boolean | null;
    debugMode?: boolean | null;
    deviceName?: string | null;
    language?: string | null;
    silentStart?: boolean | null;
    telemetryEnabled?: boolean | null;
    theme?: ThemeDto | null;
    /**
     * 旧版"统一主题预设"patch 字段。`Some(None)` = 显式清空,`None` = 不修改。
     */
    themeColor?: string | null;
    /**
     * Dark 模式预设 patch。`Some(None)` = 显式清空（回退到 `theme_color` 或引擎默认）。
     */
    themeColorDark?: string | null;
    /**
     * Light 模式预设 patch。`Some(None)` = 显式清空（回退到 `theme_color` 或引擎默认）。
     */
    themeColorLight?: string | null;
    /**
     * Dark 模式 overrides patch（语义同 light）。
     */
    themeOverridesDark?: {
        [key: string]: string;
    } | null;
    /**
     * Light 模式 overrides patch。`Some(map)` 整体替换；`None` 表示不修改。
     */
    themeOverridesLight?: {
        [key: string]: string;
    } | null;
    updateChannel?: UpdateChannelDto | null;
    usageAnalyticsEnabled?: boolean | null;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type HealthEnvelope = {
    data: HealthResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

export type HealthResponse = {
    apiRevision: string;
    packageVersion: string;
    residency?: DaemonResidency;
    status: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type ImportConfigEnvelope = {
    data: ImportConfigResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /config/import`.
 *
 * `confirmed` is a deliberate gate: the import is a device-identity move, so
 * the caller must explicitly confirm. The handler MUST never log this body.
 */
export type ImportConfigRequest = {
    confirmed: boolean;
    password: string;
    sourcePath: string;
};

/**
 * Response payload for `POST /config/import` on success (staged for the next
 * restart to apply on boot).
 */
export type ImportConfigResponse = {
    /**
     * Always `true` on success: the bundle was validated and staged.
     */
    stagedOk: boolean;
    /**
     * `true` when applying the staged migration will require the operator to
     * re-enter their passphrase to unlock after restart; `false` when the
     * staged material is sufficient to unlock without further input.
     */
    unlockRequiredAfterApply: boolean;
};

/**
 * Request body for `POST /v2/setup/initialize`. Maps to
 * `SpaceSetupFacade::initialize_space(InitializeSpaceCommand)`.
 */
export type InitializeSpaceRequest = {
    deviceName?: string | null;
    passphrase: string;
    passphraseConfirm: string;
};

/**
 * Response body for `POST /v2/setup/initialize`. Mirrors
 * `InitializeSpaceResult` flattened to wire-friendly strings.
 */
export type InitializeSpaceResponse = {
    fingerprint: string;
    selfDeviceId: string;
    spaceId: string;
};

/**
 * Response body for `POST /v2/setup/issue-invitation`. Mirrors
 * `IssuePairingInvitationResult` with an epoch-millis expiry to keep
 * the wire form free of timezone parsing on the client.
 */
export type IssueInvitationResponse = {
    code: string;
    expiresAtMs: number;
};

export type KeyboardShortcutsPatchDto = {
    shortcuts: {
        [key: string]: ShortcutKeyDto;
    };
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type KeychainAccessEnvelope = {
    data: KeychainAccessResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Response payload for GET /encryption/keychain-access.
 */
export type KeychainAccessResponse = {
    /**
     * Whether Keychain access is granted (Always Allow permission).
     */
    granted: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type LanInterfaceListEnvelope = {
    data: Array<LanInterfaceViewDto>;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * One usable IPv4 LAN interface candidate for the QR URL.
 */
export type LanInterfaceViewDto = {
    ipv4: string;
    name: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type LifecycleStatusEnvelope = {
    data: LifecycleStatusResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Response payload for GET /lifecycle/status.
 * Mirrors the frontend LifecycleStatusDto shape so the HTTP endpoint
 * can replace the Tauri get_lifecycle_status command without frontend type changes.
 */
export type LifecycleStatusResponse = {
    /**
     * Current lifecycle state.
     */
    state: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type ListEntriesEnvelope = {
    data: Array<EntryProjectionResponseDto>;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

export type LocalDeviceInfoDto = {
    deviceName: string;
    peerId: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type LocalDeviceInfoEnvelope = {
    data: LocalDeviceInfoDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type LogExportEnvelope = {
    data: LogExportResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

export type LogExportRequestDto = {
    sinceHours?: number | null;
};

export type LogExportResultDto = {
    includedFiles: Array<string>;
    path: string;
    since: string;
};

/**
 * Sync preferences recorded for a space member.
 */
export type MemberSyncPreferencesDto = {
    receiveContentTypes: ContentTypesDto;
    receiveEnabled: boolean;
    sendContentTypes: ContentTypesDto;
    sendEnabled: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type MemberSyncPreferencesEnvelope = {
    data: MemberSyncPreferencesDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Partial sync preferences for PATCH /member/:device_id/sync-preferences.
 *
 * 服务器侧 `get → merge → save` 后持久化；未提供的字段保留当前值。
 * 重置到默认值的调用方应显式传入所有字段的默认值（`MemberSyncPreferences::default()`）。
 */
export type MemberSyncPreferencesPatchDto = {
    receiveContentTypes?: ContentTypesPatchDto | null;
    receiveEnabled?: boolean | null;
    sendContentTypes?: ContentTypesPatchDto | null;
    sendEnabled?: boolean | null;
};

/**
 * Folded payload for `PATCH /member/:device_id/sync-preferences` (ADR-008 §0.1).
 *
 * The current handler returns `success` as a top-level sibling of the
 * `{data,ts}` envelope. This DTO folds it INTO the payload so the endpoint can
 * return `ApiEnvelope<MemberSyncResultDto>` with no bespoke wrapper. P1 only
 * defines the type; the handler is rewired in P2.
 */
export type MemberSyncResultDto = {
    success: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type MemberSyncResultEnvelope = {
    data: MemberSyncResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Coarse-grained migration phase exposed to the UI. The internal
 * `MigrationPhase` carries `run_id` / `target_space_id`; those are
 * implementation detail and not surfaced over the wire.
 */
export type MigrationPhaseDto = 'prepared' | 'handshake_done' | 'swapped';

/**
 * Response body for `GET /v2/setup/migration-progress`. Mirrors
 * `MigrationProgress`. `phase = null` means no migration is in
 * flight (idle / completed); polling clients should stop their loop
 * when they observe this transition while `backup_record_count == 0`.
 */
export type MigrationProgressResponse = {
    backupRecordCount: number;
    phase?: MigrationPhaseDto | null;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type MobileDeviceListEnvelope = {
    data: Array<MobileDeviceViewDto>;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * One registered device (no password hash; `username` is an identifier aid).
 */
export type MobileDeviceViewDto = {
    clientType: string;
    createdAtMs: number;
    deviceId: string;
    label: string;
    lastSeenAtMs?: number | null;
    lastSeenIp?: string | null;
    reportedName?: string | null;
    reportedOs?: string | null;
    username: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type MobileSyncActionEnvelope = {
    data: MobileSyncActionResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Result of revoking a device. Enveloped `{ success: true }` so every 200
 * carries a `{ data, ts }` body per §0.1; the FE wrapper discards it.
 */
export type MobileSyncActionResultDto = {
    success: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type MobileSyncSettingsEnvelope = {
    data: MobileSyncSettingsViewDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Synthesized mobile-sync settings view (settings + current LAN URL parts +
 * available install methods).
 */
export type MobileSyncSettingsViewDto = {
    enabled: boolean;
    lanAdvertiseBaseUrl?: string | null;
    lanAdvertiseIp?: string | null;
    lanListenEnabled: boolean;
    /**
     * Why the daemon's LAN listener failed to bind (port in use / IP absent /
     * permission). `Some` means a bind was actually attempted and failed.
     */
    lanListenerError?: string | null;
    lanPort?: number | null;
    shortcutInstallMethods: Array<ShortcutInstallMethodViewDto>;
};

/**
 * LAN-only Mode（v0.7.0）DTO 镜像。
 *
 * 反向命名规则（Pitfall 1）：业务正向语义 `allow_relay_fallback`，
 * 不在此层重命名为 `lan_only` 或类似镜像。wire 字段 = `allowRelayFallback`
 * （camelCase 自动转换）。取反唯一发生在 `uc-bootstrap/src/network_policy.rs`。
 *
 * `allow_overlay_network_addrs` 控制是否把 VPN/overlay 类虚拟网卡 IP（CGNAT
 * 100.64.0.0/10、Tailscale ULA fd7a:115c:a1e0::/48）作为 iroh 直连候选发布
 * 给对端。默认 `false`（过滤）。专业用户在两端都接入同一 VPN 时可开启。
 *
 * `custom_relay_urls` 为空时继续使用 iroh 默认 relay；非空时只使用这些
 * 用户配置的 relay URL。LAN-only 模式关闭 relay 时该列表保留但不生效。
 */
export type NetworkSettingsDto = {
    allowOverlayNetworkAddrs?: boolean;
    allowRelayFallback: boolean;
    customRelayUrls?: Array<string>;
};

/**
 * LAN-only Mode 字段 patch DTO 镜像 — `null` = 不修改。
 */
export type NetworkSettingsPatchDto = {
    allowOverlayNetworkAddrs?: boolean | null;
    allowRelayFallback?: boolean | null;
    customRelayUrls?: Array<string> | null;
};

export type PairingSettingsDto = {
    maxRetries: number;
    protocolVersion: string;
    sessionTimeout: number;
    stepTimeout: number;
    userVerificationTimeout: number;
};

export type PairingSettingsPatchDto = {
    maxRetries?: number | null;
    sessionTimeout?: number | null;
    stepTimeout?: number | null;
    userVerificationTimeout?: number | null;
};

export type PeerSnapshotDto = {
    addresses: Array<string>;
    /**
     * Phase 96 INDIC-01:连接通道 4 态 wire 字符串。
     * 取值严格限定 `"direct" | "relay" | "offline" | "unknown"`,
     * 由 application 层 `connection_channel_to_wire` 单点产出,
     * 前端按字符串模式匹配渲染徽章。**禁止**新增枚举值或缩写;
     * "Out of LAN" 灰态由前端基于 `channel + LAN-only setting`
     * 合成,不在 wire 协议里。
     */
    channel: string;
    connected: boolean;
    /**
     * 当前活跃连接地址。直连时为对端 IP:port,中转时为 relay 地址。
     */
    connectionAddress?: string | null;
    deviceName?: string | null;
    isPaired: boolean;
    pairingState: string;
    peerId: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type PeerSnapshotListEnvelope = {
    data: Array<PeerSnapshotDto>;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Per-target delivery outcome in the dispatch response.
 */
export type PerTargetOutcomeDto = {
    deviceId: string;
    error?: string | null;
    /**
     * `"accepted"` | `"duplicate"` | `"error"`.
     */
    outcome: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type PresenceRefreshEnvelope = {
    data: PresenceRefreshResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Result of a `POST /presence/refresh` round.
 *
 * 主动 probe 一轮 `ensure_reachable_all` 后的统计回执。UI 不靠这里直接判定
 * 在线状态：probe 过程中各设备的 Online/Offline 变化会通过既有
 * `peers.changed` WebSocket 链路推送，前端再走 `GET /paired-devices`
 * 重拉。该响应只用于调用方显示进度或排障。
 */
export type PresenceRefreshResponse = {
    errors: number;
    offline: number;
    online: number;
    total: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type PreviewImportEnvelope = {
    data: PreviewImportResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /config/import/preview`.
 *
 * Read-only: decrypts the bundle's manifest to surface descriptive metadata
 * for operator confirmation. The handler MUST never log this body.
 */
export type PreviewImportRequest = {
    password: string;
    sourcePath: string;
};

/**
 * Response payload for `POST /config/import/preview`.
 *
 * Carries only non-secret descriptive metadata read from the bundle manifest.
 */
export type PreviewImportResponse = {
    /**
     * Application version string of the installation that produced the bundle.
     */
    appVersion: string;
    /**
     * Bundle creation time, milliseconds since the Unix epoch.
     */
    createdAtUnixMs: number;
    /**
     * Stable identity fingerprint of the producing device, for human
     * confirmation. Adopting this bundle makes the target device present
     * itself under this same identity.
     */
    deviceFingerprint: string;
    /**
     * Profile the bundle's configuration belongs to.
     */
    profileId: string;
    /**
     * Storage layout the bundle was produced under (`portable` / `installed`).
     */
    sourceMode: string;
};

/**
 * 快捷面板出现位置 DTO。wire form: `center` | `follow_cursor`。
 */
export type QuickPanelPositionDto = 'center' | 'follow_cursor';

/**
 * 快捷面板（Spotlight 风格）功能偏好 DTO。
 *
 * wire 字段命名为 camelCase（`enabled` / `position`）。`#[serde(default)]`
 * 让缺字段时回退到 `Default`（`enabled = true`、`position = center`），与
 * `core::QuickPanelSettings` 默认保持一致——新装/老 wire 缺字段都视为
 * "启用 + 居中"，避免出现 wire 与磁盘真相撕裂。
 */
export type QuickPanelSettingsDto = {
    enabled?: boolean;
    position?: QuickPanelPositionDto;
};

/**
 * 快捷面板字段 patch DTO 镜像 — `null` = 不修改。
 */
export type QuickPanelSettingsPatchDto = {
    enabled?: boolean | null;
    position?: QuickPanelPositionDto | null;
};

/**
 * Request body for `POST /v2/setup/redeem`. Maps to
 * `SpaceSetupFacade::redeem_pairing_invitation(RedeemPairingInvitationCommand)`.
 */
export type RedeemRequest = {
    code: string;
    passphrase: string;
};

/**
 * Response body for `POST /v2/setup/redeem`. Mirrors
 * `RedeemPairingInvitationResult` flattened to wire-friendly strings.
 */
export type RedeemResponse = {
    selfDeviceId: string;
    selfIdentityFingerprint: string;
    spaceId: string;
    sponsorDeviceId: string;
    sponsorIdentityFingerprint: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type RegisterMobileDeviceEnvelope = {
    data: RegisterMobileDeviceResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /mobile-sync/devices`.
 *
 * `username` / `password` absent (missing field or explicit null) routes
 * through the auto-mint path; a value is strictly validated.
 */
export type RegisterMobileDeviceRequest = {
    label: string;
    password?: string | null;
    username?: string | null;
};

/**
 * Result of registering an iPhone Shortcut device. `password` is the **one
 * and only** plaintext echo to the frontend — afterwards it exists solely as
 * a PHC hash server-side. The two QR PNGs arrive base64-encoded (encoded
 * daemon-side) ready for `<img src="data:image/png;base64,...">`.
 */
export type RegisterMobileDeviceResultDto = {
    baseUrl: string;
    clientType: string;
    /**
     * `uniclipboard://connect?...` deep link (the main QR content).
     */
    connectUri: string;
    createdAtMs: number;
    deviceId: string;
    /**
     * Base64 PNG of the iCloud shortcut-install URL.
     */
    installQrCodePngBase64: string;
    installUrl: string;
    label: string;
    password: string;
    /**
     * ASCII-art QR encoding `connectUri` (for terminal rendering).
     */
    qrCodeAscii: string;
    /**
     * Base64 PNG encoding `connectUri`.
     */
    qrCodePngBase64: string;
    username: string;
};

/**
 * Outcome of a relay reachability probe (`POST /settings/relay-probe`).
 *
 * Mirrors the desktop `RelayProbeOutcome` Tauri DTO: a probe that fails to
 * reach the relay is a NORMAL categorized outcome (returned 200), not an HTTP
 * error — the daemon is healthy, the *relay* is the subject under test. Only a
 * missing relay-diagnostic adapter (server misconfiguration) surfaces as an
 * `ApiError`. The frontend selects user-facing copy off the `tag`.
 */
export type RelayProbeOutcomeDto = {
    latency_ms: number;
    tag: 'success';
} | {
    message: string;
    tag: 'invalidUrl';
} | {
    message: string;
    tag: 'dns';
} | {
    message: string;
    tag: 'tls';
} | {
    message: string;
    tag: 'handshake';
} | {
    tag: 'timeout';
} | {
    message: string;
    tag: 'other';
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type RelayProbeOutcomeEnvelope = {
    data: RelayProbeOutcomeDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /settings/relay-probe`.
 */
export type RelayProbeRequestDto = {
    /**
     * Candidate relay URL to probe. Not persisted; the probe is repeatable.
     */
    url: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type ResendEnvelope = {
    data: ResendResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /clipboard/resend`.
 */
export type ResendRequest = {
    /**
     * ID of the previously captured entry to resend.
     */
    entryId: string;
    /**
     * Optional target device IDs.
     */
    peers?: Array<string> | null;
};

/**
 * Response body for `POST /clipboard/resend`.
 */
export type ResendResponse = {
    accepted: number;
    duplicate: number;
    errored: number;
    offline: number;
    pending: number;
};

/**
 * POST /lifecycle/restart 202 ACCEPTED body (ADR-008 P5-L L8d-1). Echoes the
 * locked-in `generation` + `targetMode` so the requester can correlate the
 * accepted restart with the eventual handover record.
 */
export type RestartAccepted = {
    generation: number;
    targetMode: DaemonResidency;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type RestartAcceptedEnvelope = {
    data: RestartAccepted;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * POST /lifecycle/restart request body (ADR-008 P5-L L8d-1). `targetMode` is the
 * residency the successor daemon should launch in.
 */
export type RestartRequest = {
    targetMode: DaemonResidency;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type RestoreEntryEnvelope = {
    data: RestoreEntryResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Response body for `POST /clipboard/restore/:entry_id`.
 *
 * Wrapped in `ApiEnvelope` per §0.1. The success body was previously the
 * ad-hoc `{ "success": true }` shape; consumers ignore it today.
 */
export type RestoreEntryResponse = {
    success: boolean;
};

export type RetentionPolicyDto = {
    enabled: boolean;
    evaluation: RuleEvaluationDto;
    rules: Array<RetentionRuleDto>;
    skipPinned: boolean;
};

export type RetentionPolicyPatchDto = {
    enabled?: boolean | null;
    evaluation?: RuleEvaluationDto | null;
    rules?: Array<RetentionRuleDto> | null;
    skipPinned?: boolean | null;
};

export type RetentionRuleDto = {
    /**
     * 按时间清理
     */
    byAge: {
        max_age: number;
    };
} | {
    /**
     * 按总数量上限
     */
    byCount: {
        max_items: number;
    };
} | {
    /**
     * 按内容类型的最大存活时间
     */
    byContentType: {
        content_type: ContentTypesDto;
        max_age: number;
    };
} | {
    /**
     * 按磁盘占用大小
     */
    byTotalSize: {
        max_bytes: number;
    };
} | {
    /**
     * 敏感内容快速过期
     */
    sensitive: {
        max_age: number;
    };
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type RotateMobilePasswordEnvelope = {
    data: RotateMobilePasswordResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /mobile-sync/devices/{device_id}/rotate-password`.
 * `password` absent → auto-mint a new plaintext; a value is validated.
 */
export type RotateMobilePasswordRequest = {
    password?: string | null;
};

/**
 * Result of rotating a device password. `password` is the one-time plaintext
 * echo; the old password is immediately invalidated.
 */
export type RotateMobilePasswordResultDto = {
    deviceId: string;
    password: string;
    username: string;
};

export type RuleEvaluationDto = 'anyMatch' | 'allMatch';

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SearchQueryEnvelope = {
    data: SearchQueryResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Folded payload for `GET /search/query` (ADR-008 §0.1).
 *
 * The current handler returns `total` and `hasMore` as top-level siblings of
 * the `{data,ts}` envelope (`data` is the items array). This DTO folds those
 * siblings INTO the payload (renaming `data` → `items`) so the endpoint can
 * return `ApiEnvelope<SearchQueryResultDto>` with no bespoke wrapper. P1 only
 * defines the type; the handler is rewired in P2.
 */
export type SearchQueryResultDto = {
    hasMore: boolean;
    items: Array<SearchResultDto>;
    total: number;
};

/**
 * Acceptance payload — the `ApiEnvelope` payload for `POST /search/rebuild`
 * (ADR-008 §0.1).
 */
export type SearchRebuildAcceptedData = {
    accepted: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SearchRebuildEnvelope = {
    data: SearchRebuildAcceptedData;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Single search result — mirrors `SearchResult` with camelCase transport names.
 */
export type SearchResultDto = {
    activeTimeMs: number;
    contentType: string;
    entryId: string;
    fileExtensions: Array<string>;
    mimeType: string;
    textPreview?: string | null;
};

/**
 * Search index availability snapshot — the `ApiEnvelope` payload for
 * `GET /search/status` (ADR-008 §0.1).
 */
export type SearchStatusData = {
    /**
     * Millisecond timestamp of the last completed rebuild (from `SearchIndexMeta`).
     */
    lastRebuildCompletedAtMs?: number | null;
    /**
     * Millisecond timestamp of the last rebuild start (from `SearchIndexMeta`).
     */
    lastRebuildStartedAtMs?: number | null;
    /**
     * Optional reason code (present when rebuilding or unavailable).
     */
    reason?: string | null;
    /**
     * One of: "ready", "rebuilding", "unavailable"
     */
    state: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SearchStatusEnvelope = {
    data: SearchStatusData;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

export type SecuritySettingsDto = {
    /**
     * 是否启用启动时自动解锁
     *
     * 仅用于 UI 与流程判断
     * 需要用户在系统弹窗中选择“始终允许”才能静默生效
     */
    autoUnlockEnabled?: boolean;
    /**
     * 是否启用本地数据加密
     */
    encryptionEnabled: boolean;
    /**
     * 是否已经在 keyring 中设置过口令
     *
     * 仅用于 UI 与流程判断
     * 不代表当前口令是否“可用”
     */
    passphraseConfigured: boolean;
};

export type SecuritySettingsPatchDto = {
    /**
     * 写入时设置是否启用启动时自动解锁
     */
    autoUnlockEnabled?: boolean | null;
    /**
     * 写入时设置是否启用本地数据加密（需要 passphrase）
     */
    encryptionEnabled?: boolean | null;
    /**
     * 写入时设置 passphrase（由前端/daemon 内部触发解锁流程）
     */
    passphrase?: string | null;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SessionTokenEnvelope = {
    data: SessionTokenResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Response body for `POST /auth/connect`.
 *
 * Mirrors the current flat shape returned by the webserver `ConnectResponse`
 * and decoded by the native client's local `ConnectResponse`:
 * `{ "sessionToken", "expiresInSecs", "refreshAtSecs" }`.
 */
export type SessionTokenResponse = {
    /**
     * Token time-to-live in seconds.
     */
    expiresInSecs: number;
    /**
     * Recommended refresh time in seconds.
     */
    refreshAtSecs: number;
    /**
     * HS256-signed JWT session token.
     */
    sessionToken: string;
};

export type SettingsDto = {
    fileSync: FileSyncSettingsDto;
    general: GeneralSettingsDto;
    keyboardShortcuts: {
        [key: string]: ShortcutKeyDto;
    };
    network: NetworkSettingsDto;
    pairing: PairingSettingsDto;
    quickPanel?: QuickPanelSettingsDto;
    retentionPolicy: RetentionPolicyDto;
    schemaVersion: number;
    security: SecuritySettingsDto;
    sync: SyncSettingsDto;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SettingsEnvelope = {
    data: SettingsDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

export type SettingsPatchDto = {
    fileSync?: FileSyncSettingsPatchDto | null;
    general?: GeneralSettingsPatchDto | null;
    keyboardShortcuts?: KeyboardShortcutsPatchDto | null;
    network?: NetworkSettingsPatchDto | null;
    pairing?: PairingSettingsPatchDto | null;
    quickPanel?: QuickPanelSettingsPatchDto | null;
    retentionPolicy?: RetentionPolicyPatchDto | null;
    security?: SecuritySettingsPatchDto | null;
    sync?: SyncSettingsPatchDto | null;
};

/**
 * Folded payload for `PUT /settings` (ADR-008 §0.1).
 *
 * The current handler returns `success` and `restartRequired` as top-level
 * siblings of the `{data,ts}` envelope. This DTO folds those siblings INTO the
 * payload so the endpoint can return `ApiEnvelope<SettingsUpdateResultDto>`
 * with no bespoke wrapper. P1 only defines the type; the handler is rewired in
 * P2.
 */
export type SettingsUpdateResultDto = {
    /**
     * Whether the patch touched fields requiring a daemon restart (currently
     * only `network.*`).
     */
    restartRequired: boolean;
    success: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SettingsUpdateResultEnvelope = {
    data: SettingsUpdateResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SetupInitializeEnvelope = {
    data: InitializeSpaceResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SetupIssueInvitationEnvelope = {
    data: IssueInvitationResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SetupMigrationProgressEnvelope = {
    data: MigrationProgressResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SetupRedeemEnvelope = {
    data: RedeemResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SetupStateEnvelope = {
    data: SetupStateResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Response body for `GET /v2/setup/state`. Mirrors `SetupStateView`.
 */
export type SetupStateResponse = {
    currentInvitation?: CurrentInvitation | null;
    deviceName?: string | null;
    hasCompleted: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SetupSwitchSpaceEnvelope = {
    data: SwitchSpaceResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * One shortcut-install method option (`tokenInjected` / `icloudGeneric`).
 */
export type ShortcutInstallMethodViewDto = {
    available: boolean;
    disabledReason?: string | null;
    method: string;
};

/**
 * A keyboard shortcut value that can be either a single key combo or multiple alternatives.
 *
 * Serialised with `#[serde(untagged)]` so that `"Ctrl+C"` and `["Ctrl+C","Meta+C"]` are both
 * accepted without a wrapping tag, matching the TypeScript type `string | string[]`.
 */
export type ShortcutKeyDto = string | Array<string>;

export type SpaceMemberDto = {
    /**
     * Phase 96 INDIC-01:连接通道 4 态 wire 字符串。同 `PeerSnapshotDto.channel`,
     * 取值严格限定 `"direct" | "relay" | "offline" | "unknown"`。前端
     * `SpaceMember` 直接消费,`ConnectionChannelBadge` 渲染。
     */
    channel: string;
    connected: boolean;
    /**
     * 当前活跃连接地址。直连时为对端 IP:port,中转时为 relay 地址。
     */
    connectionAddress?: string | null;
    deviceName: string;
    lastSeenAtMs?: number | null;
    pairingState: string;
    peerId: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type SpaceMemberListEnvelope = {
    data: Array<SpaceMemberDto>;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type StatusEnvelope = {
    data: StatusResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

export type StatusResponse = {
    apiRevision: string;
    packageVersion: string;
    residency?: DaemonResidency;
    uptimeSeconds: number;
    workers: Array<WorkerStatusDto>;
};

/**
 * Response payload for `GET /storage/stats`.
 */
export type StorageStatsDto = {
    cacheBytes: number;
    databaseBytes: number;
    logsBytes: number;
    totalBytes: number;
    vaultBytes: number;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type StorageStatsEnvelope = {
    data: StorageStatsDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /v2/setup/switch-space`. Maps to
 * `SpaceSetupFacade::switch_space(SwitchSpaceInput)`. Pre-conditions
 * (setup completed + session unlocked + no pending migration) are
 * checked inside the facade and surface as 409 / 423 / 423 respectively.
 */
export type SwitchSpaceRequest = {
    code: string;
    newPassphrase: string;
};

/**
 * Response body for `POST /v2/setup/switch-space`. Mirrors
 * `SwitchSpaceResult` flattened to wire-friendly strings, plus
 * `migrated_records` so the UI can show "迁移了 N 条历史".
 */
export type SwitchSpaceResponse = {
    migratedRecords: number;
    selfDeviceId: string;
    selfIdentityFingerprint: string;
    spaceId: string;
    sponsorDeviceId: string;
    sponsorIdentityFingerprint: string;
};

export type SyncFrequencyDto = 'realtime' | 'interval';

export type SyncSettingsDto = {
    autoSync: boolean;
    contentTypes: ContentTypesDto;
    syncFrequency: SyncFrequencyDto;
};

export type SyncSettingsPatchDto = {
    autoSync?: boolean | null;
    contentTypes?: ContentTypesPatchDto | null;
    syncFrequency?: SyncFrequencyDto | null;
};

export type ThemeDto = 'light' | 'dark' | 'system';

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type ToggleFavoriteEnvelope = {
    data: ToggleFavoriteResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * POST /clipboard/entries/:id/favorite request body.
 */
export type ToggleFavoriteRequest = {
    isFavorited: boolean;
};

/**
 * Result of toggling favorite state.
 */
export type ToggleFavoriteResultDto = {
    success: boolean;
};

/**
 * Mirrors `analytics::DialogOpenSource`. wire: `notification` | `sidebar_icon`.
 */
export type UiDialogOpenSource = 'notification' | 'sidebar_icon';

/**
 * Mirrors `analytics::DismissSource`. wire: `dialog_later` | `dialog_closed` |
 * `package_manager_dialog_closed`.
 */
export type UiDismissSource = 'dialog_later' | 'dialog_closed' | 'package_manager_dialog_closed';

/**
 * Mirrors `analytics::InstallKind`. wire (lowercase): `macos` | `windows` |
 * `windowsportable` | `appimage` | `deb` | `rpm` | `unknown`.
 *
 * Cross-process note (ADR-008 D20): `install_kind` was historically probed
 * backend-side and the webview "knew nothing". After the process split the
 * install provenance of the *running app* is owned by the native GUI shell
 * (it holds `current_exe` / the portable marker / the `APPIMAGE` env), while
 * the daemon has no install-detection code. So the webview now supplies it —
 * it reads `get_install_kind` (native, cached) and forwards the result here.
 */
export type UiInstallKind = 'macos' | 'windows' | 'windowsportable' | 'appimage' | 'deb' | 'rpm' | 'unknown';

/**
 * Mirrors `analytics::NotificationDeliveryStatus`. wire: `sent` |
 * `permission_denied` | `send_failed`.
 */
export type UiNotificationDeliveryStatus = 'sent' | 'permission_denied' | 'send_failed';

/**
 * Mirrors `analytics::UpdateAction`. wire: `download_bg` | `install`.
 */
export type UiUpdateAction = 'download_bg' | 'install';

/**
 * Mirrors `analytics::UpdateActionOutcome`. wire: `started` | `succeeded` |
 * `failed` | `cancelled`.
 */
export type UiUpdateActionOutcome = 'started' | 'succeeded' | 'failed' | 'cancelled';

/**
 * Mirrors `analytics::UpdateCheckOutcome`. wire: `available` | `up_to_date` |
 * `failed`.
 */
export type UiUpdateCheckOutcome = 'available' | 'up_to_date' | 'failed';

/**
 * Mirrors `analytics::UpdateCheckSource`. wire: `startup` | `scheduled` |
 * `manual` | `window_show`.
 *
 * Cross-process note (ADR-008 D20): the update *check* runs in the GUI process
 * (its updater background task / tray / settings button), not the daemon. The
 * GUI therefore forwards the check outcome here so the daemon — the single
 * authoritative sender — dispatches it with its own `EventContext`.
 */
export type UiUpdateCheckSource = 'startup' | 'scheduled' | 'manual' | 'window_show';

/**
 * Mirrors `analytics::UpdateFailureKind`. wire: `network` | `http_error` |
 * `parse_error` | `other`. Only present when the check outcome is `failed`.
 */
export type UiUpdateFailureKind = 'network' | 'http_error' | 'parse_error' | 'other';

/**
 * Mirrors `analytics::UpdatePhase`. wire: `available` | `downloading` | `ready`.
 */
export type UiUpdatePhase = 'available' | 'downloading' | 'ready';

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type UnlockSpaceEnvelope = {
    data: UnlockSpaceResponse;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `POST /encryption/unlock-with-passphrase` (ADR-008 D15).
 *
 * Carries the user's plaintext passphrase over the loopback API. Per D14 the
 * endpoint is session-JWT gated (not in `PUBLIC_PATHS`) and the handler MUST
 * never log this body — see the rule in `uc-webserver` `api/encryption.rs`.
 * This formally retires the historical "passphrase 不出进程" invariant: under
 * the "same UID = trusted" model (D14) an attacker who can sniff loopback can
 * already dump the master key from daemon memory, so loopback transport adds
 * zero incremental exposure.
 */
export type UnlockSpaceRequest = {
    passphrase: string;
};

/**
 * Response payload for `POST /encryption/unlock-with-passphrase`.
 */
export type UnlockSpaceResponse = {
    spaceId: string;
};

export type UnpairDeviceRequest = {
    peerId: string;
};

export type UpdateChannelDto = 'stable' | 'alpha' | 'beta' | 'rc';

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type UpdateDebugModeEnvelope = {
    data: UpdateDebugModeResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

export type UpdateDebugModeRequestDto = {
    enabled: boolean;
};

export type UpdateDebugModeResultDto = {
    debugMode: boolean;
    restartRequired: boolean;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type UpdateMobileDeviceEnvelope = {
    data: UpdateMobileDeviceResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body for `PATCH /mobile-sync/devices/{device_id}`.
 *
 * `label` / `username` absent means keep unchanged. `password` is tri-state:
 * field absent = keep unless username changes; explicit `null` = auto-generate;
 * value = use the supplied plaintext after validation.
 */
export type UpdateMobileDeviceRequest = {
    label?: string | null;
    password?: string | null;
    username?: string | null;
};

/**
 * Result of updating an existing mobile device. `password` is `Some` only when
 * the update created a new plaintext password that must be shown once.
 */
export type UpdateMobileDeviceResultDto = {
    deviceId: string;
    label: string;
    password?: string | null;
    username: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type UpdateMobileSyncSettingsEnvelope = {
    data: UpdateMobileSyncSettingsResultDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

/**
 * Request body (patch) for `PATCH /mobile-sync/settings`.
 *
 * `lanAdvertiseIp` / `lanPort` are three-state: field absent = leave
 * untouched; explicit `null` = clear; value = set. The frontend's
 * `JSON.stringify` drops `undefined` (absent) and serializes `null`
 * explicitly. The `Option<Option<T>>` Rust type preserves the distinction;
 * the wire type is just `T | null` optional (declared via `schema(value_type)`).
 */
export type UpdateMobileSyncSettingsRequest = {
    enabled?: boolean | null;
    lanAdvertiseBaseUrl?: string | null;
    lanAdvertiseIp?: string | null;
    lanListenEnabled?: boolean | null;
    lanPort?: number | null;
};

/**
 * Result of updating mobile-sync settings.
 */
export type UpdateMobileSyncSettingsResultDto = {
    enabled: boolean;
    lanAdvertiseBaseUrl?: string | null;
    lanAdvertiseIp?: string | null;
    lanListenEnabled: boolean;
    /**
     * Reason the LAN listener failed to bind under the immediate-apply path
     * (port in use, permission, unassignable IP). `None` in the CLI fallback /
     * no-lifecycle assembly.
     */
    lanListenerBindError?: string | null;
    lanPort?: number | null;
    /**
     * Wire-compat historical flag. In the GUI/daemon path settings take effect
     * immediately so this is always false; the CLI fallback assembly still
     * returns "any field actually changed → true" to express the old
     * "next daemon restart" semantics. The frontend shows a restart banner
     * only when true.
     */
    restartRequired: boolean;
};

/**
 * Discriminated union mirroring `uc_application::facade::UpgradeStatus`.
 *
 * Wire encoding uses `kind` discriminator with snake_case variants to
 * keep parity with the CLI JSON output produced by `uniclip upgrade
 * status --json`.
 *
 * 防御性补丁(issue #606 followup):同时声明 `rename_all_fields`,
 * 避免未来新增多词字段(如 `target_version`)时 wire 字段名漂回
 * snake_case 与上层契约不一致。当前字段都是单词,加这个对 wire 无影响,
 * 但锁定未来添加字段的默认风格。详见 `docs/agent/rust-tauri-rules.md`
 * 的 "Enum Wire Serialization" 一节。
 */
export type UpgradeStatusDto = {
    current: string;
    kind: 'fresh_install';
} | {
    current: string;
    kind: 'no_change';
} | {
    from?: string | null;
    kind: 'upgraded';
    to: string;
} | {
    from: string;
    kind: 'downgraded';
    to: string;
};

/**
 * Canonical success envelope: `{ "data": T, "ts": <unix millis i64> }`.
 *
 * `ts` is `chrono::Utc::now().timestamp_millis()`, set in the webserver handler
 * via [`ApiEnvelope::now`] (the contract carries only the type + the clock
 * helper, not a hard dependency on when the handler reads the clock).
 * `rename_all = "camelCase"` is a no-op for the single-word fields here but is
 * declared for forward-compat.
 *
 * IMPORTANT (utoipa v4): every concrete `ApiEnvelope<X>` that needs a named
 * OpenAPI component is declared in the `#[aliases(...)]` block below. Add a new
 * alias line whenever a new payload type needs enveloping. NEVER register the
 * bare `ApiEnvelope` in `components(schemas(...))` — utoipa errors on a bare
 * generic, and an un-aliased generic inlines an anonymous schema.
 */
export type UpgradeStatusEnvelope = {
    data: UpgradeStatusDto;
    /**
     * Server time when the response was built (unix epoch milliseconds).
     */
    ts: number;
};

export type WorkerStatusDto = {
    health: string;
    name: string;
};

/**
 * Error response sent via HTTP status + JSON body when the WebSocket upgrade fails.
 */
export type WsErrorResponse = {
    error: string;
    /**
     * Only present for rate-limit errors.
     */
    retryAfterSecs?: number | null;
};

/**
 * Request body sent by a client to subscribe to daemon event topics.
 */
export type WsSubscribeRequest = {
    action: string;
    topics: Array<string>;
};

export type CaptureUiEventData = {
    body: CaptureUiEventRequest;
    path?: never;
    query?: never;
    url: '/analytics/capture';
};

export type CaptureUiEventResponses = {
    /**
     * Event accepted for dispatch
     */
    200: CaptureUiEventEnvelope;
};

export type CaptureUiEventResponse2 = CaptureUiEventResponses[keyof CaptureUiEventResponses];

export type AuthConnectData = {
    body: ConnectRequest;
    path?: never;
    query?: never;
    url: '/auth/connect';
};

export type AuthConnectErrors = {
    /**
     * Malformed connect request
     */
    400: ApiErrorResponse;
    /**
     * Missing or invalid bearer token
     */
    401: ApiErrorResponse;
    /**
     * Too many requests from this client IP
     */
    429: ApiErrorResponse;
    /**
     * Failed to sign the session token
     */
    500: ApiErrorResponse;
};

export type AuthConnectError = AuthConnectErrors[keyof AuthConnectErrors];

export type AuthConnectResponses = {
    /**
     * JWT session token issued
     */
    200: SessionTokenEnvelope;
};

export type AuthConnectResponse = AuthConnectResponses[keyof AuthConnectResponses];

export type GetClipboardBlobData = {
    body?: never;
    path: {
        /**
         * Blob identifier
         */
        blob_id: string;
    };
    query?: never;
    url: '/clipboard/blobs/{blob_id}';
};

export type GetClipboardBlobErrors = {
    /**
     * Blob not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetClipboardBlobError = GetClipboardBlobErrors[keyof GetClipboardBlobErrors];

export type GetClipboardBlobResponses = {
    /**
     * Raw blob bytes
     */
    200: Blob | File;
};

export type GetClipboardBlobResponse = GetClipboardBlobResponses[keyof GetClipboardBlobResponses];

export type CancelClipboardTransferData = {
    body: CancelTransferRequest;
    path: {
        /**
         * Inbound transfer ID
         */
        transfer_id: string;
    };
    query?: never;
    url: '/clipboard/cancel-transfer/{transfer_id}';
};

export type CancelClipboardTransferErrors = {
    /**
     * Unknown cancellation reason
     */
    400: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type CancelClipboardTransferError = CancelClipboardTransferErrors[keyof CancelClipboardTransferErrors];

export type CancelClipboardTransferResponses = {
    /**
     * Transfer cancellation outcome
     */
    200: CancelTransferEnvelope;
};

export type CancelClipboardTransferResponse = CancelClipboardTransferResponses[keyof CancelClipboardTransferResponses];

export type DispatchClipboardTextData = {
    body: DispatchTextRequest;
    path?: never;
    query?: never;
    url: '/clipboard/dispatch';
};

export type DispatchClipboardTextErrors = {
    /**
     * Empty or malformed request
     */
    400: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Daemon is draining a controlled restart; retry against the successor
     */
    503: ApiErrorResponse;
};

export type DispatchClipboardTextError = DispatchClipboardTextErrors[keyof DispatchClipboardTextErrors];

export type DispatchClipboardTextResponses = {
    /**
     * Dispatch fan-out outcome
     */
    200: DispatchOutcomeEnvelope;
};

export type DispatchClipboardTextResponse = DispatchClipboardTextResponses[keyof DispatchClipboardTextResponses];

export type ListClipboardEntriesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Maximum entries to return (default 50, clamped to 1000).
         */
        limit?: number;
        /**
         * Number of entries to skip.
         */
        offset?: number;
    };
    url: '/clipboard/entries';
};

export type ListClipboardEntriesErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ListClipboardEntriesError = ListClipboardEntriesErrors[keyof ListClipboardEntriesErrors];

export type ListClipboardEntriesResponses = {
    /**
     * Clipboard entries listed
     */
    200: ListEntriesEnvelope;
};

export type ListClipboardEntriesResponse = ListClipboardEntriesResponses[keyof ListClipboardEntriesResponses];

export type ClearClipboardHistoryData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/clipboard/entries/clear';
};

export type ClearClipboardHistoryErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ClearClipboardHistoryError = ClearClipboardHistoryErrors[keyof ClearClipboardHistoryErrors];

export type ClearClipboardHistoryResponses = {
    /**
     * Clipboard history cleared
     */
    200: ClearHistoryEnvelope;
};

export type ClearClipboardHistoryResponse = ClearClipboardHistoryResponses[keyof ClearClipboardHistoryResponses];

export type DeleteClipboardEntryData = {
    body?: never;
    path: {
        /**
         * Entry ID
         */
        id: string;
    };
    query?: never;
    url: '/clipboard/entries/{id}';
};

export type DeleteClipboardEntryErrors = {
    /**
     * Entry not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type DeleteClipboardEntryError = DeleteClipboardEntryErrors[keyof DeleteClipboardEntryErrors];

export type DeleteClipboardEntryResponses = {
    /**
     * Entry deleted
     */
    204: void;
};

export type DeleteClipboardEntryResponse = DeleteClipboardEntryResponses[keyof DeleteClipboardEntryResponses];

export type GetClipboardEntryData = {
    body?: never;
    path: {
        /**
         * Entry ID
         */
        id: string;
    };
    query?: never;
    url: '/clipboard/entries/{id}';
};

export type GetClipboardEntryErrors = {
    /**
     * Entry not found
     */
    404: ApiErrorResponse;
    /**
     * Entry is not text content
     */
    422: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetClipboardEntryError = GetClipboardEntryErrors[keyof GetClipboardEntryErrors];

export type GetClipboardEntryResponses = {
    /**
     * Entry detail retrieved
     */
    200: EntryDetailEnvelope;
};

export type GetClipboardEntryResponse = GetClipboardEntryResponses[keyof GetClipboardEntryResponses];

export type GetClipboardEntryDeliveryData = {
    body?: never;
    path: {
        /**
         * Entry id
         */
        id: string;
    };
    query?: never;
    url: '/clipboard/entries/{id}/delivery';
};

export type GetClipboardEntryDeliveryErrors = {
    /**
     * Entry not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetClipboardEntryDeliveryError = GetClipboardEntryDeliveryErrors[keyof GetClipboardEntryDeliveryErrors];

export type GetClipboardEntryDeliveryResponses = {
    /**
     * Entry delivery view
     */
    200: EntryDeliveryViewEnvelope;
};

export type GetClipboardEntryDeliveryResponse = GetClipboardEntryDeliveryResponses[keyof GetClipboardEntryDeliveryResponses];

export type ToggleClipboardEntryFavoriteData = {
    body: ToggleFavoriteRequest;
    path: {
        /**
         * Entry ID
         */
        id: string;
    };
    query?: never;
    url: '/clipboard/entries/{id}/favorite';
};

export type ToggleClipboardEntryFavoriteErrors = {
    /**
     * Missing isFavorited field
     */
    400: ApiErrorResponse;
    /**
     * Entry not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ToggleClipboardEntryFavoriteError = ToggleClipboardEntryFavoriteErrors[keyof ToggleClipboardEntryFavoriteErrors];

export type ToggleClipboardEntryFavoriteResponses = {
    /**
     * Favorite state toggled
     */
    200: ToggleFavoriteEnvelope;
};

export type ToggleClipboardEntryFavoriteResponse = ToggleClipboardEntryFavoriteResponses[keyof ToggleClipboardEntryFavoriteResponses];

export type GetClipboardEntryFileData = {
    body?: never;
    path: {
        /**
         * Entry identifier
         */
        id: string;
    };
    query?: never;
    url: '/clipboard/entries/{id}/file';
};

export type GetClipboardEntryFileErrors = {
    /**
     * Entry or file not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetClipboardEntryFileError = GetClipboardEntryFileErrors[keyof GetClipboardEntryFileErrors];

export type GetClipboardEntryFileResponses = {
    /**
     * Raw bytes of the entry's first materialized file
     */
    200: Blob | File;
};

export type GetClipboardEntryFileResponse = GetClipboardEntryFileResponses[keyof GetClipboardEntryFileResponses];

export type GetClipboardEntryResourceData = {
    body?: never;
    path: {
        /**
         * Entry ID
         */
        id: string;
    };
    query?: never;
    url: '/clipboard/entries/{id}/resource';
};

export type GetClipboardEntryResourceErrors = {
    /**
     * Entry not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetClipboardEntryResourceError = GetClipboardEntryResourceErrors[keyof GetClipboardEntryResourceErrors];

export type GetClipboardEntryResourceResponses = {
    /**
     * Entry resource metadata retrieved
     */
    200: EntryResourceEnvelope;
};

export type GetClipboardEntryResourceResponse = GetClipboardEntryResourceResponses[keyof GetClipboardEntryResourceResponses];

export type ResendClipboardEntryData = {
    body: ResendRequest;
    path?: never;
    query?: never;
    url: '/clipboard/resend';
};

export type ResendClipboardEntryErrors = {
    /**
     * Malformed request
     */
    400: ApiErrorResponse;
    /**
     * Entry not found
     */
    404: ApiErrorResponse;
    /**
     * Entry not resendable / target not trusted / no eligible targets
     */
    409: ApiErrorResponse;
    /**
     * Storage or dispatch failure
     */
    500: ApiErrorResponse;
    /**
     * Daemon is draining a controlled restart; retry against the successor
     */
    503: ApiErrorResponse;
};

export type ResendClipboardEntryError = ResendClipboardEntryErrors[keyof ResendClipboardEntryErrors];

export type ResendClipboardEntryResponses = {
    /**
     * Resend fan-out outcome
     */
    200: ResendEnvelope;
};

export type ResendClipboardEntryResponse = ResendClipboardEntryResponses[keyof ResendClipboardEntryResponses];

export type RestoreClipboardEntryData = {
    body?: never;
    path: {
        /**
         * Clipboard entry id to restore
         */
        entry_id: string;
    };
    query?: {
        /**
         * Restore as plain text only (strip rich representations)
         */
        plain?: boolean | null;
    };
    url: '/clipboard/restore/{entry_id}';
};

export type RestoreClipboardEntryErrors = {
    /**
     * Entry not found
     */
    404: ApiErrorResponse;
    /**
     * Entry payload is no longer available (orphaned/lost)
     */
    410: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type RestoreClipboardEntryError = RestoreClipboardEntryErrors[keyof RestoreClipboardEntryErrors];

export type RestoreClipboardEntryResponses = {
    /**
     * Entry restored to the system clipboard
     */
    200: RestoreEntryEnvelope;
};

export type RestoreClipboardEntryResponse = RestoreClipboardEntryResponses[keyof RestoreClipboardEntryResponses];

export type GetClipboardStatsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/clipboard/stats';
};

export type GetClipboardStatsErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetClipboardStatsError = GetClipboardStatsErrors[keyof GetClipboardStatsErrors];

export type GetClipboardStatsResponses = {
    /**
     * Clipboard statistics retrieved
     */
    200: ClipboardStatsEnvelope;
};

export type GetClipboardStatsResponse = GetClipboardStatsResponses[keyof GetClipboardStatsResponses];

export type GetClipboardThumbnailData = {
    body?: never;
    path: {
        /**
         * Representation identifier
         */
        rep_id: string;
    };
    query?: never;
    url: '/clipboard/thumbnails/{rep_id}';
};

export type GetClipboardThumbnailErrors = {
    /**
     * Thumbnail not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetClipboardThumbnailError = GetClipboardThumbnailErrors[keyof GetClipboardThumbnailErrors];

export type GetClipboardThumbnailResponses = {
    /**
     * Raw thumbnail bytes
     */
    200: Blob | File;
};

export type GetClipboardThumbnailResponse = GetClipboardThumbnailResponses[keyof GetClipboardThumbnailResponses];

export type ExportConfigData = {
    body: ExportConfigRequest;
    path?: never;
    query?: never;
    url: '/config/export';
};

export type ExportConfigErrors = {
    /**
     * Source installation is not initialized
     */
    409: ApiErrorResponse;
    /**
     * Session is locked
     */
    423: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ExportConfigError = ExportConfigErrors[keyof ExportConfigErrors];

export type ExportConfigResponses = {
    /**
     * Configuration bundle written
     */
    200: ExportConfigEnvelope;
};

export type ExportConfigResponse2 = ExportConfigResponses[keyof ExportConfigResponses];

export type ImportConfigData = {
    body: ImportConfigRequest;
    path?: never;
    query?: never;
    url: '/config/import';
};

export type ImportConfigErrors = {
    /**
     * Confirmation missing/false, or invalid password / corrupt bundle
     */
    400: ApiErrorResponse;
    /**
     * Incompatible bundle
     */
    422: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ImportConfigError = ImportConfigErrors[keyof ImportConfigErrors];

export type ImportConfigResponses = {
    /**
     * Bundle staged for next restart
     */
    200: ImportConfigEnvelope;
};

export type ImportConfigResponse2 = ImportConfigResponses[keyof ImportConfigResponses];

export type PreviewConfigImportData = {
    body: PreviewImportRequest;
    path?: never;
    query?: never;
    url: '/config/import/preview';
};

export type PreviewConfigImportErrors = {
    /**
     * Invalid password or corrupt bundle
     */
    400: ApiErrorResponse;
    /**
     * Incompatible bundle
     */
    422: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type PreviewConfigImportError = PreviewConfigImportErrors[keyof PreviewConfigImportErrors];

export type PreviewConfigImportResponses = {
    /**
     * Bundle preview metadata
     */
    200: PreviewImportEnvelope;
};

export type PreviewConfigImportResponse = PreviewConfigImportResponses[keyof PreviewConfigImportResponses];

export type GetLocalDeviceInfoData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/device/me';
};

export type GetLocalDeviceInfoErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetLocalDeviceInfoError = GetLocalDeviceInfoErrors[keyof GetLocalDeviceInfoErrors];

export type GetLocalDeviceInfoResponses = {
    /**
     * Local device identity
     */
    200: LocalDeviceInfoEnvelope;
};

export type GetLocalDeviceInfoResponse = GetLocalDeviceInfoResponses[keyof GetLocalDeviceInfoResponses];

export type GetDebugStatusData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/diagnostics/debug';
};

export type GetDebugStatusErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetDebugStatusError = GetDebugStatusErrors[keyof GetDebugStatusErrors];

export type GetDebugStatusResponses = {
    /**
     * Current persistent debug-mode status
     */
    200: DebugStatusEnvelope;
};

export type GetDebugStatusResponse = GetDebugStatusResponses[keyof GetDebugStatusResponses];

export type UpdateDebugModeData = {
    body: UpdateDebugModeRequestDto;
    path?: never;
    query?: never;
    url: '/diagnostics/debug';
};

export type UpdateDebugModeErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type UpdateDebugModeError = UpdateDebugModeErrors[keyof UpdateDebugModeErrors];

export type UpdateDebugModeResponses = {
    /**
     * Debug mode persisted
     */
    200: UpdateDebugModeEnvelope;
};

export type UpdateDebugModeResponse = UpdateDebugModeResponses[keyof UpdateDebugModeResponses];

export type ExportLogsData = {
    body: LogExportRequestDto;
    path?: never;
    query?: never;
    url: '/diagnostics/log-export';
};

export type ExportLogsErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ExportLogsError = ExportLogsErrors[keyof ExportLogsErrors];

export type ExportLogsResponses = {
    /**
     * Logs exported to Downloads
     */
    200: LogExportEnvelope;
};

export type ExportLogsResponse = ExportLogsResponses[keyof ExportLogsResponses];

export type FactoryResetSpaceData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/encryption/factory-reset';
};

export type FactoryResetSpaceErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type FactoryResetSpaceError = FactoryResetSpaceErrors[keyof FactoryResetSpaceErrors];

export type FactoryResetSpaceResponses = {
    /**
     * Space reset to factory state
     */
    200: EncryptionActionEnvelope;
};

export type FactoryResetSpaceResponse = FactoryResetSpaceResponses[keyof FactoryResetSpaceResponses];

export type VerifyKeychainAccessData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/encryption/keychain-access';
};

export type VerifyKeychainAccessErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type VerifyKeychainAccessError = VerifyKeychainAccessErrors[keyof VerifyKeychainAccessErrors];

export type VerifyKeychainAccessResponses = {
    /**
     * Keychain access verified
     */
    200: KeychainAccessEnvelope;
};

export type VerifyKeychainAccessResponse = VerifyKeychainAccessResponses[keyof VerifyKeychainAccessResponses];

export type LockEncryptionSessionData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/encryption/lock';
};

export type LockEncryptionSessionErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type LockEncryptionSessionError = LockEncryptionSessionErrors[keyof LockEncryptionSessionErrors];

export type LockEncryptionSessionResponses = {
    /**
     * Encryption session locked
     */
    200: EncryptionActionEnvelope;
};

export type LockEncryptionSessionResponse = LockEncryptionSessionResponses[keyof LockEncryptionSessionResponses];

export type GetEncryptionStateData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/encryption/state';
};

export type GetEncryptionStateErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetEncryptionStateError = GetEncryptionStateErrors[keyof GetEncryptionStateErrors];

export type GetEncryptionStateResponses = {
    /**
     * Encryption state retrieved
     */
    200: EncryptionStateEnvelope;
};

export type GetEncryptionStateResponse = GetEncryptionStateResponses[keyof GetEncryptionStateResponses];

export type UnlockEncryptionSessionData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/encryption/unlock';
};

export type UnlockEncryptionSessionErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type UnlockEncryptionSessionError = UnlockEncryptionSessionErrors[keyof UnlockEncryptionSessionErrors];

export type UnlockEncryptionSessionResponses = {
    /**
     * Encryption session unlocked (or already ready)
     */
    200: EncryptionActionEnvelope;
};

export type UnlockEncryptionSessionResponse = UnlockEncryptionSessionResponses[keyof UnlockEncryptionSessionResponses];

export type UnlockSpaceWithPassphraseData = {
    body: UnlockSpaceRequest;
    path?: never;
    query?: never;
    url: '/encryption/unlock-with-passphrase';
};

export type UnlockSpaceWithPassphraseErrors = {
    /**
     * Wrong passphrase
     */
    403: ApiErrorResponse;
    /**
     * Setup not completed / space not initialized
     */
    409: ApiErrorResponse;
    /**
     * Space key material corrupted
     */
    422: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type UnlockSpaceWithPassphraseError = UnlockSpaceWithPassphraseErrors[keyof UnlockSpaceWithPassphraseErrors];

export type UnlockSpaceWithPassphraseResponses = {
    /**
     * Space unlocked
     */
    200: UnlockSpaceEnvelope;
};

export type UnlockSpaceWithPassphraseResponse = UnlockSpaceWithPassphraseResponses[keyof UnlockSpaceWithPassphraseResponses];

export type GetHealthData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/health';
};

export type GetHealthResponses = {
    /**
     * Daemon is alive
     */
    200: HealthEnvelope;
};

export type GetHealthResponse = GetHealthResponses[keyof GetHealthResponses];

export type SignalLifecycleReadyData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/lifecycle/ready';
};

export type SignalLifecycleReadyResponses = {
    /**
     * Ready signal accepted; clipboard capture gate opened
     */
    204: void;
};

export type SignalLifecycleReadyResponse = SignalLifecycleReadyResponses[keyof SignalLifecycleReadyResponses];

export type RequestLifecycleRestartData = {
    body: RestartRequest;
    path?: never;
    query?: never;
    url: '/lifecycle/restart';
};

export type RequestLifecycleRestartErrors = {
    /**
     * Invalid target mode (cannot promote to a transient target)
     */
    400: ApiErrorResponse;
    /**
     * Restart unavailable (already in progress / not a transient daemon / single-instance disabled)
     */
    409: ApiErrorResponse;
};

export type RequestLifecycleRestartError = RequestLifecycleRestartErrors[keyof RequestLifecycleRestartErrors];

export type RequestLifecycleRestartResponses = {
    /**
     * Controlled restart accepted; quiescing/drain started
     */
    202: RestartAcceptedEnvelope;
};

export type RequestLifecycleRestartResponse = RequestLifecycleRestartResponses[keyof RequestLifecycleRestartResponses];

export type RetryLifecycleData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/lifecycle/retry';
};

export type RetryLifecycleErrors = {
    /**
     * Lifecycle retry failed
     */
    500: ApiErrorResponse;
    /**
     * Daemon runtime unavailable
     */
    503: ApiErrorResponse;
};

export type RetryLifecycleError = RetryLifecycleErrors[keyof RetryLifecycleErrors];

export type RetryLifecycleResponses = {
    /**
     * Retry completed; lifecycle advanced to ready
     */
    204: void;
};

export type RetryLifecycleResponse = RetryLifecycleResponses[keyof RetryLifecycleResponses];

export type GetLifecycleStatusData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/lifecycle/status';
};

export type GetLifecycleStatusErrors = {
    /**
     * Daemon runtime unavailable
     */
    503: ApiErrorResponse;
};

export type GetLifecycleStatusError = GetLifecycleStatusErrors[keyof GetLifecycleStatusErrors];

export type GetLifecycleStatusResponses = {
    /**
     * Current lifecycle state
     */
    200: LifecycleStatusEnvelope;
};

export type GetLifecycleStatusResponse = GetLifecycleStatusResponses[keyof GetLifecycleStatusResponses];

export type GetMemberSyncPreferencesData = {
    body?: never;
    path: {
        /**
         * Space member's device ID (same string as peer_id, D5)
         */
        device_id: string;
    };
    query?: never;
    url: '/member/{device_id}/sync-preferences';
};

export type GetMemberSyncPreferencesErrors = {
    /**
     * Member not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetMemberSyncPreferencesError = GetMemberSyncPreferencesErrors[keyof GetMemberSyncPreferencesErrors];

export type GetMemberSyncPreferencesResponses = {
    200: MemberSyncPreferencesEnvelope;
};

export type GetMemberSyncPreferencesResponse = GetMemberSyncPreferencesResponses[keyof GetMemberSyncPreferencesResponses];

export type UpdateMemberSyncPreferencesData = {
    body: MemberSyncPreferencesPatchDto;
    path: {
        /**
         * Space member's device ID
         */
        device_id: string;
    };
    query?: never;
    url: '/member/{device_id}/sync-preferences';
};

export type UpdateMemberSyncPreferencesErrors = {
    /**
     * Invalid request
     */
    400: ApiErrorResponse;
    /**
     * Member not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type UpdateMemberSyncPreferencesError = UpdateMemberSyncPreferencesErrors[keyof UpdateMemberSyncPreferencesErrors];

export type UpdateMemberSyncPreferencesResponses = {
    200: MemberSyncResultEnvelope;
};

export type UpdateMemberSyncPreferencesResponse = UpdateMemberSyncPreferencesResponses[keyof UpdateMemberSyncPreferencesResponses];

export type ListMobileDevicesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/mobile-sync/devices';
};

export type ListMobileDevicesErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Mobile sync facade unavailable
     */
    503: ApiErrorResponse;
};

export type ListMobileDevicesError = ListMobileDevicesErrors[keyof ListMobileDevicesErrors];

export type ListMobileDevicesResponses = {
    /**
     * Registered devices
     */
    200: MobileDeviceListEnvelope;
};

export type ListMobileDevicesResponse = ListMobileDevicesResponses[keyof ListMobileDevicesResponses];

export type RegisterMobileDeviceData = {
    body: RegisterMobileDeviceRequest;
    path?: never;
    query?: never;
    url: '/mobile-sync/devices';
};

export type RegisterMobileDeviceErrors = {
    /**
     * Username taken / LAN listener disabled
     */
    409: ApiErrorResponse;
    /**
     * Invalid label / username / password
     */
    422: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Mobile sync facade unavailable
     */
    503: ApiErrorResponse;
};

export type RegisterMobileDeviceError = RegisterMobileDeviceErrors[keyof RegisterMobileDeviceErrors];

export type RegisterMobileDeviceResponses = {
    /**
     * Device registered (one-time password echo)
     */
    200: RegisterMobileDeviceEnvelope;
};

export type RegisterMobileDeviceResponse = RegisterMobileDeviceResponses[keyof RegisterMobileDeviceResponses];

export type RevokeMobileDeviceData = {
    body?: never;
    path: {
        /**
         * Mobile device id
         */
        device_id: string;
    };
    query?: never;
    url: '/mobile-sync/devices/{device_id}';
};

export type RevokeMobileDeviceErrors = {
    /**
     * Device not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Mobile sync facade unavailable
     */
    503: ApiErrorResponse;
};

export type RevokeMobileDeviceError = RevokeMobileDeviceErrors[keyof RevokeMobileDeviceErrors];

export type RevokeMobileDeviceResponses = {
    /**
     * Device revoked
     */
    200: MobileSyncActionEnvelope;
};

export type RevokeMobileDeviceResponse = RevokeMobileDeviceResponses[keyof RevokeMobileDeviceResponses];

export type UpdateMobileDeviceData = {
    body: UpdateMobileDeviceRequest;
    path: {
        /**
         * Mobile device id
         */
        device_id: string;
    };
    query?: never;
    url: '/mobile-sync/devices/{device_id}';
};

export type UpdateMobileDeviceErrors = {
    /**
     * Device not found
     */
    404: ApiErrorResponse;
    /**
     * Username taken
     */
    409: ApiErrorResponse;
    /**
     * Invalid label / username / password
     */
    422: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Mobile sync facade unavailable
     */
    503: ApiErrorResponse;
};

export type UpdateMobileDeviceError = UpdateMobileDeviceErrors[keyof UpdateMobileDeviceErrors];

export type UpdateMobileDeviceResponses = {
    /**
     * Device updated; password is present only when reissued
     */
    200: UpdateMobileDeviceEnvelope;
};

export type UpdateMobileDeviceResponse = UpdateMobileDeviceResponses[keyof UpdateMobileDeviceResponses];

export type RotateMobilePasswordData = {
    body: RotateMobilePasswordRequest;
    path: {
        /**
         * Mobile device id
         */
        device_id: string;
    };
    query?: never;
    url: '/mobile-sync/devices/{device_id}/rotate-password';
};

export type RotateMobilePasswordErrors = {
    /**
     * Device not found
     */
    404: ApiErrorResponse;
    /**
     * Invalid password
     */
    422: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Mobile sync facade unavailable
     */
    503: ApiErrorResponse;
};

export type RotateMobilePasswordError = RotateMobilePasswordErrors[keyof RotateMobilePasswordErrors];

export type RotateMobilePasswordResponses = {
    /**
     * Password rotated (one-time echo)
     */
    200: RotateMobilePasswordEnvelope;
};

export type RotateMobilePasswordResponse = RotateMobilePasswordResponses[keyof RotateMobilePasswordResponses];

export type ListMobileLanInterfacesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/mobile-sync/lan-interfaces';
};

export type ListMobileLanInterfacesErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Mobile sync facade unavailable
     */
    503: ApiErrorResponse;
};

export type ListMobileLanInterfacesError = ListMobileLanInterfacesErrors[keyof ListMobileLanInterfacesErrors];

export type ListMobileLanInterfacesResponses = {
    /**
     * Usable IPv4 LAN interfaces
     */
    200: LanInterfaceListEnvelope;
};

export type ListMobileLanInterfacesResponse = ListMobileLanInterfacesResponses[keyof ListMobileLanInterfacesResponses];

export type GetMobileSyncSettingsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/mobile-sync/settings';
};

export type GetMobileSyncSettingsErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Mobile sync facade unavailable
     */
    503: ApiErrorResponse;
};

export type GetMobileSyncSettingsError = GetMobileSyncSettingsErrors[keyof GetMobileSyncSettingsErrors];

export type GetMobileSyncSettingsResponses = {
    /**
     * Mobile sync settings view
     */
    200: MobileSyncSettingsEnvelope;
};

export type GetMobileSyncSettingsResponse = GetMobileSyncSettingsResponses[keyof GetMobileSyncSettingsResponses];

export type UpdateMobileSyncSettingsData = {
    body: UpdateMobileSyncSettingsRequest;
    path?: never;
    query?: never;
    url: '/mobile-sync/settings';
};

export type UpdateMobileSyncSettingsErrors = {
    /**
     * Invalid LAN parameter
     */
    422: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Mobile sync facade unavailable
     */
    503: ApiErrorResponse;
};

export type UpdateMobileSyncSettingsError = UpdateMobileSyncSettingsErrors[keyof UpdateMobileSyncSettingsErrors];

export type UpdateMobileSyncSettingsResponses = {
    /**
     * Settings updated
     */
    200: UpdateMobileSyncSettingsEnvelope;
};

export type UpdateMobileSyncSettingsResponse = UpdateMobileSyncSettingsResponses[keyof UpdateMobileSyncSettingsResponses];

export type ListPairedDevicesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/paired-devices';
};

export type ListPairedDevicesErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ListPairedDevicesError = ListPairedDevicesErrors[keyof ListPairedDevicesErrors];

export type ListPairedDevicesResponses = {
    /**
     * Paired space-member list
     */
    200: SpaceMemberListEnvelope;
};

export type ListPairedDevicesResponse = ListPairedDevicesResponses[keyof ListPairedDevicesResponses];

export type UnpairDeviceData = {
    body: UnpairDeviceRequest;
    path?: never;
    query?: never;
    url: '/pairing/unpair';
};

export type UnpairDeviceErrors = {
    /**
     * Member not found
     */
    404: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Runtime unavailable
     */
    503: ApiErrorResponse;
};

export type UnpairDeviceError = UnpairDeviceErrors[keyof UnpairDeviceErrors];

export type UnpairDeviceResponses = {
    /**
     * Device unpaired (no body)
     */
    204: void;
};

export type UnpairDeviceResponse = UnpairDeviceResponses[keyof UnpairDeviceResponses];

export type ListPeersData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/peers';
};

export type ListPeersErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ListPeersError = ListPeersErrors[keyof ListPeersErrors];

export type ListPeersResponses = {
    /**
     * Peer snapshot list
     */
    200: PeerSnapshotListEnvelope;
};

export type ListPeersResponse = ListPeersResponses[keyof ListPeersResponses];

export type RefreshPresenceData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/presence/refresh';
};

export type RefreshPresenceErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type RefreshPresenceError = RefreshPresenceErrors[keyof RefreshPresenceErrors];

export type RefreshPresenceResponses = {
    /**
     * Presence refresh round completed
     */
    200: PresenceRefreshEnvelope;
};

export type RefreshPresenceResponse = RefreshPresenceResponses[keyof RefreshPresenceResponses];

export type SearchQueryData = {
    body?: never;
    path?: never;
    query: {
        /**
         * Required free-text query string.
         */
        query: string;
        /**
         * Optional explicit operator: "and" or "or".
         */
        operator?: string | null;
        /**
         * Optional time preset: today, yesterday, last_24h, last_7d, last_30d, this_week, this_month.
         */
        timePreset?: string | null;
        /**
         * Absolute range start (ms since epoch). Must be paired with `to_ms`.
         */
        fromMs?: number | null;
        /**
         * Absolute range end (ms since epoch). Must be paired with `from_ms`.
         */
        toMs?: number | null;
        /**
         * Comma-separated file types (text, html, link, file, image, other).
         */
        contentTypes?: string | null;
        /**
         * Comma-separated file extensions (e.g. "md,txt").
         */
        extensions?: string | null;
        /**
         * Maximum results. Default 50, clamped to 200.
         */
        limit?: number;
        /**
         * Pagination offset. Default 0.
         */
        offset?: number;
    };
    url: '/search/query';
};

export type SearchQueryErrors = {
    /**
     * Invalid or malformed query
     */
    400: ApiErrorResponse;
    /**
     * Encryption session is locked
     */
    423: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Search index not ready or unavailable
     */
    503: ApiErrorResponse;
};

export type SearchQueryError = SearchQueryErrors[keyof SearchQueryErrors];

export type SearchQueryResponses = {
    /**
     * Search results page
     */
    200: SearchQueryEnvelope;
};

export type SearchQueryResponse = SearchQueryResponses[keyof SearchQueryResponses];

export type RebuildSearchIndexData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/search/rebuild';
};

export type RebuildSearchIndexErrors = {
    /**
     * A rebuild is already in progress
     */
    409: ApiErrorResponse;
    /**
     * Encryption session is locked
     */
    423: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Search index unavailable
     */
    503: ApiErrorResponse;
};

export type RebuildSearchIndexError = RebuildSearchIndexErrors[keyof RebuildSearchIndexErrors];

export type RebuildSearchIndexResponses = {
    /**
     * Rebuild accepted
     */
    202: SearchRebuildEnvelope;
};

export type RebuildSearchIndexResponse = RebuildSearchIndexResponses[keyof RebuildSearchIndexResponses];

export type GetSearchStatusData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/search/status';
};

export type GetSearchStatusErrors = {
    /**
     * Encryption session is locked
     */
    423: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
    /**
     * Search index unavailable
     */
    503: ApiErrorResponse;
};

export type GetSearchStatusError = GetSearchStatusErrors[keyof GetSearchStatusErrors];

export type GetSearchStatusResponses = {
    /**
     * Search index availability snapshot
     */
    200: SearchStatusEnvelope;
};

export type GetSearchStatusResponse = GetSearchStatusResponses[keyof GetSearchStatusResponses];

export type GetSettingsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/settings';
};

export type GetSettingsErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetSettingsError = GetSettingsErrors[keyof GetSettingsErrors];

export type GetSettingsResponses = {
    /**
     * Current application settings
     */
    200: SettingsEnvelope;
};

export type GetSettingsResponse = GetSettingsResponses[keyof GetSettingsResponses];

export type UpdateSettingsData = {
    body: SettingsPatchDto;
    path?: never;
    query?: never;
    url: '/settings';
};

export type UpdateSettingsErrors = {
    /**
     * Invalid request
     */
    400: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type UpdateSettingsError = UpdateSettingsErrors[keyof UpdateSettingsErrors];

export type UpdateSettingsResponses = {
    /**
     * Settings persisted; carries success + restart-required signal
     */
    200: SettingsUpdateResultEnvelope;
};

export type UpdateSettingsResponse = UpdateSettingsResponses[keyof UpdateSettingsResponses];

export type ProbeRelayUrlData = {
    body: RelayProbeRequestDto;
    path?: never;
    query?: never;
    url: '/settings/relay-probe';
};

export type ProbeRelayUrlErrors = {
    /**
     * Relay-diagnostic adapter unavailable / internal error
     */
    500: ApiErrorResponse;
};

export type ProbeRelayUrlError = ProbeRelayUrlErrors[keyof ProbeRelayUrlErrors];

export type ProbeRelayUrlResponses = {
    /**
     * Relay probe outcome (reachable or a categorized failure)
     */
    200: RelayProbeOutcomeEnvelope;
};

export type ProbeRelayUrlResponse = ProbeRelayUrlResponses[keyof ProbeRelayUrlResponses];

export type GetStatusData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/status';
};

export type GetStatusResponses = {
    /**
     * Daemon status snapshot
     */
    200: StatusEnvelope;
};

export type GetStatusResponse = GetStatusResponses[keyof GetStatusResponses];

export type ClearStorageCacheData = {
    body: ClearCacheRequest;
    path?: never;
    query?: never;
    url: '/storage/clear-cache';
};

export type ClearStorageCacheErrors = {
    /**
     * Confirmation missing or false
     */
    400: ApiErrorResponse;
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type ClearStorageCacheError = ClearStorageCacheErrors[keyof ClearStorageCacheErrors];

export type ClearStorageCacheResponses = {
    /**
     * Cache cleared
     */
    200: ClearCacheEnvelope;
};

export type ClearStorageCacheResponse = ClearStorageCacheResponses[keyof ClearStorageCacheResponses];

export type GetStorageStatsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/storage/stats';
};

export type GetStorageStatsErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetStorageStatsError = GetStorageStatsErrors[keyof GetStorageStatsErrors];

export type GetStorageStatsResponses = {
    /**
     * Storage statistics retrieved
     */
    200: StorageStatsEnvelope;
};

export type GetStorageStatsResponse = GetStorageStatsResponses[keyof GetStorageStatsResponses];

export type AcknowledgeUpgradeData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/upgrade/ack';
};

export type AcknowledgeUpgradeErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type AcknowledgeUpgradeError = AcknowledgeUpgradeErrors[keyof AcknowledgeUpgradeErrors];

export type AcknowledgeUpgradeResponses = {
    /**
     * Upgrade acknowledged
     */
    200: AckUpgradeEnvelope;
};

export type AcknowledgeUpgradeResponse = AcknowledgeUpgradeResponses[keyof AcknowledgeUpgradeResponses];

export type GetUpgradeStatusData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/upgrade/status';
};

export type GetUpgradeStatusErrors = {
    /**
     * Internal server error
     */
    500: ApiErrorResponse;
};

export type GetUpgradeStatusError = GetUpgradeStatusErrors[keyof GetUpgradeStatusErrors];

export type GetUpgradeStatusResponses = {
    /**
     * Upgrade status detected
     */
    200: UpgradeStatusEnvelope;
};

export type GetUpgradeStatusResponse = GetUpgradeStatusResponses[keyof GetUpgradeStatusResponses];

export type SetupV2CancelData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/v2/setup/cancel';
};

export type SetupV2CancelErrors = {
    /**
     * No in-flight invitation to cancel
     */
    409: ApiErrorResponse;
    /**
     * Internal error
     */
    500: ApiErrorResponse;
    /**
     * Facade not assembled
     */
    503: ApiErrorResponse;
};

export type SetupV2CancelError = SetupV2CancelErrors[keyof SetupV2CancelErrors];

export type SetupV2CancelResponses = {
    /**
     * Invitation cancelled
     */
    204: void;
};

export type SetupV2CancelResponse = SetupV2CancelResponses[keyof SetupV2CancelResponses];

export type SetupV2InitializeData = {
    body: InitializeSpaceRequest;
    path?: never;
    query?: never;
    url: '/v2/setup/initialize';
};

export type SetupV2InitializeErrors = {
    /**
     * Passphrase mismatch or device name missing
     */
    400: ApiErrorResponse;
    /**
     * Setup already completed
     */
    409: ApiErrorResponse;
    /**
     * Internal error
     */
    500: ApiErrorResponse;
    /**
     * Facade not assembled
     */
    503: ApiErrorResponse;
};

export type SetupV2InitializeError = SetupV2InitializeErrors[keyof SetupV2InitializeErrors];

export type SetupV2InitializeResponses = {
    /**
     * Space initialised
     */
    200: SetupInitializeEnvelope;
};

export type SetupV2InitializeResponse = SetupV2InitializeResponses[keyof SetupV2InitializeResponses];

export type SetupV2IssueInvitationData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/v2/setup/issue-invitation';
};

export type SetupV2IssueInvitationErrors = {
    /**
     * Internal error
     */
    500: ApiErrorResponse;
    /**
     * Facade not assembled or network not started
     */
    503: ApiErrorResponse;
};

export type SetupV2IssueInvitationError = SetupV2IssueInvitationErrors[keyof SetupV2IssueInvitationErrors];

export type SetupV2IssueInvitationResponses = {
    /**
     * Invitation issued
     */
    200: SetupIssueInvitationEnvelope;
};

export type SetupV2IssueInvitationResponse = SetupV2IssueInvitationResponses[keyof SetupV2IssueInvitationResponses];

export type SetupV2GetMigrationProgressData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/v2/setup/migration-progress';
};

export type SetupV2GetMigrationProgressErrors = {
    /**
     * Storage failure
     */
    500: ApiErrorResponse;
    /**
     * Facade not assembled
     */
    503: ApiErrorResponse;
};

export type SetupV2GetMigrationProgressError = SetupV2GetMigrationProgressErrors[keyof SetupV2GetMigrationProgressErrors];

export type SetupV2GetMigrationProgressResponses = {
    /**
     * Migration progress snapshot
     */
    200: SetupMigrationProgressEnvelope;
};

export type SetupV2GetMigrationProgressResponse = SetupV2GetMigrationProgressResponses[keyof SetupV2GetMigrationProgressResponses];

export type SetupV2RedeemData = {
    body: RedeemRequest;
    path?: never;
    query?: never;
    url: '/v2/setup/redeem';
};

export type SetupV2RedeemErrors = {
    /**
     * Invalid request
     */
    400: ApiErrorResponse;
    /**
     * Invitation not found / expired
     */
    404: ApiErrorResponse;
    /**
     * Internal error
     */
    500: ApiErrorResponse;
    /**
     * Sponsor unreachable / service unavailable
     */
    503: ApiErrorResponse;
};

export type SetupV2RedeemError = SetupV2RedeemErrors[keyof SetupV2RedeemErrors];

export type SetupV2RedeemResponses = {
    /**
     * Invitation redeemed
     */
    200: SetupRedeemEnvelope;
};

export type SetupV2RedeemResponse = SetupV2RedeemResponses[keyof SetupV2RedeemResponses];

export type SetupV2ResetData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/v2/setup/reset';
};

export type SetupV2ResetErrors = {
    /**
     * Storage failure
     */
    500: ApiErrorResponse;
    /**
     * Facade not assembled
     */
    503: ApiErrorResponse;
};

export type SetupV2ResetError = SetupV2ResetErrors[keyof SetupV2ResetErrors];

export type SetupV2ResetResponses = {
    /**
     * Setup reset
     */
    204: void;
};

export type SetupV2ResetResponse = SetupV2ResetResponses[keyof SetupV2ResetResponses];

export type SetupV2GetStateData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/v2/setup/state';
};

export type SetupV2GetStateErrors = {
    /**
     * Storage failure
     */
    500: ApiErrorResponse;
    /**
     * Facade not assembled
     */
    503: ApiErrorResponse;
};

export type SetupV2GetStateError = SetupV2GetStateErrors[keyof SetupV2GetStateErrors];

export type SetupV2GetStateResponses = {
    /**
     * Setup state snapshot
     */
    200: SetupStateEnvelope;
};

export type SetupV2GetStateResponse = SetupV2GetStateResponses[keyof SetupV2GetStateResponses];

export type SetupV2SwitchSpaceData = {
    body: SwitchSpaceRequest;
    path?: never;
    query?: never;
    url: '/v2/setup/switch-space';
};

export type SetupV2SwitchSpaceErrors = {
    /**
     * Wrong passphrase / device name missing
     */
    400: ApiErrorResponse;
    /**
     * Invitation not found / expired
     */
    404: ApiErrorResponse;
    /**
     * Not setup, pending migration, sponsor rejected, or session locked
     */
    409: ApiErrorResponse;
    /**
     * Internal error / corrupted ciphertext / storage failure
     */
    500: ApiErrorResponse;
    /**
     * Sponsor unreachable / service unavailable
     */
    503: ApiErrorResponse;
};

export type SetupV2SwitchSpaceError = SetupV2SwitchSpaceErrors[keyof SetupV2SwitchSpaceErrors];

export type SetupV2SwitchSpaceResponses = {
    /**
     * Switched space
     */
    200: SetupSwitchSpaceEnvelope;
};

export type SetupV2SwitchSpaceResponse = SetupV2SwitchSpaceResponses[keyof SetupV2SwitchSpaceResponses];

export type WebsocketUpgradeData = {
    body?: never;
    path?: never;
    query: {
        /**
         * JWT session token prefixed with 'Session '. Used when the client cannot set custom headers (e.g., browser WebSocket). Native clients may instead send the 'Authorization: Session <jwt>' header.
         */
        auth: string;
    };
    url: '/ws';
};

export type WebsocketUpgradeErrors = {
    /**
     * Missing or invalid session token
     */
    401: WsErrorResponse;
    /**
     * PID not allowed
     */
    403: WsErrorResponse;
    /**
     * Rate limit exceeded
     */
    429: WsErrorResponse;
};

export type WebsocketUpgradeError = WebsocketUpgradeErrors[keyof WebsocketUpgradeErrors];
