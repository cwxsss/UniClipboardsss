## 0.10.0-alpha.10 - 2026-05-17

### Fixes

- Fixed Snap builds opening a localhost connection error instead of loading the app
- Fixed Snap startup failures when the system keyring is unavailable, so the app can still start and keep its identity data in the app data directory until keyring access works
- Corrected Snap installation guidance to use the alpha edge channel and include the keyring permission step
