## 0.16.0-alpha.1 - 2026-06-16

### Features

- Add Homebrew 6.0+ trust instructions so macOS Homebrew installs no longer fail at the untrusted tap step (#1078)

### Fixes

- Prevent the same copied image from being repeatedly saved and synced on some Linux desktops, and keep the storage limit enforced during long sessions (#1083)
- Restore Snap package builds so Linux users can install the Snap package again (#1075)
