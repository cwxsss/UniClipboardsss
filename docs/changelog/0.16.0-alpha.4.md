## 0.16.0-alpha.4 - 2026-06-18

### Features

- Rename the mobile sync CLI command to `uniclip mobile` and make setup easier by automatically including detected network addresses in the QR code (#1099)

### Fixes

- Keep the Debug mode restart dialog open with a waiting state while the app and background daemon restart (#1102)
- Prevent repeated duplicate image captures on Linux when using VirtualBox shared clipboard (#1105)
