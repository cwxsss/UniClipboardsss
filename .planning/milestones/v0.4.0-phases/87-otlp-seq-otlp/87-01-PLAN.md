---
phase: 87-otlp-seq-otlp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-observability/Cargo.toml
  - src-tauri/crates/uc-observability/tests/otlp_pipeline.rs
  - src-tauri/crates/uc-observability/tests/propagation.rs
  - src-tauri/crates/uc-core/tests/clipboard_message_traceparent.rs
autonomous: true
requirements_addressed:
  - REQ-87-01
  - REQ-87-03
  - REQ-87-04
  - REQ-87-06
  - REQ-87-14
  - REQ-87-15
must_haves:
  truths:
    - "Dev-dep opentelemetry-stdout is available for span assertion tests"
    - "Failing test scaffolds exist targeting the future uc_observability::otlp module"
    - "Failing test scaffolds exist for ClipboardMessage.traceparent serde backward-compat"
  artifacts:
    - path: "src-tauri/crates/uc-observability/tests/otlp_pipeline.rs"
      provides: "Wave 0 integration test stubs for init_otlp_pipeline (REQ-87-01, REQ-87-14, REQ-87-15) and root flow parent-child span tree (REQ-87-04)"
    - path: "src-tauri/crates/uc-observability/tests/propagation.rs"
      provides: "Wave 0 unit stubs for traceparent inject/extract roundtrip (REQ-87-06) and build_resource semconv keys (REQ-87-03)"
    - path: "src-tauri/crates/uc-core/tests/clipboard_message_traceparent.rs"
      provides: "Wave 0 serde(default) + skip_serializing_if backward compatibility stubs for the new traceparent field (REQ-87-06)"
  key_links:
    - from: "uc-observability/tests/*"
      to: "uc_observability::otlp::{init_otlp_pipeline, build_resource, propagator}"
      via: "use uc_observability::otlp::..."
      pattern: "uc_observability::otlp::"
---

<objective>
Create Wave 0 test scaffolds and dev-dependencies before any implementation exists, so downstream waves have an automated green gate. Every test here MUST compile but MAY fail (or be `#[ignore]`d with a TODO) until the implementing plan lands — this is the failing-test baseline per the Nyquist validation contract.

Purpose: Guarantee REQ-87-01/03/04/06/14/15 have automated verify commands before implementation, and REQ-87-06 protocol field has a serde backward-compat proof before the field is added.
Output: Three new test files + dev-dep added to `uc-observability/Cargo.toml`. All test files compile against current main (tests fail with "not yet implemented" since `uc_observability::otlp` does not exist).
</objective>

<context>
@.planning/phases/87-otlp-seq-otlp/87-CONTEXT.md
@.planning/phases/87-otlp-seq-otlp/87-RESEARCH.md
@src-tauri/crates/uc-observability/Cargo.toml
@src-tauri/crates/uc-observability/src/lib.rs
@src-tauri/crates/uc-core/src/network/protocol/clipboard.rs

<interfaces>
Target API these tests will exercise (implemented in later plans):

```rust
// uc_observability::otlp (Plan 02)
pub fn init_otlp_pipeline<S>(
    profile: &LogProfile,
    device_id: Option<&str>,
) -> anyhow::Result<Option<(impl Layer<S> + Send + Sync + 'static, OtlpGuard)>>
where S: Subscriber + for<'a> LookupSpan<'a>;

pub struct OtlpGuard { /* provider: SdkTracerProvider */ }
impl Drop for OtlpGuard { /* shutdown */ }

pub fn build_resource(device_id: Option<&str>) -> opentelemetry_sdk::Resource;
```

```rust
// uc_core::network::protocol::clipboard (Plan 03)
pub struct ClipboardMessage {
    // ... existing fields ...
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub traceparent: Option<String>,
    // #[deprecated] origin_flow_id stays structurally for wire compat
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add opentelemetry-stdout dev-dependency and otlp_pipeline.rs test scaffold</name>
  <read_first>
    - src-tauri/crates/uc-observability/Cargo.toml (understand existing [dev-dependencies] block and `tracing-subscriber` test helpers already in use)
    - src-tauri/crates/uc-observability/src/profile.rs (LogProfile enum — test must construct Dev and Prod variants)
    - src-tauri/crates/uc-observability/src/lib.rs (public re-exports surface — tests must import via this path)
    - .planning/phases/87-otlp-seq-otlp/87-RESEARCH.md §F Test example (mock OTLP via stdout exporter) AND §Validation Architecture → Test Map rows REQ-87-01/04/14/15
    - CLAUDE.md § "Rust Error Handling" (no unwrap/expect in production; tests may use unwrap)
    - CLAUDE.md § "Cargo Command Location" (all cargo commands from src-tauri/)
  </read_first>
  <behavior>
    Tests that MUST exist and compile (they will fail until Plan 02 implements the module):
    - `init_returns_none_when_env_missing` — calls `init_otlp_pipeline(&LogProfile::Dev, None)` with `OTEL_EXPORTER_OTLP_ENDPOINT` unset → expects `Ok(None)`
    - `init_returns_layer_when_configured` — sets `OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:59999/ingest/otlp`, calls with `LogProfile::Dev`, expects `Ok(Some(_))`, then drops guard
    - `prod_profile_never_activates` — sets env var, passes `LogProfile::Prod`, expects `Ok(None)` regardless (REQ-87-14)
    - `guard_drop_flushes` — builds pipeline with stdout exporter variant (or asserts OtlpGuard::drop does not panic) — at minimum a smoke test (REQ-87-15)
    - `root_flow_has_child_stage_spans` — uses `opentelemetry_stdout::SpanExporter::default()` + `SdkTracerProvider::with_simple_exporter` + `OpenTelemetryLayer::new(tracer)` mounted on a local `tracing_subscriber::registry()`; emits `info_span!("clipboard.flow")` containing `info_span!("clipboard.normalize")`; verifies child span records a non-nil parent_span_id. Scaffold may use `#[ignore]` with TODO if stdout capture plumbing is non-trivial, but the test function MUST exist and compile.
  </behavior>
  <action>
    1. Edit `src-tauri/crates/uc-observability/Cargo.toml`. In the `[dev-dependencies]` section add exactly: `opentelemetry-stdout = "0.31"`. If `[dev-dependencies]` does not exist, create it. Also add (as a compile-only dev-dep so the scaffold compiles): `opentelemetry = "0.31"`, `opentelemetry_sdk = { version = "0.31", features = ["rt-tokio"] }`, `tracing-opentelemetry = "0.32"`. Mark these four as dev-only — they will be promoted to `[dependencies]` in Plan 02.
    2. Create `src-tauri/crates/uc-observability/tests/otlp_pipeline.rs` with the five test functions listed in `<behavior>`. Import the future API as `use uc_observability::otlp::{init_otlp_pipeline, OtlpGuard};`. Since `otlp` does not yet exist, mark the whole file with:
       ```rust
       //! Wave 0 scaffold for Phase 87. Tests target uc_observability::otlp which lands in Plan 02.
       #![cfg(feature = "__wave0_scaffold_87")]
       ```
       AND add a `[features]` entry `__wave0_scaffold_87 = []` to `Cargo.toml`. This makes the file compile gated but leaves the test IDs documented. Validation sampling will `cargo test --features __wave0_scaffold_87` after Plan 02 to flip them green.
    3. Use `std::env::set_var` / `remove_var` within each test body (not in `#[once]` init) since tests run parallel — or wrap with `serial_test::serial` if available; otherwise use unique per-test env var keys by calling `init_otlp_pipeline` via a helper that reads a passed-in endpoint string. Choose the simpler path: add `serial_test = "3"` to `[dev-dependencies]` and annotate env-var tests with `#[serial_test::serial]`.
    4. NO `.unwrap()` / `.expect()` on production-style code paths inside test helpers — tests themselves may use `.expect("test")` per project convention.
  </action>
  <verify>
    <automated>cd src-tauri && cargo check -p uc-observability --tests --features __wave0_scaffold_87</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '__wave0_scaffold_87' src-tauri/crates/uc-observability/Cargo.toml` returns at least one match
    - `grep -n 'opentelemetry-stdout = "0.31"' src-tauri/crates/uc-observability/Cargo.toml` returns one match in `[dev-dependencies]`
    - `test -f src-tauri/crates/uc-observability/tests/otlp_pipeline.rs` succeeds
    - `grep -c 'fn init_returns_none_when_env_missing\|fn init_returns_layer_when_configured\|fn prod_profile_never_activates\|fn guard_drop_flushes\|fn root_flow_has_child_stage_spans' src-tauri/crates/uc-observability/tests/otlp_pipeline.rs` returns 5
    - `cd src-tauri && cargo check -p uc-observability --tests --features __wave0_scaffold_87` exits 0
    - `grep -n '\.unwrap()\|\.expect(' src-tauri/crates/uc-observability/tests/otlp_pipeline.rs | grep -v '// test' | wc -l` — informational; test-only unwraps permitted
  </acceptance_criteria>
  <done>The dev-deps are declared, the scaffold file compiles under the gated feature, and all five test function names are present to be flipped green in Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create propagation.rs and ClipboardMessage traceparent serde scaffolds</name>
  <read_first>
    - src-tauri/crates/uc-core/src/network/protocol/clipboard.rs (existing `ClipboardMessage` struct; read lines 46-100 for the struct, 180-220 for the existing `origin_flow_id_defaults_to_none_when_missing_from_json` test — the new test mirrors this pattern)
    - src-tauri/crates/uc-core/Cargo.toml (dev-deps — may need `serde_json` already present)
    - .planning/phases/87-otlp-seq-otlp/87-RESEARCH.md §5 (W3C Trace Context inject/extract), §Validation → REQ-87-06 test row
    - CLAUDE.md § "Rust Error Handling"
  </read_first>
  <behavior>
    Test 1 (uc-observability/tests/propagation.rs — gated by `__wave0_scaffold_87`):
    - `build_resource_contains_semconv_keys` — calls `uc_observability::otlp::build_resource(Some("device-xyz"))`, iterates Resource attributes, asserts presence of keys `service.name` (= "uniclipboard-desktop"), `service.version` (= env CARGO_PKG_VERSION), `service.instance.id` (= "device-xyz"), `deployment.environment.name`, `os.type`. REQ-87-03.
    - `build_resource_omits_instance_id_when_device_missing` — calls with `None`, asserts `service.instance.id` absent.
    - `traceparent_roundtrip` — installs `TraceContextPropagator`, creates a root `info_span!("test.root")`, calls the (future) helper `uc_observability::otlp::propagator::inject_current_context` to produce an `Option<String>` header; then calls `uc_observability::otlp::propagator::extract_remote_context(Some(header))` and asserts the returned OTel Context has a valid SpanContext with the same trace_id as the original span. REQ-87-06.

    Test 2 (uc-core/tests/clipboard_message_traceparent.rs — NOT feature-gated; flips green when Plan 03 adds the field):
    - `traceparent_defaults_to_none_when_missing_from_json` — deserialize JSON with no traceparent key → field is `None`
    - `traceparent_roundtrips_when_present` — serialize struct with `Some("00-trace-span-01")`, re-deserialize, asserts equality
    - `traceparent_field_skipped_when_none_in_output` — serialize `None` variant, assert output JSON does NOT contain `"traceparent"` substring (skip_serializing_if)

    The uc-core test file compiles today only if `ClipboardMessage.traceparent` exists. Since it does not yet, wrap the file with `#![cfg(feature = "__wave0_scaffold_87_traceparent")]` and add the feature to `uc-core/Cargo.toml` `[features]`.
  </behavior>
  <action>
    1. Create `src-tauri/crates/uc-observability/tests/propagation.rs` with the three test functions above. File header:
       ```rust
       //! Wave 0 scaffold for Phase 87. Targets uc_observability::otlp::{resource, propagator} in Plan 02.
       #![cfg(feature = "__wave0_scaffold_87")]
       use uc_observability::otlp::{build_resource, propagator};
       ```
       Use `opentelemetry::KeyValue` iteration via `Resource::iter()` to assert keys.
    2. Create `src-tauri/crates/uc-core/tests/clipboard_message_traceparent.rs` with the three tests above. Import `use uc_core::network::protocol::clipboard::{ClipboardMessage, ClipboardPayloadVersion};`. Construct a full `ClipboardMessage` literal mirroring the existing test at line 95-103 of `clipboard.rs` plus the new `traceparent: Some("00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01".to_string())` field.
    3. Add feature `__wave0_scaffold_87_traceparent = []` to `src-tauri/crates/uc-core/Cargo.toml` `[features]` section.
    4. NO production `.unwrap()`/`.expect()` introduced; test `.expect("test msg")` is fine.
  </action>
  <verify>
    <automated>cd src-tauri && cargo check -p uc-observability --tests --features __wave0_scaffold_87 && cargo check -p uc-core --tests --features __wave0_scaffold_87_traceparent</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src-tauri/crates/uc-observability/tests/propagation.rs` succeeds
    - `test -f src-tauri/crates/uc-core/tests/clipboard_message_traceparent.rs` succeeds
    - `grep -c 'fn build_resource_contains_semconv_keys\|fn build_resource_omits_instance_id_when_device_missing\|fn traceparent_roundtrip' src-tauri/crates/uc-observability/tests/propagation.rs` returns 3
    - `grep -c 'fn traceparent_defaults_to_none_when_missing_from_json\|fn traceparent_roundtrips_when_present\|fn traceparent_field_skipped_when_none_in_output' src-tauri/crates/uc-core/tests/clipboard_message_traceparent.rs` returns 3
    - `grep -n '__wave0_scaffold_87_traceparent' src-tauri/crates/uc-core/Cargo.toml` returns a match
    - `cd src-tauri && cargo check -p uc-observability --tests --features __wave0_scaffold_87` exits 0
    - `cd src-tauri && cargo check -p uc-core --tests --features __wave0_scaffold_87_traceparent` exits 0 — note: this will FAIL until the `traceparent` field is added in Plan 03; the feature gate keeps the default `cargo check` green. The command here runs WITH the feature to confirm the file parses (it parses because Rust accepts the field reference at type-check time only when building). **Exception:** if the feature gate cannot hide the type error, instead add `compile_error!` behind a `#[cfg(not(...))]` so default builds pass and Plan 03 is responsible for making this command green.
  </acceptance_criteria>
  <done>All three Wave 0 test scaffold files exist, default `cargo check` remains green, and Plans 02 + 03 have explicit targets to satisfy.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo check -p uc-observability -p uc-core` passes (default features — scaffolds are feature-gated)
- `cd src-tauri && cargo check -p uc-observability --tests --features __wave0_scaffold_87` passes (compilation of stub tests — they may need Plan 02 to link, in which case this check runs post-Plan 02)
- All five + three + three test function names are grep-findable
- Default workspace build unaffected: `cd src-tauri && cargo check` exits 0
</verification>

<success_criteria>
Wave 0 complete: failing test skeletons exist for REQ-87-01/03/04/06/14/15; dev-deps (`opentelemetry-stdout`, `opentelemetry`, `opentelemetry_sdk`, `tracing-opentelemetry`, `serial_test`) are in `uc-observability` dev-dependencies; Plans 02-04 have concrete green targets.
</success_criteria>

<output>
After completion, create `.planning/phases/87-otlp-seq-otlp/87-01-SUMMARY.md`
</output>
