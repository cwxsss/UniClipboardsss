---
phase: 92.1
slug: cli-search-commands
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-11
---

# Phase 92.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust unit tests + CLI smoke test |
| **Config file** | none — existing Cargo workspace and inline test modules |
| **Quick run command** | `cd src-tauri && cargo test -p uc-daemon-client search && cargo test -p uc-cli search::` |
| **Full suite command** | `cd src-tauri && cargo test -p uc-daemon-client search && cargo test -p uc-cli search:: && cargo test -p uc-cli --test cli_smoke && cargo check -p uc-cli` |
| **Estimated runtime** | ~150 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd src-tauri && cargo test -p uc-daemon-client search` for client work or `cd src-tauri && cargo test -p uc-cli search::` for CLI work
- **After every plan wave:** Run `cd src-tauri && cargo test -p uc-daemon-client search && cargo test -p uc-cli search:: && cargo test -p uc-cli --test cli_smoke && cargo check -p uc-cli`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 150 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 92.1-01-01 | 01 | 1 | SQRY-01, SQRY-02, SQRY-03, SQRY-04 | unit | `cd src-tauri && cargo test -p uc-daemon-client search_query_request` | ❌ W0 | ⬜ pending |
| 92.1-01-02 | 01 | 1 | SQRY-05, SQRY-06 | unit | `cd src-tauri && cargo test -p uc-daemon-client search_error` | ❌ W0 | ⬜ pending |
| 92.1-02-01 | 02 | 2 | SQRY-01, SQRY-02, SQRY-03, SQRY-04 | unit | `cd src-tauri && cargo test -p uc-cli search::tests::query_` | ❌ W0 | ⬜ pending |
| 92.1-02-02 | 02 | 2 | SQRY-05 | smoke | `cd src-tauri && cargo test -p uc-cli --test cli_smoke test_help_output` | ✅ existing | ⬜ pending |
| 92.1-03-01 | 03 | 3 | REBLD-04 | unit | `cd src-tauri && cargo test -p uc-cli search::tests::rebuild_accepted_waits_until_ready` | ❌ W0 | ⬜ pending |
| 92.1-03-02 | 03 | 3 | SQRY-05, REBLD-04 | unit | `cd src-tauri && cargo test -p uc-cli search::tests::rebuild_conflict_attaches_to_existing_status` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src-tauri/crates/uc-daemon-contract/src/api/dto/search.rs` — add the shared search transport DTOs
- [ ] `src-tauri/crates/uc-daemon-client/src/http/search.rs` — add the new search client module and inline tests
- [ ] `src-tauri/crates/uc-cli/src/commands/search.rs` — add the new CLI command module and inline tests
- [ ] `src-tauri/crates/uc-daemon-client/src/lib.rs` and `src-tauri/crates/uc-daemon-client/src/http/mod.rs` — export the search client through the existing context

---

## Manual-Only Verifications

All Phase 92.1 behaviors should be automatable with unit tests and CLI help/smoke coverage. No manual-only verification is expected.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 150s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-11
