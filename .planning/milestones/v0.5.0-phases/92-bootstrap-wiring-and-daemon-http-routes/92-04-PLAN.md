---
phase: 92-bootstrap-wiring-and-daemon-http-routes
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src-tauri/crates/uc-daemon/tests/search_api.rs
  - src-tauri/crates/uc-daemon/tests/search_ws.rs
autonomous: true
requirements: [SQRY-01, SQRY-02, SQRY-03, SQRY-04, SQRY-05, SQRY-06, REBLD-04]
must_haves:
  truths:
    - 'Daemon integration tests prove captured entries become searchable end to end, deleted entries leave zero `search_posting` rows, and search routes enforce 423 lock behavior'
    - 'Raw WebSocket integration tests prove the `search` topic emits at least `started` and `complete` rebuild progress for a connected client'
    - 'The phase can be verified without any frontend UI because HTTP and WS tests exercise the actual daemon transport surface directly'
  artifacts:
    - path: 'src-tauri/crates/uc-daemon/tests/search_api.rs'
      provides: 'HTTP integration fixture and end-to-end search route verification'
      contains: 'search_api_end_to_end_capture_query_and_locking'
    - path: 'src-tauri/crates/uc-daemon/tests/search_ws.rs'
      provides: 'raw websocket rebuild progress verification'
      contains: 'search_rebuild_websocket_events_include_started_and_complete'
  key_links:
    - from: 'src-tauri/crates/uc-daemon/tests/search_api.rs'
      to: 'src-tauri/crates/uc-daemon/src/api/search.rs'
      via: 'integration tests exercise the real search routes'
      pattern: '/search/query|/search/status|/search/rebuild'
    - from: 'src-tauri/crates/uc-daemon/tests/search_ws.rs'
      to: 'src-tauri/crates/uc-daemon/src/api/ws.rs'
      via: 'integration tests subscribe to the real daemon websocket server'
      pattern: 'ws_topic::SEARCH'
---

<objective>
Close Phase 92 with end-to-end daemon verification. Add dedicated HTTP and raw WebSocket integration tests that prove the phase goal without UI help: capture produces searchable data, delete removes postings, mixed operators are rejected, locked routes return 423, manual rebuild broadcasts progress, and late subscribers can observe truthful search state over the daemon transport surface.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/92-bootstrap-wiring-and-daemon-http-routes/92-CONTEXT.md
@.planning/phases/92-bootstrap-wiring-and-daemon-http-routes/92-RESEARCH.md
@.planning/phases/92-bootstrap-wiring-and-daemon-http-routes/92-VALIDATION.md
@.planning/phases/92-bootstrap-wiring-and-daemon-http-routes/92-01-PLAN.md
@.planning/phases/92-bootstrap-wiring-and-daemon-http-routes/92-02-PLAN.md
@.planning/phases/92-bootstrap-wiring-and-daemon-http-routes/92-03-PLAN.md

<read_first>
MANDATORY reading before writing any code:

1. `src-tauri/crates/uc-daemon/tests/clipboard_api.rs`
2. `src-tauri/crates/uc-daemon/tests/websocket_api.rs`
3. `src-tauri/crates/uc-daemon/tests/pairing_api.rs`
4. `src-tauri/crates/uc-daemon/src/api/search.rs`
5. `src-tauri/crates/uc-daemon/src/api/ws.rs`
6. `src-tauri/crates/uc-daemon/src/search/coordinator.rs`
7. `src-tauri/crates/uc-daemon/src/workers/clipboard_watcher.rs`
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add daemon HTTP integration tests for capture, query filters, delete cleanup, invalid_query, and 423 lock behavior</name>
  <files>
    src-tauri/crates/uc-daemon/tests/search_api.rs
  </files>
  <read_first>
    - `src-tauri/crates/uc-daemon/tests/clipboard_api.rs`
    - `src-tauri/crates/uc-daemon/tests/pairing_api.rs`
    - `src-tauri/crates/uc-daemon/src/api/search.rs`
    - `src-tauri/crates/uc-daemon/src/workers/clipboard_watcher.rs`
  </read_first>
  <action>
    Create `src-tauri/crates/uc-daemon/tests/search_api.rs` with a dedicated fixture that:

    - builds a real daemon runtime using the same bootstrap path as other daemon tests
    - exposes an `axum_test::TestServer` or equivalent HTTP test app
    - can unlock and lock the encryption session during the test
    - can inject clipboard snapshots through the real daemon capture path
    - can inspect the SQLite database directly for `search_posting` counts

    Add these exact tests:

    - `search_api_end_to_end_capture_query_and_locking`
    - `search_query_route_parses_filters_and_rejects_mixed_operators`

    `search_api_end_to_end_capture_query_and_locking` must prove all of the following in one flow:

    - capture an unlocked clipboard entry through the daemon path
    - `/search/query` finds it by keyword
    - deleting that entry leaves zero `search_posting` rows for its `entry_id`
    - after locking encryption, `/search/query`, `/search/status`, and `/search/rebuild` each return `423`

    `search_query_route_parses_filters_and_rejects_mixed_operators` must prove:

    - repeated `fileTypes` and `extensions` params filter results correctly
    - `timePreset`, `fromMs`, and `toMs` map correctly
    - mixed `AND` + `OR` query returns `{ code: "invalid_query", ... }` and not `500`

    Keep tests black-box at the transport boundary wherever possible. Only use direct DB inspection for the delete-postings success criterion.
  </action>
  <acceptance_criteria>
    - `src-tauri/crates/uc-daemon/tests/search_api.rs` contains `search_api_end_to_end_capture_query_and_locking`
    - `src-tauri/crates/uc-daemon/tests/search_api.rs` contains `search_query_route_parses_filters_and_rejects_mixed_operators`
    - `src-tauri/crates/uc-daemon/tests/search_api.rs` contains `StatusCode::LOCKED`
    - `src-tauri/crates/uc-daemon/tests/search_api.rs` contains `invalid_query`
    - `src-tauri/crates/uc-daemon/tests/search_api.rs` contains `search_posting`
    - `cd src-tauri && cargo test -p uc-daemon search_api_end_to_end_capture_query_and_locking` exits `0`
    - `cd src-tauri && cargo test -p uc-daemon search_query_route_parses_filters_and_rejects_mixed_operators` exits `0`
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-daemon search_api_end_to_end_capture_query_and_locking && cargo test -p uc-daemon search_query_route_parses_filters_and_rejects_mixed_operators</automated>
  </verify>
  <done>
    The daemon HTTP surface is proven against the actual phase goal rather than only per-module unit tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add raw websocket integration tests for search snapshots and rebuild progress</name>
  <files>
    src-tauri/crates/uc-daemon/tests/search_ws.rs
  </files>
  <read_first>
    - `src-tauri/crates/uc-daemon/tests/websocket_api.rs`
    - `src-tauri/crates/uc-daemon/tests/pairing_ws.rs`
    - `src-tauri/crates/uc-daemon/src/api/ws.rs`
    - `src-tauri/crates/uc-daemon/src/search/coordinator.rs`
  </read_first>
  <action>
    Create `src-tauri/crates/uc-daemon/tests/search_ws.rs` with raw WebSocket integration tests that connect to the real daemon websocket router, subscribe using the same JSON contract as production clients, and assert `DaemonWsEvent` payloads directly.

    Add these exact tests:

    - `search_status_and_rebuild_routes_enforce_lock_and_emit_progress`
    - `search_rebuild_websocket_events_include_started_and_complete`

    `search_status_and_rebuild_routes_enforce_lock_and_emit_progress` must prove:

    - subscribing to topic `search` yields an immediate `search.status_snapshot`
    - after unlocking, calling `POST /search/rebuild` emits at least one `search.rebuild_progress`
    - after locking again, both `/search/status` and `/search/rebuild` return `423`

    `search_rebuild_websocket_events_include_started_and_complete` must prove:

    - event stream contains one payload with `stage == "started"`
    - event stream later contains one payload with `stage == "complete"`
    - both events use `topic == "search"` and `type == "search.rebuild_progress"`

    Use the same broadcast-backed test style as existing websocket tests. Do not mock the search coordinator or bypass the actual route + ws router.
  </action>
  <acceptance_criteria>
    - `src-tauri/crates/uc-daemon/tests/search_ws.rs` contains `search_status_and_rebuild_routes_enforce_lock_and_emit_progress`
    - `src-tauri/crates/uc-daemon/tests/search_ws.rs` contains `search_rebuild_websocket_events_include_started_and_complete`
    - `src-tauri/crates/uc-daemon/tests/search_ws.rs` contains `search.status_snapshot`
    - `src-tauri/crates/uc-daemon/tests/search_ws.rs` contains `search.rebuild_progress`
    - `src-tauri/crates/uc-daemon/tests/search_ws.rs` contains `stage`
    - `cd src-tauri && cargo test -p uc-daemon search_status_and_rebuild_routes_enforce_lock_and_emit_progress` exits `0`
    - `cd src-tauri && cargo test -p uc-daemon search_rebuild_websocket_events_include_started_and_complete` exits `0`
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-daemon search_status_and_rebuild_routes_enforce_lock_and_emit_progress && cargo test -p uc-daemon search_rebuild_websocket_events_include_started_and_complete</automated>
  </verify>
  <done>
    Rebuild progress and search status are observable over the real websocket surface, completing the backend-only proof that Phase 92 works end to end.
  </done>
</task>

</tasks>
