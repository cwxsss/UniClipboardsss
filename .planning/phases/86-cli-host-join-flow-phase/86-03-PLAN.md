---
phase: 86-cli-host-join-flow-phase
plan: '03'
type: execute
wave: '3'
depends_on:
  - '02'
files_modified:
  - src-tauri/crates/uc-cli/src/commands/setup/host_flow.rs
  - src-tauri/crates/uc-cli/src/commands/setup/join_flow.rs
  - src-tauri/crates/uc-cli/src/commands/setup.rs
autonomous: true
requirements:
  - REQ-86-03
must_haves:
  truths:
    - 'Host flow correctly transitions between waiting, decision, verification, and completion states'
    - 'Join flow correctly transitions between selecting, discovering, confirming, and completion states'
    - 'Session IDs are properly maintained through phase transitions'
    - 'Idempotent deduplication prevents duplicate submissions without blocking valid retries'
  artifacts:
    - path: src-tauri/crates/uc-cli/src/commands/setup/host_flow.rs
      provides: HostCliPhase enum, derive_host_phase(), HostCliSession struct
      contains:
        - 'pub enum HostCliPhase'
        - 'pub fn derive_host_phase'
        - 'struct HostCliSession'
    - path: src-tauri/crates/uc-cli/src/commands/setup/join_flow.rs
      provides: JoinCliPhase enum, derive_join_phase(), JoinCliSession struct
      contains:
        - 'pub enum JoinCliPhase'
        - 'pub fn derive_join_phase'
        - 'struct JoinCliSession'
  key_links:
    - from: src-tauri/crates/uc-cli/src/commands/setup/host_flow.rs
      to: src-tauri/crates/uc-daemon-client/src/setup/parsed_state.rs
      via: use uc_daemon_client::setup::{ParsedSetupState, SetupHint, SetupVariant}
      pattern: parse_setup_state
---

<objective>
Phase 2: Create lightweight CLI phase enums (HostCliPhase, JoinCliPhase) and pure derive_*_phase() functions in uc-cli. Session IDs are embedded in phase variants (D-13). Old deduplication fields are kept only for idempotent deduplication, not phase gating (D-15).
</objective>

<execution_context>
@/home/wuy6/myprojects/UniClipboard/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src-tauri/crates/uc-cli/src/commands/setup.rs (current run_pair/run_connect to understand current flow)
@src-tauri/crates/uc-daemon-client/src/setup/parsed_state.rs (ParsedSetupState definition from Plan 02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create host_flow.rs with HostCliPhase enum and derive_host_phase()</name>
  <files>src-tauri/crates/uc-cli/src/commands/setup/host_flow.rs</files>
  <read_first>src-tauri/crates/uc-cli/src/commands/setup.rs (lines 142-317 -- run_pair function to understand phase transitions)</read_first>
  <action>
Create the file `src-tauri/crates/uc-cli/src/commands/setup/host_flow.rs`:

```rust
//! Host CLI flow -- HostCliPhase, HostCliSession, and run_pair.
//!
//! Phase-driven loop structure per D-16 through D-19:
//!   loop { poll -> parse -> derive phase -> match -> sleep }

use std::time::Instant;

use indicatif::ProgressBar;
use uc_daemon_client::setup::{ParsedSetupState, SetupHint, SetupVariant};

use crate::exit_codes;
use crate::ui;

// ── Phase enum ──────────────────────────────────────────────────────

/// CLI-layer phase for the host pairing flow.
///
/// Per D-11: session_id lives INSIDE the phase variant (D-13).
#[derive(Debug, Clone, PartialEq)]
pub enum HostCliPhase {
    /// Waiting for a join request from a peer.
    WaitingJoinRequest,
    /// A peer has requested to join; awaiting host accept/reject decision.
    NeedDecision { session_id: String },
    /// Peer decision accepted; awaiting short-code verification confirmation.
    NeedVerification { session_id: String },
    /// Backend processing the pairing completion.
    WaitingBackendCompletion,
    /// Pairing completed successfully.
    Completed,
    /// Pairing canceled or rejected.
    Canceled,
}

impl HostCliPhase {
    /// Returns true if this is a terminal phase (Completed or Canceled).
    #[inline]
    pub fn is_terminal(&self) -> bool {
        matches!(self, HostCliPhase::Completed | HostCliPhase::Canceled)
    }
}

// ── Session struct ─────────────────────────────────────────────────

/// Per D-16: session state carried through the host pairing loop.
#[derive(Debug)]
pub struct HostCliSession {
    /// Current CLI phase.
    pub phase: HostCliPhase,
    /// Whether pairing presence (lease) is currently enabled.
    pub pairing_presence_enabled: bool,
    /// Last time the host pairing lease was refreshed.
    pub last_lease_refresh: Instant,
    /// Active spinner, if any.
    pub spinner: Option<ProgressBar>,
}

impl Default for HostCliSession {
    fn default() -> Self {
        Self {
            phase: HostCliPhase::WaitingJoinRequest,
            pairing_presence_enabled: false,
            last_lease_refresh: Instant::now(),
            spinner: None,
        }
    }
}

// ── Phase derivation ───────────────────────────────────────────────

/// Per D-14: pure function to derive the next HostCliPhase from parsed state.
///
/// Takes the parsed daemon state and the current CLI phase to produce the next phase.
/// The `current` phase is used for transitional state (e.g., preserving session_id
/// across poll cycles when the backend hasn't changed).
///
/// Per D-15: `last_submitted_*` fields are NOT used here -- deduplication is handled
/// by the caller using the submitted session IDs.
#[must_use]
pub fn derive_host_phase(parsed: &ParsedSetupState, current: &HostCliPhase) -> HostCliPhase {
    use HostCliPhase::*;

    // Terminal states are sticky -- once Completed or Canceled, stay there.
    if current.is_terminal() {
        return current.clone();
    }

    match &parsed.hint {
        // Hint: idle -- host is not in an active pairing flow.
        SetupHint::Idle => {
            if matches!(current, WaitingBackendCompletion) {
                Canceled
            } else {
                current.clone()
            }
        }

        // Hint: completed -- pairing finished successfully.
        SetupHint::Completed => Completed,

        // Hint: host-confirm-peer -- this is the decision/verification branch.
        SetupHint::HostConfirmPeer => {
            if matches!(parsed.variant, SetupVariant::JoinSpaceConfirmPeer) {
                // NeedVerification state.
                let session_id = parsed.session_id.clone()
                    .unwrap_or_else(|| "unknown".to_string());
                NeedVerification { session_id }
            } else {
                // NeedDecision state.
                let session_id = parsed.session_id.clone()
                    .unwrap_or_else(|| "unknown".to_string());
                NeedDecision { session_id }
            }
        }

        // Any other hint (JoinSelectPeer, JoinEnterPassphrase, etc.)
        // means the host is not the active participant.
        _ => current.clone(),
    }
}

// ── Tests ─────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;
    use uc_daemon_client::setup::SetupHint;

    fn make_parsed(hint: SetupHint, variant: SetupVariant, session_id: Option<String>, completed: bool) -> ParsedSetupState {
        ParsedSetupState {
            hint,
            variant,
            session_id,
            has_completed: completed,
            short_code: None,
            selected_peer_label: None,
            error_code: None,
        }
    }

    fn idle_variant() -> SetupVariant { SetupVariant::Idle }
    fn join_confirm() -> SetupVariant { SetupVariant::JoinSpaceConfirmPeer }

    // WaitingJoinRequest

    #[test]
    fn from_waiting_idle_stays_waiting() {
        let parsed = make_parsed(SetupHint::Idle, idle_variant(), None, false);
        let phase = derive_host_phase(&parsed, &HostCliPhase::WaitingJoinRequest);
        assert!(matches!(phase, HostCliPhase::WaitingJoinRequest));
    }

    #[test]
    fn idle_after_waiting_backend_is_canceled() {
        let parsed = make_parsed(SetupHint::Idle, idle_variant(), None, false);
        let phase = derive_host_phase(&parsed, &HostCliPhase::WaitingBackendCompletion);
        assert!(matches!(phase, HostCliPhase::Canceled));
    }

    // NeedDecision

    #[test]
    fn host_confirm_without_variant_is_need_decision() {
        let parsed = make_parsed(
            SetupHint::HostConfirmPeer,
            SetupVariant::Idle,
            Some("s1".to_string()),
            false,
        );
        let phase = derive_host_phase(&parsed, &HostCliPhase::WaitingJoinRequest);
        assert!(matches!(phase, HostCliPhase::NeedDecision { session_id } if session_id == "s1"));
    }

    #[test]
    fn host_confirm_different_session_updates_session_id() {
        let parsed = make_parsed(
            SetupHint::HostConfirmPeer,
            SetupVariant::Idle,
            Some("s2".to_string()),
            false,
        );
        let phase = derive_host_phase(&parsed, &HostCliPhase::NeedDecision { session_id: "s1".to_string() });
        assert!(matches!(phase, HostCliPhase::NeedDecision { session_id } if session_id == "s2"));
    }

    // NeedVerification

    #[test]
    fn host_confirm_with_variant_is_need_verification() {
        let parsed = make_parsed(
            SetupHint::HostConfirmPeer,
            join_confirm(),
            Some("s3".to_string()),
            false,
        );
        let phase = derive_host_phase(&parsed, &HostCliPhase::WaitingJoinRequest);
        assert!(matches!(phase, HostCliPhase::NeedVerification { session_id } if session_id == "s3"));
    }

    #[test]
    fn need_decision_transitions_to_need_verification() {
        let parsed = make_parsed(
            SetupHint::HostConfirmPeer,
            join_confirm(),
            Some("s1".to_string()),
            false,
        );
        let phase = derive_host_phase(&parsed, &HostCliPhase::NeedDecision { session_id: "s1".to_string() });
        assert!(matches!(phase, HostCliPhase::NeedVerification { session_id } if session_id == "s1"));
    }

    // Completed

    #[test]
    fn completed_hint_is_completed_phase() {
        let parsed = make_parsed(SetupHint::Completed, SetupVariant::Completed, None, true);
        let phase = derive_host_phase(&parsed, &HostCliPhase::WaitingJoinRequest);
        assert!(matches!(phase, HostCliPhase::Completed));
    }

    #[test]
    fn completed_stays_completed() {
        let parsed = make_parsed(SetupHint::Completed, SetupVariant::Completed, None, true);
        let phase = derive_host_phase(&parsed, &HostCliPhase::Completed);
        assert!(matches!(phase, HostCliPhase::Completed));
    }

    // Join hints on host

    #[test]
    fn join_select_peer_while_waiting_stays_waiting() {
        let parsed = make_parsed(SetupHint::JoinSelectPeer, idle_variant(), None, false);
        let phase = derive_host_phase(&parsed, &HostCliPhase::WaitingJoinRequest);
        assert!(matches!(phase, HostCliPhase::WaitingJoinRequest));
    }
}
```

</action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo test -p uc-cli --lib setup::host_flow:: --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <done>host_flow.rs created with HostCliPhase, HostCliSession, derive_host_phase(), all unit tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Create join_flow.rs with JoinCliPhase enum and derive_join_phase()</name>
  <files>src-tauri/crates/uc-cli/src/commands/setup/join_flow.rs</files>
  <read_first>src-tauri/crates/uc-cli/src/commands/setup.rs (lines 319-468 -- run_connect function)</read_first>
  <action>
Create the file `src-tauri/crates/uc-cli/src/commands/setup/join_flow.rs`:

```rust
//! Join CLI flow -- JoinCliPhase, JoinCliSession, and run_connect.
//!
//! Phase-driven loop structure per D-16 through D-19.

use uc_daemon_client::setup::{ParsedSetupState, SetupHint, SetupVariant};

use crate::exit_codes;
use crate::ui;

// ── Phase enum ──────────────────────────────────────────────────────

/// CLI-layer phase for the join pairing flow.
///
/// Per D-12: session_id lives INSIDE the NeedPeerConfirmation variant (D-13).
#[derive(Debug, Clone, PartialEq)]
pub enum JoinCliPhase {
    /// Initial state -- prompting user to select a peer to join.
    SelectingPeer,
    /// Actively scanning/discovering peers on the network.
    WaitingPeerDiscovery,
    /// Waiting for the host to respond to our join request.
    WaitingHostResponse,
    /// Host accepted; awaiting short-code verification confirmation from joiner.
    NeedPeerConfirmation { session_id: String },
    /// Waiting for the joiner to enter the space passphrase.
    NeedPassphrase,
    /// Backend processing the pairing completion.
    WaitingBackendCompletion,
    /// Pairing completed successfully.
    Completed,
    /// Pairing canceled or rejected.
    Canceled,
}

impl JoinCliPhase {
    /// Returns true if this is a terminal phase.
    #[inline]
    pub fn is_terminal(&self) -> bool {
        matches!(self, JoinCliPhase::Completed | JoinCliPhase::Canceled)
    }
}

// ── Session struct ─────────────────────────────────────────────────

/// Per D-16: session state carried through the join pairing loop.
#[derive(Debug)]
pub struct JoinCliSession {
    /// Current CLI phase.
    pub phase: JoinCliPhase,
    /// Whether a peer request has been submitted to the host.
    pub submitted_peer_request: bool,
    /// Active spinner, if any.
    pub spinner: Option<indicatif::ProgressBar>,
}

impl Default for JoinCliSession {
    fn default() -> Self {
        Self {
            phase: JoinCliPhase::SelectingPeer,
            submitted_peer_request: false,
            spinner: None,
        }
    }
}

// ── Phase derivation ───────────────────────────────────────────────

/// Per D-14: pure function to derive the next JoinCliPhase from parsed state.
#[must_use]
pub fn derive_join_phase(parsed: &ParsedSetupState, current: &JoinCliPhase) -> JoinCliPhase {
    use JoinCliPhase::*;

    // Terminal states are sticky.
    if current.is_terminal() {
        return current.clone();
    }

    match &parsed.hint {
        // Idle: if we were waiting on backend, treat as Canceled.
        SetupHint::Idle => {
            if matches!(current, WaitingBackendCompletion | WaitingHostResponse) {
                Canceled
            } else {
                current.clone()
            }
        }

        // Completed: pairing finished.
        SetupHint::Completed => Completed,

        // JoinSelectPeer: actively selecting a peer to join.
        SetupHint::JoinSelectPeer => SelectingPeer,

        // JoinEnterPassphrase: host approved us, now we need the passphrase.
        SetupHint::JoinEnterPassphrase => NeedPassphrase,

        // HostConfirmPeer: the host is asking us to confirm the short code.
        SetupHint::HostConfirmPeer => {
            let session_id = parsed.session_id.clone()
                .unwrap_or_else(|| "unknown".to_string());
            NeedPeerConfirmation { session_id }
        }

        // Any other hint: stay in current phase.
        _ => current.clone(),
    }
}

// ── Tests ─────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;
    use uc_daemon_client::setup::SetupHint;

    fn make_parsed(hint: SetupHint, variant: SetupVariant, session_id: Option<String>, completed: bool) -> ParsedSetupState {
        ParsedSetupState {
            hint,
            variant,
            session_id,
            has_completed: completed,
            short_code: None,
            selected_peer_label: None,
            error_code: None,
        }
    }

    fn idle() -> SetupVariant { SetupVariant::Idle }
    fn join_input_passphrase() -> SetupVariant { SetupVariant::JoinSpaceInputPassphrase }
    fn join_confirm() -> SetupVariant { SetupVariant::JoinSpaceConfirmPeer }

    // SelectingPeer

    #[test]
    fn join_select_peer_is_selecting() {
        let parsed = make_parsed(SetupHint::JoinSelectPeer, idle(), None, false);
        let phase = derive_join_phase(&parsed, &JoinCliPhase::SelectingPeer);
        assert!(matches!(phase, JoinCliPhase::SelectingPeer));
    }

    #[test]
    fn join_select_peer_from_other_phase_resets_to_selecting() {
        let parsed = make_parsed(SetupHint::JoinSelectPeer, idle(), None, false);
        let phase = derive_join_phase(&parsed, &JoinCliPhase::WaitingHostResponse);
        assert!(matches!(phase, JoinCliPhase::SelectingPeer));
    }

    // NeedPassphrase

    #[test]
    fn join_enter_passphrase_is_need_passphrase() {
        let parsed = make_parsed(
            SetupHint::JoinEnterPassphrase,
            join_input_passphrase(),
            Some("s1".to_string()),
            false,
        );
        let phase = derive_join_phase(&parsed, &JoinCliPhase::SelectingPeer);
        assert!(matches!(phase, JoinCliPhase::NeedPassphrase));
    }

    // NeedPeerConfirmation

    #[test]
    fn host_confirm_peer_is_need_peer_confirmation() {
        let parsed = make_parsed(
            SetupHint::HostConfirmPeer,
            join_confirm(),
            Some("s2".to_string()),
            false,
        );
        let phase = derive_join_phase(&parsed, &JoinCliPhase::WaitingHostResponse);
        assert!(matches!(phase, JoinCliPhase::NeedPeerConfirmation { session_id } if session_id == "s2"));
    }

    #[test]
    fn need_passphrase_transitions_to_need_peer_confirmation() {
        let parsed = make_parsed(
            SetupHint::HostConfirmPeer,
            join_confirm(),
            Some("s1".to_string()),
            false,
        );
        let phase = derive_join_phase(&parsed, &JoinCliPhase::NeedPassphrase);
        assert!(matches!(phase, JoinCliPhase::NeedPeerConfirmation { session_id } if session_id == "s1"));
    }

    // Completed

    #[test]
    fn completed_hint_is_completed() {
        let parsed = make_parsed(SetupHint::Completed, SetupVariant::Completed, None, true);
        let phase = derive_join_phase(&parsed, &JoinCliPhase::SelectingPeer);
        assert!(matches!(phase, JoinCliPhase::Completed));
    }

    // Canceled

    #[test]
    fn idle_after_waiting_is_canceled() {
        let parsed = make_parsed(SetupHint::Idle, idle(), None, false);
        let phase = derive_join_phase(&parsed, &JoinCliPhase::WaitingBackendCompletion);
        assert!(matches!(phase, JoinCliPhase::Canceled));
    }

    #[test]
    fn idle_while_selecting_stays_selecting() {
        let parsed = make_parsed(SetupHint::Idle, idle(), None, false);
        let phase = derive_join_phase(&parsed, &JoinCliPhase::SelectingPeer);
        assert!(matches!(phase, JoinCliPhase::SelectingPeer));
    }
}
```

</action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo test -p uc-cli --lib setup::join_flow:: --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <done>join_flow.rs created with JoinCliPhase, JoinCliSession, derive_join_phase(), all unit tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Wire host_flow and join_flow as submodules of setup module</name>
  <files>
    src-tauri/crates/uc-cli/src/commands/setup.rs
  </files>
  <read_first>src-tauri/crates/uc-cli/src/commands/setup.rs (first 30 lines for module declarations)</read_first>
  <action>
The `setup.rs` file needs to declare `host_flow` and `join_flow` as submodules so the types are accessible.

Add at the TOP of `setup.rs` (right after the file-level doc comment and before the first `use` statement):

```rust
// Submodules for phase-driven flow
mod host_flow;
mod join_flow;
```

This tells Rust to look for `setup/host_flow.rs` and `setup/join_flow.rs` relative to `setup.rs`'s location.

The existing `setup.rs` file structure is:

```rust
//! Interactive setup commands over daemon-owned setup state.

use std::fmt;
...
```

After the change:

```rust
//! Interactive setup commands over daemon-owned setup state.

mod host_flow;
mod join_flow;

use std::fmt;
...
```

Also add a re-export at the top of setup.rs so that integration tests and other modules can access the types:

After the `mod host_flow; mod join_flow;` declarations, add:

```rust
pub use host_flow::{HostCliPhase, HostCliSession};
pub use join_flow::{JoinCliPhase, JoinCliSession};
```

This makes `HostCliPhase`, `HostCliSession`, `JoinCliPhase`, `JoinCliSession` accessible as `setup::HostCliPhase` etc.

Also add the import for `uc_daemon_client::setup::parse_setup_state` which is needed by the current `run_pair` and `run_connect` functions. This should already be present from Plan 02's migration.

Verify compilation:

```bash
cd src-tauri && cargo check -p uc-cli
```

The check should succeed with no errors about missing modules.
</action>
<verify>
<automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check -p uc-cli 2>&1 | tail -20</automated>
</verify>
<done>host_flow and join_flow modules wired as submodules of setup, all types publicly accessible</done>
</task>

</tasks>

<verification>
- `cargo test -p uc-cli --lib setup::host_flow:: --test-threads=1` passes
- `cargo test -p uc-cli --lib setup::join_flow:: --test-threads=1` passes
- `cargo check -p uc-cli` passes with no missing module errors
</verification>

<success_criteria>

- D-11: HostCliPhase enum with 6 variants (WaitingJoinRequest, NeedDecision{session_id}, NeedVerification{session_id}, WaitingBackendCompletion, Completed, Canceled) -- DONE
- D-12: JoinCliPhase enum with 8 variants -- DONE
- D-13: session_id IN the phase variant -- DONE
- D-14: Pure derive\_\*\_phase() functions -- DONE
- D-15: last*submitted*\* deduplication not used in derive functions -- DONE
- Phase-driven loop implementation (Plan 04)
  </success_criteria>

<output>
After completion, create `.planning/phases/86-cli-host-join-flow-phase/86-03-SUMMARY.md`
</output>
