---
phase: 87-otlp-seq-otlp
plan: 05
type: execute
wave: 5
depends_on: [87-04]
files_modified:
  - src-tauri/crates/uc-observability/src/lib.rs
  - src-tauri/crates/uc-observability/src/seq/mod.rs
  - src-tauri/crates/uc-observability/src/seq/layer.rs
  - src-tauri/crates/uc-observability/src/seq/sender.rs
  - src-tauri/crates/uc-observability/src/clef_format.rs
  - src-tauri/crates/uc-observability/src/span_fields.rs
  - src-tauri/crates/uc-observability/Cargo.toml
autonomous: true
requirements_addressed:
  - REQ-87-01
must_haves:
  truths:
    - "The legacy seq/ submodule + clef_format.rs + span_fields.rs are removed from uc-observability"
    - "uc-observability no longer exports SeqGuard, build_seq_layer, CLEFFormat, or span_fields helpers"
    - "Workspace build remains green without any legacy Seq code"
  artifacts:
    - path: "src-tauri/crates/uc-observability/src/lib.rs"
      provides: "Updated public exports with no seq/clef references"
  key_links:
    - from: "uc-observability/src/lib.rs"
      to: "(absence of) pub mod seq"
      via: "deletion of re-export"
      pattern: "pub mod seq"
---

<objective>
Hard-delete the legacy Seq layer (D-02, D-03) now that the new OTLP pipeline is fully wired and green. This plan is PURE deletion — no behavior change beyond removing unused code. Any remaining references discovered during deletion MUST be fixed to use the new OTLP module.

Purpose: Complete the hard switch. After this plan, the only telemetry pipeline in uc-observability is OTLP.
Output: seq/ directory deleted, clef_format.rs + span_fields.rs deleted, lib.rs cleaned, workspace green.
</objective>

<context>
@.planning/phases/87-otlp-seq-otlp/87-CONTEXT.md
@.planning/phases/87-otlp-seq-otlp/87-RESEARCH.md
@src-tauri/crates/uc-observability/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete seq module, clef_format.rs, span_fields.rs, clean lib.rs</name>
  <read_first>
    - src-tauri/crates/uc-observability/src/lib.rs (identify every `pub mod seq`, `pub use seq::`, `pub mod clef_format`, `pub use clef_format::`, `pub mod span_fields` line to remove)
    - src-tauri/crates/uc-observability/src/seq/mod.rs (confirm no external module still imports from it — Plans 03+04 removed all callers)
    - src-tauri/crates/uc-observability/Cargo.toml (check whether any `[dependencies]` entry exists SOLELY to support seq/clef — candidates: `chrono` (for CLEF timestamps), `reqwest` (still needed by opentelemetry-otlp so keep), `uuid` etc.)
    - .planning/phases/87-otlp-seq-otlp/87-RESEARCH.md §1 (structure shows exactly what to delete), §Don't Hand-Roll (confirms the ~890 LOC replacement)
    - CLAUDE.md § "Cargo Command Location"
  </read_first>
  <action>
    1. Verify no non-test code imports the legacy module:
       ```
       grep -rn 'uc_observability::seq\|uc_observability::SeqGuard\|uc_observability::build_seq_layer\|uc_observability::CLEFFormat\|uc_observability::span_fields' src-tauri/crates/ --include='*.rs'
       ```
       Expected: empty. If any hit appears OUTSIDE `uc-observability/src/seq/` or `uc-observability/src/clef_format.rs` itself, fix the caller first (it should have been done in Plan 03; this is the safety net).

    2. Delete the files:
       ```
       rm -r src-tauri/crates/uc-observability/src/seq/
       rm src-tauri/crates/uc-observability/src/clef_format.rs
       rm src-tauri/crates/uc-observability/src/span_fields.rs
       ```

    3. Edit `src-tauri/crates/uc-observability/src/lib.rs`:
       - Remove `pub mod seq;` and any `pub use seq::...` re-exports (SeqGuard, build_seq_layer).
       - Remove `pub mod clef_format;` and any re-exports (CLEFFormat, CLEFEvent).
       - Remove `pub mod span_fields;` and any re-exports.
       - Ensure `pub mod otlp;` (added in Plan 02) is still present.
       - Add a top-of-file comment: `//! Phase 87: legacy Seq (CLEF) pipeline removed. The OTLP pipeline under `otlp::` is the sole telemetry exporter.`

    4. Edit `src-tauri/crates/uc-observability/Cargo.toml`: remove any `[dependencies]` entries that were EXCLUSIVELY for the legacy Seq path. Conservative list to inspect (keep if used elsewhere in crate; remove if only referenced by deleted files):
       - `chrono` (if used only for CLEF timestamps)
       - `serde_json` (probably still needed for JSON file layer — KEEP)
       - `reqwest` (still used by opentelemetry-otlp — KEEP)
       - `uuid` (used by flow.rs — KEEP for now; RESEARCH.md Q3 says flow.rs stays as deprecation target)

       For each candidate, run `grep -rn '<crate_name>' src-tauri/crates/uc-observability/src/` after file deletion — if zero hits, remove from Cargo.toml; otherwise keep.

    5. Run the full workspace build + tests:
       ```
       cd src-tauri && cargo build
       cd src-tauri && cargo test -p uc-observability
       cd src-tauri && cargo test
       ```

    6. Fix any remaining compile errors — they should be nil if Plan 03 did its job. If any emerge, point them at the new OTLP module equivalents.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build && cargo test -p uc-observability</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d src-tauri/crates/uc-observability/src/seq` — directory removed
    - `test ! -f src-tauri/crates/uc-observability/src/clef_format.rs` — file removed
    - `test ! -f src-tauri/crates/uc-observability/src/span_fields.rs` — file removed
    - `grep -n 'pub mod seq\|pub mod clef_format\|pub mod span_fields' src-tauri/crates/uc-observability/src/lib.rs` returns nothing
    - `grep -n 'SeqGuard\|build_seq_layer\|CLEFFormat' src-tauri/crates/uc-observability/src/lib.rs` returns nothing
    - `grep -rn 'use uc_observability::seq\|uc_observability::SeqGuard\|uc_observability::build_seq_layer\|uc_observability::CLEFFormat' src-tauri/crates/ --include='*.rs'` returns nothing
    - `grep -rn 'use uc_observability::span_fields' src-tauri/crates/ --include='*.rs'` returns nothing
    - `cd src-tauri && cargo build` exits 0
    - `cd src-tauri && cargo test -p uc-observability` exits 0
    - `cd src-tauri && cargo test` exits 0 (full workspace)
    - `cd src-tauri && cargo clippy -p uc-observability -- -D warnings` exits 0 (no dead-code warnings)
  </acceptance_criteria>
  <done>All legacy Seq code removed from uc-observability, no dangling references, full workspace builds and tests green.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test` exits 0
- `cd src-tauri && cargo clippy --workspace -- -D warnings` exits 0 (optional — if baseline has warnings, at least no NEW ones)
- `wc -l src-tauri/crates/uc-observability/src/**/*.rs` shows net LOC reduction of ~800+ lines vs pre-Phase 87 baseline
</verification>

<success_criteria>
Hard switch complete: zero legacy Seq code remains, workspace green, `uc_observability::otlp` is the sole exporter path.
</success_criteria>

<output>
After completion, create `.planning/phases/87-otlp-seq-otlp/87-05-SUMMARY.md`
</output>
