---
phase: 87-otlp-seq-otlp
plan: 06
type: execute
wave: 5
depends_on: [87-04]
files_modified:
  - docs/architecture/logging-architecture.md
  - docs/seq/signals/flow-timeline.json
  - docs/seq/signals/cross-device-flow.json
  - docker-compose.seq.yml
autonomous: true
requirements_addressed:
  - REQ-87-11
  - REQ-87-12
  - REQ-87-13
must_haves:
  truths:
    - "docs/architecture/logging-architecture.md documents the new OTLP pipeline, OTel env vars, and traceparent propagation"
    - "Seq saved-search signals query by TraceId + SpanName, not flow_id / origin_flow_id"
    - "docker-compose.seq.yml exposes the Seq OTLP ingestion endpoint and documents the canonical OTEL_EXPORTER_OTLP_ENDPOINT value"
  artifacts:
    - path: "docs/architecture/logging-architecture.md"
      provides: "Updated observability docs — OTLP integration, traceparent, env vars"
    - path: "docs/seq/signals/flow-timeline.json"
      provides: "Seq signal rewritten for OTel trace_id semantics"
    - path: "docs/seq/signals/cross-device-flow.json"
      provides: "Seq signal rewritten for W3C traceparent-linked cross-device traces"
    - path: "docker-compose.seq.yml"
      provides: "Seq container with OTLP ingestion enabled and documented"
  key_links:
    - from: "docs/architecture/logging-architecture.md"
      to: "OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:5341/ingest/otlp"
      via: "Configuration section"
      pattern: "OTEL_EXPORTER_OTLP_ENDPOINT"
---

<objective>
Rewrite Phase 22/23 observability documentation and Seq saved searches to match the new OTel data model. Runs in parallel with Plan 05 (Wave 5) — no code dependency.

Purpose: Documentation and developer-facing queries must match runtime behavior, otherwise Phase 87's telemetry is invisible to anyone reading docs.
Output: 4 docs/config files updated with new env vars, span names, and Seq queries.
</objective>

<context>
@.planning/phases/87-otlp-seq-otlp/87-CONTEXT.md
@.planning/phases/87-otlp-seq-otlp/87-RESEARCH.md
@docs/architecture/logging-architecture.md
@docs/seq/signals/flow-timeline.json
@docs/seq/signals/cross-device-flow.json
@docker-compose.seq.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite logging-architecture.md and Seq signals JSON for OTel semantics</name>
  <read_first>
    - docs/architecture/logging-architecture.md (ENTIRE file — identify Seq/CLEF/UC_SEQ_URL/origin_flow_id sections for rewrite)
    - docs/seq/signals/flow-timeline.json (current query shape — queries by `@Properties.flow_id`)
    - docs/seq/signals/cross-device-flow.json (current — queries by `@Properties.origin_flow_id`)
    - .planning/phases/87-otlp-seq-otlp/87-RESEARCH.md §6 (env vars), §7 (Seq query migration table), §8 (doc rewrite shape), Pitfall 7 (endpoint base URL vs full URL)
  </read_first>
  <action>
    1. Rewrite `docs/architecture/logging-architecture.md`:
       - Replace the "Seq Integration (Local Visualization)" section with "OpenTelemetry OTLP Integration (Local Visualization)". Content must cover:
         - Env vars: `OTEL_EXPORTER_OTLP_ENDPOINT` (canonical value: `http://localhost:5341/ingest/otlp`), `OTEL_EXPORTER_OTLP_HEADERS` (optional `X-Seq-ApiKey=...`), `OTEL_SERVICE_NAME` (override), `OTEL_RESOURCE_ATTRIBUTES`
         - CRITICAL endpoint-base-URL note (Pitfall #7): Seq docs show full path `/ingest/otlp/v1/traces`, but the OTLP SDK appends `/v1/traces` + `/v1/logs` to the base URL, so the correct value is `http://localhost:5341/ingest/otlp` (no trailing `/v1/...`).
         - Resource attributes: `service.name=uniclipboard-desktop`, `service.version=<CARGO_PKG_VERSION>`, `service.instance.id=<device_id>`, `deployment.environment.name=development|production`, `os.type`
         - Span naming: dotted form `clipboard.flow`, `clipboard.normalize`, `clipboard.cache_representations`, `clipboard.outbound_send`, `clipboard.inbound_apply`, etc.
         - Activation: presence of `OTEL_EXPORTER_OTLP_ENDPOINT` + dev/debug_clipboard profile. Prod profile skips layer entirely.
       - Replace the "Cross-Device Tracing" / `origin_flow_id` section with "Distributed Tracing with W3C Trace Context":
         - `ClipboardMessage.traceparent: Option<String>` field with serde(default) backward compat
         - Outbound inject via `inject_current_context()`, inbound extract via `extract_remote_context()` + `Span::set_parent`
         - Legacy peer fallback behavior (new local root span + rate-limited warn)
         - Note that `origin_flow_id` is deprecated and scheduled for removal
       - Replace all `UC_SEQ_URL` / `UC_SEQ_API_KEY` references with the OTel standard env vars. Add a migration note: "UC_SEQ_URL is no longer consulted; if set, the application logs a warn on startup pointing users here."
       - Cross-link to `docs/seq/signals/*.json` for ready-to-import queries.

    2. Rewrite `docs/seq/signals/flow-timeline.json`:
       - Change the query filter from `Has(@Properties.flow_id)` or `flow_id = '...'` to `SpanName like 'clipboard.%'` (groups all clipboard pipeline spans).
       - Change the group/pivot from `flow_id` to `TraceId`.
       - Rename the signal title and description to reflect "OTel trace view per clipboard flow".
       - Signal schema: preserve the JSON shape Seq expects (Id, Title, Description, Filters, Columns). Use `SpanName` and `TraceId` as Seq property references.
       - NOTE: exact Seq property name casing for OTel-ingested data (`TraceId` vs `@TraceId` vs `trace_id`) may need adjustment. If unsure, include BOTH variants as alternative filter expressions in comments within the JSON (Seq ignores unknown properties), OR accept that a 5-minute manual verification step against a running Seq 2025.2 instance is the final gate (this is documented as MEDIUM confidence in RESEARCH.md §7).

    3. Rewrite `docs/seq/signals/cross-device-flow.json`:
       - Remove all `origin_flow_id` references.
       - Replace with: filter by `SpanName = 'clipboard.flow'` (root spans only) AND group by `TraceId`. With W3C traceparent, a cross-device trace has spans from BOTH peers under the same TraceId — Seq's built-in trace view handles this natively; the signal just needs to surface the root span.
       - Add a description comment: "Post-Phase 87: cross-device flows are unified by W3C traceparent. A single TraceId covers the entire sync across peers."

    4. Update `docker-compose.seq.yml`:
       - Ensure Seq is version 2025.2 or newer (OTLP ingestion was added in Seq 2024.1; 2025.2+ is the tested baseline per RESEARCH.md).
       - Add a comment above the `seq` service block documenting the OTLP endpoint:
         ```yaml
         # Phase 87: Seq accepts OTLP/HTTP-protobuf on the same port (5341) as the UI.
         # Set OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:5341/ingest/otlp in your shell.
         # The OTLP SDK appends /v1/traces and /v1/logs to this base URL automatically.
         ```
       - Confirm the container exposes port 5341 (it almost certainly already does — Phase 22 established this).
       - If the current compose file has any env vars like `SEQ_FIRSTRUN_ACCEPT_EULA=Y`, `SEQ_API_CANONICALURI`, keep them.
       - No image downgrade; no breaking changes to the dev workflow.
  </action>
  <verify>
    <automated>grep -q 'OTEL_EXPORTER_OTLP_ENDPOINT' docs/architecture/logging-architecture.md && ! grep -q 'UC_SEQ_URL' docs/architecture/logging-architecture.md && ! grep -q 'flow_id' docs/seq/signals/flow-timeline.json && ! grep -q 'origin_flow_id' docs/seq/signals/cross-device-flow.json && grep -q 'ingest/otlp' docker-compose.seq.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'OTEL_EXPORTER_OTLP_ENDPOINT' docs/architecture/logging-architecture.md` exits 0
    - `grep -q 'traceparent' docs/architecture/logging-architecture.md` exits 0
    - `grep -q 'clipboard.flow' docs/architecture/logging-architecture.md` exits 0
    - `grep -q 'service.instance.id' docs/architecture/logging-architecture.md` exits 0
    - `! grep -q 'UC_SEQ_URL' docs/architecture/logging-architecture.md` exits 0 (no legacy env var references outside a migration note; if mentioned in migration note, the grep may match — accept a single controlled occurrence explicitly labelled "removed in Phase 87")
    - `! grep -q 'flow_id' docs/seq/signals/flow-timeline.json` exits 0
    - `! grep -q 'origin_flow_id' docs/seq/signals/cross-device-flow.json` exits 0
    - `grep -q 'TraceId\|SpanName' docs/seq/signals/flow-timeline.json` exits 0
    - `grep -q 'TraceId\|SpanName' docs/seq/signals/cross-device-flow.json` exits 0
    - `grep -q 'ingest/otlp' docker-compose.seq.yml` exits 0
    - `grep -q '5341' docker-compose.seq.yml` exits 0
    - JSON files still parse: `python3 -c "import json; json.load(open('docs/seq/signals/flow-timeline.json')); json.load(open('docs/seq/signals/cross-device-flow.json'))"` exits 0
    - YAML file still parses: `python3 -c "import yaml; yaml.safe_load(open('docker-compose.seq.yml'))"` exits 0 (or use `docker compose -f docker-compose.seq.yml config` if docker is available)
  </acceptance_criteria>
  <done>Docs + signals + compose file reflect the OTel pipeline. Manual Seq smoke test remains the final gate (REQ-87-11, documented in VALIDATION.md).</done>
</task>

</tasks>

<verification>
- All grep-based acceptance criteria above pass
- JSON and YAML files remain valid
- `grep -rn 'flow_id\|origin_flow_id\|UC_SEQ_URL' docs/` returns only controlled migration-note occurrences (if any)
</verification>

<success_criteria>
A developer reading `docs/architecture/logging-architecture.md` can set up the Seq dev environment end-to-end using only OTel standard env vars, understand the new span topology, and import the updated Seq signals to see live traces.
</success_criteria>

<output>
After completion, create `.planning/phases/87-otlp-seq-otlp/87-06-SUMMARY.md`
</output>
