---
phase: 95
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/zh-CN.json
  - src/i18n/locales/en-US.json
autonomous: true
requirements: [NETSET-04, NETSET-05, NETSET-06]
tags: [frontend, i18n, lan-only, copywriting]
must_haves:
  truths:
    - "zh-CN + en-US 都含 settings.sections.network.lanOnly.{label,description,infoIconAriaLabel,saveError}"
    - "zh-CN + en-US 都含 settings.sections.network.lanOnly.disclosure.{title,intro,rendezvous.{title,description},otlp.{title,description},pkarr.{title,description},autoUpdate.{title,description}}"
    - "zh-CN + en-US 都含 settings.sections.network.restartBanner.{message,restartButton,restartingButton,dismissAriaLabel,errorMessage,retryButton}"
    - "禁词清单（fully offline / 完全离线 / 绝对私有 / no internet / private mode / encrypted-and-local）在两份 i18n 中均不存在（ROADMAP 核心约束 §边界透明 + Pitfall 5 防御）"
    - "旧 settings.sections.network.{title,syncMethod,webserverPort,customPeerDevice,cloudServer,loadError} 整块已删除（ROADMAP 验收 #1 + Pitfall 11）"
  artifacts:
    - path: "src/i18n/locales/zh-CN.json"
      provides: "Phase 95 完整 zh-CN i18n 树（lanOnly + disclosure + restartBanner）"
      contains: "lanOnly"
    - path: "src/i18n/locales/en-US.json"
      provides: "Phase 95 完整 en-US i18n 树（与 zh-CN 同结构）"
      contains: "LAN-only Mode"
  key_links:
    - from: "src/i18n/locales/zh-CN.json"
      to: "src/i18n/locales/en-US.json"
      via: "key 路径必须完全镜像（key 树同构）"
      pattern: "settings\\.sections\\.network\\.lanOnly\\.disclosure"
    - from: "Phase 95 i18n（基准）"
      to: "Phase 97 docs/lan-only.md (DOC-01) + changelog (DOC-03)"
      via: "Phase 97 复制粘贴 — Phase 95 措辞不可改"
      pattern: "rendezvous\\.uniclipboard\\.app"
---

<objective>
把 Phase 95 完整文案双语写进 i18n 文件，作为 **Phase 97 反向复制基准**：用户在 Settings → Network 看到的所有可见文字（LAN-only Switch label/description、info-icon Popover 4 类外网请求清单、RestartBanner 提示与按钮、错误回退）。同步删除旧 NetworkSection 占位 i18n 残留 `network.{title,syncMethod,webserverPort,customPeerDevice,cloudServer,loadError}` 整块（grep 已确认全工程无外部引用 — UI-SPEC.md `## i18n 残留清理` Discretion 决议）。

**Purpose（4 条）：**
1. NETSET-04 / 05 / 06 用户可见文字必须存在，否则 Plan 06 NetworkSection 的 `t(...)` 调用会渲染 raw key
2. UI-SPEC.md `## Copywriting Contract` 明确禁词清单 — 营销语 vs 边界透明（Pitfall 5）
3. Phase 97 `docs/lan-only.md` (DOC-01) / changelog (DOC-03) 反向引用 Phase 95 i18n —— **本文案是基准**，不是预览
4. Pitfall 11 防御：旧占位 `placeholder` 在 NetworkSection.tsx fallback 字符串里（不在 i18n key），但旧 `network.{title,syncMethod,...}` 整块在 i18n 是孤儿 — 顺手清

**Output:** 两份 JSON 各完成一处替换 — 删除旧 network 块 + 写入新 lanOnly + restartBanner 双子树。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/095-networksection-ux/095-CONTEXT.md
@.planning/phases/095-networksection-ux/095-UI-SPEC.md
@.planning/phases/095-networksection-ux/095-PATTERNS.md
@.planning/research/PITFALLS.md
@src/i18n/locales/zh-CN.json
@src/i18n/locales/en-US.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 替换 zh-CN.json 的 settings.sections.network 整块为 Phase 95 双子树</name>
  <read_first>
    - `src/i18n/locales/zh-CN.json:192-237`（旧 network 块完整内容 — 替换目标）
    - `.planning/phases/095-networksection-ux/095-UI-SPEC.md` §`## Copywriting Contract`（lines 95-174 — 完整 zh-CN 文案契约）
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/i18n/locales/zh-CN.json` 段
    - `.planning/research/PITFALLS.md` Pitfall 5（禁词清单警告）
    - 旧 NetworkSection.tsx 是否还引用 `settings.sections.network.placeholder`：grep `'settings.sections.network.placeholder'` `src/` —— 已知仅 NetworkSection.tsx:18 一处引用，且作为 `t(key) || 'fallback string'` 形式，删除 i18n 块不会运行时报 missing-key（`t()` fallback 兜底）。Plan 06 重写 NetworkSection 时连这一行 fallback 都删除。
  </read_first>
  <files>src/i18n/locales/zh-CN.json</files>
  <action>
    在 `src/i18n/locales/zh-CN.json` 中找到 `"network": { "title": "网络设置", "syncMethod": ... }` 这一整个 object（line 192-237），把它**完整替换**为下面的 zh-CN 完整 object。**注意保持 JSON 合法**（替换后仍是 `"network": { ... },` 形态，结尾 `,` 紧接 `"storage":`）。

    ```json
    "network": {
      "lanOnly": {
        "label": "LAN-only 模式",
        "description": "关闭公网中继回落，仅通过同局域网完成设备同步。重启后生效。",
        "infoIconAriaLabel": "查看 LAN-only 模式开启后仍走外网的请求清单",
        "saveError": "保存失败：{{message}}。已恢复到上一次的设置。",
        "disclosure": {
          "title": "LAN-only 开启后仍会走外网的请求",
          "intro": "以下 4 类请求由独立模块控制，不受 LAN-only 影响：",
          "rendezvous": {
            "title": "首次配对 rendezvous",
            "description": "配对新设备时仍需联网经 rendezvous.uniclipboard.app 完成 NodeId 交换；已配对设备的日常同步不再使用 rendezvous。"
          },
          "otlp": {
            "title": "OTLP 遥测",
            "description": "遥测由「通用 → 遥测」开关独立控制，与 LAN-only 无关；如需关闭，请到「通用」分类。"
          },
          "pkarr": {
            "title": "pkarr DHT NodeId 解析",
            "description": "跨网段连接通过 pkarr 公网 DHT 解析对端 NodeId，性质类似 DNS。关闭会导致跨网段连接率从约 90% 跌至接近 0。"
          },
          "autoUpdate": {
            "title": "自动更新 GitHub 检查",
            "description": "由「通用 → 自动更新」开关独立控制，访问 GitHub Release API 检查新版本。"
          }
        }
      },
      "restartBanner": {
        "message": "需要重启应用以使 LAN-only 模式更改生效。",
        "restartButton": "立即重启",
        "restartingButton": "正在重启…",
        "dismissAriaLabel": "收起重启提示",
        "errorMessage": "自动重启失败，请手动退出并重新打开应用以使更改生效。",
        "retryButton": "重试"
      }
    }
    ```

    **重要约束（per UI-SPEC.md `## Copywriting Contract`）：**
    1. `saveError` 用 `{{message}}` 占位（i18next interpolation 标准格式 — UI-SPEC.md 写作 `%{message}` 是 ruby-style，但本项目用 i18next 默认 `{{ }}`，已 grep `src/i18n/locales/zh-CN.json` 确认现有 i18n 都用 `{{ }}` —— 例如 `{{percentage}}`）
    2. **不要**用 backtick (`) 或 markdown code 标记包裹 `rendezvous.uniclipboard.app` —— JSON 字符串里直接是 plain text（UI-SPEC 中的 backtick 是 markdown 装饰，i18n string 不留）
    3. 所有 zh-CN 中文标点用全角（：。）—— UI-SPEC 已敲定形态

    **JSON 验证**：替换完成后用 `python3 -m json.tool src/i18n/locales/zh-CN.json > /dev/null` 检查合法。
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && python3 -m json.tool src/i18n/locales/zh-CN.json > /dev/null && grep -c '"lanOnly"' src/i18n/locales/zh-CN.json && grep -c '"restartBanner"' src/i18n/locales/zh-CN.json && grep -c '"disclosure"' src/i18n/locales/zh-CN.json && (grep -E "完全离线|绝对私有|encrypted-and-local|fully offline|no internet|private mode" src/i18n/locales/zh-CN.json ; test $? -eq 1)</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m json.tool src/i18n/locales/zh-CN.json > /dev/null` 退出码 0（合法 JSON）
    - `grep -c '"lanOnly"' src/i18n/locales/zh-CN.json` 返回 ≥ 1
    - `grep -c '"restartBanner"' src/i18n/locales/zh-CN.json` 返回 ≥ 1
    - `grep -c '"disclosure"' src/i18n/locales/zh-CN.json` 返回 ≥ 1
    - `grep -c '"rendezvous"' src/i18n/locales/zh-CN.json` 返回 ≥ 1（disclosure 子树存在）
    - `grep -c '"pkarr"' src/i18n/locales/zh-CN.json` 返回 ≥ 1
    - `grep -c '"autoUpdate"' src/i18n/locales/zh-CN.json` 返回 ≥ 1
    - `grep -c '"otlp"' src/i18n/locales/zh-CN.json` 返回 ≥ 1
    - `grep -c '"infoIconAriaLabel"' src/i18n/locales/zh-CN.json` 返回 ≥ 1
    - `grep -c '"errorMessage"' src/i18n/locales/zh-CN.json` 返回 ≥ 1
    - 旧 keys 已删除：`grep -E '"syncMethod"|"webserverPort"|"customPeerDevice"|"cloudServer"' src/i18n/locales/zh-CN.json` 返回 0 匹配
    - 禁词清单（zh-CN）：`grep -E "完全离线|绝对私有|encrypted-and-local|fully offline|no internet|private mode" src/i18n/locales/zh-CN.json` 返回 0 匹配
    - `grep '"立即重启"' src/i18n/locales/zh-CN.json` 返回 ≥ 1（CTA 存在）
    - `grep "rendezvous.uniclipboard.app" src/i18n/locales/zh-CN.json` 返回 1 行（首次配对说明含真实域名）
  </acceptance_criteria>
  <done>zh-CN.json 完整替换且 JSON 合法；新 keys 全部存在；旧残留全清；禁词全无。</done>
</task>

<task type="auto">
  <name>Task 2: 替换 en-US.json 的 settings.sections.network 整块为 Phase 95 双子树（en-US 镜像）</name>
  <read_first>
    - `src/i18n/locales/en-US.json:201-246`（旧 network 块完整内容 — 替换目标）
    - `.planning/phases/095-networksection-ux/095-UI-SPEC.md` §`## Copywriting Contract`（en-US 列）
    - Task 1 替换后的 `src/i18n/locales/zh-CN.json`（确认 key 树形态）
  </read_first>
  <files>src/i18n/locales/en-US.json</files>
  <action>
    在 `src/i18n/locales/en-US.json` 中找到 `"network": { "title": "Network", "syncMethod": ... }` 这一整个 object（line 201-246），完整替换为下面的 en-US 镜像。**key 路径必须与 Task 1 完全相同**（i18next 双语必须 key tree 同构）。

    ```json
    "network": {
      "lanOnly": {
        "label": "LAN-only Mode",
        "description": "Disable public-relay fallback. Devices sync only over the same local network. Takes effect after restart.",
        "infoIconAriaLabel": "View the list of requests that still reach the internet when LAN-only Mode is on",
        "saveError": "Save failed: {{message}}. Reverted to the previous setting.",
        "disclosure": {
          "title": "Requests that still reach the internet with LAN-only Mode on",
          "intro": "The following 4 request types are controlled by independent modules and are not affected by LAN-only Mode:",
          "rendezvous": {
            "title": "First-pairing rendezvous",
            "description": "Pairing a new device still requires reaching rendezvous.uniclipboard.app to exchange NodeIds. Once paired, day-to-day sync no longer uses rendezvous."
          },
          "otlp": {
            "title": "OTLP telemetry",
            "description": "Telemetry is controlled independently by General → Telemetry, not by LAN-only Mode. To disable it, go to the General section."
          },
          "pkarr": {
            "title": "pkarr DHT NodeId resolution",
            "description": "Cross-subnet connections resolve peer NodeIds via the public pkarr DHT, similar in nature to DNS. Disabling it would drop cross-subnet connection rate from ~90% to near zero."
          },
          "autoUpdate": {
            "title": "Auto-update GitHub check",
            "description": "Controlled independently by General → Auto-update, which queries the GitHub Release API for new versions."
          }
        }
      },
      "restartBanner": {
        "message": "Restart the app to apply the LAN-only Mode change.",
        "restartButton": "Restart now",
        "restartingButton": "Restarting…",
        "dismissAriaLabel": "Dismiss restart notice",
        "errorMessage": "Automatic restart failed. Please quit the app manually and reopen it to apply the change.",
        "retryButton": "Retry"
      }
    }
    ```

    **JSON 验证**：替换完成后用 `python3 -m json.tool src/i18n/locales/en-US.json > /dev/null`。
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && python3 -m json.tool src/i18n/locales/en-US.json > /dev/null && grep -c '"lanOnly"' src/i18n/locales/en-US.json && grep -c '"LAN-only Mode"' src/i18n/locales/en-US.json && (grep -iE "fully offline|no internet|private mode|encrypted-and-local" src/i18n/locales/en-US.json ; test $? -eq 1)</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m json.tool src/i18n/locales/en-US.json > /dev/null` 退出码 0
    - `grep -c '"lanOnly"' src/i18n/locales/en-US.json` 返回 ≥ 1
    - `grep '"label": "LAN-only Mode"' src/i18n/locales/en-US.json` 返回 1
    - `grep '"restartButton": "Restart now"' src/i18n/locales/en-US.json` 返回 1
    - `grep "rendezvous.uniclipboard.app" src/i18n/locales/en-US.json` 返回 1
    - 禁词清单（en-US）：`grep -iE "fully offline|no internet|private mode|encrypted-and-local" src/i18n/locales/en-US.json` 返回 0 匹配
    - 旧 keys 已删除：`grep -E '"syncMethod"|"webserverPort"|"customPeerDevice"|"cloudServer"|"lanFirst"|"cloudOnly"' src/i18n/locales/en-US.json` 返回 0 匹配（注意：`"lanFirst"` 在旧 syncMethod block 才有；新 lanOnly 树绝不出现）
    - en-US 与 zh-CN key 路径同构（自动验证）：
      ```bash
      python3 -c "import json; zh=json.load(open('src/i18n/locales/zh-CN.json'))['settings']['sections']['network']; en=json.load(open('src/i18n/locales/en-US.json'))['settings']['sections']['network']; assert sorted(zh['lanOnly'].keys()) == sorted(en['lanOnly'].keys()), 'lanOnly key 树不同'; assert sorted(zh['lanOnly']['disclosure'].keys()) == sorted(en['lanOnly']['disclosure'].keys()), 'disclosure key 树不同'; assert sorted(zh['restartBanner'].keys()) == sorted(en['restartBanner'].keys()), 'restartBanner key 树不同'; print('OK')"
      ```
      期望输出 `OK`
  </acceptance_criteria>
  <done>en-US.json 完整替换且与 zh-CN key tree 同构；禁词全无。</done>
</task>

<task type="auto">
  <name>Task 3: 全工程禁词审计 + 旧 i18n key 引用清查</name>
  <read_first>
    - Task 1 + Task 2 替换后的两份 i18n
    - `.planning/research/PITFALLS.md` Pitfall 5 + Pitfall 11 + Pitfall 9（措辞陷阱）
    - `.planning/phases/095-networksection-ux/095-UI-SPEC.md` §`## Copywriting Contract` 禁词清单
  </read_first>
  <files>(no file modifications — pure audit task)</files>
  <action>
    1. **禁词清单铁律 — 全工程（含 src/ 与文档）grep**：
       ```bash
       cd /Users/mark/conductor/workspaces/uniclipboard/lagos
       grep -rnE "fully offline|完全离线|绝对私有|no internet|private mode|encrypted-and-local" \
         src/i18n/ \
         docs/ \
         README.md \
         .changelog/ \
         CHANGELOG.md \
         2>/dev/null | grep -v "禁词\|forbidden\|prohibited\|dont_use\|ban_list" || echo "OK"
       ```
       期望输出 `OK`（grep 命中可能在审计/规范文档中合法，要排除自我引用 — 用 `grep -v` 过滤"禁词"自述）。如有真实违规，失败修复。

    2. **旧 i18n keys 全工程引用清查**：
       ```bash
       grep -rE "settings\.sections\.network\.(syncMethod|webserverPort|customPeerDevice|cloudServer|placeholder|loadError|title)\b" \
         src/ 2>/dev/null
       ```
       期望命中：**最多 1 行** —— 即 `src/components/setting/NetworkSection.tsx:18` 的 `t('settings.sections.network.placeholder')` fallback（Plan 06 会一并删除）。其它任何引用必须为 0。

    3. **Phase 97 反向复制 fence — 文案锁定 marker**：在 plan 完成提交时，git commit message 含 "Phase 97 baseline" 关键字，便于未来 phase 97 实施者 grep 找到本 plan：
       提交消息：`feat(95-03): i18n 双语 LAN-only 文案 + Pitfall 5 禁词清单 — Phase 97 docs baseline`

    4. **运行 Phase 95 i18n 完整性 sanity check**（双语 key tree 同构 — 已在 Task 2 acceptance 内验证，本 task 重跑确认）：
       ```bash
       python3 -c "import json; zh=json.load(open('src/i18n/locales/zh-CN.json'))['settings']['sections']['network']; en=json.load(open('src/i18n/locales/en-US.json'))['settings']['sections']['network']; assert sorted(zh.keys())==sorted(en.keys()); assert sorted(zh['lanOnly'].keys())==sorted(en['lanOnly'].keys()); assert sorted(zh['lanOnly']['disclosure'].keys())==sorted(en['lanOnly']['disclosure'].keys()); print('OK')"
       ```
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && (grep -rnE "fully offline|完全离线|绝对私有|no internet|private mode|encrypted-and-local" src/i18n/ ; test $? -eq 1) && (grep -rE "settings\.sections\.network\.(syncMethod|webserverPort|customPeerDevice|cloudServer|loadError)" src/ ; test $? -eq 1) && python3 -c "import json; zh=json.load(open('src/i18n/locales/zh-CN.json'))['settings']['sections']['network']; en=json.load(open('src/i18n/locales/en-US.json'))['settings']['sections']['network']; assert sorted(zh.keys())==sorted(en.keys()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "fully offline|完全离线|绝对私有|no internet|private mode|encrypted-and-local" src/i18n/` 返回 0 匹配
    - `grep -rE "settings\.sections\.network\.(syncMethod|webserverPort|customPeerDevice|cloudServer|loadError)" src/` 返回 0 匹配
    - `grep -rE "settings\.sections\.network\.placeholder" src/` 至多 1 行（即 NetworkSection.tsx:18，由 Plan 06 删除）
    - 双语 key tree 同构：python3 验证脚本输出 `OK`
    - 提交消息含 "Phase 97 baseline" 关键字
    - `git log --oneline -1` 标题以 `feat(95-03):` 开头
  </acceptance_criteria>
  <done>禁词清单 fence 已落 + 旧 i18n 残留清查完成 + 双语 key tree 同构。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户阅读 UI 文字 → 形成对 LAN-only 行为的心智 | 文案错误（营销语 / 绝对化）会让用户对产品产生错误信任，泄漏到生产数据 |
| Phase 95 i18n → Phase 97 docs/changelog | Phase 97 复制粘贴 Phase 95 文案；Phase 95 文案错则 Phase 97 也错（Pitfall 9） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03-01 | Information Disclosure | i18n 营销语过度承诺（"fully offline"）让用户误以为流量绝对不出局域网 — Pitfall 5 transparency-boundary | mitigate | 禁词清单 grep fence（Task 3 acceptance）+ Popover 4 类外网请求清单显式披露（rendezvous / OTLP / pkarr / auto-update） |
| T-95-03-02 | Information Disclosure | Phase 97 docs 与 i18n 文案不一致导致 reviewer/blogger 引用错误版本 | mitigate | Phase 95 i18n 是 baseline；UI-SPEC.md `## Cross-Phase Consistency` 锁定 Phase 97 必须逐字复制；commit message 明确标记 "Phase 97 baseline" |
| T-95-03-03 | Tampering | 双语 key tree 不同步导致一种语言下渲染 raw key | mitigate | Task 3 python3 脚本验证两份 JSON key 树同构；CI 可后续加同样脚本 |
| T-95-03-04 | Repudiation | 旧 i18n 残留（network.syncMethod 等）让未来开发者参考过期文案 | mitigate | Task 3 grep 全工程确认无外部引用；本 plan 完成时整块清除（Pitfall 11） |
</threat_model>

<verification>
- 两份 JSON 都通过 `python3 -m json.tool` 合法检查
- 双语 key tree 同构（python3 sanity script PASS）
- 禁词清单 grep 全工程 0 匹配（包括 src/i18n/, docs/, README.md, CHANGELOG.md — 若存在）
- 旧 keys 全清（除 NetworkSection.tsx:18 一处 placeholder fallback —— Plan 06 同步清）
- Phase 95 文案契约可被 Phase 97 直接复制粘贴到 docs/lan-only.md（DOC-01）和 changelog（DOC-03）
</verification>

<success_criteria>
- `settings.sections.network.lanOnly.{label,description,infoIconAriaLabel,saveError}` 双语都存在
- `settings.sections.network.lanOnly.disclosure.{title,intro,rendezvous,otlp,pkarr,autoUpdate}` 完整 4 类清单双语都存在
- `settings.sections.network.restartBanner.{message,restartButton,restartingButton,dismissAriaLabel,errorMessage,retryButton}` 双语都存在
- 禁词清单全工程为 0
- 旧 network 整块（除 categories.network 导航 key 外）已删
- 3 commits（zh-CN 替换 → en-US 替换 → 审计）
</success_criteria>

<output>
完成后创建 `.planning/phases/095-networksection-ux/095-03-SUMMARY.md`
</output>
