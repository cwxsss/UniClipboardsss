---
phase: 95
plan: 06
type: tdd
wave: 3
depends_on: [95-01, 95-02, 95-03, 95-04, 95-05]
files_modified:
  - src/components/setting/NetworkSection.tsx
  - src/components/setting/__tests__/NetworkSection.test.tsx
autonomous: false
requirements: [NETSET-04, NETSET-05, NETSET-06]
tags: [frontend, components, lan-only, network-section, integration]
must_haves:
  truths:
    - "用户在 Settings → Network 看到 LAN-only Mode Switch，默认 OFF（allowRelayFallback=true）"
    - "用户切 Switch 后 RestartBanner 立即可见（乐观 pending — D-D2），useDebounce 500ms 后触发 PUT /settings"
    - "用户点「立即重启」→ invokeWithTrace('restart_app') → Tauri 进程退出 + relaunch（D-B2）"
    - "PUT /settings 失败时 Switch 视觉回滚 + 显示 saveError inline 5s 自动消失（UI-SPEC interaction contract）"
    # SUPERSEDED (commit 7e49605a): 原 truth #5 要求 mount 时调 get_restart_state + mtime > processStartedAt 推导，但 mtime 无法区分 settings.json 中具体改动的字段（会在用户改其它设置后误报 LAN-only pending）。改为前端 in-memory pending — 仅当前 session 切换后显示，不跨 session 持久化。已移除符号: get_restart_state / RestartState / settingsMtime / processStartedAt。请勿重新引入 mtime-based 跨 session 逻辑。
    - "占位组件 + placeholder fallback 字符串完全删除（Pitfall 11 + ROADMAP 验收 #1）"
    - "Switch 用 checked={!allowRelayFallback}（D-D2 反向命名 — 唯一允许的取反点）"
    - "Switch 三态视觉只靠 RestartBanner appear/disappear 表达（D-A3）"
  artifacts:
    - path: "src/components/setting/NetworkSection.tsx"
      provides: "重写后的 NetworkSection — 整合 RestartBanner + LanOnlyDisclosure + Switch + debounce + 乐观 pending"
      contains: "useDebounce"
    - path: "src/components/setting/__tests__/NetworkSection.test.tsx"
      provides: "NetworkSection 全集成测试覆盖 NETSET-04/05/06"
      contains: "describe('NetworkSection"
  key_links:
    - from: "Switch onCheckedChange"
      to: "useState setAllowRelayFallback + useDebounce"
      via: "本地 state + debounced value useEffect → updateNetworkSetting"
      pattern: "useDebounce\\("
    - from: "RestartBanner onRestart"
      to: "invokeWithTrace('restart_app')"
      via: "包装函数注入 props.onRestart"
      pattern: "invokeWithTrace.*restart_app"
    - from: "组件 mount"
      to: "invokeWithTrace('get_restart_state')"
      via: "useEffect on mount"
      pattern: "invokeWithTrace.*get_restart_state"
---

<objective>
Phase 95 的"最后一公里"：把 Plan 01-05 ship 的所有积木装配成最终用户可见的 NetworkSection。**完全重写** `src/components/setting/NetworkSection.tsx`（删除占位组件 + fallback 字符串），整合 RestartBanner / LanOnlyDisclosure / Switch / useDebounce / Tauri commands 与 SettingContext.updateNetworkSetting，覆盖 ROADMAP 全部 4 条 success criteria 与 3 个 Pitfall 防御（5 / 10 / 11）。

**Purpose:**
- ROADMAP 验收 #1：用户看到 LAN-only Switch + 占位组件无残留
- ROADMAP 验收 #2：切换后看到持久 inline RestartBanner 三态视觉 + 「立即重启」按钮
- ROADMAP 验收 #3：info icon Popover 披露 4 类外网请求
- ROADMAP 验收 #4：state 名 allowRelayFallback + debounce ≥ 500ms 写入

**注意 autonomous: false**：本 plan 含 1 个 `checkpoint:human-verify` task —— 用户需肉眼验证开/关切换 + RestartBanner appear/disappear + Popover 4 项展开 + 「立即重启」实际能让进程重启。这是 Phase 95 唯一的 checkpoint，因为 Tauri `app.restart()` 集成层无法在 jsdom 内自动验证（需要真实 Tauri 进程 + webview）。

**Output:**
- 完全重写 NetworkSection.tsx（占位组件清零，新代码 ~80-120 行）
- 完整集成测试（mock Tauri commands + SettingContext + i18n）
- 1 个人工验证 checkpoint（Tauri 真实进程 e2e）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/095-networksection-ux/095-CONTEXT.md
@.planning/phases/095-networksection-ux/095-PATTERNS.md
@.planning/phases/095-networksection-ux/095-UI-SPEC.md
@.planning/research/PITFALLS.md
@src/components/setting/SyncSection.tsx
@src/components/setting/SettingGroup.tsx
@src/components/setting/SettingRow.tsx
@src/components/setting/RestartBanner.tsx
@src/components/setting/LanOnlyDisclosure.tsx
@src/contexts/SettingContext.tsx
@src/hooks/useDebounce.ts
@src/lib/tauri-command.ts

<interfaces>
<!-- Plan 04 已 ship 的 useSetting().updateNetworkSetting -->
const { setting, error, updateNetworkSetting } = useSetting()
// updateNetworkSetting: (Partial<NetworkSettings>) => Promise<{ restartRequired: boolean }>

<!-- Plan 05 已 ship 的 RestartBanner -->
import { RestartBanner } from './RestartBanner'
// Props: { visible, onRestart, loading?, error?, onDismissError? }

<!-- Plan 05 已 ship 的 LanOnlyDisclosure -->
import { LanOnlyDisclosure } from './LanOnlyDisclosure'
// 无 props

<!-- Plan 02 已 ship 的 Tauri commands -->
const state = await invokeWithTrace<{
  processStartedAt: number
  settingsMtime: number
}>('get_restart_state')

await invokeWithTrace<void>('restart_app')

<!-- 现有 useDebounce -->
const debouncedValue = useDebounce(value, 500)
</interfaces>

<analog_excerpt>
<!-- SyncSection.tsx:1-70 — Section 组件骨架 -->
```typescript
import { useEffect, useState } from 'react'
import { useTranslation } from 'react-i18next'
import { SettingGroup } from './SettingGroup'
import { SettingRow } from './SettingRow'
import { Switch } from '@/components/ui'
import { useSetting } from '@/hooks/useSetting'

const SyncSection: React.FC = () => {
  const { t } = useTranslation()
  const { setting, error, updateSyncSetting } = useSetting()
  const [autoSync, setAutoSync] = useState(setting?.sync.autoSync ?? true)

  useEffect(() => {
    if (setting) {
      setAutoSync(setting.sync.autoSync)
    }
  }, [setting])

  const handleAutoSyncChange = (checked: boolean) => {
    setAutoSync(checked)
    updateSyncSetting({ autoSync: checked })
  }

  if (error) {
    return (
      <div className="text-destructive py-4">
        {t('settings.sections.sync.loadError')} {error}
      </div>
    )
  }

  return (
    <SettingGroup title={t('settings.categories.sync')}>
      <SettingRow ...>...</SettingRow>
    </SettingGroup>
  )
}
```
</analog_excerpt>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — NetworkSection 集成测试覆盖三态、debounce、pending 推导、错误回滚</name>
  <read_first>
    - `.planning/phases/095-networksection-ux/095-UI-SPEC.md` §`## Interaction Contract`（每条 trigger → behavior 必须覆盖一个测试）
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/components/setting/NetworkSection.tsx` 段
    - `src/components/setting/SyncSection.tsx`（Section 骨架 analog）
    - `src/components/setting/__tests__/RestartBanner.test.tsx`（Plan 05 创建 — 学习 RTL i18n setup 模式）
    - `src/contexts/__tests__/SettingContext.network.test.tsx`（Plan 04 创建 — mock useSetting / updateSettings 模式）
    - `src/hooks/useDebounce.ts`（debounce 行为：vi.useFakeTimers + vi.advanceTimersByTime）
  </read_first>
  <files>src/components/setting/__tests__/NetworkSection.test.tsx</files>
  <behavior>
    Test 1（applied OFF 默认态）: setting.network.allowRelayFallback=true、settingsMtime ≤ processStartedAt → Switch=OFF（unchecked）+ RestartBanner 不可见
    Test 2（applied ON 默认态）: 设置 allowRelayFallback=false 且 settingsMtime ≤ processStartedAt → Switch=ON（checked）+ RestartBanner 不可见
    Test 3（用户点 Switch 触发 pending change，乐观）: 起始 OFF → 点击 Switch → Switch 立即变 ON + RestartBanner 立即可见（不等 debounce）
    Test 4（debounce 500ms 后才调 updateNetworkSetting）: vi.useFakeTimers → click Switch → 100ms 内不调 mockUpdateNetworkSetting → 500ms 后调一次（最后值）
    Test 5（连击 Switch 只 PUT 一次）: 100ms 内点击 3 次 → advance 600ms → mockUpdateNetworkSetting 只被调一次（最后状态）
    Test 6（点击「立即重启」调 invokeWithTrace('restart_app')）: pending 状态下点击 RestartBanner CTA → mockInvokeWithTrace 调 'restart_app' 一次
    Test 7（restart_app 失败 → RestartBanner.error prop 渲染）: mockInvokeWithTrace.mockRejectedValueOnce → 期望 RestartBanner 显示 errorMessage + 「重试」按钮
    Test 8（PUT 失败回滚 Switch + 显示 saveError）: mockUpdateNetworkSetting.mockRejectedValueOnce → debounce 触发后 Switch 视觉回到旧值 + 显示 saveError 文本
    Test 9（mount 时 settings_mtime > process_started_at → RestartBanner 可见 D-D1 跨 session）: mockInvokeWithTrace('get_restart_state') 返回 `{ processStartedAt: 1000, settingsMtime: 2000 }` → mount 后 RestartBanner 可见
    Test 10（mount 时 settings_mtime ≤ process_started_at → RestartBanner 不可见）: 返回 `{ processStartedAt: 2000, settingsMtime: 1000 }` → mount 后 RestartBanner 不可见
    Test 11（LanOnlyDisclosure trigger button 在 SettingRow 内可见）: getByRole('button', { name: /查看 LAN-only|View the list/ })
    Test 12（component error 状态显示 loadError 占位）: useSetting 返回 error="加载设置失败" → 渲染 destructive 文本（沿用 SyncSection 模式）
    Test 13（占位组件残留 fence）: render 后 querySelector 不应找到 "Network settings are not yet available" 文本（Pitfall 11）
    Test 14（Switch 反向命名 fence — checked=!allowRelayFallback）: setting.network.allowRelayFallback=false → Switch checked=true（即 LAN-only ON ⇔ allowRelay=false）
  </behavior>
  <action>
    1. 创建 `src/components/setting/__tests__/NetworkSection.test.tsx`
    2. mock 链：
       ```typescript
       vi.mock('@/lib/tauri-command', () => ({
         invokeWithTrace: vi.fn(),
       }))
       vi.mock('@/hooks/useSetting', () => ({
         useSetting: vi.fn(),
       }))
       ```
    3. 用 `vi.useFakeTimers()` + `vi.advanceTimersByTime(500)` 测试 debounce
    4. helper `setupSetting(overrides)` 返回 `useSetting` mock value 含 `setting` + `updateNetworkSetting`
    5. helper `setupRestartState({ processStartedAt, settingsMtime })` mock invokeWithTrace 第一次调 `get_restart_state` 时返回这俩值
    6. 测试 1-14 完整实装
    7. 运行 `pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx` 期望**失败**（NetworkSection 仍是占位，14 个测试全 fail）
    8. 提交：`test(95-06): RED — NetworkSection 集成测试覆盖三态、debounce、pending 推导、错误回滚`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/setting/__tests__/NetworkSection.test.tsx` 文件存在
    - 文件含 `describe('NetworkSection`
    - 文件含至少 14 个 `it(` / `test(`
    - 文件含 `vi.useFakeTimers`
    - 文件含 `advanceTimersByTime(500)` 或类似 timer 操作
    - 文件至少 6 处 `RestartBanner`、6 处 `allowRelayFallback`、3 处 `invokeWithTrace`、2 处 `'restart_app'`（注：原 `'get_restart_state'` 计数已删除 — 见 commit 7e49605a 的 supersession，mtime 推导改为 in-memory pending）
    - 运行 `pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx` 退出码非 0（RED — 占位组件不能 PASS 这些测试）
    - `git log --oneline -1` 标题以 `test(95-06):` 开头
  </acceptance_criteria>
  <done>测试文件提交，运行返回 ≥10 失败。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — 完全重写 NetworkSection.tsx 整合所有 Phase 95 积木</name>
  <read_first>
    - Task 1 创建的失败测试（明确所有期望行为）
    - `.planning/phases/095-networksection-ux/095-UI-SPEC.md` §`## Layout Contract` + §`## Interaction Contract`（完整 DOM 与交互规范）
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/components/setting/NetworkSection.tsx` 段
    - `src/components/setting/NetworkSection.tsx` 当前占位代码（删除目标）
    - `src/components/setting/SyncSection.tsx`（骨架 analog）
    - Plan 02 / 04 / 05 ship 的所有 imports
  </read_first>
  <files>src/components/setting/NetworkSection.tsx</files>
  <action>
    **完全重写**（删除当前 25 行占位 + 写入下面的实装）：

    ```typescript
    import { useEffect, useRef, useState } from 'react'
    import { useTranslation } from 'react-i18next'
    import { Switch } from '@/components/ui'
    import { useSetting } from '@/hooks/useSetting'
    import { useDebounce } from '@/hooks/useDebounce'
    import { invokeWithTrace } from '@/lib/tauri-command'
    import { createLogger } from '@/lib/logger'
    import { LanOnlyDisclosure } from './LanOnlyDisclosure'
    import { RestartBanner } from './RestartBanner'
    import { SettingGroup } from './SettingGroup'
    import { SettingRow } from './SettingRow'

    const log = createLogger('network-section')

    /** Wire shape returned by `get_restart_state` Tauri command (Plan 02). */
    interface RestartState {
      processStartedAt: number
      settingsMtime: number
    }

    /**
     * NetworkSection — Phase 95.
     *
     * 用户在 Settings → Network 切换 LAN-only Mode；切换后看到持久 inline RestartBanner，
     * 点「立即重启」触发 Tauri app.restart()。Pending 跨 session 由 Tauri command
     * `get_restart_state` 推导（settingsMtime > processStartedAt ⇒ pending — D-D1）。
     *
     * # 反向命名规则（per ROADMAP 核心约束 + Pitfall 1）
     * UI checked === ON === allowRelayFallback === false。
     * 本组件含**唯一一处**前端取反点：`<Switch checked={!allowRelayFallback}>` 与
     * onCheckedChange 内的 `setAllowRelayFallback(!checked)`。
     */
    const NetworkSection: React.FC = () => {
      const { t } = useTranslation()
      const { setting, error, updateNetworkSetting } = useSetting()

      // 当前持久值（来自 SettingContext，作为 baseline）
      const persistedAllowRelay = setting?.network?.allowRelayFallback ?? true

      // 本地乐观 state（D-D2：切换后立即更新，不等 PUT 返回）
      const [allowRelayFallback, setAllowRelayFallback] = useState(persistedAllowRelay)

      // pending 状态（来自三个源：用户切换 / mtime 推导 / Tauri 重启失败回滚）
      const [pending, setPending] = useState(false)
      const [restartLoading, setRestartLoading] = useState(false)
      const [restartError, setRestartError] = useState<string | null>(null)
      const [saveError, setSaveError] = useState<string | null>(null)

      // 防止 useEffect 在 mount 时立即触发 PUT（pristine flag）
      const isPristineRef = useRef(true)

      // debounced 写盘值（D-D3：500ms after last user change）
      const debouncedAllowRelay = useDebounce(allowRelayFallback, 500)

      // ── Effect 1: setting 加载后同步本地 state baseline ─────────────
      useEffect(() => {
        if (setting?.network) {
          setAllowRelayFallback(setting.network.allowRelayFallback)
        }
      }, [setting])

      // ── Effect 2: mount 时调 get_restart_state 推导跨-session pending ──
      useEffect(() => {
        let cancelled = false
        ;(async () => {
          try {
            const state = await invokeWithTrace<RestartState>('get_restart_state')
            if (cancelled) return
            // D-D1: settingsMtime > processStartedAt ⇒ 本进程启动后 settings.json 改过
            // ⇒ 还没 relaunch ⇒ pending
            if (state.settingsMtime > state.processStartedAt && state.processStartedAt > 0) {
              setPending(true)
            }
          } catch (err) {
            log.error({ err }, 'Failed to query restart state')
          }
        })()
        return () => {
          cancelled = true
        }
      }, [])

      // ── Effect 3: debounced PUT /settings ───────────────────────────
      useEffect(() => {
        if (isPristineRef.current) {
          isPristineRef.current = false
          return
        }
        if (!setting) return
        // 用户实际改变了值才发 PUT
        if (debouncedAllowRelay === persistedAllowRelay) return

        ;(async () => {
          try {
            const result = await updateNetworkSetting({
              allowRelayFallback: debouncedAllowRelay,
            })
            if (result.restartRequired) {
              setPending(true)
            }
          } catch (err) {
            log.error({ err }, '保存 LAN-only 设置失败')
            // PUT 失败回滚 Switch 视觉到 persisted 值 + 显示 inline saveError
            setAllowRelayFallback(persistedAllowRelay)
            setPending(false)
            setSaveError(err instanceof Error ? err.message : String(err))
            // 5s 后自动清除 saveError（per UI-SPEC interaction contract）
            window.setTimeout(() => setSaveError(null), 5000)
          }
        })()
      }, [debouncedAllowRelay])

      // ── Switch 切换 handler（反向命名唯一取反点） ──────────────────
      const handleSwitchChange = (checked: boolean) => {
        const newAllowRelay = !checked  // ← 唯一取反点
        setAllowRelayFallback(newAllowRelay)
        setPending(true)
        setSaveError(null)
        setRestartError(null)
      }

      // ── 「立即重启」按钮 handler ───────────────────────────────────
      const handleRestart = async () => {
        setRestartLoading(true)
        setRestartError(null)
        try {
          await invokeWithTrace<void>('restart_app')
          // app.restart() 不返回；以下不可达
        } catch (err) {
          log.error({ err }, 'app.restart() 失败')
          setRestartError(t('settings.sections.network.restartBanner.errorMessage'))
          setRestartLoading(false)
        }
      }

      // ── error state（getSettings 失败）─────────────────────────────
      if (error) {
        return (
          <div className="text-destructive py-4">
            {t('settings.sections.sync.loadError')} {error}
          </div>
        )
      }

      return (
        <SettingGroup title={t('settings.categories.network')}>
          <RestartBanner
            visible={pending}
            onRestart={handleRestart}
            loading={restartLoading}
            error={restartError}
            onDismissError={() => setRestartError(null)}
          />
          <SettingRow
            label={t('settings.sections.network.lanOnly.label')}
            labelExtra={<LanOnlyDisclosure />}
            description={t('settings.sections.network.lanOnly.description')}
          >
            <Switch
              id="lan-only-switch"
              checked={!allowRelayFallback}
              onCheckedChange={handleSwitchChange}
            />
          </SettingRow>
          {saveError && (
            <div className="px-4 pb-3 text-xs text-destructive" role="alert">
              {t('settings.sections.network.lanOnly.saveError', { message: saveError })}
            </div>
          )}
        </SettingGroup>
      )
    }

    export default NetworkSection
    ```

    **关键约束（多重 Pitfall 防御）：**
    1. **Pitfall 1**：唯一取反点是 `handleSwitchChange` 内的 `!checked` 与 `<Switch checked={!allowRelayFallback}>` —— grep `!allowRelayFallback` 全工程应仅命中 NetworkSection.tsx 一个文件
    2. **Pitfall 11**：旧占位 `t('settings.sections.network.placeholder')` fallback 字符串完全删除 — 整个文件不出现 "placeholder" / "Network settings are not yet available" / "网络设置功能在新架构中尚未实现"
    3. **Pitfall 10**：useDebounce 500ms + 持久 RestartBanner（不是 toast）+ 「立即重启」按钮路径完整
    4. **D-D2 乐观 pending**：`handleSwitchChange` 立即 `setPending(true)`，**不等** updateNetworkSetting 返回
    5. **D-D3 debounce 与 Banner 解耦**：Banner 显示来自 `pending` state（用户切换瞬间设 true），PUT 写盘是 debounced effect 后续事件
    6. **i18n interpolation**：`t('...saveError', { message })` 使用 `{{message}}` placeholder（与 Plan 03 i18n 文件 `{{message}}` 形态一致）

    运行 `pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx` 期望 14/14 PASS。

    提交：`feat(95-06): GREEN — NetworkSection 重写整合 RestartBanner + LanOnlyDisclosure + Switch + debounce + pending 推导`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/setting/NetworkSection.tsx` 含 `useDebounce`
    - `src/components/setting/NetworkSection.tsx` 含 `useSetting`
    - `src/components/setting/NetworkSection.tsx` 含 `RestartBanner`
    - `src/components/setting/NetworkSection.tsx` 含 `LanOnlyDisclosure`
    - `src/components/setting/NetworkSection.tsx` 含 `invokeWithTrace<RestartState>('get_restart_state')`
    - `src/components/setting/NetworkSection.tsx` 含 `invokeWithTrace<void>('restart_app')`
    - `src/components/setting/NetworkSection.tsx` 含 `<Switch` `checked={!allowRelayFallback}` 模式
    - `src/components/setting/NetworkSection.tsx` 含 `setAllowRelayFallback(!checked)`（handleSwitchChange 内）
    - `src/components/setting/NetworkSection.tsx` 不含 `'placeholder'`（Pitfall 11 fence）
    - `src/components/setting/NetworkSection.tsx` 不含 `Network settings are not yet available`
    - `src/components/setting/NetworkSection.tsx` 不含 `网络设置功能在新架构中尚未实现`
    - `src/components/setting/NetworkSection.tsx` 不含 `lanOnly` 字段定义（仅 i18n key 字符串中含 lanOnly 路径，那是 i18n 字面量不算字段）—— 用 `grep -E "(let|const|var)[[:space:]]+lanOnly|lanOnly[[:space:]]*[:=]" src/components/setting/NetworkSection.tsx` 期望 0 匹配
    - 全工程 `grep -rE "!allowRelayFallback|!.+\.allowRelayFallback" src/` 仅命中 src/components/setting/NetworkSection.tsx（其它文件应为 0 — 即 types/api/contexts 都不取反）
    - `pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx` 退出码 0（14/14 PASS）
    - `pnpm test --run src/components/setting/__tests__/RestartBanner.test.tsx` 退出码 0（不破 Plan 05 测试）
    - `pnpm test --run src/components/setting/__tests__/LanOnlyDisclosure.test.tsx` 退出码 0（不破 Plan 05 测试）
    - `git log --oneline -1` 标题以 `feat(95-06):` 开头
  </acceptance_criteria>
  <done>NetworkSection 完全重写、占位清零、14/14 测试 PASS、唯一取反点 fence 落实。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR — Pitfall 5/10/11 全工程 fence 验证 + 唯一取反点最终 fence</name>
  <read_first>
    - Task 2 重写后的 NetworkSection.tsx
    - `.planning/research/PITFALLS.md` Pitfall 5 / 10 / 11 完整 warning signs
    - Plan 03 创建的 i18n 文件
  </read_first>
  <files>src/components/setting/__tests__/NetworkSection.test.tsx</files>
  <action>
    1. 在测试文件末尾追加 ROADMAP 验收 fence 测试块：
       ```typescript
       describe('Phase 95 ROADMAP fence — 4 验收 + 3 Pitfall 防御', () => {
         it('Pitfall 11 — 占位组件残留全清', () => {
           // 静态约束：本测试断言 NetworkSection.tsx 文件**没有**渲染占位文本
           const { container } = renderNetworkSection({ /* default */ })
           expect(container.textContent).not.toMatch(/Network settings are not yet available/)
           expect(container.textContent).not.toMatch(/网络设置功能在新架构中尚未实现/)
         })

         it('Pitfall 10 — RestartBanner 是持久 inline 不是 toast', () => {
           // toast 通常不是 role=status，是 role=alert + 自动 dismiss
           const { container } = renderNetworkSection({ pending: true })
           const banner = container.querySelector('[role="status"]')
           expect(banner).not.toBeNull()
           // 确认不是 sonner / react-hot-toast 容器
           expect(container.querySelector('[data-sonner-toaster]')).toBeNull()
         })

         it('Pitfall 5 — 4 类外网请求清单存在（与 docs/lan-only.md baseline 对齐）', async () => {
           const { user } = renderNetworkSection({})
           await user.click(screen.getByRole('button', { name: /查看 LAN-only|View the list/ }))
           // 4 类清单标题必须存在
           expect(screen.getByText(/首次配对 rendezvous|First-pairing rendezvous/)).toBeInTheDocument()
           expect(screen.getByText(/OTLP 遥测|OTLP telemetry/)).toBeInTheDocument()
           expect(screen.getByText(/pkarr DHT|pkarr DHT NodeId/)).toBeInTheDocument()
           expect(screen.getByText(/自动更新|Auto-update GitHub/)).toBeInTheDocument()
         })

         it('反向命名 — 唯一取反点是 NetworkSection.tsx', () => {
           // 静态约束 — 由 acceptance grep 强制
           // 此测试 always PASS，存在意义：让 PR diff reviewer 看到这条约束
           expect(true).toBe(true)
         })
       })
       ```

    2. 全工程 grep 验证 fence：

       a. **Pitfall 1 唯一取反点**：
       ```bash
       grep -rnE "!allowRelayFallback|!.+\.allowRelayFallback|!setting\.network\.allowRelayFallback" src/ 2>/dev/null
       ```
       期望命中位置仅 `src/components/setting/NetworkSection.tsx` 多行（handleSwitchChange + Switch checked 表达式）；其它文件 0 匹配。

       b. **Pitfall 1 lanOnly 字段镜像**：
       ```bash
       grep -rnE "(let|const|var)[[:space:]]+lanOnly|lanOnly[[:space:]]*[:=][^a-zA-Z=]" src/ 2>/dev/null | grep -v "i18n/locales"
       ```
       期望 0 匹配（i18n key 字符串文字不算字段）。

       c. **Pitfall 11 占位组件残留**：
       ```bash
       grep -rE "Network settings are not yet available|网络设置功能在新架构中尚未实现|settings\.sections\.network\.placeholder" src/ 2>/dev/null
       ```
       期望 0 匹配。

       d. **Pitfall 5 禁词清单**（在所有 src/ 与已写文档面）：
       ```bash
       grep -rE "fully offline|完全离线|绝对私有|no internet|private mode|encrypted-and-local" src/ 2>/dev/null
       ```
       期望 0 匹配。

       e. **D-A1 fence — RestartBanner 不复用 Alert / sonner**：
       ```bash
       grep -E "from '@/components/ui/alert'|from 'sonner'|react-hot-toast" src/components/setting/RestartBanner.tsx src/components/setting/NetworkSection.tsx 2>/dev/null
       ```
       期望 0 匹配。

       f. **D-C1 fence — Disclosure 不用 hover Tooltip**：
       ```bash
       grep -E "Tooltip|onMouseEnter|onHover" src/components/setting/LanOnlyDisclosure.tsx 2>/dev/null
       ```
       期望 0 匹配。

    3. `pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx` 期望 18/18 PASS（原 14 + fence 4）

    4. 提交：`refactor(95-06): Pitfall 5/10/11 全工程 fence + 唯一取反点 + 禁词清单 final audit`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx 2>&1 | tail -10 && (grep -rE "Network settings are not yet available|网络设置功能在新架构中尚未实现|settings\.sections\.network\.placeholder" src/ ; test $? -eq 1) && (grep -rE "fully offline|完全离线|绝对私有|no internet|private mode|encrypted-and-local" src/ ; test $? -eq 1)</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --run src/components/setting/__tests__/NetworkSection.test.tsx` 退出码 0（≥18 PASS）
    - `grep -rE "Network settings are not yet available|网络设置功能在新架构中尚未实现|settings\.sections\.network\.placeholder" src/` 返回 0 匹配（Pitfall 11）
    - `grep -rE "fully offline|完全离线|绝对私有|no internet|private mode|encrypted-and-local" src/` 返回 0 匹配（Pitfall 5）
    - `grep -rnE "!allowRelayFallback|!.+\.allowRelayFallback" src/` 返回的命中行只在 `src/components/setting/NetworkSection.tsx`（其它文件 0 行 — 唯一取反点 fence）
    - `grep -E "from '@/components/ui/alert'|from 'sonner'" src/components/setting/RestartBanner.tsx` 返回 0 匹配（D-A1）
    - `grep -E "Tooltip|onMouseEnter|onHover" src/components/setting/LanOnlyDisclosure.tsx` 返回 0 匹配（D-C1）
    - `git log --oneline -1` 标题以 `refactor(95-06):` 开头
  </acceptance_criteria>
  <done>所有 Pitfall 防御 fence 已落 + 唯一取反点验证 + 禁词清单全工程 0。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 人工 UAT — 真实 Tauri 进程 LAN-only Switch + 重启循环</name>
  <what-built>
    Phase 95 完整 NetworkSection 实装 — Switch 切换 / RestartBanner 三态 / 「立即重启」端到端、Popover 4 类外网请求展开。
    所有自动化测试都过了，但 Tauri `app.restart()` 的真实进程退出 + relaunch 行为 jsdom 测不到，必须真机验证。
  </what-built>
  <how-to-verify>
    在仓库根运行 `pnpm tauri dev`（或 macOS 上 `pnpm tauri:dev`），等 Tauri 主窗口起来后：

    **1. 验收 #1（Pitfall 11 — 占位无残留）**
    - 打开 Settings → Network 分类
    - 期望：看到 "LAN-only 模式" 标题与开关（不是 "网络设置功能在新架构中尚未实现" 占位文字）
    - 期望：开关默认 OFF（slider 在左、灰色）

    **2. 验收 #3（NETSET-06 — info icon Popover 4 类外网请求）**
    - 点击开关旁边的 info icon（lucide Info ⓘ 图标）
    - 期望：弹出 Popover，含 4 类清单标题：
      - 首次配对 rendezvous（描述含 `rendezvous.uniclipboard.app`）
      - OTLP 遥测（描述含 "通用 → 遥测"）
      - pkarr DHT NodeId 解析（描述含 "类似 DNS"）
      - 自动更新 GitHub 检查（描述含 "GitHub Release API"）
    - 期望：**hover** info icon 时不应该自动弹 Popover（D-C1 — 仅 click）
    - 期望：Popover 文本可选可复制
    - 期望：Esc 或点击 Popover 外部关闭

    **3. 验收 #2（NETSET-05 — 持久 RestartBanner + 三态 + 立即重启）**
    - 切换开关到 ON
    - 期望：开关立即变蓝/ON（slider 滑右）
    - 期望：开关上方**立即出现** RestartBanner，背景灰色（bg-accent/40）+ 旋转/刷新 icon + 主文案 "需要重启应用以使 LAN-only 模式更改生效。" + 蓝色「立即重启」按钮
    - 期望：Banner **不会自动消失**（不是 toast — 等 5 秒确认仍可见）
    - **Pitfall 10 防御** ✓
    - 期望：Switch 视觉本身**没**有 "Pending" Badge（D-A3 — 三态视觉只靠 Banner 出现/消失表达）
    - 期望：500ms 后 settings.json 应被写入（可在另一个终端 `cat ~/Library/Application\ Support/com.uniclipboard.uniclipboard/settings.json | grep -A 2 network` 验证含 `"allowRelayFallback": false`）

    **4. 验收 #4（NETSET-04 → restart loop）**
    - 点击 RestartBanner 内的「立即重启」按钮
    - 期望：按钮文案变 "正在重启…" + 按钮 disabled
    - 期望：1-2 秒内 Tauri 整个 GUI 进程退出 + 自动 relaunch
    - 期望：新进程起来后，Settings → Network：开关仍是 ON（已 persisted），**RestartBanner 已消失**（mtime ≤ processStartedAt）
    - **Pitfall 3 + Pitfall 10 防御** ✓

    **5. 反向命名验证**
    - 当前 Switch=ON（LAN-only ON）→ 打开 daemon 日志看（dev console / `tail -f ~/.../uniclipboard.log`）应有 `disable_relays = true` 或 `RelayMode::Disabled` 启动 trace
    - 关闭开关回到 OFF + 重启 → daemon 日志应回到允许 relay
    - **Pitfall 1 防御** ✓

    **6. 跨 session pending 推导（D-D1）**
    - 在第二个终端：`touch ~/Library/Application\ Support/com.uniclipboard.uniclipboard/settings.json`（更新 mtime）
    - 立即在 Tauri webview 中 reload Settings 页（或导航离开 Network 再回来）
    - 期望：RestartBanner 又出现（mtime > processStartedAt）

    **7. 边界情形（如有时间）**
    - 切换开关后 1 秒内连击 3 次 → 期望最终只 PUT 一次（debounce 生效；可 daemon 日志 grep `PUT /settings` 验证）

    **报告格式：**
    - PASS / FAIL × 6 验收点
    - 任何不符合预期的截图或 daemon 日志贴出
    - 如发现 i18n 文案有误或 fence 漏网，描述具体位置
  </how-to-verify>
  <resume-signal>
    输入 "approved" 视为 6/6 验收 PASS；否则描述具体失败点（哪个验收 # + 实际行为 + 期望行为），由 planner 走 `/gsd-plan-phase 95 --gaps` 决定补丁方案。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 UI 操作 → settings.json 写盘 | Switch 切换触发 PUT，daemon 通过 `apply_settings_patch` 合并 — Phase 94 已 ship；前端只发 patch，不直接写文件 |
| 用户「立即重启」点击 → app.restart() | Plan 02 的 restart_app command 走 `app.restart()`；进程退出是用户主动行为（点了按钮），无外部触发面 |
| Tauri webview ↔ NetworkSection ↔ daemon HTTP | 三跳：webview JS → invokeWithTrace → Tauri Rust command → daemon HTTP；每跳都有自身验证（Plan 01 类型 + Plan 02 trace span + Phase 94 wire schema） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-06-01 | Spoofing | NetworkSection 显示 LAN-only ON 但 daemon 仍 bind 在允许 relay 模式 — 用户被欺骗以为流量已局域网化（Pitfall 5 transparency violation） | mitigate | "重启生效"语义通过 RestartBanner 显式告知用户当前是 pending 不是 applied；Pitfall 3 防御已锁（OnceCell + bind 时常量）；任何 Switch=ON 但 banner 隐藏的状态 = 已重启 = daemon 已 bind 新值；Phase 96 ConnectionChannelPort 提供肉眼可见的 bind 真实状态 |
| T-95-06-02 | Tampering | 反向命名 bug（如 Switch checked={allowRelayFallback}）让用户开 LAN-only 实际反而禁用了局域网 | mitigate | Test 14 反向命名断言；Pitfall 1 唯一取反点 grep fence 只允许 NetworkSection.tsx 出现；Plan 01 + 02 + 04 + 05 全部 fence 落地 |
| T-95-06-03 | Information Disclosure | LAN-only 营销语 vs 现实边界（fully offline / 完全离线）泄漏到 UI（Pitfall 5） | mitigate | Plan 03 禁词清单 grep fence；Task 3 重跑 fence；Popover 4 类外网请求清单显式披露（与 Plan 03 i18n 文案对齐 — 双语 baseline 已锁定） |
| T-95-06-04 | DoS | 用户连击 Switch 100 次/秒导致 disk I/O 爆 → daemon 卡 | mitigate | useDebounce 500ms 强制合并；Test 5 验证连击只 PUT 一次；Pitfall 10 防御 |
| T-95-06-05 | Repudiation | restart_app 执行后无审计日志 — 用户事后不知道是谁触发了重启 | mitigate | Plan 02 的 restart_app 内 `info!("restarting app for settings change (LAN-only Mode)")` + tracing span；handleRestart 在前端 log.error 失败路径；用户行为通过 trace_id 关联 |
| T-95-06-06 | UX deception | RestartBanner 一闪而过（toast 形态）让用户怀疑没生效反复切换（Pitfall 10） | mitigate | RestartBanner 是持久 inline（visible=true 持续可见，仅 dismiss 是 error sub-state 而不是主信息）；Test 7（Pitfall 10 fence）静态断言 role=status 存在 + 无 sonner toast 容器 |
| T-95-06-07 | UX deception | 占位组件残留 — 旧 "Network settings are not yet available" 还在让用户混淆（Pitfall 11） | mitigate | Task 3 grep fence 全工程 0；测试 Test 13 静态断言不渲染占位文案 |
</threat_model>

<verification>
- 14 个集成测试 + 4 个 Pitfall fence 测试 = 18/18 PASS
- 全工程 grep fence 全部 0 匹配（Pitfall 1 唯一取反点 / lanOnly 镜像 / Pitfall 5 禁词 / Pitfall 11 占位 / D-A1 Alert / D-C1 Tooltip）
- Plan 05 的 RestartBanner.test.tsx + LanOnlyDisclosure.test.tsx 不破
- Plan 04 的 SettingContext.network.test.tsx 不破
- Plan 01 的 settings.test.ts 不破
- 人工 UAT 6/6 验收 PASS（Tauri 真机进程重启 + Switch 切换 + Popover 展开 + 反向命名 + pending 跨 session）
- daemon 日志确认 `disable_relays` 与 Switch 状态对齐
</verification>

<success_criteria>
- ROADMAP 验收 #1：占位组件 + placeholder fallback 全清，Settings → Network 显示真实 LAN-only Switch
- ROADMAP 验收 #2：持久 RestartBanner（不是 toast）+ 三态视觉 + 「立即重启」端到端可触发 Tauri 重启
- ROADMAP 验收 #3：info icon Popover 含 4 类外网请求清单，文案与 Phase 97 docs/lan-only.md 一致（baseline 已锁）
- ROADMAP 验收 #4：state 名 `allowRelayFallback` 不维护 lanOnly 镜像；debounce 500ms 写入
- Pitfall 5 / 10 / 11 三条防御铁律全 fence 落地
- 4 commits（test → feat → refactor → human-UAT 不产 commit）
</success_criteria>

<output>
完成后创建 `.planning/phases/095-networksection-ux/095-06-SUMMARY.md`，并附加 Phase 95 整体 closeout note：14 条 NETSET requirement（4-6）全部交付。
</output>
