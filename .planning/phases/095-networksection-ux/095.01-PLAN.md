---
phase: 95
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/setting.ts
  - src/api/daemon/settings.ts
  - src/api/daemon/__tests__/settings.test.ts
autonomous: true
requirements: [NETSET-04, NETSET-05]
tags: [frontend, types, api-client, settings, lan-only]
must_haves:
  truths:
    - "TypeScript Settings 类型暴露 network: NetworkSettings 必填字段，wire 字段名 allowRelayFallback（驼峰）"
    - "updateSettings() 返回 { success, restartRequired }，调用方可读 restartRequired 信号"
    - "toSettingsPatchRequest 在 settings.network 存在时把 { allowRelayFallback } 镜像进 PATCH body"
    - "前端 store 内部状态名 allowRelayFallback，不维护 lanOnly 镜像（反向命名规则铁律）"
  artifacts:
    - path: "src/types/setting.ts"
      provides: "NetworkSettings interface + Settings.network 必填 + SettingContextType.updateNetworkSetting 签名"
      contains: "export interface NetworkSettings"
    - path: "src/api/daemon/settings.ts"
      provides: "NetworkSettings + Settings.network + SettingsPatchRequest.network + updateSettings 返回 { restartRequired }"
      contains: "restartRequired: boolean"
    - path: "src/api/daemon/__tests__/settings.test.ts"
      provides: "toSettingsPatchRequest network 段 + updateSettings 解析 restartRequired 单元测试"
      contains: "describe('settings api"
  key_links:
    - from: "src/api/daemon/settings.ts updateSettings"
      to: "daemon HTTP wire restartRequired field（Phase 94 锁定）"
      via: "res.data.restartRequired 直读"
      pattern: "res\\.data\\.restartRequired"
    - from: "src/types/setting.ts SettingContextType"
      to: "下游 SettingContext.tsx (Plan 04)"
      via: "updateNetworkSetting 签名定义"
      pattern: "updateNetworkSetting.*Promise<\\{ restartRequired"
---

<objective>
建立 Phase 95 的类型契约层：在 `src/types/setting.ts` 和 `src/api/daemon/settings.ts` 中加入 `NetworkSettings` 类型与 wire 字段，让 `updateSettings()` 返回 `{ success, restartRequired }`，并把 `SettingContextType` 扩展 `updateNetworkSetting` helper 签名。这是**所有下游 plan 的契约源头**。

**Purpose:** 上游 daemon HTTP 已经在 Phase 94 中暴露 `restartRequired: boolean` 字段（wire 字段名已 grep 验证 — `uc-daemon-contract/src/api/dto/settings.rs:26` + `uc-webserver/src/api/settings.rs:91`）。前端 API 客户端当前 `updateSettings()` 丢弃响应 body（`src/api/daemon/settings.ts:201-207`），必须改造以承载 `restartRequired` 信号供 UI 决定是否显示 RestartBanner。同时 `Settings` 接口现在缺 `network` 段 —— 任何下游试图读 `setting.network.allowRelayFallback` 都会 TypeScript 报错。

**Output:**
- `src/types/setting.ts` 加 `NetworkSettings` interface + `Settings.network` 必填 + `SettingContextType.updateNetworkSetting`
- `src/api/daemon/settings.ts` 加 `NetworkSettings` + `Settings.network` + `SettingsPatchRequest.network` + `updateSettings` 返回类型升级
- 单元测试覆盖 `toSettingsPatchRequest` network 段镜像 + `updateSettings` 返回 `restartRequired` 解析
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/095-networksection-ux/095-CONTEXT.md
@.planning/phases/095-networksection-ux/095-PATTERNS.md
@.planning/phases/095-networksection-ux/095-UI-SPEC.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-VERIFICATION.md
@src/types/setting.ts
@src/api/daemon/settings.ts

<interfaces>
<!-- 上游 Phase 94 wire 契约（已锁定，前端复刻） -->

From `src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs:208-209` 的 `NetworkSettingsDto`（Rust `#[serde(rename_all = "camelCase")]`）:
```rust
pub struct NetworkSettingsDto {
    pub allow_relay_fallback: bool,  // wire JSON: allowRelayFallback
}
```

From `src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs:24-30` 的 `UpdateSettingsResponse`:
```rust
pub struct UpdateSettingsResponse {
    pub success: bool,
    pub restart_required: bool,  // wire JSON: restartRequired
}
```

From `src-tauri/crates/uc-webserver/src/api/settings.rs:75-91` 的 `update_settings_handler` 中 `restart_required = payload.network.is_some()` —— 所以 PUT 带 `network` 段时 daemon 返回 `restartRequired: true`，否则 `false`。

<!-- 下游 SettingContext.tsx 期望的 helper 签名（Plan 04 实现，本 plan 仅声明接口） -->
updateNetworkSetting: (newNetworkSetting: Partial<NetworkSettings>) => Promise<{ restartRequired: boolean }>
</interfaces>

<analog_excerpt>
<!-- src/types/setting.ts:110-119 (FileSyncSettings 模板) -->
```typescript
export interface FileSyncSettings {
  fileSyncEnabled: boolean
  smallFileThreshold: number
  maxFileSize: number
  fileCacheQuotaPerDevice: number
  fileRetentionHours: number
  fileAutoCleanup: boolean
}
```

<!-- src/api/daemon/settings.ts:281-298 (toSettingsPatchRequest fileSync 段模板) -->
```typescript
if (settings.fileSync) {
  const {
    fileSyncEnabled,
    smallFileThreshold,
    maxFileSize,
    fileCacheQuotaPerDevice,
    fileRetentionHours,
    fileAutoCleanup,
  } = settings.fileSync
  patch.fileSync = {
    fileSyncEnabled,
    smallFileThreshold,
    maxFileSize,
    fileCacheQuotaPerDevice,
    fileRetentionHours,
    fileAutoCleanup,
  }
}
```
</analog_excerpt>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — 编写 settings API 客户端的失败测试</name>
  <read_first>
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/api/daemon/settings.ts` 段（analog: fileSync 镜像模式 lines 281-298）
    - `.planning/phases/094-backend-network-allow-relay-fallback/094-VERIFICATION.md`（确认 wire 字段 `restartRequired` camelCase 已 ship）
    - `src/api/daemon/settings.ts` 现状（updateSettings 现忽略响应 body — `src/api/daemon/settings.ts:201-207`）
    - `src/api/daemon/__tests__/client.test.ts`（已有 vitest mock 模式 — 学习 daemonClient.request mock 模式）
    - `src/api/daemon/client.ts`（确认 daemonClient.request 接口形态）
  </read_first>
  <files>src/api/daemon/__tests__/settings.test.ts</files>
  <behavior>
    Test 1（toSettingsPatchRequest 镜像 network 段）: 输入 `{ network: { allowRelayFallback: false } }`，期望生成的 patch 体含 `{ network: { allowRelayFallback: false } }`，且不含其它顶层段
    Test 2（toSettingsPatchRequest 反向值）: 输入 `{ network: { allowRelayFallback: true } }` 期望 patch 含 `{ network: { allowRelayFallback: true } }`（验证不被悄悄取反 — Pitfall 1 防御）
    Test 3（toSettingsPatchRequest 无 network 段）: 输入 `{ general: { autoStart: true } }` 期望 patch 不含 `network` key
    Test 4（updateSettings 返回 restartRequired=true）: mock daemonClient.request 返回 `{ data: { success: true, restartRequired: true }, ts: 0 }`，期望 updateSettings 返回 `{ success: true, restartRequired: true }`
    Test 5（updateSettings 返回 restartRequired=false）: mock 返回 `restartRequired: false`，期望函数返回 `{ success: true, restartRequired: false }`
    Test 6（updateSettings PUT body 含 network 段）: 调用 `updateSettings({ network: { allowRelayFallback: false } } as Partial<Settings>)`，验证 daemonClient.request 被调用时 args[1].body.network 为 `{ allowRelayFallback: false }`
  </behavior>
  <action>
    1. 创建 `src/api/daemon/__tests__/settings.test.ts`，使用 vitest + `vi.mock('@/api/daemon/client', ...)`
    2. mock daemonClient.request: `vi.fn().mockResolvedValue({ data: { success: true, restartRequired: true }, ts: 0 })`
    3. 因为 `toSettingsPatchRequest` 当前是 `function`（非 export），需要通过 `updateSettings` 间接断言 PUT body 形态（mock daemonClient.request 检查参数）—— 不要为了测试而 export 内部函数
    4. 写完所有 6 个测试用例。运行 `pnpm test settings.test.ts` 期望**失败**（TypeScript 报错：`Settings` 上没有 `network` 字段；`SettingsUpdateResponse.data` 上没有 `restartRequired` 字段）
    5. 提交：`test(95-01): RED — settings api 测试覆盖 network 段镜像与 restartRequired 信号`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/api/daemon/__tests__/settings.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/api/daemon/__tests__/settings.test.ts` 文件存在
    - 文件含 `describe('toSettingsPatchRequest`（或类似 describe）至少 1 个块
    - 文件含至少 6 个 `it(` / `test(` 语句
    - 文件至少出现 6 处 `allowRelayFallback`、3 处 `restartRequired`
    - 运行 `pnpm test --run src/api/daemon/__tests__/settings.test.ts` 退出码非 0（RED 状态）
    - `git log --oneline -1` 标题以 `test(95-01):` 开头
  </acceptance_criteria>
  <done>测试文件提交，运行返回 6 个失败，提示类型字段缺失。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — 在 types + api client 中实装 NetworkSettings 与 restartRequired 让测试转绿</name>
  <read_first>
    - `src/types/setting.ts`（当前完整 — 110-133 是 FileSyncSettings + Settings 接口；181-192 是 SettingContextType）
    - `src/api/daemon/settings.ts`（当前完整 — 84-91 / 129-138 / 149-152 / 201-207 / 281-298 都是镜像模板）
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/types/setting.ts` 与 §`src/api/daemon/settings.ts`
    - `src/api/daemon/__tests__/settings.test.ts`（Task 1 创建的失败测试）
    - `src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs:24-30, 208-209`（确认 wire 字段 camelCase 名称）
  </read_first>
  <files>src/types/setting.ts, src/api/daemon/settings.ts</files>
  <action>
    **Step A — `src/types/setting.ts` 修改（仿 FileSyncSettings 模板，per D-D2 决策反向命名规则）：**

    1. 在 FileSyncSettings 接口之后（line 119 后）插入：
       ```typescript
       /**
        * Network settings — corresponds to Rust NetworkSettings / uc-daemon-contract NetworkSettingsDto.
        *
        * 网络设置 — 对应 Rust NetworkSettings / daemon-contract NetworkSettingsDto。
        *
        * # Reverse-naming rule / 反向命名规则
        * UI checked = "LAN-only Mode = ON" ⇔ allowRelayFallback === false
        * 前端只允许在 NetworkSection.tsx 一处用 `!allowRelayFallback` 取反。
        * 永远不要在前端 store 维护 lanOnly 镜像（Pitfall 1 防御）。
        */
       export interface NetworkSettings {
         allowRelayFallback: boolean
       }
       ```

    2. 在 Settings interface（line 124-133）的 `fileSync?: FileSyncSettings` 之后加必填字段：
       ```typescript
       export interface Settings {
         schemaVersion: number
         general: GeneralSettings
         sync: SyncSettings
         retentionPolicy: RetentionPolicy
         security: SecuritySettings
         pairing: PairingSettings
         keyboardShortcuts?: Record<string, string | string[]>
         fileSync?: FileSyncSettings
         network: NetworkSettings   // ← 新增，必填
       }
       ```

    3. 在 SettingContextType（line 181-192）的 `updateFileSyncSetting` 后加：
       ```typescript
       updateNetworkSetting: (
         newNetworkSetting: Partial<NetworkSettings>
       ) => Promise<{ restartRequired: boolean }>
       ```

    **Step B — `src/api/daemon/settings.ts` 修改（仿 fileSync 镜像模板，4 处变更）：**

    1. 在 FileSyncSettings 接口之后（line 91 后）加：
       ```typescript
       /** Network settings — wire field allowRelayFallback (camelCase). */
       export interface NetworkSettings {
         allowRelayFallback: boolean
       }
       ```

    2. 在 Settings interface（line 129-138）的 `fileSync: FileSyncSettings` 后加：
       ```typescript
       export interface Settings {
         schemaVersion: number
         general: GeneralSettings
         sync: SyncSettings
         retentionPolicy: RetentionPolicy
         security: SecuritySettings
         pairing: PairingSettings
         keyboardShortcuts: Record<string, ShortcutKey>
         fileSync: FileSyncSettings
         network: NetworkSettings  // ← 新增，必填
       }
       ```

    3. 修改 `SettingsUpdateResponse` interface（line 149-152）— 在 data 内加 `restartRequired`：
       ```typescript
       interface SettingsUpdateResponse {
         data: { success: boolean; restartRequired: boolean }
         ts: number
       }
       ```

    4. 修改 `SettingsPatchRequest` interface（line 154-173）—— 在 fileSync? 之后加：
       ```typescript
       network?: Partial<NetworkSettings>
       ```

    5. 修改 `updateSettings`（line 201-207）的签名 + 实现：
       ```typescript
       export async function updateSettings(
         settings: Partial<Settings>
       ): Promise<{ success: boolean; restartRequired: boolean }> {
         const patch = toSettingsPatchRequest(settings)
         const res = await daemonClient.request<SettingsUpdateResponse>('/settings', {
           method: 'PUT',
           body: patch,
         })
         return { success: res.data.success, restartRequired: res.data.restartRequired }
       }
       ```

    6. 修改 `toSettingsPatchRequest`（line 209-301）—— 在 fileSync 段之后（line 298 `}` 后、`return patch` 前）加：
       ```typescript
       if (settings.network) {
         patch.network = { allowRelayFallback: settings.network.allowRelayFallback }
       }
       ```

    7. 运行 `pnpm test --run src/api/daemon/__tests__/settings.test.ts` 期望 6/6 PASS。
    8. 运行 `pnpm tsc --noEmit`（如有 tsconfig）期望 src/types/setting.ts 与 src/api/daemon/settings.ts 无 TypeScript 错误（注意：本步会让 `src/contexts/SettingContext.tsx` 报错，因为 SettingContextType 加了未实装的 `updateNetworkSetting` —— 这是预期的 Plan 04 责任，本 plan 不修；可暂时在 SettingContext.tsx value 对象添加 `updateNetworkSetting: async () => ({ restartRequired: false })` 占位让 tsc 过，Plan 04 实装时替换）。

       **占位补丁规则**：在 `src/contexts/SettingContext.tsx` 的 `value: SettingContextType = { ... }` 对象内加临时占位：
       ```typescript
       updateNetworkSetting: async () => ({ restartRequired: false }),  // TODO(95-04): real impl
       ```
       Plan 04 会替换。这一行使全局 tsc 不破，是本 plan 唯一允许动 SettingContext.tsx 的地方。

    9. 提交：`feat(95-01): GREEN — types + api client 暴露 network.allowRelayFallback 与 restartRequired 信号`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/api/daemon/__tests__/settings.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/setting.ts` 含 `export interface NetworkSettings`
    - `src/types/setting.ts` 含 `allowRelayFallback: boolean`
    - `src/types/setting.ts` 含 `network: NetworkSettings`（注意：必填，无 `?`）
    - `src/types/setting.ts` 含 `updateNetworkSetting` 在 SettingContextType
    - `src/api/daemon/settings.ts` 含 `export interface NetworkSettings`
    - `src/api/daemon/settings.ts` 含 `restartRequired: boolean`
    - `src/api/daemon/settings.ts` 含 `network?: Partial<NetworkSettings>` 在 SettingsPatchRequest
    - `src/api/daemon/settings.ts` 含 `Promise<{ success: boolean; restartRequired: boolean }>` 在 updateSettings 签名
    - `src/api/daemon/settings.ts` 含 `patch.network = { allowRelayFallback`
    - 全工程 `grep -rE "lanOnly[^A-Za-z]" src/types/ src/api/` 返回 0 匹配（反向命名铁律 — 不维护 lanOnly 镜像）
    - `cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/api/daemon/__tests__/settings.test.ts` 退出码 0（GREEN 状态，6/6 PASS）
    - 全工程 `grep -rE "!allowRelayFallback|!.*\.allowRelayFallback" src/types/ src/api/` 返回 0 匹配（仅 NetworkSection.tsx 允许取反，本 plan 不动 NetworkSection — Plan 06 责任）
    - `git log --oneline -1` 标题以 `feat(95-01):` 开头
  </acceptance_criteria>
  <done>所有 6 个测试 PASS，types/api client 完整覆盖 network 段。SettingContext.tsx 含 TODO 占位（待 Plan 04 替换）。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR — 验证全链路类型一致 + 反向命名铁律审计</name>
  <read_first>
    - Task 2 修改后的 `src/types/setting.ts` 与 `src/api/daemon/settings.ts`
    - `.planning/research/PITFALLS.md` Pitfall 1（反向命名搞反方向 — warning signs 列表）
  </read_first>
  <files>src/api/daemon/__tests__/settings.test.ts</files>
  <action>
    1. 在 `src/api/daemon/__tests__/settings.test.ts` 末尾追加一组**反向命名审计断言**（regression fence）：

       ```typescript
       describe('反向命名审计 (Pitfall 1 fence)', () => {
         it('Settings.network 字段名是 allowRelayFallback 不是 lanOnly', () => {
           const sample: Settings['network'] = { allowRelayFallback: true }
           expect(Object.keys(sample)).toContain('allowRelayFallback')
           expect(Object.keys(sample)).not.toContain('lanOnly')
         })

         it('toSettingsPatchRequest 不取反 — true 进 true 出', () => {
           // 通过 mock daemonClient.request 间接验证 PUT body 中字段值与输入一致
           // (不允许 toSettingsPatchRequest 内部出现 !settings.network.allowRelayFallback)
           // —— 实际断言已在 Test 2 覆盖（输入 true 期望 patch 含 true）
         })
       })
       ```

    2. 全工程 grep 反向命名违规：
       ```bash
       grep -rnE "lanOnly[^A-Za-z]" src/types/ src/api/ 2>/dev/null
       ```
       期望 0 行。如果有命中（除注释外），失败修复。

    3. 全工程 grep 取反越界（仅 NetworkSection.tsx 在 Plan 06 才允许 `!allowRelayFallback`，本 plan 完成时该文件还没改 — 应为 0）：
       ```bash
       grep -rnE "![[:space:]]*allowRelayFallback|![[:space:]]*\\\.allowRelayFallback|allowRelayFallback[[:space:]]*===[[:space:]]*false|allowRelayFallback[[:space:]]*!==[[:space:]]*true" src/types/ src/api/ 2>/dev/null
       ```
       期望 0 行（前端取反点不允许出现在 types / api client 层）。

    4. 运行测试套件最终确认：`pnpm test --run src/api/daemon/__tests__/settings.test.ts`
    5. 提交：`refactor(95-01): 反向命名铁律审计 fence + 全链路类型一致性确认`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/api/daemon/__tests__/settings.test.ts && grep -rnE "lanOnly[^A-Za-z]" src/types/ src/api/ ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/api/daemon/__tests__/settings.test.ts` 含 `describe('反向命名审计`
    - 测试套件运行 8/8 PASS（原 6 + 新增 2）
    - `grep -rnE "lanOnly[^A-Za-z]" src/types/ src/api/` 返回 0 匹配
    - `grep -rnE "![[:space:]]*allowRelayFallback|![[:space:]]*\\\.allowRelayFallback" src/types/ src/api/` 返回 0 匹配
    - `git log --oneline -1` 标题以 `refactor(95-01):` 开头
  </acceptance_criteria>
  <done>反向命名 fence 已落，未来引入 lanOnly 镜像或 types/api 层取反点会被自动检测。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon HTTP → frontend | Phase 94 已锁定 wire 字段 `allowRelayFallback` / `restartRequired`；前端解析输入信任 daemon（同进程，本地 loopback） |
| frontend store → UI 组件 | Settings 类型契约通过 TypeScript 检查，运行时无验证（信任 daemon 返回结构） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-01 | Tampering | toSettingsPatchRequest 反向取反 bug | mitigate | Test 2 + Test 3 双向断言（true/false 各一组），加 Pitfall 1 审计 fence；任何 `!allowRelayFallback` 出现在 types/api 层即失败 |
| T-95-01-02 | Information Disclosure | Settings 类型 `lanOnly` 镜像泄漏到 IPC | accept→mitigate | grep `lanOnly[^A-Za-z]` 全工程为 0；ROADMAP 反向命名规则铁律明确"前后端 IPC 永远以 allowRelayFallback 流动" |
| T-95-01-03 | Denial of Service | updateSettings 解析 `restartRequired` undefined 时崩溃 | accept | daemon 已 ship 该字段（Phase 94 VERIFICATION），TypeScript 类型保证；运行时若 daemon 降级（极不可能），daemonClient.request 已有 axios 错误处理 |
</threat_model>

<verification>
- 测试 8/8 PASS（含反向命名 fence）
- `pnpm tsc --noEmit` 在 src/types/setting.ts 与 src/api/daemon/settings.ts 上无错误（SettingContext.tsx 含 TODO 占位让全局通过）
- 全工程无 `lanOnly` 字段名残留（除 NetworkSetting deprecated 类型 — 那是 v0.6.0 旧字段，本 plan 不动）
- 全工程 types/api 层无 `!allowRelayFallback` 取反（仅 NetworkSection.tsx 在 Plan 06 实现时允许）
</verification>

<success_criteria>
- `Settings.network: NetworkSettings` 必填字段已暴露
- `updateSettings()` 返回 `{ success, restartRequired }`
- `toSettingsPatchRequest` 在有 network 段时镜像 `{ allowRelayFallback }` 进 PUT body
- `SettingContextType.updateNetworkSetting` 签名声明（实装在 Plan 04）
- 反向命名铁律 fence 已落
- 3 commits（test → feat → refactor）
</success_criteria>

<output>
完成后创建 `.planning/phases/095-networksection-ux/095-01-SUMMARY.md`
</output>
