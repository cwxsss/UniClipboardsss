---
phase: 95
plan: 04
type: tdd
wave: 2
depends_on: [95-01]
files_modified:
  - src/contexts/SettingContext.tsx
  - src/contexts/__tests__/SettingContext.network.test.tsx
autonomous: true
requirements: [NETSET-04, NETSET-05]
tags: [frontend, context, settings, lan-only, network]
must_haves:
  truths:
    - "SettingContext 暴露 updateNetworkSetting(partial) -> Promise<{ restartRequired }> helper"
    - "saveSetting 改为返回 { restartRequired }，所有现有调用方继续 ignore（向后兼容）"
    - "updateNetworkSetting 把 partial 镜像合并进 setting.network 后调 saveSetting"
    - "PUT /settings 失败时 saveSetting 抛错；调用方（updateNetworkSetting）不消化错误，让 NetworkSection 决定如何回滚 Switch"
  artifacts:
    - path: "src/contexts/SettingContext.tsx"
      provides: "updateNetworkSetting helper + saveSetting 返回 { restartRequired }"
      contains: "updateNetworkSetting"
    - path: "src/contexts/__tests__/SettingContext.network.test.tsx"
      provides: "updateNetworkSetting + saveSetting restartRequired 行为单元测试"
      contains: "describe('SettingContext network"
  key_links:
    - from: "SettingContext.updateNetworkSetting"
      to: "src/api/daemon/settings.ts updateSettings (Plan 01 已 ship)"
      via: "saveSetting 内部调 updateSettings(newSetting)"
      pattern: "await updateSettings"
    - from: "NetworkSection.tsx (Plan 06)"
      to: "useSetting().updateNetworkSetting"
      via: "switch onCheckedChange 触发"
      pattern: "updateNetworkSetting\\("
---

<objective>
在 `src/contexts/SettingContext.tsx` 中新增 `updateNetworkSetting(partial)` helper，并把 `saveSetting` 改为返回 `Promise<{ restartRequired: boolean }>`，让 NetworkSection 能在 Plan 06 中读取 daemon 返回的 `restartRequired` 信号决定是否显示 RestartBanner。**这是连接 Plan 01 (类型契约) 与 Plan 06 (NetworkSection UI) 的中间枢纽**。

**Purpose:**
- D-D2 锁定：updateNetworkSetting 实装走 partial 合并 + saveSetting 路径，与 `updateFileSyncSetting` 同形（PATTERNS.md analog: lines 123-143）
- Plan 01 在 SettingContextType 已声明 `updateNetworkSetting` 签名 + 在 SettingContext.tsx 已加 TODO 占位让 tsc 过 — 本 plan 替换占位为真实实装
- saveSetting 改返回 `{ restartRequired }` 是契约升级 — 所有现有调用方（updateGeneralSetting / updateSyncSetting / 等）当前 `await saveSetting(...)` 不读返回值，TypeScript 不破

**Output:**
- 新 helper `updateNetworkSetting`
- saveSetting 签名升级
- 完整单元测试覆盖（mock daemonClient + 验证 PUT body + 验证 restartRequired 透传）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/095-networksection-ux/095-CONTEXT.md
@.planning/phases/095-networksection-ux/095-PATTERNS.md
@src/contexts/SettingContext.tsx
@src/types/setting.ts
@src/api/daemon/settings.ts

<interfaces>
<!-- Plan 01 已 ship 的契约（本 plan 实装的目标接口） -->

From `src/types/setting.ts`:
```typescript
export interface NetworkSettings {
  allowRelayFallback: boolean
}

export interface SettingContextType {
  // ... 既有 fields
  updateNetworkSetting: (
    newNetworkSetting: Partial<NetworkSettings>
  ) => Promise<{ restartRequired: boolean }>
}
```

From `src/api/daemon/settings.ts`:
```typescript
export async function updateSettings(
  settings: Partial<Settings>
): Promise<{ success: boolean; restartRequired: boolean }>
```

<!-- updateFileSyncSetting analog (SettingContext.tsx:123-143) — 本 plan 镜像形态 -->
const updateFileSyncSetting = async (
  newFileSyncSetting: Partial<Settings['fileSync'] & object>
) => {
  if (!setting) return
  const updatedSetting: Settings = {
    ...setting,
    fileSync: {
      ...(setting.fileSync ?? { /* defaults */ }),
      ...newFileSyncSetting,
    },
  }
  await saveSetting(updatedSetting)
}
</interfaces>

<analog_excerpt>
<!-- SettingContext.tsx:46-64 — saveSetting 当前形态（要扩 restartRequired） -->
```typescript
const saveSetting = async (newSetting: Settings) => {
  try {
    setLoading(true)
    await updateSettings(newSetting)        // ← 当前丢弃返回值
    setSetting(newSetting)
    setError(null)
    try {
      await emitSettingsChanged(newSetting)
    } catch (err) {
      log.error({ err }, 'Failed to broadcast settings change')
    }
  } catch (err) {
    log.error({ err }, '保存设置失败')
    setError(`保存设置失败: ${err}`)
    throw err
  } finally {
    setLoading(false)
  }
}
```

<!-- SettingContext.tsx:123-143 — updateFileSyncSetting analog (本 plan 镜像) -->
```typescript
const updateFileSyncSetting = async (
  newFileSyncSetting: Partial<Settings['fileSync'] & object>
) => {
  if (!setting) return
  const updatedSetting: Settings = {
    ...setting,
    fileSync: {
      ...(setting.fileSync ?? { /* hardcoded defaults */ }),
      ...newFileSyncSetting,
    },
  }
  await saveSetting(updatedSetting)
}
```
</analog_excerpt>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — 编写 SettingContext.network 测试覆盖 updateNetworkSetting + saveSetting restartRequired</name>
  <read_first>
    - `src/contexts/__tests__/SettingContext.cross-window.test.tsx`（现有 SettingContext 测试 — 学习 mock + Provider wrapping 模式）
    - `src/contexts/SettingContext.tsx`（当前实现 — 含 Plan 01 加的 TODO 占位）
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/contexts/SettingContext.tsx` 段
    - `src/api/daemon/settings.ts` 现状（Plan 01 已 ship — `updateSettings` 返回 `{ success, restartRequired }`）
  </read_first>
  <files>src/contexts/__tests__/SettingContext.network.test.tsx</files>
  <behavior>
    Test 1（updateNetworkSetting 调 saveSetting 后返回 restartRequired=true）: mock `updateSettings` 返回 `{ success: true, restartRequired: true }`，验证 `updateNetworkSetting({ allowRelayFallback: false })` 返回 `{ restartRequired: true }`
    Test 2（updateNetworkSetting 镜像 partial 进 setting.network）: 验证 mock updateSettings 接收到的 `Partial<Settings>` 入参的 `network.allowRelayFallback === false`
    Test 3（updateNetworkSetting 在 setting === null 时 graceful return）: 验证 mock updateSettings 未被调用，updateNetworkSetting 返回 `{ restartRequired: false }`
    Test 4（updateNetworkSetting 抛错时 caller 可 catch — 不被消化）: mock updateSettings reject，验证 `await expect(updateNetworkSetting(...)).rejects.toThrow()`
    Test 5（saveSetting 返回 restartRequired 契约）: 通过 updateNetworkSetting 间接验证 saveSetting 透传 restartRequired
  </behavior>
  <action>
    1. 创建 `src/contexts/__tests__/SettingContext.network.test.tsx`
    2. 用 vitest + RTL，参考 `src/contexts/__tests__/SettingContext.cross-window.test.tsx` 现有 mock 模式
    3. mock 链：
       ```typescript
       vi.mock('@/api/daemon', () => ({
         getSettings: vi.fn(),
         updateSettings: vi.fn(),
       }))
       vi.mock('@/lib/daemon-ws-bootstrap', () => ({
         connectDaemonWs: vi.fn().mockResolvedValue(undefined),
       }))
       vi.mock('@/lib/settings-events', () => ({
         emitSettingsChanged: vi.fn().mockResolvedValue(undefined),
       }))
       vi.mock('@/lib/tauri-command', () => ({
         invokeWithTrace: vi.fn().mockResolvedValue(undefined),
       }))
       ```
    4. 写一个 helper 函数 `renderWithProvider`，wrap `<SettingProvider>` + `useSetting` hook 暴露给测试。
    5. mock `getSettings` 返回完整 Settings 对象，含 `network: { allowRelayFallback: true }`（默认）
    6. 测试 1-5 实装：
       - Test 1: `mockUpdateSettings.mockResolvedValueOnce({ success: true, restartRequired: true })`，await `updateNetworkSetting({ allowRelayFallback: false })`，期望返回 `{ restartRequired: true }`
       - Test 2: 上述调用后 `expect(mockUpdateSettings).toHaveBeenCalledWith(expect.objectContaining({ network: { allowRelayFallback: false } }))`
       - Test 3: setting 未加载时（mock getSettings reject 或返回 null），调 updateNetworkSetting 不应触发 updateSettings；返回 `{ restartRequired: false }`
       - Test 4: `mockUpdateSettings.mockRejectedValueOnce(new Error('PUT failed'))`，期望 updateNetworkSetting 抛错
       - Test 5: 第二次调 updateNetworkSetting，mock 返回 `restartRequired: false`，期望返回 `{ restartRequired: false }`
    7. 运行 `pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx` 期望**失败**（因为 `updateNetworkSetting` 当前是 TODO 占位 always returns `{ restartRequired: false }`，Test 1 / 2 / 4 / 5 都会失败）
    8. 提交：`test(95-04): RED — SettingContext.updateNetworkSetting + saveSetting restartRequired 测试`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/contexts/__tests__/SettingContext.network.test.tsx` 文件存在
    - 文件含 `describe('SettingContext network` 或类似 block
    - 文件含至少 5 个 `it(` / `test(` 语句
    - 文件至少出现 5 处 `updateNetworkSetting`、3 处 `restartRequired`、4 处 `allowRelayFallback`
    - 运行 `pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx` 退出码非 0（RED — 期望 ≥ 3 失败：Test 1, 2, 5 因 TODO 占位返回错误结构）
    - `git log --oneline -1` 标题以 `test(95-04):` 开头
  </acceptance_criteria>
  <done>测试文件提交，运行返回失败。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — 实装 saveSetting 返回 restartRequired + updateNetworkSetting helper</name>
  <read_first>
    - `src/contexts/SettingContext.tsx`（当前完整 — 含 Plan 01 TODO 占位 line ~252）
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/contexts/SettingContext.tsx` 段（complete analog excerpt）
    - Task 1 创建的失败测试
    - `src/types/setting.ts`（确认 `NetworkSettings` 与 `SettingContextType.updateNetworkSetting` 签名）
  </read_first>
  <files>src/contexts/SettingContext.tsx</files>
  <action>
    **Step A — 修改 saveSetting 签名（line 46-64）：**

    把现有：
    ```typescript
    const saveSetting = async (newSetting: Settings) => {
      try {
        setLoading(true)
        await updateSettings(newSetting)
        setSetting(newSetting)
        setError(null)
        try {
          await emitSettingsChanged(newSetting)
        } catch (err) {
          log.error({ err }, 'Failed to broadcast settings change')
        }
      } catch (err) {
        log.error({ err }, '保存设置失败')
        setError(`保存设置失败: ${err}`)
        throw err
      } finally {
        setLoading(false)
      }
    }
    ```

    改为：
    ```typescript
    const saveSetting = async (
      newSetting: Settings
    ): Promise<{ restartRequired: boolean }> => {
      try {
        setLoading(true)
        const result = await updateSettings(newSetting)
        setSetting(newSetting)
        setError(null)
        try {
          await emitSettingsChanged(newSetting)
        } catch (err) {
          log.error({ err }, 'Failed to broadcast settings change')
        }
        return { restartRequired: result.restartRequired }
      } catch (err) {
        log.error({ err }, '保存设置失败')
        setError(`保存设置失败: ${err}`)
        throw err
      } finally {
        setLoading(false)
      }
    }
    ```

    **Step B — 现有 update*Setting helpers 不需要变更**：他们 `await saveSetting(...)` 现在 ignore 返回值，TypeScript 兼容（Promise<X> 可以 await 不读结果）。

    **Step C — 实装 `updateNetworkSetting`（替换 Plan 01 加的 TODO 占位）：**

    1. 找到 Plan 01 加的 TODO 占位（在 `value: SettingContextType = { ... }` 内的 `updateNetworkSetting: async () => ({ restartRequired: false })`）
    2. 把这行从 value 对象内删除
    3. 在 `updateFileSyncSetting`（line 123-143）之后、`updateKeyboardShortcuts`（line 145-157）之前，新增：
       ```typescript
       // Update network settings (Phase 95)
       const updateNetworkSetting = async (
         newNetworkSetting: Partial<Settings['network']>
       ): Promise<{ restartRequired: boolean }> => {
         if (!setting) return { restartRequired: false }
         const updatedSetting: Settings = {
           ...setting,
           network: {
             ...setting.network,
             ...newNetworkSetting,
           },
         }
         return await saveSetting(updatedSetting)
       }
       ```

    4. 在 `value: SettingContextType = { ... }` 对象（line 242-253）的 `updateFileSyncSetting,` 之后加：
       ```typescript
       updateNetworkSetting,
       ```

    **Step D — 测试 + 编译：**

    1. `pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx` 期望 5/5 PASS
    2. `pnpm test --run src/contexts/__tests__/SettingContext.cross-window.test.tsx` 期望 PASS（不破现有测试）
    3. 提交：`feat(95-04): GREEN — SettingContext.updateNetworkSetting + saveSetting 透传 restartRequired`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx 2>&1 | tail -15 && pnpm test --run src/contexts/__tests__/SettingContext.cross-window.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/contexts/SettingContext.tsx` 含 `updateNetworkSetting` 函数定义（不是 TODO 占位）
    - `src/contexts/SettingContext.tsx` 含 `Promise<{ restartRequired: boolean }>` 在 saveSetting 签名
    - `src/contexts/SettingContext.tsx` 含 `return { restartRequired: result.restartRequired }`
    - `src/contexts/SettingContext.tsx` 含 `network: { ...setting.network, ...newNetworkSetting }`
    - `src/contexts/SettingContext.tsx` value 对象含 `updateNetworkSetting,`
    - `grep -c "TODO(95-04)" src/contexts/SettingContext.tsx` 返回 0（占位已替换）
    - `pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx` 退出码 0（5/5 PASS）
    - `pnpm test --run src/contexts/__tests__/SettingContext.cross-window.test.tsx` 退出码 0（不破现有测试）
    - `pnpm test --run src/contexts/__tests__/SettingContext.theme.test.tsx` 退出码 0（不破 theme 测试）
    - `git log --oneline -1` 标题以 `feat(95-04):` 开头
  </acceptance_criteria>
  <done>updateNetworkSetting 实装 + saveSetting 透传 restartRequired + 现有测试套件不破。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR — 反向命名审计 + saveSetting 调用方契约 fence</name>
  <read_first>
    - Task 2 修改后的 SettingContext.tsx
    - `.planning/research/PITFALLS.md` Pitfall 1（反向命名 warning signs）+ Pitfall 10（重启 UX 半生效）
  </read_first>
  <files>src/contexts/__tests__/SettingContext.network.test.tsx</files>
  <action>
    1. 在测试文件末尾追加一组反向命名 + 契约 fence 断言：
       ```typescript
       describe('反向命名 + 契约 fence (Pitfall 1 + Pitfall 10)', () => {
         it('updateNetworkSetting 不在 SettingContext 内取反 allowRelayFallback', async () => {
           // 输入 false 期望 PUT body 中 false（不取反）
           mockUpdateSettings.mockResolvedValueOnce({ success: true, restartRequired: true })
           const ctx = renderHookForSettingContext()
           await act(async () => {
             await ctx.result.current.updateNetworkSetting({ allowRelayFallback: false })
           })
           const lastCall = mockUpdateSettings.mock.calls[mockUpdateSettings.mock.calls.length - 1]
           expect(lastCall[0].network.allowRelayFallback).toBe(false)  // 真值传递，非 !x
         })

         it('SettingContext 不维护 lanOnly 镜像字段', () => {
           // 静态 fence — 通过 grep enforced，此处 sanity check
           const ctx = renderHookForSettingContext()
           expect(ctx.result.current).not.toHaveProperty('lanOnly')
           expect(ctx.result.current).not.toHaveProperty('updateLanOnly')
         })
       })
       ```

    2. 全工程 grep 反向命名违规：
       ```bash
       grep -rnE "lanOnly[^A-Za-z]" src/contexts/ 2>/dev/null
       ```
       期望 0 行。

    3. 全工程 grep 取反越界（Plan 06 的 NetworkSection.tsx 不在本 plan 修改范围 — 应该是 0 行）：
       ```bash
       grep -rnE "![[:space:]]*allowRelayFallback|![[:space:]]*\\\.allowRelayFallback" src/contexts/ 2>/dev/null
       ```
       期望 0 行（SettingContext 不允许出现取反点）。

    4. `pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx` 期望 7/7 PASS
    5. 提交：`refactor(95-04): SettingContext 反向命名 + saveSetting 契约 fence`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos && pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx 2>&1 | tail -10 && (grep -rnE "lanOnly[^A-Za-z]" src/contexts/ ; test $? -eq 1) && (grep -rnE "![[:space:]]*allowRelayFallback|![[:space:]]*\\.allowRelayFallback" src/contexts/ ; test $? -eq 1)</automated>
  </verify>
  <acceptance_criteria>
    - `src/contexts/__tests__/SettingContext.network.test.tsx` 含 `describe('反向命名` 或类似 fence describe
    - `pnpm test --run src/contexts/__tests__/SettingContext.network.test.tsx` 退出码 0（7/7 PASS）
    - `grep -rnE "lanOnly[^A-Za-z]" src/contexts/` 返回 0 匹配
    - `grep -rnE "![[:space:]]*allowRelayFallback|![[:space:]]*\\.allowRelayFallback" src/contexts/` 返回 0 匹配
    - `git log --oneline -1` 标题以 `refactor(95-04):` 开头
  </acceptance_criteria>
  <done>SettingContext 反向命名 fence 已落，契约升级保留向后兼容。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React component → SettingContext | useSetting().updateNetworkSetting 是单向调用，无副作用越界（不改其他 setting 段） |
| SettingContext → daemon API | saveSetting 透传 restartRequired，不消化错误（让 caller 决定回滚策略） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-04-01 | Tampering | updateNetworkSetting 在合并 partial 时悄悄修改其它段（如 telemetry_enabled） | mitigate | spread 模式 `{ ...setting, network: { ...setting.network, ...partial } }` 严格隔离 network 段；测试 Test 2 验证 PUT body 仅含 network 段变更 |
| T-95-04-02 | Information Disclosure | restartRequired 信号被错误透传成 false 让用户看不到 RestartBanner | mitigate | Test 1 验证 true 透传；Test 5 验证 false 透传；Pitfall 10 防御 |
| T-95-04-03 | DoS | PUT 失败时 saveSetting 抛错被 SettingContext 消化导致调用方看不到错误 | mitigate | saveSetting `throw err` 重抛错误（line 60）；Test 4 验证 caller 可 catch；NetworkSection (Plan 06) 据此回滚 Switch + 显示 saveError |
</threat_model>

<verification>
- 7/7 单元测试 PASS（5 行为 + 2 fence）
- 现有 SettingContext.cross-window.test.tsx + SettingContext.theme.test.tsx 不破
- `grep -rnE "lanOnly[^A-Za-z]" src/contexts/` 0 匹配
- `grep -rnE "![[:space:]]*allowRelayFallback" src/contexts/` 0 匹配
- TODO(95-04) 占位已替换为实装
</verification>

<success_criteria>
- updateNetworkSetting 实装且覆盖 partial 合并 + setting=null 安全降级
- saveSetting 返回 `{ restartRequired }`，向后兼容（现有调用方 ignore 仍 OK）
- 反向命名 fence + saveSetting 契约 fence 已落
- 3 commits（test → feat → refactor）
</success_criteria>

<output>
完成后创建 `.planning/phases/095-networksection-ux/095-04-SUMMARY.md`
</output>
