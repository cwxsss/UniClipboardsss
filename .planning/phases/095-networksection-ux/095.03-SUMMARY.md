---
phase: 095-networksection-ux
plan: 03
subsystem: frontend-i18n
tags: [frontend, i18n, lan-only, copywriting, zh-CN, en-US, pitfall-5, pitfall-11]

# Dependency graph
requires:
  - phase: 094-backend-network-allow-relay-fallback
    provides: "settings.network.allowRelayFallback wire contract — defines the toggle that the new i18n labels describe"
provides:
  - "Bilingual i18n key tree settings.sections.network.lanOnly + restartBanner — frozen Phase 97 docs/changelog reverse-copy baseline"
  - "Pitfall 5 forbidden-word fence inside src/i18n/ (zero hits for fully offline / 完全离线 / 绝对私有 / no internet / private mode / encrypted-and-local)"
  - "Pitfall 11 cleanup: legacy network.{title,syncMethod,webserverPort,customPeerDevice,cloudServer,loadError} block deleted from both locales"
affects:
  - "Phase 95 Plan 06 (NetworkSection rewrite — t() consumer of these keys)"
  - "Phase 95 Plan 04 / 05 (RestartBanner + LanOnlyDisclosure components — consume restartBanner.* and lanOnly.disclosure.* keys)"
  - "Phase 97 DOC-01 docs/lan-only.md (verbatim copy from Phase 95 i18n)"
  - "Phase 97 DOC-03 changelog (verbatim copy from Phase 95 i18n)"
  - "Phase 97 README.md:222 FAQ rewrite (DEF-95-03-01 — pre-existing fully offline violation deferred here)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "i18next interpolation uses {{var}} (existing project convention — verified against zh-CN.json existing keys like {{percentage}})"
    - "Copywriting baseline rule: Phase 95 i18n is canonical, Phase 97 docs/changelog reverse-copy verbatim (commit messages tagged 'Phase 97 baseline')"

key-files:
  created:
    - ".planning/phases/095-networksection-ux/deferred-items.md"
  modified:
    - "src/i18n/locales/zh-CN.json"
    - "src/i18n/locales/en-US.json"

key-decisions:
  - "JSON 字符串内不使用 markdown backtick 包裹 rendezvous.uniclipboard.app — UI-SPEC 中的 backtick 仅是 markdown 装饰，i18n string 是 plain text"
  - "i18next interpolation 使用 {{message}} 而非 UI-SPEC 中标的 ruby-style %{message} — 与项目现有 i18n 形态一致"
  - "Plan 03 verify automated 子句严格定义为 src/i18n/ 范围；README.md:222 命中『fully offline』属 Phase 97 文档范畴的 pre-existing violation，记入 deferred-items.md 转交 Phase 97"

patterns-established:
  - "i18n 双语 key tree 同构验证：python3 sanity script 比较 zh-CN/en-US sorted(keys()) 等价（可作为未来 CI gate）"
  - "禁词 fence：grep -rE 全工程扫描配合 commit message 『Phase 97 baseline』标记，便于反向追溯"

requirements-completed: [NETSET-04, NETSET-05, NETSET-06]

# Metrics
duration: 12min
completed: 2026-05-04
---

# Phase 95 Plan 03: i18n 文案 — zh-CN + en-US lanOnly/disclosure/restartBanner Summary

**zh-CN + en-US 双语 settings.sections.network 整块替换为 LAN-only + 4 类外网请求披露 + RestartBanner 三子树（Phase 97 docs/changelog 反向复制基准），同步删除 6 个旧占位 key、Pitfall 5 禁词清单 src/i18n/ 0 命中、双语 key tree 同构验证 PASS。**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-04T00:00:00Z (approx — plan_start_epoch captured at agent boot)
- **Completed:** 2026-05-04
- **Tasks:** 3 / 3
- **Files modified:** 2 (src/i18n/locales/{zh-CN,en-US}.json)
- **Files created:** 1 (.planning/phases/095-networksection-ux/deferred-items.md)

## Accomplishments

- **NETSET-04 / 05 / 06 用户可见文字双语就位** — `lanOnly.{label,description,infoIconAriaLabel,saveError}` + `lanOnly.disclosure.{title,intro,rendezvous,otlp,pkarr,autoUpdate}` + `restartBanner.{message,restartButton,restartingButton,dismissAriaLabel,errorMessage,retryButton}`，Plan 06 NetworkSection 的 `t(...)` 调用直接命中。
- **Pitfall 5 边界透明 fence 落地** — src/i18n/ 全工程 grep 对禁词清单（fully offline / 完全离线 / 绝对私有 / no internet / private mode / encrypted-and-local）返回 0；commit messages 含 "Phase 97 baseline" 关键字。
- **Pitfall 11 旧占位 i18n 残留清除** — 旧 `network.{title,syncMethod,webserverPort,customPeerDevice,cloudServer,loadError}` 块整组从两份 JSON 删除；src/ grep 验证无外部引用残留（除 Plan 06 待清的 NetworkSection.tsx:18 fallback 一行）。
- **双语 key tree 同构验证脚本** — python3 sanity check 比较 zh-CN/en-US 三层 keys（顶层 / lanOnly / lanOnly.disclosure / restartBanner）全部等价，可作为未来 CI gate 的种子。
- **DEF-95-03-01 deferred-items.md 落记** — README.md:222 "Does it work fully offline / LAN-only?" 属 Phase 97 docs 范畴的 pre-existing Pitfall 5 违规，本 Plan 03 范围严格守 i18n 边界，转交 Phase 97 处理（DOC-01/03 反向同步时一并改写）。

## Task Commits

Each task was committed atomically on branch `worktree-agent-a2ec6e69aac316324`:

1. **Task 1: 替换 zh-CN.json 的 settings.sections.network 整块** — `d718a7fc` (feat)
2. **Task 2: 替换 en-US.json 的 settings.sections.network 整块（en-US 镜像）** — `5343c0b6` (feat)
3. **Task 3: 全工程禁词审计 + 旧 i18n key 引用清查** — `a05c5057` (docs)

_Note: 上述 hashes 在 worktree 分支；orchestrator 合并回 main 时 hash 会重写但 commit message + Phase 97 baseline tag 保留。_

## Files Created/Modified

- `src/i18n/locales/zh-CN.json` — 删除旧 `network.{title,syncMethod,webserverPort,customPeerDevice,cloudServer,loadError}` 块（line 192-237），写入新 `network.lanOnly.* + network.restartBanner.*` 双子树（zh-CN 全角标点）
- `src/i18n/locales/en-US.json` — 删除旧 `network.{title,syncMethod,webserverPort,customPeerDevice,cloudServer,loadError}` 块（line 201-246），写入新 `network.lanOnly.* + network.restartBanner.*` 双子树（与 zh-CN key tree 同构）
- `.planning/phases/095-networksection-ux/deferred-items.md` — 新建，记录 DEF-95-03-01（README.md:222 转交 Phase 97）

## Decisions Made

1. **`{{message}}` 占位语法 vs UI-SPEC 中的 `%{message}`** — UI-SPEC 写的是 ruby-style，但本项目 i18n 现有代码（如 `storage.storageUsage.usage: "已使用 {{percentage}}%"`）一律用 i18next 默认 `{{ }}`。Plan 03 沿用现有形态；UI-SPEC 中的 `%{message}` 视为非锁定示意。
2. **`rendezvous.uniclipboard.app` 在 JSON 字符串内是 plain text** — UI-SPEC `## Copywriting Contract` 中用 markdown backtick 装饰，但 i18n string 必须是 plain text；React 渲染层若需高亮再做 markup（不在本 plan 范围）。
3. **README.md:222 不在本 plan 修改范围** — Plan 03 frontmatter `files_modified` 严格限定两个 i18n JSON；README.md 是 Phase 97 DOC 范畴；本 plan 顺手修会越界且与 ROADMAP §Phase 95 vs §Phase 97 边界冲突。改为转交 deferred-items.md。
4. **半角逗号回退到全角逗号修复** — Task 1 初次 Edit 时把 zh-CN 描述里的全角"，"误打成半角","，发现后 3 处 Edit 修复（在同一 Task 1 的同次 commit 内，未引入额外 commit）。

## Deviations from Plan

### Auto-fixed Issues

无 Rule 1-3 触发的功能性 deviation。

### Out-of-scope Discovery (Forwarded)

**1. [Rule N/A — Out of scope] README.md:222 contains 'fully offline' (Pitfall 5 violation)**
- **Found during:** Task 3 audit (`grep -rnE "fully offline|..." README.md`)
- **Issue:** README FAQ 句"Does it work fully offline / LAN-only?" 命中禁词，违反 ROADMAP §边界透明 + Pitfall 5
- **Decision:** Pre-existing violation in **Phase 97 documentation surface**，不在 Plan 03 frontmatter `files_modified` 范围内
- **Fix forwarded to:** Phase 97 — DOC-01 `docs/lan-only.md` 计划必须含 README.md:222 改写子任务，使用 Phase 95 i18n 作为 canonical wording
- **Tracking:** `.planning/phases/095-networksection-ux/deferred-items.md` DEF-95-03-01

---

**Total deviations:** 0 auto-fixed; 1 out-of-scope discovery deferred to Phase 97
**Impact on plan:** 严守 Plan 03 frontmatter scope；将 README.md 违规转交 Phase 97 是最干净路径（避免 i18n / docs 跨边界 commit）。

## Issues Encountered

- **半角/全角中文标点错配（Task 1）** — 初次 Edit 替换时在 3 处把 zh-CN 描述里的全角"，"误写成半角","。后续在同 Task 1 内通过 3 次 Edit 修复，commit 提交前已矫正，未污染最终 commit 内容。
- **GitNexus index stale warning** — Bash hook 输出 `GitNexus index is stale (last indexed: 5904c2d)` 与 `FTS index ensure failed (read-only database)`，与本任务无关，是工具基础设施警告。未影响任何 i18n 操作；如需重建索引由 orchestrator 在 phase 完成后统一处理。

## User Setup Required

None — pure i18n string changes，无外部服务配置需求。

## Next Phase Readiness

- **Plan 04 (RestartBanner)** ready：可消费 `settings.sections.network.restartBanner.{message,restartButton,restartingButton,dismissAriaLabel,errorMessage,retryButton}` 6 个 key。
- **Plan 05 (LanOnlyDisclosure)** ready：可消费 `settings.sections.network.lanOnly.{infoIconAriaLabel,disclosure.title,disclosure.intro,disclosure.{rendezvous,otlp,pkarr,autoUpdate}.{title,description}}` 10 个 key。
- **Plan 06 (NetworkSection 重写)** ready：可消费 `settings.sections.network.lanOnly.{label,description,saveError}` + 调用 RestartBanner / LanOnlyDisclosure。需在 Plan 06 删除 NetworkSection.tsx:18 残留的 `t('settings.sections.network.placeholder') || ...` fallback 行。
- **Phase 97** baseline 锁定：本 plan 三个 commit message 均含 "Phase 97 baseline"，未来 Phase 97 实施者可 `git log --grep="Phase 97 baseline"` 找到本 plan 作为 docs 反向复制源。

## Self-Check: PASSED

文件存在性：
- src/i18n/locales/zh-CN.json — FOUND
- src/i18n/locales/en-US.json — FOUND
- .planning/phases/095-networksection-ux/deferred-items.md — FOUND

Commit 存在性（worktree branch worktree-agent-a2ec6e69aac316324）：
- d718a7fc — FOUND (feat zh-CN i18n)
- 5343c0b6 — FOUND (feat en-US i18n)
- a05c5057 — FOUND (docs audit + deferred-items.md)

验收：
- python3 -m json.tool 两份 JSON：合法
- 双语 key tree 同构：PASS
- src/i18n/ Pitfall 5 禁词：0 命中
- src/ 旧 keys 引用：0（除 NetworkSection.tsx:18 一行 placeholder fallback，Plan 06 同步清）

## Threat Flags

无新增超出 plan `<threat_model>` 的威胁面 — 本 plan 仅是 i18n string 替换，不引入新网络端点 / 鉴权路径 / 文件访问 / 信任边界。

---
*Phase: 095-networksection-ux*
*Completed: 2026-05-04*
