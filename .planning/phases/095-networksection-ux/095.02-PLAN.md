---
phase: 95
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-tauri/src/commands/restart.rs
  - src-tauri/crates/uc-tauri/src/commands/mod.rs
  - src-tauri/crates/uc-tauri/src/run.rs
autonomous: true
requirements: [NETSET-05]
tags: [tauri, commands, restart, lan-only, backend]
must_haves:
  truths:
    - "前端可调 Tauri command `restart_app` 触发整个 GUI 进程退出 + relaunch（per D-B1, D-B2）"
    - "前端可调 Tauri command `get_restart_state` 返回 `{ processStartedAt: i64, settingsMtime: i64 }`（millis since epoch，per D-D1）"
    - "PROCESS_STARTED_AT 在 Tauri builder setup 阶段早期被赋值一次（OnceLock 强制单次）"
    - "settings_mtime 来自 std::fs::metadata(settings_path).modified() 实时读取，非缓存值"
  artifacts:
    - path: "src-tauri/crates/uc-tauri/src/commands/restart.rs"
      provides: "restart_app + get_restart_state 两个 #[tauri::command] + RestartState serde 结构 + PROCESS_STARTED_AT OnceLock"
      contains: "pub async fn restart_app"
    - path: "src-tauri/crates/uc-tauri/src/commands/mod.rs"
      provides: "pub mod restart 注册 + pub use restart::*"
      contains: "pub mod restart"
    - path: "src-tauri/crates/uc-tauri/src/run.rs"
      provides: "PROCESS_STARTED_AT 初始化 + invoke_handler 注册新 commands"
      contains: "PROCESS_STARTED_AT.set"
  key_links:
    - from: "前端 invokeWithTrace('restart_app')"
      to: "Tauri restart_app command → app.restart()"
      via: "Tauri IPC"
      pattern: "app\\.restart\\(\\)"
    - from: "前端 invokeWithTrace('get_restart_state')"
      to: "Tauri get_restart_state command → fs::metadata + OnceLock 读"
      via: "Tauri IPC"
      pattern: "get_restart_state"
---

<objective>
新建 Tauri 后端命令 `restart_app` 与 `get_restart_state`，让前端 NetworkSection 在 Plan 06 中可以：(a) 触发整个 GUI 进程退出 + relaunch（沿用 `app.restart()` 与 updater.rs:301 同模式 — 满足 D-B2）、(b) 在 mount 时读取 `processStartedAt` + `settingsMtime` 推断 pending 状态（满足 D-D1：`settingsMtime > processStartedAt` ⇒ pending）。本 plan 完整覆盖 Tauri 后端的两个 command surface，并在 `run.rs` 启动早期初始化 `PROCESS_STARTED_AT: OnceLock<SystemTime>`。

**Purpose:**
- D-B1 锁定：Phase 95 仅 cover Tauri GUI 模式，daemon-only CLI 不在范围
- D-B2 锁定：复用 `app.restart()` 调用模式（参见 `uc-tauri/src/commands/updater.rs:301`），不显式 graceful shutdown daemon —— 进程退出会触发 `task_registry::shutdown` cancel cascade
- D-D1 锁定：pending 推导只走 mtime 比对（`settings_mtime > process_started_at`），不动 Phase 94 已锁 daemon HTTP 契约

**Output:** 新建文件 + 注册 + 初始化共 3 处变更，完整 Tauri 后端 surface 准备就绪。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/095-networksection-ux/095-CONTEXT.md
@.planning/phases/095-networksection-ux/095-PATTERNS.md
@src-tauri/crates/uc-tauri/src/commands/mod.rs
@src-tauri/crates/uc-tauri/src/commands/storage.rs
@src-tauri/crates/uc-tauri/src/commands/error.rs

<interfaces>
<!-- 现有 Tauri command 模式（complete extract from storage.rs:1-43） -->

```rust
//! Storage management Tauri commands

use crate::commands::error::CommandError;
use crate::commands::record_trace_fields;
use tauri_plugin_opener::OpenerExt;
use tracing::{info_span, Instrument};
use uc_platform::ports::observability::TraceMetadata;

#[tauri::command]
pub async fn open_data_directory(
    app: tauri::AppHandle,
    runtime: tauri::State<'_, std::sync::Arc<crate::bootstrap::TauriAppRuntime>>,
    _trace: Option<TraceMetadata>,
) -> Result<(), CommandError> {
    let span = info_span!(
        "command.storage.open_data_dir",
        trace_id = tracing::field::Empty,
        trace_ts = tracing::field::Empty,
    );
    record_trace_fields(&span, &_trace);

    async move {
        let dir = runtime.storage_paths().app_data_root_dir.clone();
        // ...
    }
    .instrument(span)
    .await
}
```

<!-- 现有 commands/mod.rs:1-44 完整结构（要在 line 5 后加 `pub mod restart;`，line 33 后加 `pub use restart::*;`） -->

<!-- record_trace_fields 来源：commands/mod.rs:38-43 — 已 pub(crate) 暴露 -->

<!-- CommandError 来源：commands/error.rs:8-26 — 已有 NotFound / InternalError / 等变体 -->

<!-- updater.rs:300-301 — 现有 app.restart() 调用模式（本 plan 复用） -->
info!("update installed, restarting app");
app.restart();

<!-- run.rs:142-148 — Tauri builder 装配 .manage() 链；PROCESS_STARTED_AT.set() 应在 .invoke_handler 之前的 setup 块内或这之前 -->
let builder = tauri::Builder::default()
    .manage(runtime.clone())
    .manage(DaemonConnectionState::clone(&daemon_connection_state))
    .manage(DaemonOwnership::clone(&daemon_ownership))
    // ...

<!-- run.rs:416-444 — invoke_handler 注册列表 -->
.invoke_handler(tauri::generate_handler![
    crate::commands::tray::set_tray_language,
    crate::commands::get_tauri_pid,
    // ... existing commands
    crate::commands::storage::open_data_directory,
    // ... etc
])

<!-- TauriAppRuntime.storage_paths() 暴露 settings_path —— bootstrap/runtime.rs:161 -->
pub fn storage_paths(&self) -> &AppPaths { /* ... */ }
// AppPaths 含 settings_path: PathBuf（uc-application/src/facade/app_paths.rs:50：app_data_root.join("settings.json")）
```
</interfaces>

<wire_contract>
<!-- get_restart_state 必须返回的 wire 形态（前端 invokeWithTrace 解析） -->

```typescript
// 前端期望的 RestartState wire JSON:
{
  "processStartedAt": 1714823400000,  // i64 millis since UNIX_EPOCH
  "settingsMtime":     1714823500000   // i64 millis since UNIX_EPOCH
}
```

Rust struct 必须 `#[serde(rename_all = "camelCase")]`，字段名 `process_started_at` + `settings_mtime` 自动转 camelCase。
</wire_contract>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — 编写 restart.rs 单元测试覆盖 RestartState serde + PROCESS_STARTED_AT 行为</name>
  <read_first>
    - `src-tauri/crates/uc-tauri/src/commands/storage.rs`（最近 analog 完整实现）
    - `src-tauri/crates/uc-tauri/src/commands/mod.rs`（commands 注册模式 + record_trace_fields）
    - `src-tauri/crates/uc-tauri/src/commands/error.rs`（CommandError 变体）
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src-tauri/.../commands/restart.rs`
    - `src-tauri/crates/uc-tauri/Cargo.toml`（确认 dev-deps 含 tokio test runtime）
  </read_first>
  <files>src-tauri/crates/uc-tauri/src/commands/restart.rs</files>
  <behavior>
    Test 1（PROCESS_STARTED_AT OnceLock 单次写入语义）: `set` 第一次成功；第二次 `set` 必须被 OnceLock 拒绝（返回 Err — OnceLock::set 失败语义）
    Test 2（RestartState serde camelCase）: 序列化 `RestartState { process_started_at: 1000, settings_mtime: 2000 }` 期望 JSON 含 `"processStartedAt":1000` 和 `"settingsMtime":2000`，**不含** `process_started_at` snake_case
    Test 3（settings_mtime 计算）: 单元辅助函数 `read_settings_mtime_millis(path: &Path) -> Result<i64, CommandError>` 给定一个临时存在文件 → 返回 > 0 的 millis；给定不存在路径 → 返回 `CommandError::InternalError`
    Test 4（process_started_at 读取）: `read_process_started_at_millis()` 在 `PROCESS_STARTED_AT` 已 set 时返回 > 0；未 set 时返回 0（不 panic）
  </behavior>
  <action>
    1. 创建 `src-tauri/crates/uc-tauri/src/commands/restart.rs`，仅放骨架 + tests 模块。骨架内容：
       ```rust
       //! Restart-related Tauri commands.
       //! 重启相关的 Tauri 命令。
       //!
       //! Phase 95: covers GUI mode only (D-B1). CLI daemon mode is out of scope.

       use crate::commands::error::CommandError;
       use crate::commands::record_trace_fields;
       use serde::Serialize;
       use std::path::Path;
       use std::sync::OnceLock;
       use std::time::SystemTime;
       use tracing::{info, info_span, Instrument};
       use uc_platform::ports::observability::TraceMetadata;

       /// Process boot timestamp — set ONCE in `uc_tauri::run` setup.
       /// 进程启动时间戳 — 在 uc_tauri::run setup 阶段唯一写入。
       pub static PROCESS_STARTED_AT: OnceLock<SystemTime> = OnceLock::new();

       /// Returned by `get_restart_state` Tauri command.
       /// `get_restart_state` Tauri 命令的返回类型。
       ///
       /// # Wire format / 线协议
       /// `#[serde(rename_all = "camelCase")]` —— wire 字段名为
       /// `processStartedAt` / `settingsMtime`，与 daemon-contract camelCase 风格一致。
       #[derive(Debug, Clone, Serialize)]
       #[serde(rename_all = "camelCase")]
       pub struct RestartState {
           pub process_started_at: i64,
           pub settings_mtime: i64,
       }

       fn system_time_to_millis(t: SystemTime) -> i64 {
           t.duration_since(SystemTime::UNIX_EPOCH)
               .unwrap_or_default()
               .as_millis() as i64
       }

       fn read_process_started_at_millis() -> i64 {
           PROCESS_STARTED_AT
               .get()
               .copied()
               .map(system_time_to_millis)
               .unwrap_or(0)
       }

       fn read_settings_mtime_millis(path: &Path) -> Result<i64, CommandError> {
           let metadata = std::fs::metadata(path).map_err(CommandError::internal)?;
           let modified = metadata.modified().map_err(CommandError::internal)?;
           Ok(system_time_to_millis(modified))
       }

       // restart_app + get_restart_state #[tauri::command] 实装在 Task 2 GREEN 阶段加入。

       #[cfg(test)]
       mod tests {
           use super::*;
           use std::sync::OnceLock;
           use std::time::Duration;

           // Test 1: OnceLock 单次写入
           #[test]
           fn process_started_at_oncelock_rejects_double_set() {
               let cell: OnceLock<SystemTime> = OnceLock::new();
               assert!(cell.set(SystemTime::now()).is_ok());
               // 第二次 set 必失败
               assert!(cell.set(SystemTime::now() + Duration::from_secs(1)).is_err());
           }

           // Test 2: RestartState serde camelCase
           #[test]
           fn restart_state_serializes_camel_case() {
               let state = RestartState {
                   process_started_at: 1000,
                   settings_mtime: 2000,
               };
               let json = serde_json::to_string(&state).expect("serialize");
               assert!(
                   json.contains(r#""processStartedAt":1000"#),
                   "missing camelCase processStartedAt — got: {json}"
               );
               assert!(
                   json.contains(r#""settingsMtime":2000"#),
                   "missing camelCase settingsMtime — got: {json}"
               );
               assert!(
                   !json.contains("process_started_at"),
                   "snake_case leak — got: {json}"
               );
           }

           // Test 3: settings_mtime 读取
           #[test]
           fn read_settings_mtime_returns_positive_for_existing_file() {
               use std::io::Write as _;
               let dir = tempfile::tempdir().expect("tempdir");
               let path = dir.path().join("settings.json");
               let mut f = std::fs::File::create(&path).expect("create");
               writeln!(f, "{{}}").expect("write");
               drop(f);

               let mtime = read_settings_mtime_millis(&path).expect("read mtime");
               assert!(mtime > 0, "expected positive mtime, got {mtime}");
           }

           #[test]
           fn read_settings_mtime_returns_internal_error_for_missing_file() {
               let path = Path::new("/nonexistent/path/settings.json");
               match read_settings_mtime_millis(path) {
                   Err(CommandError::InternalError(_)) => {}
                   other => panic!("expected InternalError, got {other:?}"),
               }
           }

           // Test 4: process_started_at 读取（未 set 返回 0）
           // 注意：实际进程内 PROCESS_STARTED_AT 可能已被 set；此测试仅验证 helper 单调行为
           #[test]
           fn read_process_started_at_returns_non_negative() {
               let val = read_process_started_at_millis();
               assert!(val >= 0, "expected non-negative, got {val}");
           }
       }
       ```

    2. 确保 `tempfile` 在 `[dev-dependencies]` 内 —— 检查 `src-tauri/crates/uc-tauri/Cargo.toml`，如缺加：
       ```toml
       [dev-dependencies]
       tempfile = "3"
       serde_json = "1"  # 已存在则跳过
       ```

    3. 在 `commands/mod.rs:5` 的 `pub mod storage;` 后插入 `pub mod restart;`（仅注册模块；本 task 暂不 `pub use restart::*`，那是 Task 2 责任）。

    4. 运行 `cargo test -p uc-tauri --lib commands::restart::tests` 期望 4/4 PASS（这些是辅助函数测试，**不**需要 `restart_app`/`get_restart_state` command 已实装）。

    5. 提交：`test(95-02): RED — restart commands 辅助函数与 RestartState wire 形态单元测试`

    **澄清**：本 task RED 阶段重点是测试**纯函数 helper**（serde + mtime + OnceLock 语义），4 个测试在骨架完成时即应 PASS。下一 task 加入 `#[tauri::command]` 函数（带 AppHandle / State 参数，集成 + e2e 由 Plan 06 NetworkSection mount 实测覆盖）。RED 在这里指"测试存在并能 catch 未来 regression"，4 个 PASS 是预期 — 这是 commands 层 TDD 的现实形态：纯 IO + serde 测得到，AppHandle 调用测不到。
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos/src-tauri && cargo test -p uc-tauri --lib commands::restart::tests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/crates/uc-tauri/src/commands/restart.rs` 存在
    - 文件含 `pub static PROCESS_STARTED_AT: OnceLock<SystemTime>`
    - 文件含 `#[derive(Debug, Clone, Serialize)]` + `#[serde(rename_all = "camelCase")]` 紧贴 `pub struct RestartState`
    - 文件含 `pub struct RestartState`
    - 文件含 `pub process_started_at: i64`
    - 文件含 `pub settings_mtime: i64`
    - 文件含 `#[cfg(test)] mod tests`
    - 文件含至少 4 处 `#[test]`
    - `commands/mod.rs` 含 `pub mod restart`
    - `cargo test -p uc-tauri --lib commands::restart::tests` 退出码 0，输出含 `test result: ok. 4 passed`
    - `cargo check -p uc-tauri` 退出码 0
    - `git log --oneline -1` 标题以 `test(95-02):` 开头
  </acceptance_criteria>
  <done>骨架 + helper + RestartState + 4 个单元测试 PASS。restart_app/get_restart_state 还未实装。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — 实装 restart_app + get_restart_state Tauri commands + run.rs 注册 + PROCESS_STARTED_AT 初始化</name>
  <read_first>
    - Task 1 创建的 `src-tauri/crates/uc-tauri/src/commands/restart.rs`（骨架）
    - `src-tauri/crates/uc-tauri/src/commands/storage.rs:13-43`（complete analog — `info_span!` + `record_trace_fields` + `Instrument` + `runtime.storage_paths()` 模式）
    - `src-tauri/crates/uc-tauri/src/commands/updater.rs:300-301`（`app.restart()` 现有调用点）
    - `src-tauri/crates/uc-tauri/src/run.rs:130-148`（builder 装配序）
    - `src-tauri/crates/uc-tauri/src/run.rs:416-444`（invoke_handler 注册列表）
    - `src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs:161`（`storage_paths()` 暴露形态）
    - `src-tauri/crates/uc-application/src/facade/app_paths.rs:50`（确认 settings_path = app_data_root.join("settings.json")）
  </read_first>
  <files>src-tauri/crates/uc-tauri/src/commands/restart.rs, src-tauri/crates/uc-tauri/src/commands/mod.rs, src-tauri/crates/uc-tauri/src/run.rs</files>
  <action>
    **Step A — `commands/restart.rs` 加入两个 #[tauri::command]：**

    在 helper 函数之后、`#[cfg(test)]` 之前插入：

    ```rust
    /// Trigger graceful Tauri process restart for settings change effect.
    /// 触发 Tauri 进程优雅重启使设置变更生效。
    ///
    /// # Scope (per D-B1)
    /// 仅 cover GUI mode；CLI daemon (`uniclip daemon`) 不在范围。
    ///
    /// # Mechanism (per D-B2)
    /// 复用 `app.restart()`（与 `updater.rs:300-301` 同模式）。进程退出会触发
    /// `task_registry::shutdown` cancel cascade，daemon 子系统随 Tauri 进程
    /// 一起 graceful 关闭 —— 不显式调用 `DaemonHandle::shutdown`。
    #[tauri::command]
    pub async fn restart_app(
        app: tauri::AppHandle,
        _trace: Option<TraceMetadata>,
    ) -> Result<(), CommandError> {
        let span = info_span!(
            "command.restart.restart_app",
            trace_id = tracing::field::Empty,
            trace_ts = tracing::field::Empty,
        );
        record_trace_fields(&span, &_trace);

        async move {
            info!("restarting app for settings change (LAN-only Mode)");
            app.restart();
            // app.restart() 会调用 process exit；以下不可达，仅满足类型签名。
            #[allow(unreachable_code)]
            Ok(())
        }
        .instrument(span)
        .await
    }

    /// Read process boot timestamp + settings.json mtime for pending-state derivation.
    /// 读取进程启动时间戳与 settings.json mtime 用于 pending 状态推导。
    ///
    /// # Pending derivation (per D-D1)
    /// `settings_mtime > process_started_at` ⇒ pending（settings.json 在本进程
    /// 启动后被改过，重启后才能让新 `disable_relays` 值生效）。
    #[tauri::command]
    pub async fn get_restart_state(
        runtime: tauri::State<'_, std::sync::Arc<crate::bootstrap::TauriAppRuntime>>,
        _trace: Option<TraceMetadata>,
    ) -> Result<RestartState, CommandError> {
        let span = info_span!(
            "command.restart.get_restart_state",
            trace_id = tracing::field::Empty,
            trace_ts = tracing::field::Empty,
        );
        record_trace_fields(&span, &_trace);

        async move {
            let settings_path = runtime.storage_paths().settings_path.clone();
            let settings_mtime = read_settings_mtime_millis(&settings_path)?;
            let process_started_at = read_process_started_at_millis();
            Ok(RestartState {
                process_started_at,
                settings_mtime,
            })
        }
        .instrument(span)
        .await
    }
    ```

    **Step B — `commands/mod.rs` 加 `pub use`：**

    在 line 33 `pub use storage::*;` 之后插入：
    ```rust
    pub use restart::*;
    ```

    **Step C — `run.rs` 在 `tauri::Builder::default()` setup 早期初始化 `PROCESS_STARTED_AT`：**

    1. 找到 `let builder = tauri::Builder::default()`（约 line 142），在第一行 `.manage(runtime.clone())` 之前的语句加：
       ```rust
       // Phase 95: 启动早期赋值 PROCESS_STARTED_AT，供 NetworkSection 推导 pending。
       // 必须在第一次 invoke_handler 之前 set；放这里 = setup 最早期。
       let _ = crate::commands::restart::PROCESS_STARTED_AT.set(std::time::SystemTime::now());
       ```

       **关键约束**：这一行必须在 `tauri::Builder::default()` 之前**或**作为 Builder 链外的语句执行（不影响 builder 链，因为 OnceLock::set 只用 SystemTime::now，不依赖 app handle）。建议放置位置：line 142 `let builder = tauri::Builder::default()` **之前的独立语句**，避免与 builder 链交织。

    2. 找到 `.invoke_handler(tauri::generate_handler![ ... ])`（约 line 416-444），在 `// Lifecycle commands` 块（line 419-422）之后插入 2 行：
       ```rust
       // Restart commands (Phase 95)
       crate::commands::restart::restart_app,
       crate::commands::restart::get_restart_state,
       ```

       推荐插入位置：在 `crate::commands::get_daemon_connection_info,`（line 422）之后，在 `// Autostart commands` 之前。

    **Step D — 编译 + 测试：**

    1. `cargo build -p uc-tauri` 确认无 unresolved symbol
    2. `cargo test -p uc-tauri --lib commands::restart::tests` 期望 4/4 仍 PASS
    3. `cargo clippy -p uc-tauri --lib -- -D warnings` 期望 0 warnings（特别是 unused imports 与 dead_code）
    4. 提交：`feat(95-02): GREEN — 新增 restart_app + get_restart_state Tauri commands + PROCESS_STARTED_AT 初始化`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos/src-tauri && cargo test -p uc-tauri --lib commands::restart::tests 2>&1 | tail -10 && cargo build -p uc-tauri 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/crates/uc-tauri/src/commands/restart.rs` 含 `pub async fn restart_app`
    - `src-tauri/crates/uc-tauri/src/commands/restart.rs` 含 `pub async fn get_restart_state`
    - `src-tauri/crates/uc-tauri/src/commands/restart.rs` 含 `app.restart();`
    - `src-tauri/crates/uc-tauri/src/commands/restart.rs` 含 `runtime.storage_paths().settings_path`
    - `src-tauri/crates/uc-tauri/src/commands/mod.rs` 含 `pub use restart::*;`
    - `src-tauri/crates/uc-tauri/src/run.rs` 含 `PROCESS_STARTED_AT.set`
    - `src-tauri/crates/uc-tauri/src/run.rs` 含 `crate::commands::restart::restart_app,`
    - `src-tauri/crates/uc-tauri/src/run.rs` 含 `crate::commands::restart::get_restart_state,`
    - `cd src-tauri && cargo build -p uc-tauri` 退出码 0
    - `cd src-tauri && cargo test -p uc-tauri --lib commands::restart::tests` 退出码 0（4/4 PASS）
    - `cd src-tauri && cargo clippy -p uc-tauri --lib -- -D warnings` 退出码 0
    - `git log --oneline -1` 标题以 `feat(95-02):` 开头
  </acceptance_criteria>
  <done>两个 Tauri commands 编译过 + 注册到 invoke_handler + PROCESS_STARTED_AT 在 setup 早期初始化 + clippy clean。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: REFACTOR — daemon HTTP 契约 fence 与 LAN-only 边界审计</name>
  <read_first>
    - Task 2 创建的 `restart.rs`
    - `.planning/research/PITFALLS.md` Pitfall 5（"LAN-only" 营销语 vs 边界透明）
    - `src-tauri/crates/uc-tauri/src/commands/restart.rs`（确认未引用 daemon HTTP 端点）
  </read_first>
  <files>src-tauri/crates/uc-tauri/src/commands/restart.rs</files>
  <action>
    1. 在 `restart.rs` 末尾、`#[cfg(test)] mod tests` 之前加 fence 注释（README 风格 doc comment）：
       ```rust
       // ===== Phase 95 边界 fence =====
       //
       // 1. D-B1: 仅 cover GUI mode。本文件 NOT 暴露任何 daemon HTTP admin/restart 端点。
       //    CLI daemon (`uniclip daemon`) 用户走 systemctl/launchd（PROJECT.md §Out of Scope）。
       //
       // 2. D-D1: pending 推导仅走 mtime 比对。本文件 NOT 反查 daemon 当前 bind 值
       //    （bind-value 反查由 Phase 96 ConnectionChannelPort 承担）。
       //
       // 3. Pitfall 5 防御: 本文件 NOT 引用 telemetry / OTLP / pkarr / auto-update 任何字段；
       //    `restart_app` 只是 `app.restart()` thin wrapper，没有副作用越界（不 disable 遥测、不 reset state）。
       ```

    2. 在 `#[cfg(test)] mod tests` 内追加一个边界审计测试：
       ```rust
       #[test]
       fn restart_module_does_not_reference_daemon_http_or_telemetry() {
           // 静态约束：本文件 use 列表不允许引用 daemon HTTP 客户端或 telemetry 模块
           // —— 此测试不 cover 运行时，仅作 reviewer 注意力提示。真正 enforcement
           // 由本 plan 的 grep acceptance_criteria 完成。
           // 这个 test 永远 PASS，存在意义：让 PR diff reviewer 看到这条约束。
           assert!(true, "fence test — see acceptance_criteria grep checks");
       }
       ```

    3. 全工程 grep 验证：
       ```bash
       grep -nE "daemon_client|DaemonClient|admin/restart|telemetry_enabled|otlp" \
         src-tauri/crates/uc-tauri/src/commands/restart.rs
       ```
       期望 0 行。

    4. 全工程 grep 验证 `app.restart()` 的唯一性约束（仅 updater.rs 与本文件允许）：
       ```bash
       grep -rn "app\.restart()" src-tauri/crates/uc-tauri/src/commands/
       ```
       期望 2 行：`updater.rs:301` + `restart.rs::restart_app`。

    5. `cargo test -p uc-tauri --lib commands::restart::tests` 期望 5/5 PASS（原 4 + 新增 1 fence test）
    6. 提交：`refactor(95-02): 加边界 fence — restart 模块禁触 daemon HTTP / telemetry 字段`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard/lagos/src-tauri && cargo test -p uc-tauri --lib commands::restart::tests 2>&1 | tail -10 && grep -cE "daemon_client|DaemonClient|admin/restart|telemetry_enabled|otlp" /Users/mark/conductor/workspaces/uniclipboard/lagos/src-tauri/crates/uc-tauri/src/commands/restart.rs ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/crates/uc-tauri/src/commands/restart.rs` 含 `===== Phase 95 边界 fence =====` 注释
    - `cd src-tauri && cargo test -p uc-tauri --lib commands::restart::tests` 退出码 0（5/5 PASS）
    - `grep -nE "daemon_client|DaemonClient|admin/restart|telemetry_enabled|otlp" src-tauri/crates/uc-tauri/src/commands/restart.rs` 返回 0 行
    - `grep -rn "app\.restart()" src-tauri/crates/uc-tauri/src/commands/` 返回恰好 2 行（updater.rs + restart.rs）
    - `cd src-tauri && cargo clippy -p uc-tauri --lib -- -D warnings` 退出码 0
    - `git log --oneline -1` 标题以 `refactor(95-02):` 开头
  </acceptance_criteria>
  <done>边界 fence 已落，未来引入 daemon HTTP admin 端点或 telemetry 字段会被自动检测。5/5 PASS。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend webview → Tauri commands (IPC) | 任何 webview 内 JS 都可调 invokeWithTrace；本质上同进程信任，但 `restart_app` 是破坏性操作（进程退出） |
| Tauri command → 本地文件系统 | `get_restart_state` 读 settings.json 的 mtime；路径来自 runtime.storage_paths()，由 GuiBootstrapContext 在启动时确定 — 无路径注入面 |
| Tauri command → SystemTime API | `PROCESS_STARTED_AT.set(SystemTime::now())` 在 setup 阶段唯一调用 — OnceLock 强制单次 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02-01 | Tampering / DoS | restart_app 被恶意 webview 内容反复调用导致重启循环 | accept | webview 仅加载 app://localhost（vite dev / bundled assets），无第三方脚本注入面；`app.restart()` 即便被反复调用也只是重新启动一次（每次进程都需要重新加载 webview） |
| T-95-02-02 | Information Disclosure | settings.json 路径泄漏（如 mtime read 错误信息含完整 path） | mitigate | `read_settings_mtime_millis` 错误走 `CommandError::internal(e)`，IO 错误信息已是 std::io::Error::Display（含 path）；前端只把错误展示在 RestartBanner inline，不发 telemetry，本地用户已知自己的 settings 路径 — 接受 |
| T-95-02-03 | Information Disclosure | 透明边界违规：restart 模块意外读取 telemetry / OTLP / pkarr 状态 | mitigate | Pitfall 5 fence + grep acceptance_criteria 强制 0 匹配；本模块严格仅做 mtime 比对，不反查任何与 LAN-only 周边的开关 |
| T-95-02-04 | Elevation of Privilege | get_restart_state 路径遍历 — 攻击者构造恶意 settings_path 读任意文件 | mitigate | `runtime.storage_paths()` 从 GuiBootstrapContext 装配时硬编码 `app_data_root.join("settings.json")` —— 无 webview-controlled 输入；该 command 不接受任何来自前端的 path 参数 |
| T-95-02-05 | Repudiation | restart_app 调用无审计日志，用户重启后无 trace 关联 | mitigate | `info_span!` + `record_trace_fields` 已在 command 内置；`info!("restarting app for settings change (LAN-only Mode)")` 在 app.restart() 之前打日志 |
| T-95-02-06 | Spoofing | daemon-only CLI 模式被错误暴露 admin/restart 端点 | mitigate | D-B1 fence + grep acceptance_criteria 强制本模块不出现 admin/restart 字串；本 plan 不动 daemon HTTP 契约（Phase 94 锁定） |
</threat_model>

<verification>
- 5/5 单元测试 PASS（4 个 helper + 1 个 fence）
- `cargo build -p uc-tauri` 退出码 0
- `cargo clippy -p uc-tauri --lib -- -D warnings` 退出码 0
- `grep -nE "daemon_client|DaemonClient|admin/restart|telemetry_enabled|otlp" src-tauri/crates/uc-tauri/src/commands/restart.rs` 返回 0 行
- `grep -rn "app\.restart()" src-tauri/crates/uc-tauri/src/commands/` 返回恰好 2 行（updater.rs + restart.rs）
- `commands/mod.rs` 含 `pub mod restart;` 与 `pub use restart::*;`
- `run.rs` 含 PROCESS_STARTED_AT.set(...) + 两条 invoke_handler 注册行
- 整个 Tauri 应用 `cargo build` 通过（uc-tauri 不破其它 crate）
</verification>

<success_criteria>
- restart_app + get_restart_state 两个 #[tauri::command] 已实装
- PROCESS_STARTED_AT OnceLock 在 run.rs setup 早期初始化（在第一次 invoke_handler 之前）
- RestartState wire 形态 camelCase 与前端 invokeWithTrace 期望一致
- D-B1 / D-B2 / D-D1 三条决策实装到位
- Pitfall 5 fence 已落（grep 0 匹配 telemetry/OTLP/pkarr）
- 3 commits（test → feat → refactor）
</success_criteria>

<output>
完成后创建 `.planning/phases/095-networksection-ux/095-02-SUMMARY.md`
</output>
