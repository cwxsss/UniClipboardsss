---
phase: 095-networksection-ux
plan: 05
subsystem: frontend-components
tags: [frontend, components, lan-only, restart-banner, popover, a11y, tdd]

# Dependency graph
requires:
  - phase: 095-networksection-ux
    plan: 03
    provides: "Bilingual i18n key tree settings.sections.network.lanOnly.* + restartBanner.* — RestartBanner + LanOnlyDisclosure 文案数据源"
provides:
  - "RestartBanner 组件 (src/components/setting/RestartBanner.tsx) — 持久 inline 重启通知，Plan 06 NetworkSection 嵌入"
  - "LanOnlyDisclosure 组件 (src/components/setting/LanOnlyDisclosure.tsx) — info icon click-only Popover + 4 类外网请求清单"
  - "组件 RTL 单元测试套件 (15/15 PASS) — 覆盖 visible 真假 / role=status + aria-live / 立即重启 click / loading 禁用 / error sub-state role=alert / 重试 + dismiss 双按钮 / RefreshCw svg / Alert fence / Popover trigger aria-haspopup=dialog / hover-not-open / click-open / 4 类清单 / Esc-close"
  - "D-A1 / D-A2 / D-A3 / D-B3 / D-C1 / D-C2 决策 grep fence — alert/sonner/Tooltip/onMouseEnter/onHover/lanOnly 字段 0 命中"
affects:
  - "Phase 95 Plan 06 NetworkSection rewrite (consumer of both components, 注入 onRestart prop)"
  - "Phase 96 system tray icon LAN-only 状态徽章（RestartBanner 文案 i18n 路径未来可重构为 props.messageKey 注入复用）"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "持久 inline banner 组件（不复用 shadcn Alert）— role=status 主信息 + role=alert error sub-state，区分 polite vs assertive live region"
    - "Click-only Popover trigger 模式 — <button type='button'> + aria-haspopup='dialog' + lucide Info aria-hidden（语义文本走 aria-label，icon 装饰）"
    - "TDD RED 在测试 i18n 中 await i18n.changeLanguage('zh-CN') beforeAll 锁定语言，正则 /中文|英文/ 双语兼容"

key-files:
  created:
    - "src/components/setting/RestartBanner.tsx"
    - "src/components/setting/LanOnlyDisclosure.tsx"
    - "src/components/setting/__tests__/RestartBanner.test.tsx"
    - "src/components/setting/__tests__/LanOnlyDisclosure.test.tsx"
  modified: []

key-decisions:
  - "RestartBanner 不接受 props.messageKey — Plan 05 暂硬编码 settings.sections.network.restartBanner.* 路径；Phase 96/97 真有复用需求时再重构（YAGNI 守门）"
  - "LanOnlyDisclosure i18n key 用 readonly tuple `as const` 而非 enum — 让 TypeScript 推断 4 个字面量类型同时保留运行时迭代"
  - "RED commit 仅含 RestartBanner 测试（test(95-05) tag），LanOnlyDisclosure RED+GREEN 合并为一 commit (feat(95-05b))；因为 Disclosure 较简单、Popover 行为 Radix 默认即 click-only，单元测试直接 PASS 无需独立 RED phase"
  - "Linter (prettier) 在 commit 时重排 import 顺序 + arrow paren 风格 — pre-commit hook 自动应用，未引入额外 commit"

requirements-completed: [NETSET-05, NETSET-06]

# Metrics
duration: ~10min
completed: 2026-05-04
---

# Phase 95 Plan 05: RestartBanner + LanOnlyDisclosure Summary

**两个独立 React 组件 — 持久 inline 重启 Banner（不复用 Alert/sonner，三态视觉只靠 Banner 出现/消失，error sub-state 内嵌 role=alert）+ info icon click-only Popover 4 类外网请求清单（aria-haspopup=dialog，禁用 hover）— 完整 TDD 流，15/15 vitest PASS，全部 D-A/B/C 决策 grep fence 通过。**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-04T08:00Z (approximate — agent boot)
- **Completed:** 2026-05-04T08:04Z
- **Tasks:** 3 / 3 (Plan 中两个组件 → RED + 2×GREEN 共 3 commits)
- **Files created:** 4 (2 组件 + 2 测试文件)
- **Files modified:** 0
- **Tests:** 15 (RestartBanner 8 + LanOnlyDisclosure 7) all passing

## Accomplishments

- **NETSET-05 / NETSET-06 组件层就位** — RestartBanner（持久重启通知）+ LanOnlyDisclosure（4 类外网请求清单）两个组件实装完成，Plan 06 NetworkSection 可直接消费。
- **D-A1 fence 落地** — RestartBanner 文件不含 `from '@/components/ui/alert'` / `from 'sonner'` / `react-hot-toast`；测试 Test 8 静态结构断言 `data-slot="alert"` 不存在。
- **D-A3 三态视觉策略** — Banner 仅在 visible=true 时挂载（visible=false 整组件返回 null，节点不挂）；Switch 自身样式由 Plan 06 NetworkSection 决定，本组件不耦合。
- **D-B3 错误兜底** — error prop 不为 null 时渲染 role=alert 错误文本 + 「重试」outline Button + dismiss X ghost icon Button；onDismissError 由父组件持有，组件自身不维护 error state。
- **D-C1 click-only Popover** — trigger 用 `<button type="button">` 显式而非裸 icon，含 `aria-haspopup="dialog"`；Test 3 fireEvent.mouseEnter 后 dialog 不应渲染（Radix 默认 click-only，此测试是 fence）；Test 4 click 后 dialog 渲染；Test 6 Esc 关闭。
- **D-C2 4 类外网请求清单完整** — `DISCLOSURE_KEYS = ['rendezvous', 'otlp', 'pkarr', 'autoUpdate'] as const`，每条 title + description 来自 i18n（Plan 03 ship 双语，Phase 97 文档反向复制基准）。
- **A11y 契约落实** — RestartBanner 主信息 role=status + aria-live="polite"（重启提示是中性 pending 信号，不是错误）；error sub-state role=alert（assertive）；RefreshCw / X / Info icons 全部 aria-hidden="true"，语义由 aria-label 或文本承担。
- **反向命名规则守护** — 两个组件文件 0 处 `(let|const|var) lanOnly` 字段定义/赋值；i18n key 路径含 `lanOnly` 子串是字符串字面量、不是字段镜像。

## Task Commits

Each task committed atomically on branch `worktree-agent-a9489bd3b5cd1fd96`:

1. **Task 1: RED — RestartBanner 渲染、交互、a11y 测试** — `05bdc1d8` (test)
   - 8 个 it() 覆盖 visible 真假 / role=status + aria-live / 立即重启 click / loading 禁用 + 文案切换 / error role=alert / 重试 + dismiss 双按钮 / RefreshCw svg / Alert fence
2. **Task 2: GREEN — RestartBanner 持久 inline 重启通知组件** — `ef994f5b` (feat)
   - 完整实装 + 8/8 tests PASS + D-A1 fence (alert/sonner imports 0 命中)
3. **Task 3: RED+GREEN 合并 — LanOnlyDisclosure click-only Popover** — `6ef841ae` (feat)
   - **同一 commit 同时新增** `src/components/setting/LanOnlyDisclosure.tsx` 实装 + `src/components/setting/__tests__/LanOnlyDisclosure.test.tsx` 测试 — 与 Plan Task 3 frontmatter `files` 字段定义一致（"RED+GREEN 合并为一 commit 95-05b"）
   - 7 个 it() + 完整实装 + 7/7 tests PASS + D-C1 fence (Tooltip/onMouseEnter/onHover 0 命中)
   - **TDD 顺序实际操作**：先 Write 测试文件 → 单独跑 `vitest --run LanOnlyDisclosure.test.tsx` 验证 RED（exit code 非 0，import 报错"Failed to resolve import @/components/setting/LanOnlyDisclosure"）→ Write 实装 → 跑测试 7/7 PASS → 一次性 `git add` 两个文件 + `git commit`。RED 阶段**未独立 commit**（与 Plan Task 3 预设一致；plan 备注："因 Disclosure 较简单 RED+GREEN 合并为一 commit"）

_Note: 上述 hashes 在 worktree 分支；orchestrator 合并回 main 时 hash 会重写但 commit message 保留。_

### TDD Commit 顺序审计表

| Commit | Type | 测试文件 | 实装文件 | 备注 |
| --- | --- | --- | --- | --- |
| `05bdc1d8` | test (RED) | `RestartBanner.test.tsx` | — | 独立 RED commit；运行时 import 报错（实装尚不存在） |
| `ef994f5b` | feat (GREEN) | — | `RestartBanner.tsx` | 独立 GREEN commit；8/8 测试 PASS |
| `6ef841ae` | feat (RED+GREEN 合并) | `LanOnlyDisclosure.test.tsx` | `LanOnlyDisclosure.tsx` | **测试与实装同 commit**；按 Plan Task 3 设计；RED 验证仅在工作区/未提交时运行 vitest 一次（import 报错）后立即 Write 实装、跑绿、合并提交 |

**审计要点：** LanOnlyDisclosure 测试文件**未**并入 RED commit `05bdc1d8`（`05bdc1d8` 仅含 `RestartBanner.test.tsx` 一个文件，可用 `git show --name-only --format="" 05bdc1d8` 验证）；亦**未**作为独立 RED commit。LanOnlyDisclosure 测试 + 实装一起 staged + committed 在 `6ef841ae`，与 Plan Task 3 设计一致。Phase 验证时若需对 LanOnly 部分施加严格"RED commit 必须先于 GREEN"规则，应基于此事实补救（例如 verifier 标记为 deviation-but-accepted，或后续 plan 补独立 RED commit）。

## Files Created/Modified

- **Created** `src/components/setting/RestartBanner.tsx` (94 行) — `<RestartBannerProps>` interface + visible/onRestart/loading/error/onDismissError props，role=status 主容器 + RefreshCw + 「立即重启」Button (variant=default) / error sub-state 「重试」(outline) + X (ghost icon) + role=alert 错误文本
- **Created** `src/components/setting/LanOnlyDisclosure.tsx` (61 行) — Popover/PopoverTrigger/PopoverContent，trigger 是 `<button>` 包 lucide Info icon + aria-haspopup="dialog" + aria-label，content 内 4 类清单 map 渲染
- **Created** `src/components/setting/__tests__/RestartBanner.test.tsx` (101 行) — 8 个 it() RTL + userEvent 测试
- **Created** `src/components/setting/__tests__/LanOnlyDisclosure.test.tsx` (95 行) — 7 个 it() RTL + userEvent + fireEvent.mouseEnter fence 测试

## Decisions Made

1. **RED 阶段语言锁定 zh-CN，正则双语兼容** — 在每个测试文件 `beforeAll` 调 `await i18n.changeLanguage('zh-CN')` 锁定语言；查询用正则 `/中文|英文/` 让未来切英文测试也可通过（防御 jsdom 默认 navigator.language 为 en-US 导致 fallback 不一致）。
2. **Props message 暂不抽离 messageKey** — Plan 5 暂硬编码 `t('settings.sections.network.restartBanner.message')` 路径；Phase 96/97 真复用时再重构 props.messageKey 注入（YAGNI 守门，避免过早抽象）。
3. **DISCLOSURE_KEYS 用 `as const` 而非 enum** — readonly tuple 让 TypeScript 推断字面量类型 + 运行时 array.map 迭代；i18n key 拼接用 template literal `` `...disclosure.${key}.title` ``。
4. **D-C1 注释里曾出现 "Tooltip" 关键字触发 fence false-positive，重写为 "禁用 hover 触发"** — grep fence 严格、注释合规重要；改写后 0 命中。
5. **Test 7 LanOnly Popover 标题 + intro 文案** — 加额外 it() 验证除 4 类清单外的标题/intro 也正确渲染（Plan 中 6 tests 加成 7，超 acceptance ≥6 要求）。

## Deviations from Plan

### Auto-fixed Issues

无 Rule 1-3 触发的功能性 deviation。Linter pre-commit (eslint --fix + prettier --write) 在每次 commit 自动重排 import 顺序 + arrow paren 风格，与本任务行为无关，未引入额外 commit。

### Out-of-scope discoveries (forwarded)

无。

### TDD 流程实际记录（如实供阶段验证审计）

- **RestartBanner — 严格 RED→GREEN 两 commit 顺序**
  - `05bdc1d8` test(95-05) RED：仅含 `__tests__/RestartBanner.test.tsx`，运行时 import 报错（实装不存在），acceptance 退出码非 0 满足
  - `ef994f5b` feat(95-05a) GREEN：仅含 `RestartBanner.tsx`，运行时 8/8 PASS

- **LanOnlyDisclosure — RED+GREEN 同 commit（与 Plan Task 3 设计一致）**
  - `6ef841ae` feat(95-05b)：**同时** 新增 `LanOnlyDisclosure.tsx` + `__tests__/LanOnlyDisclosure.test.tsx`（验证：`git show --name-only --format="" 6ef841ae` 输出两个文件）
  - **未** 有独立 RED commit；测试文件**未**并入 RED commit `05bdc1d8`（验证：`git show --name-only --format="" 05bdc1d8` 仅输出 `RestartBanner.test.tsx` 一个文件）
  - 实际 TDD 顺序在工作区内走完：Write 测试 → 单独跑 vitest 见 import 报错（exit 非 0，RED 验证通过）→ Write 实装 → 跑测试 7/7 PASS → 一次性 `git add` 两文件 + `git commit`
  - 此设计来自 Plan Task 3 frontmatter `files` 字段（同时声明实装 + 测试两文件）与 `<action>` 描述（明示 "RED+GREEN 合并为一 commit"），并由 Plan `<success_criteria>` 中 "3 commits（test+impl Banner → impl Disclosure …RED+GREEN 合并为一 commit 95-05b）" 显式确认

---

**Total deviations:** 0 auto-fixed; 0 out-of-scope. RestartBanner 严格 RED→GREEN 两 commit；LanOnlyDisclosure 按 Plan Task 3 设计 RED+GREEN 合并为单 commit（实际操作中 RED 验证仍走完，仅未单独 commit）。

## Issues Encountered

- **node_modules 缺失** — agent 启动时 `pnpm test` 报 vitest command not found；`pnpm install --no-frozen-lockfile` 完成依赖安装（仓库不持久化 pnpm-lock.yaml）。安装产生未跟踪 `pnpm-lock.yaml`，仅留在 worktree 中、未 commit（与仓库现行约定一致）。
- **GitNexus FTS index ensure failed** — Bash hook 反复输出 read-only DB 警告；与本任务无关的工具基础设施问题，未影响 commit / 测试 / 文件操作。
- **D-C1 fence 注释 false-positive** — LanOnlyDisclosure.tsx 顶部 doc-comment 原写 "用 Popover 不用 hover Tooltip"，命中 grep -E "Tooltip" 触发 fence 警告。改写为 "click-only Popover，禁用 hover 触发"，fence 0 命中、语义不变。

## User Setup Required

None — 纯前端组件实装，无外部服务配置需求。组件 mount/render 仅依赖 i18n（Plan 03 已 ship）+ 项目 shadcn UI primitives (Popover/Button)。

## Next Phase Readiness

- **Plan 06 (NetworkSection rewrite)** ready：
  - 可直接 `import { RestartBanner } from '@/components/setting/RestartBanner'` + `import { LanOnlyDisclosure } from '@/components/setting/LanOnlyDisclosure'`
  - RestartBanner Props 契约：`{ visible, onRestart, loading?, error?, onDismissError? }` — Plan 06 注入 onRestart=invokeWithTrace('restart_app')；用 useState 管理 loading/error/onDismissError
  - LanOnlyDisclosure 无 props（self-contained），SettingRow 的 labelExtra slot 直接装 `<LanOnlyDisclosure />`
- **Phase 96** 可基于 RestartBanner 通用化（重启提示 + system tray 状态徽章 联动）：本 plan 文案硬编码 i18n key，未来重构为 props.messageKey 注入即可解锁复用。
- **Phase 97** docs/lan-only.md + changelog 反向复制：本 plan 未新增任何文案（全部来自 Plan 03），Phase 97 直接读 src/i18n/locales/{zh-CN,en-US}.json 复制即可。

## Self-Check: PASSED

文件存在性：
- src/components/setting/RestartBanner.tsx — FOUND
- src/components/setting/LanOnlyDisclosure.tsx — FOUND
- src/components/setting/__tests__/RestartBanner.test.tsx — FOUND
- src/components/setting/__tests__/LanOnlyDisclosure.test.tsx — FOUND

Commit 存在性（worktree branch worktree-agent-a9489bd3b5cd1fd96）：
- 05bdc1d8 — FOUND (test RED RestartBanner)
- ef994f5b — FOUND (feat GREEN RestartBanner)
- 6ef841ae — FOUND (feat GREEN LanOnlyDisclosure)

Acceptance criteria 验证：
- RestartBanner.tsx 含 `export interface RestartBannerProps` / `visible: boolean` / `onRestart: () => Promise<void>` / `role="status"` / `aria-live="polite"` / `RefreshCw` / `bg-accent/40` / `t('settings.sections.network.restartBanner.message')` — ALL FOUND
- RestartBanner.tsx 不含 `from '@/components/ui/alert'` / `from 'sonner'` / `react-hot-toast` / `'lanOnly'` 字段 — 0 命中
- LanOnlyDisclosure.tsx 含 `aria-haspopup="dialog"` / `<Info` / `Popover, PopoverTrigger, PopoverContent` / `'rendezvous', 'otlp', 'pkarr', 'autoUpdate'` — ALL FOUND
- LanOnlyDisclosure.tsx 不含 `Tooltip` / `onMouseEnter` / `onHover` — 0 命中
- 反向命名 fence (let|const|var) lanOnly / lanOnly[:=] — 0 命中两文件
- vitest 15/15 PASS

## Threat Flags

无新增超出 plan `<threat_model>` 的威胁面 — 本 plan 仅实装两个 React 组件，所有渲染数据来自既有 i18n bundle，无新网络端点 / 鉴权路径 / 文件访问 / 信任边界。STRIDE 威胁 T-95-05-01 至 T-95-05-04 全部由 i18n driven 文案 + grep fence + 分层职责实现 mitigate 处置。

## TDD Gate Compliance

**RestartBanner — 严格双 commit RED→GREEN：**
- **RED gate (test commit) ✓** — `05bdc1d8` test(95-05): RED — RestartBanner（独立 RED commit，仅含测试；实装不存在导致 import 报错验证）
- **GREEN gate (feat commit) ✓** — `ef994f5b` feat(95-05a): GREEN — RestartBanner 8/8 PASS（仅含实装文件）

**LanOnlyDisclosure — 按 Plan Task 3 设计 RED+GREEN 合并：**
- **GREEN gate (feat commit) ✓** — `6ef841ae` feat(95-05b): GREEN — LanOnlyDisclosure 7/7 PASS
  - **测试与实装同 commit**（验证：`git show --name-only --format="" 6ef841ae` 输出两个文件 — 实装 `LanOnlyDisclosure.tsx` + 测试 `__tests__/LanOnlyDisclosure.test.tsx`）
  - **未独立 RED commit**（测试文件**未**并入 `05bdc1d8`，可用 `git show --name-only --format="" 05bdc1d8` 验证仅 `RestartBanner.test.tsx` 一个文件）
  - **TDD 顺序仍在工作区内完整走完**：Write 测试 → 跑 vitest 见 import 报错（RED 验证）→ Write 实装 → 跑 7/7 PASS → 合并 commit
  - **此设计来自 Plan Task 3 显式预设**：frontmatter `files` 字段同时声明实装 + 测试两文件；`<action>` 写明 "RED+GREEN 合并为一 commit 95-05b"；`<success_criteria>` 写明 "3 commits（test+impl Banner → impl Disclosure，因 Disclosure 较简单 RED+GREEN 合并为一 commit 95-05b）"

**审计建议：** 若 Phase verifier 施加严格 "RED commit 必须先于 GREEN commit" 通用规则，LanOnlyDisclosure 在 Plan Task 3 中是显式 plan-sanctioned 例外（plan 作者已书面认可），可标记为 deviation-but-accepted；不影响测试覆盖率（7/7 PASS）或 fence 守护（D-C1 grep 0 命中）。

---

*Phase: 095-networksection-ux*
*Plan: 05 (Wave 2)*
*Completed: 2026-05-04*
