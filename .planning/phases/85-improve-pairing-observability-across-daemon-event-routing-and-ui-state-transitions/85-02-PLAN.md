---
phase: 85-improve-pairing-observability-across-daemon-event-routing-and-ui-state-transitions
plan: '02'
type: execute
wave: '2'
depends_on:
  - 85-01
files_modified:
  - src/hooks/useDaemonEvents.ts
  - src/components/PairingNotificationProvider.tsx
  - src/api/setup.ts
  - src/store/setupRealtimeStore.ts
  - src/observability/breadcrumbs.ts
  - src/components/__tests__/PairingDialog.test.tsx
  - src/hooks/__tests__/useDaemonEvents.test.ts
autonomous: true
requirements:
  - PH85-01
  - PH85-02
  - PH85-04
  - PH85-05
must_haves:
  truths:
    - frontend pairing/setup consumers record when they accept, ignore, dedupe, or reject realtime events
    - event-consumption diagnostics stay session-centered and explain why a path was ignored
    - pairing/setup UI observability uses a shared contract instead of scattered console-only messages
  artifacts:
    - path: src/hooks/useDaemonEvents.ts
      provides: typed pairing/setup routing diagnostics at the shared hook boundary
    - path: src/components/PairingNotificationProvider.tsx
      provides: structured handling records for pairing request/verification/complete/fail paths
    - path: src/store/setupRealtimeStore.ts
      provides: explicit dedupe/ignore handling records for setup realtime state
  key_links:
    - from: src/hooks/useDaemonEvents.ts
      to: src/components/PairingNotificationProvider.tsx
      via: shared session-aware event routing callbacks
    - from: src/api/setup.ts
      to: src/store/setupRealtimeStore.ts
      via: setup.stateChanged and setup.spaceAccessCompleted consumption decisions
---

<objective>
Make frontend event handling diagnosable. This wave should expose which realtime events the UI accepted, ignored, deduped, or failed to parse, and connect that to the session that the user actually sees.

This is not a UX redesign. It is targeted visibility around current pairing/setup consumers.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-improve-pairing-observability-across-daemon-event-routing-and-ui-state-transitions/85-RESEARCH.md
@src/hooks/useDaemonEvents.ts
@src/components/PairingNotificationProvider.tsx
@src/components/PairingDialog.tsx
@src/api/setup.ts
@src/store/setupRealtimeStore.ts
@src/observability/breadcrumbs.ts
@src/components/__tests__/PairingDialog.test.tsx
@src/hooks/__tests__/useDaemonEvents.test.ts
</context>

<interfaces>
From `src/hooks/useDaemonEvents.ts`:

```typescript
if (event.eventType === 'pairing.updated') { ... }
if (event.eventType === 'pairing.verification_required') { ... }
if (event.eventType === 'setup.spaceAccessCompleted') { ... }
```

From `src/api/setup.ts`:

```typescript
if (!enrichedEvent.sessionId) {
  return
}

if (seenEventKeys.has(dedupeKey)) {
  return
}
```

From `src/components/PairingNotificationProvider.tsx`:

```typescript
const currentSessionId = activeSessionIdRef.current
if (!currentSessionId || sessionId !== currentSessionId) return
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Harden the shared frontend hook boundary</name>
  <files>
    src/hooks/useDaemonEvents.ts
    src/hooks/__tests__/useDaemonEvents.test.ts
  </files>
  <action>
Move pairing event handling closer to the same standard already used for setup/peer payloads:

- add explicit typed handling or guards for pairing payloads at the hook boundary
- record routing decisions for request, verification, verifying, complete, failed, and setup-space-access-completed paths
- when an event is ignored because it is malformed or unsupported, emit a structured diagnostic instead of silently returning

Prefer one shared helper inside the hook over repeated inline logging.
</action>
<verify>
<automated>bun test src/hooks/**tests**/useDaemonEvents.test.ts</automated>
</verify>
<done>The shared hook records routing decisions and unsupported payload cases explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument PairingNotificationProvider decision points</name>
  <files>
    src/components/PairingNotificationProvider.tsx
    src/components/__tests__/PairingDialog.test.tsx
  </files>
  <action>
Add structured diagnostics around the provider's major decision points:

- request accepted
- request rejected
- verification accepted for the active session
- event ignored because the session does not match
- dialog canceled
- success/failure completion handling

If the project already has a safe breadcrumb utility, prefer that over raw `console.*`. Keep the wording short and machine-useful.
</action>
<verify>
<automated>bun test src/components/**tests**/PairingDialog.test.tsx</automated>
</verify>
<done>Provider behavior is observable per session and regression tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Make setup realtime store dedupe and ignore paths visible</name>
  <files>
    src/api/setup.ts
    src/store/setupRealtimeStore.ts
  </files>
  <action>
Expose the important setup realtime decisions:

- missing session id dropped
- duplicate state dropped
- active session switched
- space access completion ignored because setup already completed
- retry scheduling or initialization failure

Do not spam logs on every normal update; focus on state changes, suppressions, retries, and failure paths.
</action>
<verify>
<automated>bun test src/**tests**/api/daemon/setup.test.ts src/store/**tests**/setupRealtimeStore.test.ts</automated>
</verify>
<done>Setup realtime flow explains why events were applied or ignored instead of failing silently.</done>
</task>

</tasks>

<acceptance_criteria>

- Frontend pairing/setup consumers expose structured, session-aware handling records for normal and ignored paths
- Silent drops caused by dedupe, session filtering, or malformed payloads become diagnosable
- Existing race-fix regression tests remain green
- No raw secrets, fingerprints, or short codes are emitted into frontend observability output
  </acceptance_criteria>
