---
phase: 85-improve-pairing-observability-across-daemon-event-routing-and-ui-state-transitions
plan: '03'
type: execute
wave: '3'
depends_on:
  - 85-01
  - 85-02
files_modified:
  - src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
  - src-tauri/crates/uc-daemon/tests/setup_api.rs
  - src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
  - src/store/__tests__/setupRealtimeStore.test.ts
  - .planning/phases/85-improve-pairing-observability-across-daemon-event-routing-and-ui-state-transitions/85-VALIDATION.md
autonomous: true
requirements:
  - PH85-04
  - PH85-06
must_haves:
  truths:
    - older low-latency pairing race fixes stay covered
    - automated tests prove observability survives the critical pairing and setup transitions
    - validation evidence shows one session can be followed across daemon, bridge, and frontend state handling
  artifacts:
    - path: src-tauri/crates/uc-daemon/tests/setup_api.rs
      provides: backend coverage for pairing/setup transition observability expectations
    - path: src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
      provides: frontend coverage for session-aware observability in realtime handling
    - path: .planning/phases/85-improve-pairing-observability-across-daemon-event-routing-and-ui-state-transitions/85-VALIDATION.md
      provides: verification notes and known limits for the completed phase
  key_links:
    - from: src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
      to: src/store/__tests__/setupRealtimeStore.test.ts
      via: setup transition behavior and frontend realtime consumption expectations
    - from: src-tauri/crates/uc-daemon/tests/setup_api.rs
      to: src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
      via: same session-centered flow verified on backend and frontend
---

<objective>
Prove that Phase 85 observability works in practice and that the previously fixed pairing races remain fixed. This wave is validation-heavy by design.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-improve-pairing-observability-across-daemon-event-routing-and-ui-state-transitions/85-RESEARCH.md
@src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
@src-tauri/crates/uc-daemon/tests/setup_api.rs
@src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
@src/store/__tests__/setupRealtimeStore.test.ts
@src/components/__tests__/PairingDialog.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend regression coverage around pairing-driven setup transitions</name>
  <files>
    src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
    src-tauri/crates/uc-daemon/tests/setup_api.rs
  </files>
  <action>
Add or extend tests so the critical pairing-driven setup transitions remain covered while observability instrumentation is present:

- low-latency verification after initiate
- reject/failure return path back to device selection
- completion path that leads to final setup completion

Where practical, assert on the presence of the new structured observability behavior or metadata at the boundary under test.
</action>
<verify>
<automated>cd src-tauri && cargo test -p uc-app setup_orchestrator -- --nocapture && cargo test -p uc-daemon setup_api -- --nocapture</automated>
</verify>
<done>Backend regression coverage proves pairing-driven setup transitions still behave and remain diagnosable.</done>
</task>

<task type="auto">
  <name>Task 2: Extend frontend realtime tests to assert observability outcomes</name>
  <files>
    src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
    src/store/__tests__/setupRealtimeStore.test.ts
  </files>
  <action>
Expand frontend realtime tests so they verify the new observability paths, especially:

- ignored event because session mismatch
- deduped setup state event
- success and failure completion handling
- no accidental reintroduction of silent-drop behavior

Prefer assertions on the shared observability helper or breadcrumb sink instead of brittle console text matching.
</action>
<verify>
<automated>bun test src/components/**tests**/PairingNotificationProvider.realtime.test.tsx src/store/**tests**/setupRealtimeStore.test.ts</automated>
</verify>
<done>Frontend tests prove observability behavior on the same high-risk flows that previously regressed.</done>
</task>

<task type="auto">
  <name>Task 3: Record phase validation evidence and remaining blind spots</name>
  <files>.planning/phases/85-improve-pairing-observability-across-daemon-event-routing-and-ui-state-transitions/85-VALIDATION.md</files>
  <action>
Write `85-VALIDATION.md` after implementation and test runs. Capture:

- what was verified automatically
- what was verified manually
- what one real pairing session can now be traced through
- any remaining blind spots or future follow-up gaps

The document must be evidence-focused, not aspirational.
</action>
<verify>
<automated>test -f .planning/phases/85-improve-pairing-observability-across-daemon-event-routing-and-ui-state-transitions/85-VALIDATION.md</automated>
</verify>
<done>Validation record exists and explains the actual proof for Phase 85.</done>
</task>

</tasks>

<acceptance_criteria>

- Existing race-fix coverage remains intact
- Backend and frontend tests both cover observability behavior on pairing-critical transitions
- The finished phase includes written validation evidence, not just passing tests
  </acceptance_criteria>
