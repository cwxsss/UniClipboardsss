---
phase: 094-backend-network-allow-relay-fallback
plan: 02
type: execute
wave: 2
depends_on:
  - "094-01"
files_modified:
  - src-tauri/crates/uc-application/src/facade/settings/models.rs
  - src-tauri/crates/uc-application/src/facade/settings/mod.rs
autonomous: true
requirements:
  - NETSET-01
  - NETSET-02
tags:
  - uc-application
  - settings
  - facade
  - lan-only

must_haves:
  truths:
    - "`NetworkSettingsView { allow_relay_fallback: bool }` + `NetworkSettingsPatch { allow_relay_fallback: Option<bool> }` 镜像于 application facade 层"
    - "`SettingsView.network: NetworkSettingsView` 与 `SettingsPatch.network: Option<NetworkSettingsPatch>` 顶层挂载，与 core::Settings 同形"
    - "`apply_settings_patch` 末段追加 `network` 处理 — 旧客户端不带 `network` 段 PUT 自动 = no-op，不抹掉已存在 `network` 字段（NETSET-02 success criterion #2 硬约束 — Pitfall 历史欠账边界）"
    - "`From<core::Settings> for SettingsView` 末段映射补 `network` 段（业务正向语义透明流过，不取反 — Pitfall 1 铁律）"
    - "facade 对外 `pub use` 白名单（`mod.rs:5-12`）追加 `NetworkSettingsView, NetworkSettingsPatch`（uc-application/AGENTS.md §11.4 强制：新增类型必须显式 pub use；Phase 94 不引入新越界 import）"
    - "facade.rs 与 SettingsFacade::update 公共签名**不动**（仍接 `SettingsPatch` 返回 `SettingsView`）— `restart_required: bool` 计算下沉到 webserver handler（plan 04），不污染 application 公共契约"
  artifacts:
    - path: "src-tauri/crates/uc-application/src/facade/settings/models.rs"
      provides: "View/Patch 镜像 + apply_settings_patch network 段 + From<core::Settings> 映射 network"
      contains: "pub struct NetworkSettingsView"
    - path: "src-tauri/crates/uc-application/src/facade/settings/mod.rs"
      provides: "facade `pub use` 白名单含 NetworkSettingsView / NetworkSettingsPatch"
      contains: "NetworkSettingsPatch, NetworkSettingsView"
  key_links:
    - from: "uc-application::SettingsView.network"
      to: "uc-core::Settings.network (plan 01)"
      via: "From<core::Settings> for SettingsView 末段"
      pattern: "network: NetworkSettingsView \\{"
    - from: "uc-application::apply_settings_patch network 段"
      to: "uc-core::Settings.network.allow_relay_fallback"
      via: "patch.network -> existing.network.allow_relay_fallback"
      pattern: "if let Some\\(network\\) = patch.network"
    - from: "外部消费者 (uc-webserver, plan 04)"
      to: "facade::settings::{NetworkSettingsView, NetworkSettingsPatch}"
      via: "facade re-export 白名单"
      pattern: "pub use models::\\{[^}]*NetworkSettings"
---

<objective>
在 `uc-application::facade::settings` 层新增 `NetworkSettingsView` / `NetworkSettingsPatch` 镜像；扩展 `SettingsView` / `SettingsPatch` 顶层挂载、`apply_settings_patch` 处理逻辑、`From<core::Settings> for SettingsView` 映射；并在 `mod.rs` 白名单显式 pub use 新类型。

Purpose: facade 层是 daemon-contract / webserver 接 core 的唯一通道（uc-application/AGENTS.md §11.4 铁律）。Phase 94 的下游 plans（03 daemon-contract / 04 webserver）必须通过此层访问 NetworkSettings；不补这层，下游 import 形成越界。同时 `apply_settings_patch` 的 "only Some fields update" 语义是 NETSET-02 success criterion #2 的硬约束。

Output:
- `NetworkSettingsView` 结构体（`Debug, Clone, PartialEq, Eq` —— 与 `FileSyncSettingsView` 同 derive）
- `NetworkSettingsPatch` 结构体（`Debug, Clone, Default` —— 与 `FileSyncSettingsPatch` 同 derive）
- `SettingsView.network: NetworkSettingsView` + `SettingsPatch.network: Option<NetworkSettingsPatch>`
- `From<core::Settings> for SettingsView` 末段补 `network` 映射
- `apply_settings_patch` 末段补 `if let Some(network) = patch.network { ... }` 分支
- `mod.rs` `pub use` 列表追加 `NetworkSettingsPatch, NetworkSettingsView`（按 alphabetic 顺序插入）
- facade 单元测试：apply_patch 的 None / Some(true) / Some(false) 三组覆盖
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

# 受影响代码
@src-tauri/crates/uc-application/src/facade/settings/models.rs
@src-tauri/crates/uc-application/src/facade/settings/mod.rs
@src-tauri/crates/uc-application/src/facade/settings/facade.rs

<interfaces>
<!-- 现有 view/patch 镜像模式 + apply_settings_patch 末段模式 -->
<!-- 来源：src-tauri/crates/uc-application/src/facade/settings/models.rs -->

现有 View（line 124-131，FileSyncSettingsView 镜像）：
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct FileSyncSettingsView {
    pub file_sync_enabled: bool,
    pub small_file_threshold: u64,
    pub max_file_size: u64,
    pub file_cache_quota_per_device: u64,
    pub file_retention_hours: u32,
    pub file_auto_cleanup: bool,
}
```

现有 Patch（line 207-215，FileSyncSettingsPatch 镜像）：
```rust
#[derive(Debug, Clone, Default)]
pub struct FileSyncSettingsPatch {
    pub file_sync_enabled: Option<bool>,
    // ...
}
```

现有 SettingsView 顶层（line 133-143）：
```rust
#[derive(Debug, Clone)]
pub struct SettingsView {
    pub schema_version: u32,
    pub general: GeneralSettingsView,
    pub sync: SyncSettingsView,
    pub retention_policy: RetentionPolicyView,
    pub security: SecuritySettingsView,
    pub pairing: PairingSettingsView,
    pub keyboard_shortcuts: HashMap<String, ShortcutKeyView>,
    pub file_sync: FileSyncSettingsView,
}
```

现有 SettingsPatch 顶层（line 217-226）：
```rust
#[derive(Debug, Clone, Default)]
pub struct SettingsPatch {
    pub general: Option<GeneralSettingsPatch>,
    pub sync: Option<SyncSettingsPatch>,
    pub retention_policy: Option<RetentionPolicyPatch>,
    pub security: Option<SecuritySettingsPatch>,
    pub pairing: Option<PairingSettingsPatch>,
    pub keyboard_shortcuts: Option<HashMap<String, Option<ShortcutKeyView>>>,
    pub file_sync: Option<FileSyncSettingsPatch>,
}
```

现有 From 映射末段（line 434-442，紧跟 keyboard_shortcuts 之后）：
```rust
            file_sync: FileSyncSettingsView {
                file_sync_enabled: value.file_sync.file_sync_enabled,
                // ...
                file_auto_cleanup: value.file_sync.file_auto_cleanup,
            },
        }
    }
}
```

现有 apply_settings_patch 末段（line 544-563）：
```rust
    if let Some(file_sync) = patch.file_sync {
        if let Some(v) = file_sync.file_sync_enabled {
            existing.file_sync.file_sync_enabled = v;
        }
        // ... 其它字段
    }

    existing
}
```

现有 mod.rs pub use 白名单（line 1-13）：
```rust
mod facade;
mod models;

pub use facade::{SettingsFacade, SettingsFacadeError};
pub use models::{
    ContentTypesPatch, ContentTypesView, FileSyncSettingsPatch, FileSyncSettingsView,
    GeneralSettingsPatch, GeneralSettingsView, PairingSettingsPatch, PairingSettingsView,
    RetentionPolicyPatch, RetentionPolicyView, RetentionRulePatchValue, RetentionRuleView,
    RuleEvaluationView, SecuritySettingsPatch, SecuritySettingsView, SettingsPatch, SettingsView,
    ShortcutKeyView, SyncFrequencyView, SyncSettingsPatch, SyncSettingsView, ThemeView,
    UpdateChannelView,
};
```

注意：alphabetic 顺序中，`Network*` 排在 `Pairing*` 之前、`GeneralSettings*` 之后。

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 models.rs 新增 NetworkSettingsView/NetworkSettingsPatch + 顶层 SettingsView/SettingsPatch 挂载 + From 映射 + apply_settings_patch 末段</name>
  <files>src-tauri/crates/uc-application/src/facade/settings/models.rs</files>

  <read_first>
    1. `src-tauri/crates/uc-application/src/facade/settings/models.rs`（必读完整文件，重点 line 124-226 的 view/patch 定义、line 386-444 的 From 映射、line 446-566 的 apply_settings_patch；line 124-131 与 207-215 是镜像模板）
    2. **【checker WARNING 2】** `src-tauri/crates/uc-application/src/facade/settings/models.rs:217-226` —— `SettingsPatch` 顶层完整字段列表 + `derive(Default)` 行；测试 baseline 用 `..Default::default()` 必须验证此 derive 仍存在
    3. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` D-D2（`UpdateNetworkSettings` use case 命名 — Claude's discretion；本 plan 不拆 use case，沿用 `SettingsFacade::update`）
    4. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §3（`models.rs` 模式映射 + Pattern C "View/Patch 镜像 + apply_patch 末段追加"）
    5. `.planning/research/PITFALLS.md` Pitfall 1（DTO ↔ View ↔ core 三层只搬运 `allow_relay_fallback`，不取反）+ Pitfall 5 边界（apply_patch 处理 `network` 时不触发 OTLP / telemetry 联动 —— Pitfall 6）
    6. `src-tauri/crates/uc-core/src/settings/model.rs`（plan 01 产出的 `NetworkSettings` 类型 — 需要 import）
  </read_first>

  <behavior>
    - Test 1: `NetworkSettingsView` 字段对照 `core::NetworkSettings`（`allow_relay_fallback: bool`）
    - Test 2: `NetworkSettingsPatch::default()` 全部 None（Patch "only Some fields update" 语义初值）
    - Test 3: `From<core::Settings> for SettingsView` 把 `core.network.allow_relay_fallback` 透明搬运到 `view.network.allow_relay_fallback`（不取反，业务正向语义）
    - Test 4 (核心硬约束 — NETSET-02 #2): `apply_settings_patch` 当 `patch.network = None` 时，**不抹掉**已有 `existing.network.allow_relay_fallback`（保持原值）
    - Test 5: `apply_settings_patch` 当 `patch.network = Some(NetworkSettingsPatch { allow_relay_fallback: None })` 时，**不抹掉** `existing.network.allow_relay_fallback`（嵌套 None 也保持原值）
    - Test 6: `apply_settings_patch` 当 `patch.network = Some(NetworkSettingsPatch { allow_relay_fallback: Some(false) })` 时，`existing.network.allow_relay_fallback == false`（写入生效）
    - Test 7: `apply_settings_patch` 当 `patch.network = Some(NetworkSettingsPatch { allow_relay_fallback: Some(true) })` 时，`existing.network.allow_relay_fallback == true`（双向覆盖）
  </behavior>

  <action>
在 `src-tauri/crates/uc-application/src/facade/settings/models.rs` 中执行以下精确改动：

**改动点 1（在 line 131 之后，紧跟 `FileSyncSettingsView` 之后）：** 新增 View 镜像。完整代码：

```rust
/// LAN-only Mode 业务字段镜像 — 业务正向语义 `allow_relay_fallback`。
/// UI = "LAN-only Mode = ON" → 此字段 = false。
/// 取反唯一发生在 `uc-bootstrap/src/network_policy.rs`，本层只搬运。
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct NetworkSettingsView {
    pub allow_relay_fallback: bool,
}
```

**改动点 2（在 line 142 的 `pub file_sync: FileSyncSettingsView,` 之后追加一行）：** `SettingsView` 顶层挂载：

```rust
    pub file_sync: FileSyncSettingsView,
    pub network: NetworkSettingsView,
}
```

**改动点 3（在 line 215 之后，紧跟 `FileSyncSettingsPatch` 之后）：** 新增 Patch 镜像（必须 `derive(Default)` —— Patch 全 None 是 "only Some fields update" 的语义起点；与 `FileSyncSettingsPatch` derive 一致；**这与禁止 `NetworkSettings derive Default` 不冲突**：core 层禁 derive 是因为字段语义反转危险，patch 层全 None 永远代表"无操作"，无歧义）：

```rust
/// LAN-only Mode 字段 patch 镜像 — `None` = 不修改。
#[derive(Debug, Clone, Default)]
pub struct NetworkSettingsPatch {
    pub allow_relay_fallback: Option<bool>,
}
```

**改动点 4（在 line 225 的 `pub file_sync: Option<FileSyncSettingsPatch>,` 之后追加一行）：** `SettingsPatch` 顶层挂载：

```rust
    pub file_sync: Option<FileSyncSettingsPatch>,
    pub network: Option<NetworkSettingsPatch>,
}
```

**改动点 5（在 line 442 的 From 映射 `file_sync: FileSyncSettingsView { ... }` 块之后，闭合 `}` 前补一行 `network` 映射）：** 找到（model.rs line 434-442）：

```rust
            file_sync: FileSyncSettingsView {
                file_sync_enabled: value.file_sync.file_sync_enabled,
                small_file_threshold: value.file_sync.small_file_threshold,
                max_file_size: value.file_sync.max_file_size,
                file_cache_quota_per_device: value.file_sync.file_cache_quota_per_device,
                file_retention_hours: value.file_sync.file_retention_hours,
                file_auto_cleanup: value.file_sync.file_auto_cleanup,
            },
        }
    }
}
```

修改为（在 `file_sync: ...` 块闭合 `}` 之后追加 `network: NetworkSettingsView { ... }`）：

```rust
            file_sync: FileSyncSettingsView {
                file_sync_enabled: value.file_sync.file_sync_enabled,
                small_file_threshold: value.file_sync.small_file_threshold,
                max_file_size: value.file_sync.max_file_size,
                file_cache_quota_per_device: value.file_sync.file_cache_quota_per_device,
                file_retention_hours: value.file_sync.file_retention_hours,
                file_auto_cleanup: value.file_sync.file_auto_cleanup,
            },
            network: NetworkSettingsView {
                allow_relay_fallback: value.network.allow_relay_fallback,
            },
        }
    }
}
```

**改动点 6（在 `apply_settings_patch` 函数末尾 `existing` 返回语句之前，紧跟 `file_sync` 块）：** 找到（line 544-563）：

```rust
    if let Some(file_sync) = patch.file_sync {
        if let Some(v) = file_sync.file_sync_enabled {
            existing.file_sync.file_sync_enabled = v;
        }
        // ... 其它字段省略 ...
        if let Some(v) = file_sync.file_auto_cleanup {
            existing.file_sync.file_auto_cleanup = v;
        }
    }

    existing
}
```

在 `existing` 返回语句之前追加：

```rust
    if let Some(network) = patch.network {
        if let Some(v) = network.allow_relay_fallback {
            existing.network.allow_relay_fallback = v;
        }
    }

    existing
}
```

**改动点 7（在 `models.rs` 文件末尾追加 `#[cfg(test)] mod tests` 块；如已存在 tests 模块则在末尾追加测试函数）：** 完整代码：

```rust
#[cfg(test)]
mod network_settings_apply_patch_tests {
    use super::*;
    use uc_core::settings::model::{NetworkSettings, Settings};

    fn baseline_with_network(allow: bool) -> Settings {
        let mut s = Settings::default();
        s.network = NetworkSettings { allow_relay_fallback: allow };
        s
    }

    /// NETSET-02 #2 硬约束：旧客户端 PUT 不带 `network` 段时，
    /// 不抹掉已存在 `network` 字段。
    #[test]
    fn apply_patch_with_no_network_section_keeps_existing() {
        let existing = baseline_with_network(false);
        let patch = SettingsPatch::default(); // patch.network = None
        let result = apply_settings_patch(existing, patch);
        assert!(!result.network.allow_relay_fallback, "patch.network=None must keep existing false");
    }

    /// 嵌套 None — patch.network = Some 但内部字段 None — 仍不抹掉。
    #[test]
    fn apply_patch_with_empty_network_section_keeps_existing() {
        let existing = baseline_with_network(true);
        let patch = SettingsPatch {
            network: Some(NetworkSettingsPatch { allow_relay_fallback: None }),
            ..Default::default()
        };
        let result = apply_settings_patch(existing, patch);
        assert!(result.network.allow_relay_fallback, "inner None must keep existing true");
    }

    /// 显式 Some(false) 写入生效。
    #[test]
    fn apply_patch_with_explicit_false_writes_through() {
        let existing = baseline_with_network(true);
        let patch = SettingsPatch {
            network: Some(NetworkSettingsPatch { allow_relay_fallback: Some(false) }),
            ..Default::default()
        };
        let result = apply_settings_patch(existing, patch);
        assert!(!result.network.allow_relay_fallback);
    }

    /// 显式 Some(true) 写入生效（双向覆盖）。
    #[test]
    fn apply_patch_with_explicit_true_writes_through() {
        let existing = baseline_with_network(false);
        let patch = SettingsPatch {
            network: Some(NetworkSettingsPatch { allow_relay_fallback: Some(true) }),
            ..Default::default()
        };
        let result = apply_settings_patch(existing, patch);
        assert!(result.network.allow_relay_fallback);
    }

    /// From<core::Settings> 透明搬运（不取反）。
    #[test]
    fn from_core_settings_passes_through_business_semantics() {
        let mut s = Settings::default();
        s.network.allow_relay_fallback = false;
        let view: SettingsView = s.into();
        assert!(!view.network.allow_relay_fallback, "view 必须保留业务正向语义，不取反");
    }
}
```

**禁止动作（Pitfall 1 / 6 防御）：**
- ❌ 禁止在 View / Patch 字段命名上做语义反转（`lan_only` / `disable_relay` 全部禁止）
- ❌ 禁止 `apply_settings_patch` 处理 network 段时同步修改 `existing.general.telemetry_enabled`（OTLP 独立由 `general.telemetry_enabled` 控制；`network` 模块不得引用 `general.telemetry_enabled`，Pitfall 6 PR review 必查）
- ❌ 禁止改 `SettingsFacade::update` 公共签名（`facade.rs:35-48`，仍 `(SettingsPatch) -> SettingsView`；`restart_required: bool` 计算下沉到 webserver handler，plan 04）
- ❌ 禁止把 `NetworkSettingsView` / `NetworkSettingsPatch` 标 `#[non_exhaustive]`（与现有兄弟类型保持一致）
  </action>

  <verify>
    <automated>cargo check -p uc-application 2>&amp;1 | tail -10</automated>
    <automated>cargo test -p uc-application --lib facade::settings::models::network_settings_apply_patch_tests -- --nocapture 2>&amp;1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-application` 退出码 0
    - 5 个新测试全部通过：apply_patch_with_no_network_section_keeps_existing / apply_patch_with_empty_network_section_keeps_existing / apply_patch_with_explicit_false_writes_through / apply_patch_with_explicit_true_writes_through / from_core_settings_passes_through_business_semantics
    - `rg -n 'pub struct NetworkSettingsView' src-tauri/crates/uc-application/` 命中 1 处
    - `rg -n 'pub struct NetworkSettingsPatch' src-tauri/crates/uc-application/` 命中 1 处
    - `rg -n 'pub network: NetworkSettingsView' src-tauri/crates/uc-application/src/facade/settings/models.rs` 命中 1 处
    - `rg -n 'pub network: Option<NetworkSettingsPatch>' src-tauri/crates/uc-application/src/facade/settings/models.rs` 命中 1 处
    - `rg -n 'if let Some\(network\) = patch.network' src-tauri/crates/uc-application/src/facade/settings/models.rs` 命中 1 处
    - `rg -n '!.*allow_relay_fallback|allow_relay_fallback.*!' src-tauri/crates/uc-application/` 返回 0 行（**禁止反向写法**，业务正向语义透明流过）
    - `rg -n 'telemetry_enabled' src-tauri/crates/uc-application/src/facade/settings/models.rs | grep -v -i 'general' | grep -v '^.*://' || true`：在新增 network 处理代码上下文 5 行内不引用 telemetry_enabled
    - **【checker WARNING 2】** `rg -n '#\[derive\(.*Default.*\)\][\s\n]+pub struct SettingsPatch\b' src-tauri/crates/uc-application/src/facade/settings/models.rs --multiline` 命中 1 处（`SettingsPatch` 仍保留 derive(Default)；本 plan 测试用 `..Default::default()` 依赖此 derive）
  </acceptance_criteria>

  <done>
View / Patch 镜像 + 顶层挂载 + From 映射 + apply_patch 末段 + 5 个 facade 单元测试全部到位；网络字段独立流动，不与 telemetry 联动；测试覆盖 NETSET-02 #2 的核心硬约束（旧 patch no-op 不抹掉）。
  </done>
</task>

<task type="auto">
  <name>Task 2: facade pub use 白名单追加 NetworkSettingsView / NetworkSettingsPatch（uc-application/AGENTS.md §11.4 强制）</name>
  <files>src-tauri/crates/uc-application/src/facade/settings/mod.rs</files>

  <read_first>
    1. `src-tauri/crates/uc-application/src/facade/settings/mod.rs`（必读完整文件，仅 13 行；line 5-12 是 pub use 白名单）
    2. `src-tauri/crates/uc-application/AGENTS.md` §11.4（外部 crate 访问 uc-application 的唯一合法路径是 `src/facade/`；新增类型必须显式 pub use；不允许下游直接 import `models.rs`；§11.4.7 历史欠账明令"新增 PR 不得引入新越界"）
    3. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §4 + Pattern E（`pub use` 白名单 + alphabetic 顺序）
    4. Task 1 已新增的 `NetworkSettingsView` / `NetworkSettingsPatch` 类型
  </read_first>

  <action>
在 `src-tauri/crates/uc-application/src/facade/settings/mod.rs` 中改动 line 5-12 的 `pub use models::{...}` 列表：

**当前现状：**
```rust
pub use models::{
    ContentTypesPatch, ContentTypesView, FileSyncSettingsPatch, FileSyncSettingsView,
    GeneralSettingsPatch, GeneralSettingsView, PairingSettingsPatch, PairingSettingsView,
    RetentionPolicyPatch, RetentionPolicyView, RetentionRulePatchValue, RetentionRuleView,
    RuleEvaluationView, SecuritySettingsPatch, SecuritySettingsView, SettingsPatch, SettingsView,
    ShortcutKeyView, SyncFrequencyView, SyncSettingsPatch, SyncSettingsView, ThemeView,
    UpdateChannelView,
};
```

**目标状态：** 在 alphabetic 顺序中插入 `NetworkSettingsPatch, NetworkSettingsView,`。`N` 排在 `M`（无）和 `P`（PairingSettings*）之间，所以位置在 `GeneralSettingsView,` 之后、`PairingSettingsPatch,` 之前。改动后：

```rust
pub use models::{
    ContentTypesPatch, ContentTypesView, FileSyncSettingsPatch, FileSyncSettingsView,
    GeneralSettingsPatch, GeneralSettingsView, NetworkSettingsPatch, NetworkSettingsView,
    PairingSettingsPatch, PairingSettingsView, RetentionPolicyPatch, RetentionPolicyView,
    RetentionRulePatchValue, RetentionRuleView, RuleEvaluationView, SecuritySettingsPatch,
    SecuritySettingsView, SettingsPatch, SettingsView, ShortcutKeyView, SyncFrequencyView,
    SyncSettingsPatch, SyncSettingsView, ThemeView, UpdateChannelView,
};
```

**禁止动作（uc-application/AGENTS.md §11.4 红线）：**
- ❌ 禁止在外部 crate 直接 `use uc_application::facade::settings::models::NetworkSettingsView`（必须经 `facade::settings::NetworkSettingsView`）
- ❌ 禁止把 `NetworkSettingsView` / `NetworkSettingsPatch` 暴露在 `mod.rs` 之外的位置
- ❌ 禁止修改 `pub use facade::{SettingsFacade, SettingsFacadeError}` 那一行（与本 plan 无关）
  </action>

  <verify>
    <automated>cargo check -p uc-application 2>&amp;1 | tail -5</automated>
    <automated>cargo check -p uc-webserver 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-application` 退出码 0
    - `cargo check -p uc-webserver` 退出码 0（webserver 已通过 facade re-export 访问 settings 类型，本 plan 不该 break）
    - `rg -n 'NetworkSettingsPatch, NetworkSettingsView' src-tauri/crates/uc-application/src/facade/settings/mod.rs` 命中 1 处
    - `rg -n '^pub use models' src-tauri/crates/uc-application/src/facade/settings/mod.rs` 命中 1 处（且块内含 NetworkSettings*）
    - `rg -n 'use uc_application::facade::settings::models::' src-tauri/ --type rust` 返回 0 行（无外部 crate 越界 import models）
  </acceptance_criteria>

  <done>
facade `pub use` 白名单已合规扩展，外部消费者（plan 04 webserver）可以通过 `uc_application::facade::settings::{NetworkSettingsView, NetworkSettingsPatch}` 访问。`uc-application` / `uc-webserver` 编译通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP `PUT /settings` 入口（webserver, plan 04）→ uc-application::SettingsFacade::update | 客户端提交的 patch 跨"表示层 → 应用层"边界 |
| 外部 crate import → uc-application 公共 API | uc-application/AGENTS.md §11.4 边界：外部只能从 `facade/` 入；models.rs 内部类型不对外暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094.02-01 | Tampering | 恶意 patch.network 字段试图修改其它字段（如 telemetry） | mitigate | `apply_settings_patch` 严格按 namespace 隔离处理（`if let Some(network) = patch.network` 只触达 `existing.network.*`，不跨 namespace 写）；测试 `from_core_settings_passes_through_business_semantics` 间接验证 |
| T-094.02-02 | Information Disclosure | View 字段名 `allow_relay_fallback` 暴露给前端 | accept | 业务正向语义无 PII；前端 store 字段名 `allowRelayFallback`（plan 03 DTO 走 camelCase 自动转换） |
| T-094.02-03 | Denial of Service | 旧客户端 PUT 不带 `network` 段被错误抹掉值 | mitigate | `apply_patch_with_no_network_section_keeps_existing` 单元测试自动断言；`apply_settings_patch` 严格 "only Some fields update" 语义 |
| T-094.02-04 | Elevation of Privilege | facade 越界 import（外部 crate 直接 import models） | mitigate | uc-application/AGENTS.md §11.4 红线 + grep 守护 `rg 'use uc_application::facade::settings::models::'` 返回 0 |
| T-094.02-05 | Spoofing | DTO 层（plan 03）或 webserver（plan 04）反转字段语义 | mitigate | facade 层只搬运正向语义（无 `!`）；plan 03 / 04 / 05 沿用同模式；plan 05 truth-table 单测兜底 |
| T-094.02-06 | Repudiation | apply_patch 写入未留痕 | accept | `SettingsFacade::update` 已有 `#[instrument(skip_all)]`（facade.rs:26, 35）；本 plan 不重复 |

无 `high` 严重等级威胁。
</threat_model>

<verification>
- `cargo check -p uc-application` 通过
- `cargo check -p uc-webserver` 通过（确认 facade re-export 路径打通，下游 plan 04 可消费）
- `cargo test -p uc-application --lib facade::settings` 全部通过（含本 plan 5 个新测试）
- grep 守护：
  - `rg -n 'pub struct NetworkSettings(View|Patch)' src-tauri/crates/uc-application/` 共 2 处（View / Patch 各 1）
  - `rg -n 'use uc_application::facade::settings::models::' src-tauri/` 返回 0
  - `rg -n '!\s*(?:settings|patch|view)\.network\.allow_relay_fallback' src-tauri/crates/uc-application/` 返回 0（无反向写法）
- application crate 无 `restart_required` 字段（保留至 plan 03 DTO + plan 04 handler）
</verification>

<success_criteria>
- [ ] `NetworkSettingsView { allow_relay_fallback: bool }` 已镜像，与 `FileSyncSettingsView` 同 derive
- [ ] `NetworkSettingsPatch { allow_relay_fallback: Option<bool> }` 已镜像，含 `derive(Default)`
- [ ] `SettingsView.network: NetworkSettingsView` + `SettingsPatch.network: Option<NetworkSettingsPatch>` 顶层挂载
- [ ] `From<core::Settings> for SettingsView` 末段补 `network` 映射，业务正向语义透明流过（无 `!`）
- [ ] `apply_settings_patch` 末段补 `network` 处理，遵循 "only Some fields update"
- [ ] facade `pub use` 白名单含 `NetworkSettingsView, NetworkSettingsPatch`
- [ ] `SettingsFacade::update` 公共签名**未动**（仍 `(SettingsPatch) -> SettingsView`；`restart_required` 计算下沉到 webserver handler 见 plan 04）
- [ ] 5 个 apply_patch 单元测试覆盖 None / 嵌套 None / Some(true) / Some(false) / From 映射场景
- [ ] `cargo check -p uc-application` + `cargo check -p uc-webserver` 全过
- [ ] **决策 D-D2 实施路径已写入 must_haves.truths：facade 公共签名不变，restart_required 留给 webserver**
</success_criteria>

<output>
After completion, create `.planning/phases/094-backend-network-allow-relay-fallback/094-02-SUMMARY.md` with:
- 实际改动的 LOC（models.rs 6 处插入 + mod.rs 1 处行追加）
- 5 个 apply_patch 单元测试 pass 输出
- 是否有任何对 `SettingsFacade::update` 公共签名的改动（应为 0）
- 给下游 plan 03（daemon-contract）/ plan 04（webserver）的"NetworkSettings(View|Patch) 已 re-export"信号
- Pitfall 6 自查证据：apply_patch network 段处理无 telemetry_enabled / OTLP 引用
</output>
