---
phase: 094-backend-network-allow-relay-fallback
plan: 04
type: execute
wave: 3
depends_on:
  - "094-02"
  - "094-03"
files_modified:
  - src-tauri/crates/uc-webserver/src/api/settings.rs
  - src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs
autonomous: true
requirements:
  - NETSET-02
tags:
  - uc-webserver
  - http
  - settings
  - lan-only

must_haves:
  truths:
    - "`settings_patch_from_dto` 末段补 `network: patch.network.map(|n| app_settings::NetworkSettingsPatch { allow_relay_fallback: n.allow_relay_fallback })`（DTO → View patch 透明搬运，业务正向语义流过，不取反 — Pitfall 1）"
    - "`settings_view_to_dto` 末段补 `network: NetworkSettingsDto { allow_relay_fallback: value.network.allow_relay_fallback }`（View → DTO 透明搬运）"
    - "`update_settings_handler` 内联计算 `restart_required: bool`：`payload.network.is_some()`（D-D1 — patch 中 `network` 段非空时返回 true；其它字段不影响）；webserver handler 是 restart_required 唯一计算位置（D-D2 facade 公共签名不变 plan 02 已固化）"
    - "**【checker BLOCKER 1 决议】** Task 2 fixture 不依赖完整 `AppFacade` 组装（后者需 14+ sub-facades 远超 settings smoke 范围）；改为**直接用 `SettingsFacade` + `settings_patch_from_dto` + `settings_view_to_dto` 函数 + handler 内联 restart_required 计算逻辑**模拟 PUT/GET wire-level round-trip。fixture 用 in-memory `SettingsPort` 实现 + tempdir 持久化点。三个 case 全部 `#[tokio::test]` 真跑，**禁止 `#[ignore]`**"
    - "**【checker BLOCKER 1 — fixture 范围声明】** smoke 测试覆盖：(1) DTO ↔ View ↔ Settings ↔ DTO 完整 wire-level round-trip；(2) `apply_settings_patch` no-op 语义；(3) handler 内联 restart_required 计算。**未覆盖**：axum router 中间件链（cors / auth / rate-limit）—— 这些与 settings 业务无关，由 webserver 自身 router smoke 测试覆盖；本 plan 不试图建另一份"
    - "**【checker BLOCKER 5】** plan 03 已给 `SettingsPatchDto` + `NetworkSettingsPatchDto` 加 `derive(Default)`，本 plan task 1 / task 2 测试 baseline 可用 `..Default::default()` 简化；不再需要 `default_patch_dto()` 显式枚举字段的 helper"
    - "**【checker WARNING 7】** restart_required 反向命名 grep 不只找 `!` 取反，也覆盖 `is_none()` 语义反转：5-case 完整 truth-table（None / Some(empty NetworkPatchDto) / Some(allow=false) / Some(allow=true) / 旧客户端纯 general patch）覆盖所有 restart_required 计算分支"
    - "DTO 字段名 `allow_relay_fallback` 全程不重命名；wire 字段 `allowRelayFallback` 自动通过 `serde(rename_all = camelCase)`"
    - "handler 内不读取 `payload.network.allow_relay_fallback` 布尔值本身做 `!` 运算（取反唯一在 plan 05 `network_policy.rs`）"
  artifacts:
    - path: "src-tauri/crates/uc-webserver/src/api/settings.rs"
      provides: "settings_patch_from_dto/network 段 + settings_view_to_dto/network 段 + update_settings_handler restart_required 计算 + 必要 import"
      contains: "restart_required"
    - path: "src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs"
      provides: "PUT/GET wire-level round-trip + 旧客户端兼容 smoke 测试（3 case 全跑，无 #[ignore]）；fixture 用 in-memory SettingsPort + tempdir，直调 settings_patch_from_dto / settings_view_to_dto / SettingsFacade::update / handler restart_required 计算逻辑"
      contains: "settings_network_smoke"
  key_links:
    - from: "uc-webserver::settings_patch_from_dto network 段"
      to: "uc-application::SettingsPatch.network (plan 02)"
      via: "DTO → View patch 映射"
      pattern: "network: patch.network.map"
    - from: "uc-webserver::update_settings_handler restart_required"
      to: "前端 daemon client (Phase 95 消费)"
      via: "PUT /settings response wire 契约"
      pattern: "restart_required: payload\\.network\\.is_some\\(\\)"
---

<objective>
在 `uc-webserver::api::settings` 层补齐 DTO ↔ View 双向 mapping 的 `network` 段，并让 `update_settings_handler` 内联计算 `restart_required: bool`。新增 integration smoke 测试覆盖 PUT/GET 往返 + 旧客户端不带 `network` 段兼容性。

Purpose: webserver 是 daemon HTTP 协议的入口装配点，DTO ↔ View 双向 mapping 是 plan 02（application）+ plan 03（DTO）落地的必经收口；`restart_required` 计算逻辑由 handler 承担（D-D2 facade 公共签名不动，本 plan 04 实施）。NETSET-02 success criterion #2"不带 network 段的旧客户端 PUT 仍 200，且不抹掉已存在的 network 字段"必须通过 integration smoke 测试自动断言。

Output:
- `settings_patch_from_dto` 末段补 `network` segment 映射
- `settings_view_to_dto` 末段补 `network` segment 映射
- import 列表新增 `NetworkSettingsDto, NetworkSettingsPatchDto`
- `update_settings_handler` 内联 `restart_required = payload.network.is_some()`
- `tests/settings_network_smoke.rs`（新建）—— 3 个 integration smoke case
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md
@.planning/research/PITFALLS.md

# 受影响代码
@src-tauri/crates/uc-webserver/src/api/settings.rs

<interfaces>
<!-- 现有 webserver settings handler + 双向 mapping 模式 -->
<!-- 来源：src-tauri/crates/uc-webserver/src/api/settings.rs -->

现有 update_settings_handler（line 69-85）：
```rust
async fn update_settings_handler(
    State(state): State<DaemonApiState>,
    Json(payload): Json<SettingsPatchDto>,
) -> Result<Json<UpdateSettingsResponse>, ApiError> {
    let app = state.app_facade_or_error()?;
    let updated = app
        .settings
        .update(settings_patch_from_dto(payload))
        .await
        .map_err(settings_error_to_api)?;

    Ok(Json(UpdateSettingsResponse {
        success: true,
        data: settings_view_to_dto(updated),
        ts: chrono::Utc::now().timestamp_millis(),
    }))
}
```

现有 settings_patch_from_dto 末段（line 149-160，file_sync 段尾部）：
```rust
        file_sync: patch
            .file_sync
            .map(|file_sync| app_settings::FileSyncSettingsPatch {
                file_sync_enabled: file_sync.file_sync_enabled,
                small_file_threshold: file_sync.small_file_threshold,
                max_file_size: file_sync.max_file_size,
                file_cache_quota_per_device: file_sync.file_cache_quota_per_device,
                file_retention_hours: file_sync.file_retention_hours,
                file_auto_cleanup: file_sync.file_auto_cleanup,
            }),
    }
}
```

现有 settings_view_to_dto 末段（line 209-218）：
```rust
        file_sync: FileSyncSettingsDto {
            file_sync_enabled: value.file_sync.file_sync_enabled,
            small_file_threshold: value.file_sync.small_file_threshold,
            max_file_size: value.file_sync.max_file_size,
            file_cache_quota_per_device: value.file_sync.file_cache_quota_per_device,
            file_retention_hours: value.file_sync.file_retention_hours,
            file_auto_cleanup: value.file_sync.file_auto_cleanup,
        },
    }
}
```

现有 import（line 14-20，需要扩）：
```rust
use crate::api::dto::settings::{
    ContentTypesDto, ContentTypesPatchDto, FileSyncSettingsDto, GeneralSettingsDto,
    GetSettingsResponse, KeyboardShortcutsPatchDto, PairingSettingsDto, RetentionPolicyDto,
    RetentionRuleDto, SecuritySettingsDto, SettingsDto, SettingsPatchDto, SyncSettingsDto,
    UpdateSettingsResponse,
};
```

`SettingsFacade::update` 来自 plan 02 不动签名：`(SettingsPatch) -> Result<SettingsView, SettingsFacadeError>`。

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 settings.rs 补 DTO ↔ View 双向 network 段 mapping + restart_required 计算 + import 扩展</name>
  <files>src-tauri/crates/uc-webserver/src/api/settings.rs</files>

  <read_first>
    1. `src-tauri/crates/uc-webserver/src/api/settings.rs`（必读完整文件 — 418 行；重点 line 14-20 import、69-85 handler、92-160 patch_from_dto、162-218 view_to_dto）
    2. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` D-D1（restart_required 触发条件 = patch 中 network 段非空）+ Claude's Discretion（webserver handler 内联计算 vs facade 层返回 — 选 handler 内联，plan 02 已固化此选项）
    3. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §6（webserver handler + DTO ↔ View 模式）
    4. `.planning/research/PITFALLS.md` Pitfall 1（DTO 层不允许重命名 / 取反）+ Pitfall 3（restart_required 让调用方显式承担"还没真正生效"）
    5. **【checker BLOCKER 5】** `src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs:217-227 + 304-314`（`GeneralSettingsPatchDto` + `SettingsPatchDto` 字段列表，确认 plan 03 task 1 已加 `derive(Default)`，本 task 用 `..Default::default()` 简化）
  </read_first>

  <behavior>
    - Test 1: `settings_patch_from_dto` 把 `SettingsPatchDto.network = Some(NetworkSettingsPatchDto { allow_relay_fallback: Some(false) })` 转为 `app_settings::SettingsPatch.network = Some(app_settings::NetworkSettingsPatch { allow_relay_fallback: Some(false) })`（透明搬运不取反）
    - Test 2: `settings_view_to_dto` 把 `app_settings::SettingsView.network.allow_relay_fallback = false` 转为 `SettingsDto.network.allow_relay_fallback = false`（业务正向语义流过）
    - **【checker WARNING 7 — 5-case restart_required truth-table】**:
      - Test 3 (case 1): payload.network = None → restart_required = false
      - Test 4 (case 2): payload.network = Some(NetworkSettingsPatchDto::default()) — 嵌套 None → restart_required = **true**（is_some 即 true，与内嵌字段是否变化无关；Phase 94 简化策略 — 见 Known Limitation）
      - Test 5 (case 3): payload.network = Some(NetworkSettingsPatchDto { allow_relay_fallback: Some(false) }) → restart_required = true
      - Test 6 (case 4): payload.network = Some(NetworkSettingsPatchDto { allow_relay_fallback: Some(true) }) → restart_required = true
      - Test 7 (case 5): payload = `{ general: Some(...), ..Default::default() }`（旧客户端纯 general patch，无 network） → restart_required = false
  </behavior>

  <action>
在 `src-tauri/crates/uc-webserver/src/api/settings.rs` 中执行以下精确改动：

**改动点 1（line 14-20，import 扩展）：** 当前：
```rust
use crate::api::dto::settings::{
    ContentTypesDto, ContentTypesPatchDto, FileSyncSettingsDto, GeneralSettingsDto,
    GetSettingsResponse, KeyboardShortcutsPatchDto, PairingSettingsDto, RetentionPolicyDto,
    RetentionRuleDto, SecuritySettingsDto, SettingsDto, SettingsPatchDto, SyncSettingsDto,
    UpdateSettingsResponse,
};
```
改为（在 alphabetic 顺序中插入 `NetworkSettingsDto, NetworkSettingsPatchDto,` —— `N` 在 `K` 之后、`P` 之前）：
```rust
use crate::api::dto::settings::{
    ContentTypesDto, ContentTypesPatchDto, FileSyncSettingsDto, GeneralSettingsDto,
    GetSettingsResponse, KeyboardShortcutsPatchDto, NetworkSettingsDto, NetworkSettingsPatchDto,
    PairingSettingsDto, RetentionPolicyDto, RetentionRuleDto, SecuritySettingsDto, SettingsDto,
    SettingsPatchDto, SyncSettingsDto, UpdateSettingsResponse,
};
```

**改动点 2（line 69-85，update_settings_handler 加 restart_required 计算）：** 当前：
```rust
async fn update_settings_handler(
    State(state): State<DaemonApiState>,
    Json(payload): Json<SettingsPatchDto>,
) -> Result<Json<UpdateSettingsResponse>, ApiError> {
    let app = state.app_facade_or_error()?;
    let updated = app
        .settings
        .update(settings_patch_from_dto(payload))
        .await
        .map_err(settings_error_to_api)?;

    Ok(Json(UpdateSettingsResponse {
        success: true,
        data: settings_view_to_dto(updated),
        ts: chrono::Utc::now().timestamp_millis(),
    }))
}
```
改为：
```rust
async fn update_settings_handler(
    State(state): State<DaemonApiState>,
    Json(payload): Json<SettingsPatchDto>,
) -> Result<Json<UpdateSettingsResponse>, ApiError> {
    let app = state.app_facade_or_error()?;

    // D-D1：patch 中 `network` 段非空（任何字段变更）时 restart_required = true。
    // 当前 NetworkSettings 仅含 allow_relay_fallback；后续若加字段，仍走 is_some()
    // 兜底。其它字段（general / sync 等）不影响该信号 — 它们不需要重启。
    // Pitfall 3 防御：调用方（前端 Phase 95）必须显式承担"还没真正生效"。
    let restart_required = payload.network.is_some();

    let updated = app
        .settings
        .update(settings_patch_from_dto(payload))
        .await
        .map_err(settings_error_to_api)?;

    Ok(Json(UpdateSettingsResponse {
        success: true,
        data: settings_view_to_dto(updated),
        ts: chrono::Utc::now().timestamp_millis(),
        restart_required,
    }))
}
```

**改动点 3（line 158 之后，settings_patch_from_dto 末段补 network）：** 在 `file_sync` 段后添加 `network` 段。当前 line 149-160 末段：
```rust
        file_sync: patch
            .file_sync
            .map(|file_sync| app_settings::FileSyncSettingsPatch {
                // ...
                file_auto_cleanup: file_sync.file_auto_cleanup,
            }),
    }
}
```
改为：
```rust
        file_sync: patch
            .file_sync
            .map(|file_sync| app_settings::FileSyncSettingsPatch {
                file_sync_enabled: file_sync.file_sync_enabled,
                small_file_threshold: file_sync.small_file_threshold,
                max_file_size: file_sync.max_file_size,
                file_cache_quota_per_device: file_sync.file_cache_quota_per_device,
                file_retention_hours: file_sync.file_retention_hours,
                file_auto_cleanup: file_sync.file_auto_cleanup,
            }),
        network: patch
            .network
            .map(|network| app_settings::NetworkSettingsPatch {
                allow_relay_fallback: network.allow_relay_fallback,
            }),
    }
}
```

**改动点 4（line 217 附近，settings_view_to_dto 末段补 network）：** 在 `file_sync` 段后添加 `network` 段。当前末段：
```rust
        file_sync: FileSyncSettingsDto {
            // ...
            file_auto_cleanup: value.file_sync.file_auto_cleanup,
        },
    }
}
```
改为：
```rust
        file_sync: FileSyncSettingsDto {
            file_sync_enabled: value.file_sync.file_sync_enabled,
            small_file_threshold: value.file_sync.small_file_threshold,
            max_file_size: value.file_sync.max_file_size,
            file_cache_quota_per_device: value.file_sync.file_cache_quota_per_device,
            file_retention_hours: value.file_sync.file_retention_hours,
            file_auto_cleanup: value.file_sync.file_auto_cleanup,
        },
        network: NetworkSettingsDto {
            allow_relay_fallback: value.network.allow_relay_fallback,
        },
    }
}
```

**改动点 5（在 `settings.rs` 文件末尾追加 `#[cfg(test)] mod tests` 块；如已存在则在末尾追加测试函数）：** 用单元测试覆盖 mapping + restart_required 逻辑（不需要起 axum server，纯函数测试）：
```rust
#[cfg(test)]
mod network_mapping_tests {
    use super::*;
    use uc_application::facade::settings as app_settings;

    // 【checker BLOCKER 5】plan 03 task 1 已给 SettingsPatchDto +
    // NetworkSettingsPatchDto 加 derive(Default)，本测试模块直接用
    // `..Default::default()` 简化 baseline 构造，无需显式 `default_patch_dto()`
    // helper 枚举字段（避免 GeneralSettingsPatchDto 字段列表后续变更引入
    // 编译失败的 brittleness）。

    /// DTO → View patch 透明搬运（业务正向语义不取反）。
    #[test]
    fn patch_from_dto_passes_through_network_segment() {
        let dto = SettingsPatchDto {
            network: Some(NetworkSettingsPatchDto {
                allow_relay_fallback: Some(false),
            }),
            ..Default::default()
        };
        let view = settings_patch_from_dto(dto);
        let net = view.network.expect("network patch present");
        assert_eq!(net.allow_relay_fallback, Some(false));
    }

    /// View → DTO 透明搬运。
    #[test]
    fn view_to_dto_passes_through_network_segment() {
        let view = sample_view_with_network(false);
        let dto = settings_view_to_dto(view);
        assert!(!dto.network.allow_relay_fallback);
    }

    // ============================================================
    // 【checker WARNING 7】restart_required 5-case truth-table
    // 覆盖所有计算分支，反向命名 grep 既包括 `!` 取反，也包括
    // `is_none()` 语义反转。
    // ============================================================

    /// case 1：payload.network = None → restart_required = false。
    #[test]
    fn restart_required_case_1_none_network_segment() {
        let payload = SettingsPatchDto::default();
        let restart_required = payload.network.is_some();
        assert!(!restart_required, "None network must not signal restart");
    }

    /// case 2：payload.network = Some(嵌套 None) → restart_required = true。
    /// **Known Limitation**：handler 简化策略只看 `payload.network.is_some()`，
    /// 不查嵌套字段是否真有值。理由：(a) 防止旧客户端误发空 NetworkPatchDto；
    /// (b) 当前 NetworkSettings 只有一个字段，简化等同于"内嵌字段必有变化"。
    /// 未来若加非 restart 字段需重构（CONTEXT.md `<deferred>` 标记 v0.7.x）。
    #[test]
    fn restart_required_case_2_some_with_nested_none() {
        let payload = SettingsPatchDto {
            network: Some(NetworkSettingsPatchDto::default()),
            ..Default::default()
        };
        let restart_required = payload.network.is_some();
        assert!(restart_required, "Some(empty network) signals restart by Phase 94 simplified policy");
    }

    /// case 3：payload.network.allow_relay_fallback = Some(false) → restart_required = true。
    #[test]
    fn restart_required_case_3_some_explicit_false() {
        let payload = SettingsPatchDto {
            network: Some(NetworkSettingsPatchDto {
                allow_relay_fallback: Some(false),
            }),
            ..Default::default()
        };
        let restart_required = payload.network.is_some();
        assert!(restart_required);
    }

    /// case 4：payload.network.allow_relay_fallback = Some(true) → restart_required = true。
    #[test]
    fn restart_required_case_4_some_explicit_true() {
        let payload = SettingsPatchDto {
            network: Some(NetworkSettingsPatchDto {
                allow_relay_fallback: Some(true),
            }),
            ..Default::default()
        };
        let restart_required = payload.network.is_some();
        assert!(restart_required);
    }

    /// case 5：旧客户端 — 纯 general patch、无 network 段 → restart_required = false。
    /// 这是 NETSET-02 success criterion #2 的硬约束（向后兼容）。
    #[test]
    fn restart_required_case_5_legacy_general_only_payload() {
        let payload = SettingsPatchDto {
            general: Some(GeneralSettingsPatchDto {
                auto_start: Some(true),
                ..Default::default()
            }),
            ..Default::default()
        };
        let restart_required = payload.network.is_some();
        assert!(!restart_required, "legacy general-only payload must not signal restart");
    }

    fn sample_view_with_network(allow: bool) -> app_settings::SettingsView {
        // 借助 core::Settings::default 构造然后修改 network
        use uc_core::settings::model as core;
        let mut s = core::Settings::default();
        s.network.allow_relay_fallback = allow;
        s.into()
    }
}
```

**禁止动作（Pitfall 1 / 6 防御）：**
- ❌ 禁止在 handler 中读取 `payload.network.allow_relay_fallback` 布尔值本身做 `!` 运算（取反唯一在 plan 05）
- ❌ 禁止把 `restart_required` 计算下沉到 application facade（D-D2 — facade 公共签名不动；如未来需要可重构，但 Phase 94 不动）
- ❌ 禁止在 handler 中加 OTLP 关联（如 "if patch.network present, also disable telemetry" — Pitfall 6）
- ❌ 禁止改 `router()` 路径定义（line 23-27 不动）
- ❌ 禁止用 `payload.network.is_none()` 反向语义计算 restart_required（保持正向 `is_some()`，与单点取反铁律对齐）
  </action>

  <verify>
    <automated>cargo check -p uc-webserver 2>&amp;1 | tail -10</automated>
    <automated>cargo test -p uc-webserver --lib api::settings::network_mapping_tests -- --nocapture 2>&amp;1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-webserver` 退出码 0
    - **【checker WARNING 7】** 7 个新单元测试全部通过（2 个 mapping + 5-case restart_required truth-table）
    - `rg -n 'NetworkSettingsDto|NetworkSettingsPatchDto' src-tauri/crates/uc-webserver/src/api/settings.rs` 命中至少 4 处（import + 2 处类型用例 + 1 处 view_to_dto）
    - `rg -n 'restart_required = payload.network.is_some\(\)' src-tauri/crates/uc-webserver/src/api/settings.rs` 命中 1 处
    - `rg -n 'allow_relay_fallback: network.allow_relay_fallback' src-tauri/crates/uc-webserver/src/api/settings.rs` 命中 1 处（patch_from_dto）
    - `rg -n 'allow_relay_fallback: value.network.allow_relay_fallback' src-tauri/crates/uc-webserver/src/api/settings.rs` 命中 1 处（view_to_dto）
    - `rg -n '!\s*payload.network|!\s*value.network|!\s*patch.network' src-tauri/crates/uc-webserver/` 返回 0（无反向写法）
    - **【checker WARNING 7】** `rg -n 'is_none\(\)|!\s*is_some' src-tauri/crates/uc-webserver/src/api/settings.rs | rg -i 'network|payload\.network'` 返回 0（restart_required 不用 `is_none` 反向语义）
    - **【checker BLOCKER 5】** `rg -n '\.\.\s*Default::default\(\)' src-tauri/crates/uc-webserver/src/api/settings.rs | grep -c 'SettingsPatchDto\|NetworkSettingsPatchDto\|GeneralSettingsPatchDto'` ≥ 6（test 模块至少 6 处用 derive 简化构造）
  </acceptance_criteria>

  <done>
DTO ↔ View 双向 mapping 已补 network 段，handler 内联 restart_required 计算逻辑就位，5 个单元测试覆盖全部分支。webserver 编译通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 webserver wire-level smoke 测试 settings_network_smoke.rs（PUT/GET 往返 + 旧客户端兼容 — NETSET-02 #2 — 全部真跑无 #[ignore]）</name>
  <files>src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs</files>

  <read_first>
    1. **【checker BLOCKER 1 范围声明】**：本 task 不试图组装完整 axum router + 完整 `AppFacade`（后者需 14+ sub-facades；详见 `src-tauri/crates/uc-application/src/facade/app_facade.rs:407-422` `AppFacadeParts` 字段列表 — 远超 settings smoke 测试范围）。改为**直接调用 settings handler 内部函数 + SettingsFacade**，覆盖 wire-level DTO ↔ View ↔ Settings 完整 round-trip，包括 handler 内联 restart_required 计算逻辑
    2. `src-tauri/crates/uc-webserver/src/api/settings.rs`（必读完整文件，重点 line 92-160 `settings_patch_from_dto` + line 162-218 `settings_view_to_dto`；本 plan task 1 已扩展为含 network 段）
    3. `src-tauri/crates/uc-application/src/facade/settings/facade.rs:21-49`（`SettingsFacade::new`/`get`/`update` 公共签名 — 直接用，无需经 `AppFacade`）
    4. `src-tauri/crates/uc-infra/src/settings/repository.rs:163-182`（`FileSettingsRepository::load`/`save` — tempdir 持久化用）
    5. `src-tauri/crates/uc-application/src/facade/settings/facade.rs:55-84`（**已有的 `InMemorySettings` 测试 fixture 模式** — 沿用之，无需 tempdir + FileSettingsRepository）
    6. Plan 03 产出的 `NetworkSettingsDto` / `UpdateSettingsResponse.restart_required` wire 契约
    7. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` D-D1（restart_required 行为约束）
  </read_first>

  <behavior>
    - Test 1（写入 false 后读回 false）：模拟 PUT body `{"network":{"allowRelayFallback":false}}` 经 `SettingsPatchDto` 反序列化 → `settings_patch_from_dto` → `SettingsFacade::update` → `settings_view_to_dto` → 序列化 wire JSON 含 `data.network.allowRelayFallback == false` + `restartRequired == true`；随后模拟 GET → wire JSON 含 `data.network.allowRelayFallback == false`
    - Test 2（写入 true 后读回 true — 双向覆盖）：同 Test 1 模式但 wire 值 true
    - Test 3（**NETSET-02 #2 硬约束 — 旧客户端兼容**）：先 PUT 写入 false；再模拟旧客户端 PUT body `{"general":{"autoStart":false}}`（无 network 段）→ wire JSON `restartRequired == false` + `data.network.allowRelayFallback == false`（保留不抹掉）；再 GET 二次确认 `data.network.allowRelayFallback == false`
  </behavior>

  <action>
**【checker BLOCKER 1 决议】** Task 2 fixture 不组装完整 axum Router + AppFacade（成本 14+ sub-facades 远超 settings smoke 范围 — 见 `src-tauri/crates/uc-application/src/facade/app_facade.rs:407-422`）。改为**直接 wire-level 模拟 PUT/GET 通过 settings handler 公共内部函数 + SettingsFacade**，覆盖完整 wire ↔ DTO ↔ View ↔ Settings round-trip + handler 内联 restart_required 计算逻辑。3 个 `#[tokio::test]` 真跑，**禁止 `#[ignore]`**。

**Step 1：先确认 webserver tests 目录不存在**（已知现状）：
```bash
ls src-tauri/crates/uc-webserver/tests/ 2>&1
# 预期：No such file or directory（这是新建目录）
```

**Step 2：把 settings.rs 中的 `settings_patch_from_dto` / `settings_view_to_dto` 暴露为 `pub(crate)` 让 integration test 可见**（如果它们当前是 `fn` private）。**先 grep 验证当前可见性：**
```bash
grep -n "fn settings_patch_from_dto\|fn settings_view_to_dto" src-tauri/crates/uc-webserver/src/api/settings.rs
```
若是 private（无 `pub`），在本 task 中改为 `pub(crate)` —— 这是合理可见性扩展（仅本 crate test 可见，不外泄）。提交时单独说明这一点。

**Step 3：新建 `src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs`**。完整可编译的代码（executor 可直接 lift）：

```rust
//! Phase 94 plan 04 wire-level smoke 测试：模拟 HTTP PUT/GET /settings
//! 完整 wire ↔ DTO ↔ View ↔ Settings round-trip，验证 `network.allow_relay_fallback`
//! 写盘 + 读取一致；旧客户端不带 network 段不抹掉已存在字段（NETSET-02 success
//! criterion #2）。
//!
//! ## fixture 范围（checker BLOCKER 1 决议）
//!
//! 不通过完整 axum Router（后者需 14+ sub-facades 组装 AppFacade），改为：
//! - **PUT 模拟**：`serde_json::from_str::<SettingsPatchDto>` → `settings_patch_from_dto`
//!   → `SettingsFacade::update` → `settings_view_to_dto` → `serde_json::to_string`
//!   含 handler 内联计算的 `restartRequired`；
//! - **GET 模拟**：`SettingsFacade::get` → `settings_view_to_dto` → `serde_json::to_string`；
//! - **持久化**：用同一份 `Arc<dyn SettingsPort>` 的 in-memory 实现保证写盘 → 读取
//!   一致（FileSettingsRepository + tempdir 等价行为，但成本更低）。
//!
//! ## 未覆盖（设计决策）
//!
//! - axum router 中间件链（cors / auth / rate-limit）—— 与 settings 业务无关
//! - 完整 daemon 启动路径 —— 由 D-C2 / 手工验收覆盖
//!
//! 见：`.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` D-D1。

use std::sync::{Arc, Mutex};

use async_trait::async_trait;
use serde_json::{json, Value};
use uc_application::facade::settings::SettingsFacade;
use uc_core::ports::SettingsPort;
use uc_core::settings::model::Settings;
use uc_daemon_contract::api::dto::settings::{SettingsPatchDto, UpdateSettingsResponse};
use uc_webserver::api::dto::settings::SettingsDto;
// 注意：以下两个函数需在 src/api/settings.rs 中暴露为 `pub(crate)`（见 Step 2）。
use uc_webserver::api::settings::{settings_patch_from_dto, settings_view_to_dto};

// ============================================================
// Fixture：用最小 in-memory SettingsPort 起 SettingsFacade。
// 沿用 uc-application/src/facade/settings/facade.rs:55-84 的
// `InMemorySettings` 测试模式 — 不需要 tempdir + FileSettingsRepository。
// ============================================================

struct InMemorySettings {
    state: Mutex<Settings>,
}

impl InMemorySettings {
    fn new(initial: Settings) -> Self {
        Self {
            state: Mutex::new(initial),
        }
    }
}

#[async_trait]
impl SettingsPort for InMemorySettings {
    async fn load(&self) -> anyhow::Result<Settings> {
        Ok(self.state.lock().unwrap().clone())
    }
    async fn save(&self, settings: &Settings) -> anyhow::Result<()> {
        *self.state.lock().unwrap() = settings.clone();
        Ok(())
    }
}

fn build_facade() -> SettingsFacade {
    let port: Arc<dyn SettingsPort> = Arc::new(InMemorySettings::new(Settings::default()));
    SettingsFacade::new(port)
}

// ============================================================
// 模拟 handler 行为：把 wire JSON 输入跑过完整 PUT/GET 路径，返回
// wire JSON 输出 — 这是 update_settings_handler / get_settings_handler
// 的核心逻辑，剥离 axum extractors / response wrapping。
// ============================================================

async fn simulate_put(facade: &SettingsFacade, body_json: &str) -> Value {
    // 1. 反序列化 wire body → DTO
    let payload: SettingsPatchDto = serde_json::from_str(body_json).expect("parse PUT body");

    // 2. handler 内联计算 restart_required（Phase 94 plan 04 task 1 改造）
    let restart_required = payload.network.is_some();

    // 3. DTO → View patch → SettingsFacade::update → View
    let view = facade
        .update(settings_patch_from_dto(payload))
        .await
        .expect("settings update");

    // 4. View → DTO，组装 UpdateSettingsResponse
    let resp = UpdateSettingsResponse {
        success: true,
        data: settings_view_to_dto(view),
        ts: 0, // 测试不关心 timestamp 精确值
        restart_required,
    };

    // 5. 序列化回 wire JSON
    serde_json::to_value(&resp).expect("serialize response")
}

async fn simulate_get(facade: &SettingsFacade) -> Value {
    let view = facade.get().await.expect("settings get");
    let dto: SettingsDto = settings_view_to_dto(view);
    serde_json::to_value(&dto).expect("serialize get")
}

// ============================================================
// 测试用例
// ============================================================

/// Test 1（NETSET-02 #1 + Pitfall 3 wire 信号）：写入 false 后读回 false。
#[tokio::test]
async fn settings_network_writes_and_reads_back_explicit_false() {
    let facade = build_facade();

    // PUT body: {"network":{"allowRelayFallback":false}}
    let put_body = json!({"network": {"allowRelayFallback": false}}).to_string();
    let put_resp = simulate_put(&facade, &put_body).await;
    assert_eq!(put_resp["success"], Value::Bool(true));
    assert_eq!(
        put_resp["restartRequired"], Value::Bool(true),
        "restartRequired wire must be true"
    );
    assert_eq!(
        put_resp["data"]["network"]["allowRelayFallback"], Value::Bool(false),
        "PUT response data must reflect written value"
    );

    // GET 二次确认（写盘 → 读取一致）
    let get_resp = simulate_get(&facade).await;
    assert_eq!(
        get_resp["network"]["allowRelayFallback"], Value::Bool(false),
        "GET must read back written value"
    );
}

/// Test 2（双向覆盖）：写入 true 后读回 true。
#[tokio::test]
async fn settings_network_writes_and_reads_back_explicit_true() {
    let facade = build_facade();

    // 先写入 false 然后再覆盖为 true，验证 update 是 patch（不是 replace）
    let _ = simulate_put(
        &facade,
        &json!({"network": {"allowRelayFallback": false}}).to_string(),
    )
    .await;
    let put_resp = simulate_put(
        &facade,
        &json!({"network": {"allowRelayFallback": true}}).to_string(),
    )
    .await;
    assert_eq!(put_resp["restartRequired"], Value::Bool(true));
    assert_eq!(
        put_resp["data"]["network"]["allowRelayFallback"], Value::Bool(true),
        "PUT response data must reflect written value"
    );

    let get_resp = simulate_get(&facade).await;
    assert_eq!(get_resp["network"]["allowRelayFallback"], Value::Bool(true));
}

/// Test 3（**NETSET-02 success criterion #2 硬约束 — 旧客户端兼容**）：
/// 先 PUT 写入 false；再 PUT 不带 network 段（旧客户端 PUT 只改其他字段）；
/// 必须不抹掉已存在的 network.allow_relay_fallback。
#[tokio::test]
async fn legacy_client_put_without_network_section_does_not_clobber_existing_value() {
    let facade = build_facade();

    // 步骤 1：写入 false
    let _ = simulate_put(
        &facade,
        &json!({"network": {"allowRelayFallback": false}}).to_string(),
    )
    .await;

    // 步骤 2：旧客户端 PUT 只改 general.autoStart，**不带 network 段**
    let legacy_resp = simulate_put(
        &facade,
        &json!({"general": {"autoStart": false}}).to_string(),
    )
    .await;
    assert_eq!(
        legacy_resp["restartRequired"], Value::Bool(false),
        "legacy patch (no network) must NOT signal restart"
    );
    assert_eq!(
        legacy_resp["data"]["network"]["allowRelayFallback"], Value::Bool(false),
        "legacy PUT MUST NOT clobber existing network field (NETSET-02 #2)"
    );

    // 步骤 3：GET 二次确认（持久层未被抹掉）
    let get_resp = simulate_get(&facade).await;
    assert_eq!(
        get_resp["network"]["allowRelayFallback"], Value::Bool(false),
        "after legacy PUT, GET must still see preserved network value"
    );
}
```

**Cargo.toml 注意：** `uc-webserver/Cargo.toml` 的 `[dev-dependencies]` 必须含 `tokio`（with `macros` + `rt-multi-thread`）+ `serde_json` + `async-trait` —— 这些已是项目级现有 dev-deps。如缺失则添加（不引入新 crate）。

**【checker BLOCKER 1 — 显式约束】**
- ✅ 三个 `#[tokio::test]` 全部真跑，**禁止 `#[ignore]`**
- ✅ fixture 函数 `build_facade()` 必须可直接编译，**禁止 `unimplemented!()`** 或 panic stub
- ✅ `simulate_put` / `simulate_get` 使用 `pub(crate)` 暴露的 handler 内部函数 + SettingsFacade，覆盖完整 wire-level 路径
- ✅ 测试断言全部通过 `serde_json::Value::Bool`（与生产代码同 wire 协议）

**禁止动作：**
- ❌ 【**checker BLOCKER 1**】禁止 fixture 含 `unimplemented!()` 或 panic stub —— executor 必须直接 lift 上面 fixture 代码
- ❌ 【**checker BLOCKER 1**】禁止任何 `#[ignore]` 标注或"fixture pending"注释
- ❌ 禁止 `tests/settings_network_smoke.rs` 引入 mock relay server（Tier C 是手工验收 D-C1 决策外）
- ❌ 禁止试图组装完整 `AppFacade`（只为这个 smoke 测试不值得 — 见 BLOCKER 1 范围声明）
- ❌ 禁止把测试改为 axum router 形式（与本 task 决议不一致；router 中间件 smoke 测试由其他 webserver test fixture 覆盖）
  </action>

  <verify>
    <automated>cargo build -p uc-webserver --tests 2>&amp;1 | tail -10</automated>
    <automated>cargo test -p uc-webserver --test settings_network_smoke -- --nocapture 2>&amp;1 | tail -40</automated>
  </verify>

  <acceptance_criteria>
    - `cargo build -p uc-webserver --tests` 退出码 0
    - **【checker BLOCKER 1】** 3 个 `#[tokio::test]` 全过：`settings_network_writes_and_reads_back_explicit_false` / `settings_network_writes_and_reads_back_explicit_true` / `legacy_client_put_without_network_section_does_not_clobber_existing_value`
    - **【checker BLOCKER 1 — 铁律】** `rg -n '#\[ignore\]' src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs` 返回 **0**
    - **【checker BLOCKER 1 — 铁律】** `rg -n 'unimplemented!\(' src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs` 返回 **0**
    - **【checker BLOCKER 1 — fixture 真实性】** `rg -n 'panic!\(' src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs` 返回 0（无 stub panic — `expect()` 等错误处理是合法的）
    - `rg -n 'pub\(crate\) fn settings_patch_from_dto\|pub\(crate\) fn settings_view_to_dto' src-tauri/crates/uc-webserver/src/api/settings.rs` 命中 2 行（暴露给 integration test）
    - `rg -n 'allowRelayFallback' src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs` 命中至少 6 处
    - `rg -n 'restartRequired' src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs` 命中至少 3 处（3 个测试各 ≥ 1 次断言）
  </acceptance_criteria>

  <done>
NETSET-02 success criterion #2 wire-level smoke 测试就位 —— PUT/GET 往返一致 + 旧客户端不抹掉已有 network 字段 + restart_required wire 信号正确。3 个 `#[tokio::test]` 全部真跑无 `#[ignore]`，fixture 真实可编译无 `unimplemented!`。webserver integration test 编译通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP 客户端 → webserver `/settings` | 不可信 JSON 输入跨"传输 → daemon"边界；通过 axum + serde + session JWT 中间件 |
| webserver handler → SettingsFacade | 跨"表示层 → 应用层"边界；plan 02 / 03 已固化 patch 不抹掉语义 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094.04-01 | Tampering | 恶意 PUT 含 `{"network":{"allowRelayFallback":"yes"}}`（错类型） | mitigate | serde-strict typing — bool 字段非 bool 直接 422 反序列化错误；与 sibling 字段一致 |
| T-094.04-02 | Tampering | 恶意客户端 PUT 全空 patch 触发不必要重启 hint | accept | `restart_required = false` 因 `payload.network.is_none()`；不会误报；最坏只是无操作 |
| T-094.04-03 | Information Disclosure | restart_required wire 暴露 daemon 内部状态 | accept | 是预期 wire 契约（D-D1 / D-D3）；无 PII；前端 Phase 95 显式消费 |
| T-094.04-04 | Spoofing | 中间人改写响应 `restartRequired: false` 让用户误以为已生效 | accept | 已由 webserver 既有 HTTPS / IPC 信任假设覆盖；本 plan 不引入新攻击面 |
| T-094.04-05 | Denial of Service | 巨大 patch JSON | accept | axum 默认 body limit + sibling 字段同等风险等级 |
| T-094.04-06 | Elevation of Privilege | 旧客户端 PUT 不带 network 段抹掉用户已设的 false → 突然走外网中继 | mitigate | NETSET-02 #2 硬约束 + plan 02 单元测试 + 本 plan task 2 integration smoke 双重断言 |
| T-094.04-07 | Tampering（反向命名） | handler 实施时把 `payload.network.is_some()` 误写为 `!payload.network.is_some()` 或 `payload.network.is_none()` 等反向语义 | mitigate | **【checker WARNING 7】** grep 守护 `rg '!\s*payload.network\|is_none\(\)' src-tauri/crates/uc-webserver/src/api/settings.rs \| rg -i 'network\|payload'` 返回 0；测试 5-case 完整 truth-table（None / Some(empty) / Some(false) / Some(true) / 旧客户端纯 general）覆盖所有分支 |

无 `high` 严重等级威胁。
</threat_model>

<verification>
- `cargo check -p uc-webserver` + `cargo build -p uc-webserver --tests` 全过
- `cargo test -p uc-webserver --lib api::settings::network_mapping_tests`（**checker WARNING 7** — 7 测试 含 5-case truth-table）+ `cargo test -p uc-webserver --test settings_network_smoke`（**checker BLOCKER 1** — 3 测试全跑无 ignore）全过
- grep 守护：
  - `rg -n '!\s*payload.network|!\s*value.network|!\s*patch.network' src-tauri/crates/uc-webserver/` 返回 0
  - **【checker WARNING 7】** `rg -n 'is_none\(\)|!\s*is_some' src-tauri/crates/uc-webserver/src/api/settings.rs | rg -i 'network|payload' || true` 返回 0（无反向语义）
  - `rg -n 'restart_required' src-tauri/crates/uc-webserver/src/api/settings.rs` 命中 2（赋值 + 字段填充）
  - `rg -n 'allow_relay_fallback' src-tauri/crates/uc-webserver/` 共 4 处（patch_from_dto / view_to_dto / 单测 / smoke 测试）
  - **【checker BLOCKER 1 — 铁律】** `rg -n '#\[ignore\]|unimplemented!\(' src-tauri/crates/uc-webserver/tests/settings_network_smoke.rs` 返回 0
  - **【checker BLOCKER 5】** `rg -n '\.\.\s*Default::default\(\)' src-tauri/crates/uc-webserver/` ≥ 6 处（test 模块用 derive 简化构造）
</verification>

<success_criteria>
- [ ] `settings_patch_from_dto` 末段补 `network` segment（透明搬运 `n.allow_relay_fallback`）
- [ ] `settings_view_to_dto` 末段补 `network` segment（业务正向语义流过）
- [ ] `update_settings_handler` 内联计算 `restart_required = payload.network.is_some()`
- [ ] `UpdateSettingsResponse` 构造含 `restart_required` 字段填充
- [ ] import 列表新增 `NetworkSettingsDto, NetworkSettingsPatchDto`
- [ ] **【checker WARNING 7】** 7 个单元测试覆盖 mapping (2) + 5-case restart_required truth-table (None / Some(empty) / Some(false) / Some(true) / 旧客户端纯 general patch)
- [ ] **【checker BLOCKER 1】** 3 个 wire-level smoke 测试覆盖 PUT/GET 往返 + 旧客户端兼容（NETSET-02 #2）—— **全部 `#[tokio::test]` 真跑，禁止 `#[ignore]` / `unimplemented!`**
- [ ] **【checker BLOCKER 1】** task 2 fixture 用 in-memory `SettingsPort` + `SettingsFacade` 直接调 handler 内部 `pub(crate)` 函数 + handler 内联 restart_required 模拟逻辑（不组装完整 axum Router / AppFacade）
- [ ] **【checker BLOCKER 5】** task 1 测试模块 6+ 处 `..Default::default()` 简化构造（plan 03 已加 derive(Default)）
- [ ] `cargo check -p uc-webserver` + `cargo test -p uc-webserver --test settings_network_smoke` 全过
- [ ] **决策 D-D1 / Pitfall 3 实施已写入 must_haves.truths：handler 内联 restart_required，调用方显式承担"还没真正生效"**
</success_criteria>

<output>
After completion, create `.planning/phases/094-backend-network-allow-relay-fallback/094-04-SUMMARY.md` with:
- 实际改动的 LOC（settings.rs handler + 双向 mapping 各一段；tests/settings_network_smoke.rs 新建一个文件，~150 行；settings.rs 新增 `pub(crate) fn` 暴露 2 处）
- 7 单元测试（含 5-case truth-table）+ 3 wire-level smoke 测试 全部 pass 输出
- **【checker BLOCKER 1】** fixture 决策：用 in-memory SettingsPort + SettingsFacade 直接 wire-level round-trip（不组装完整 AppFacade），3 个 #[tokio::test] 全跑无 #[ignore] / unimplemented!
- restart_required 计算位置最终决策（handler 内联，未下沉到 facade）
- Pitfall 1 grep 自查：`rg 'disable_relays|allow_relay_fallback' src-tauri/crates/uc-webserver/` 全部命中应可解释为业务正向语义流过，无反向写法
- **【checker WARNING 7】** is_none / 反向语义 grep 自查 0 命中
- **【checker BLOCKER 5】** Default derive 用法验证：`..Default::default()` 在 webserver tests 中 ≥ 6 处简化构造
- 已知 limitation（**checker WARNING 7 case 2** 显式承认）：handler 内联 restart_required 仅看 `payload.network.is_some()`，Some(嵌套 None) 仍触发 restart_required = true（Phase 94 简化策略）；未来若 NetworkSettings 加非 restart 字段需重构（留给 v0.7.x）
</output>
