---
phase: 094-backend-network-allow-relay-fallback
plan: 05
type: execute
wave: 3
depends_on:
  - "094-01"
  - "094-06"
files_modified:
  - src-tauri/crates/uc-bootstrap/src/network_policy.rs
  - src-tauri/crates/uc-bootstrap/src/lib.rs
  - src-tauri/crates/uc-bootstrap/src/builders.rs
  - src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs
autonomous: true
requirements:
  - NETSET-03
tags:
  - uc-bootstrap
  - network-policy
  - lan-only
  - iroh-bind

must_haves:
  truths:
    - "新建 `uc-bootstrap/src/network_policy.rs`，提供 `pub(crate) fn relay_policy_to_iroh_config(allow_relay_fallback: bool, rendezvous_base_url: Option<String>) -> IrohNodeConfig` —— 全工程**唯一**取反点（`disable_relays = !allow_relay_fallback`），D-A1 物理位置"
    - "truth-table 单元测试覆盖 `(allow=true → disable=false)` + `(allow=false → disable=true)` 两组（ROADMAP success criterion #4 硬约束 — Pitfall 1 防御）"
    - "`builders.rs:178` 与 `non_gui_runtime.rs:280` 两处装配点改造：先 `wired.deps.settings.load().await`（`anyhow::Result` 错误硬失败 — D-B1 选项 B 接受现状）→ 调 `relay_policy_to_iroh_config(...)` 拿 `iroh_config` → 从 `iroh_config.disable_relays` 读值打 tracing log → 喂给 `build_space_setup_assembly`"
    - "**【checker BLOCKER 4】** 启动期 `tracing::info!` 打日志时，`disable_relays` 的值**直接从 `iroh_config.disable_relays` 字段读取**，而**不是**在 `builders.rs` / `non_gui_runtime.rs` 内联 `let disable_relays = !allow_relay_fallback;` —— 后者会让取反点泄漏到第二、第三处，违反 Pattern A 单一取反点铁律"
    - "**【checker BLOCKER 4 grep 守护铁律 — 收紧版】** `rg -n -v '^\s*//' src-tauri/crates/ --type rust | rg 'disable_relays\s*=\s*!|disable_relays:\s*!' | wc -l` == **1**（仅 `network_policy.rs` 取反点；`builders.rs` / `non_gui_runtime.rs` / 全工程其他位置 0 命中）"
    - "全工程除 `uc-bootstrap/src/network_policy.rs`（取反点）+ `uc-infra/src/network/iroh/node.rs:153-162`（infra 字段定义）+ 测试文件外，无 `disable_relays = !` 类反向写法（grep 守护铁律 — Pitfall 1）"
    - "`network_policy` 模块禁止引用 `general.telemetry_enabled` / OTLP exporter（Pitfall 6 — OTLP 独立由 telemetry_enabled 控制，PR review grep 必查）"
    - "`lib.rs` 仅 `mod network_policy;`（不 `pub mod`）— `relay_policy_to_iroh_config` 是 `pub(crate)` 内部翻译 helper，不对外暴露"
    - "**【checker BLOCKER 3 修订 — 选项 B 决议】** Phase 94 不实施 D-B1 + D-B2 的 port 错误细分；NotFound → default 的语义实质由 `uc-infra::FileSettingsRepository::load` (`repository.rs:166-168`) 的 NotFound 兜底**作为 SettingsPort 实现的隐式契约一部分**承担；`SettingsPort::load` 当前错误返回类型 `anyhow::Result<Settings>` 保持不变（不在 Phase 94 内重构错误类型）；后续 builders 层（本 plan）按硬失败处理 load Err。这违反 uc-infra/AGENTS.md §4.2，是**有意接受**的临时技术债务，CONTEXT.md `<deferred>` 已记录后续 phase 必须偿还（实施 `SettingsLoadError` discriminated enum）"
  artifacts:
    - path: "src-tauri/crates/uc-bootstrap/src/network_policy.rs"
      provides: "唯一取反 helper relay_policy_to_iroh_config + truth-table 单测（3 case）"
      contains: "pub(crate) fn relay_policy_to_iroh_config"
    - path: "src-tauri/crates/uc-bootstrap/src/lib.rs"
      provides: "`mod network_policy;` 私有模块声明（pub(crate) 即可）"
      contains: "mod network_policy;"
    - path: "src-tauri/crates/uc-bootstrap/src/builders.rs"
      provides: "GUI/daemon 装配点 #1：read settings → 调取反 helper → tracing::info! 启动日志 → 喂给 build_space_setup_assembly"
      contains: "relay_policy_to_iroh_config"
    - path: "src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs"
      provides: "CLI 装配点 #2：同模式改造"
      contains: "relay_policy_to_iroh_config"
  key_links:
    - from: "uc-bootstrap::network_policy::relay_policy_to_iroh_config"
      to: "uc-infra::IrohNodeConfig.disable_relays"
      via: "唯一一处取反 disable_relays = !allow_relay_fallback"
      pattern: "disable_relays: !allow_relay_fallback"
    - from: "uc-bootstrap::builders.rs:178"
      to: "relay_policy_to_iroh_config"
      via: "settings.network.allow_relay_fallback → IrohNodeConfig"
      pattern: "relay_policy_to_iroh_config\\(settings\\.network\\.allow_relay_fallback"
    - from: "uc-bootstrap::non_gui_runtime.rs:280"
      to: "relay_policy_to_iroh_config"
      via: "settings.network.allow_relay_fallback → IrohNodeConfig"
      pattern: "relay_policy_to_iroh_config\\(settings\\.network\\.allow_relay_fallback"
---

<objective>
新建 `uc-bootstrap/src/network_policy.rs` 模块，提供全工程**唯一**的取反 helper `relay_policy_to_iroh_config(allow_relay_fallback, rendezvous_base_url) -> IrohNodeConfig`（含 truth-table 单元测试）；改造 `builders.rs:178` 与 `non_gui_runtime.rs:280` 两个 iroh bind 装配点，先读 settings、再调 helper 翻译、最后喂给 `build_space_setup_assembly`，并打启动 audit log。

Purpose: 这是 NETSET-03 的核心交付 —— "用户在 settings 中关闭允许中继回落后重启 daemon，启动路径会把字段值通过唯一的取反 helper 注入 `IrohNodeConfig.disable_relays`，使 iroh endpoint 以 `RelayMode::Disabled` 模式 bind"。整个里程碑的反向命名规则在此处收口（Pitfall 1 防御铁律）。

Output:
- `network_policy.rs`（新文件，~50 行含 truth-table 测试）
- `lib.rs` 加 `mod network_policy;`（私有声明）
- `builders.rs:178` 与 `non_gui_runtime.rs:280` 两处改造
- `tracing::info!` 启动日志（D-B3 字段名规范）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md

# 受影响代码 / interface 来源
@src-tauri/crates/uc-bootstrap/src/space_setup.rs
@src-tauri/crates/uc-bootstrap/src/builders.rs
@src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs
@src-tauri/crates/uc-bootstrap/src/lib.rs

<interfaces>
<!-- 现有装配点 + IrohNodeConfig + SettingsPort -->

`IrohNodeConfig` 已在 `uc-bootstrap::space_setup` re-export（`space_setup.rs:48`）：
```rust
pub use uc_infra::network::iroh::IrohNodeConfig;
```

`IrohNodeConfig` 字段（`uc-infra/src/network/iroh/node.rs:152-162`）：
```rust
#[derive(Debug, Clone, Default)]
pub struct IrohNodeConfig {
    pub rendezvous_base_url: Option<String>,
    pub disable_relays: bool,
}
```

`builders.rs:178` 现状：
```rust
let space_setup_assembly = build_space_setup_assembly(&wired, IrohNodeConfig::default())
    .await
    .map_err(|e| anyhow::anyhow!("Slice 1+ assembly build failed: {e}"))?;
```

`non_gui_runtime.rs:278-282` 现状：
```rust
let (config, wired) = crate::builders::build_slice1_cli_context(log_profile)?;
let storage_paths = get_storage_paths(&config)?;
let assembly = build_space_setup_assembly(&wired, IrohNodeConfig::default())
    .await
    .map_err(|err| anyhow::anyhow!("failed to bind iroh endpoint: {err}"))?;
```

`wired.deps.settings` 类型：`Arc<dyn SettingsPort>`（`assembly.rs:861` 装配 `infra.settings_repo`）。

`SettingsPort::load`：`async fn load(&self) -> anyhow::Result<Settings>`（`uc-core/src/ports/settings.rs:7`）。

`FileSettingsRepository::load`（`uc-infra/src/settings/repository.rs:166-168`）当前已对 NotFound 兜底返回 `Settings::default()`：
```rust
async fn load(&self) -> Result<Settings> {
    let content = match fs::read_to_string(&self.path).await {
        Ok(s) => s,
        Err(e) if e.kind() == std::io::ErrorKind::NotFound => {
            return Ok(Settings::default());  // ← NotFound 在 infra 层已被吃掉
        }
        Err(e) => return Err(e).with_context(...),
    };
    // ... migrate + return
}
```

`uc-bootstrap/src/lib.rs` 现状（45 行）：
```rust
pub mod assembly;
pub mod background_tasks;
pub mod builders;
pub mod config;
pub mod file_transfer_lifecycle;
pub mod init;
pub mod non_gui_runtime;
pub mod space_setup;
pub mod task_registry;
pub mod tracing;
// ...
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 network_policy.rs 模块（含取反 helper + truth-table 单元测试）+ lib.rs 加 mod 声明</name>
  <files>
    src-tauri/crates/uc-bootstrap/src/network_policy.rs
    src-tauri/crates/uc-bootstrap/src/lib.rs
  </files>

  <read_first>
    1. `src-tauri/crates/uc-bootstrap/src/space_setup.rs:1-60`（重点 line 48 的 `pub use uc_infra::network::iroh::IrohNodeConfig`）
    2. `src-tauri/crates/uc-bootstrap/src/lib.rs`（必读完整，仅 45 行）
    3. `src-tauri/crates/uc-infra/src/network/iroh/node.rs:140-162`（确认 `IrohNodeConfig` 字段顺序与 `Default` derive）
    4. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` D-A1（helper 物理位置 + 单测就近）
    5. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §10（network_policy.rs 模式映射 — 完整代码模板）+ Pattern A（反向命名只翻译一次）
    6. `.planning/research/PITFALLS.md` Pitfall 1（truth-table 防御 — 两个测试不能合并）+ Pitfall 6（不引用 OTLP / telemetry）
  </read_first>

  <behavior>
    - Test 1（Pitfall 1 truth-table — 正向）：`relay_policy_to_iroh_config(true, None)` 返回 `IrohNodeConfig { disable_relays: false, rendezvous_base_url: None }`
    - Test 2（Pitfall 1 truth-table — 反向）：`relay_policy_to_iroh_config(false, None)` 返回 `IrohNodeConfig { disable_relays: true, rendezvous_base_url: None }`
    - Test 3（rendezvous override 透明传递）：`relay_policy_to_iroh_config(true, Some("http://test".into()))` 返回 `IrohNodeConfig { disable_relays: false, rendezvous_base_url: Some("http://test".into()) }`
  </behavior>

  <action>
**Step 1：新建 `src-tauri/crates/uc-bootstrap/src/network_policy.rs`**：

```rust
//! 单一翻译点：`network.allow_relay_fallback`（业务正向语义）→
//! `IrohNodeConfig.disable_relays`（infra 反向语义）。
//!
//! ## 反向命名铁律（Pitfall 1 防御 — 见 .planning/research/PITFALLS.md §Pitfall 1）
//!
//! - UI = "LAN-only Mode = ON"
//! - 后端 = `network.allow_relay_fallback = false`
//! - infra (iroh) = `IrohNodeConfig.disable_relays = true`
//!
//! 三层语义两次反转。**全工程除本模块 + `uc-infra/src/network/iroh/node.rs:153-162`
//! 字段定义 + 测试文件外，严禁在其他位置出现 `disable_relays = !allow_relay_fallback`
//! 类的取反**。DTO ↔ View ↔ core 三层只搬运 `allow_relay_fallback` 业务正向语义。
//!
//! ## OTLP 不联动（Pitfall 6 防御）
//!
//! 本模块**禁止**引用 `general.telemetry_enabled` 或任何 OTLP 配置 ——
//! 网络策略与遥测开关独立。
//!
//! ## D-A1 物理位置 + D-B3 启动日志规范
//!
//! 见 `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md`。

use crate::space_setup::IrohNodeConfig;

/// 把业务侧 `network.allow_relay_fallback` 翻译为 infra 侧 `IrohNodeConfig`。
///
/// 语义反转点（**唯一**）：
/// - `allow_relay_fallback = true`  → `disable_relays = false`（允许 fallback，
///   存量行为；老用户跨网段同步仍可工作）
/// - `allow_relay_fallback = false` → `disable_relays = true`（LAN-only，跨
///   网段设备会失联）
///
/// 参数：
/// - `allow_relay_fallback`：业务正向语义，由 `uc-core::Settings.network` 透传
/// - `rendezvous_base_url`：`None` 走 `RENDEZVOUS_BASE_URL` 默认；production 调
///   用方传 `None`；集成测试覆盖 override
pub(crate) fn relay_policy_to_iroh_config(
    allow_relay_fallback: bool,
    rendezvous_base_url: Option<String>,
) -> IrohNodeConfig {
    IrohNodeConfig {
        // ↓ 全工程**唯一**取反点 — Pitfall 1 防御铁律。
        disable_relays: !allow_relay_fallback,
        rendezvous_base_url,
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    /// Pitfall 1 防御 truth-table（正向）：allow=true 必须导致 disable=false。
    /// 这与下面 `allow_false_means_disable_true` 两个测试**不能合并** —
    /// 单方向断言无法捕捉"代码错把恒等当取反"或"反向写漏"两类 bug。
    #[test]
    fn allow_true_means_disable_false() {
        let cfg = relay_policy_to_iroh_config(true, None);
        assert!(!cfg.disable_relays, "allow=true MUST produce disable=false");
        assert!(cfg.rendezvous_base_url.is_none());
    }

    /// Pitfall 1 防御 truth-table（反向）：allow=false 必须导致 disable=true。
    #[test]
    fn allow_false_means_disable_true() {
        let cfg = relay_policy_to_iroh_config(false, None);
        assert!(cfg.disable_relays, "allow=false MUST produce disable=true");
        assert!(cfg.rendezvous_base_url.is_none());
    }

    /// rendezvous override 透明传递（产线 None；集成测试 Some(url)）。
    #[test]
    fn rendezvous_override_passes_through() {
        let cfg = relay_policy_to_iroh_config(true, Some("http://test".into()));
        assert_eq!(cfg.rendezvous_base_url, Some("http://test".into()));
    }
}
```

**Step 2：在 `src-tauri/crates/uc-bootstrap/src/lib.rs` 中追加私有模块声明**。

当前 line 8-17 块：
```rust
pub mod assembly;
pub mod background_tasks;
pub mod builders;
pub mod config;
pub mod file_transfer_lifecycle;
pub mod init;
pub mod non_gui_runtime;
pub mod space_setup;
pub mod task_registry;
pub mod tracing;
```

改为（在 alphabetic 顺序中插入 `mod network_policy;`，注意**不**用 `pub mod`，保持 `pub(crate)` 内部）：
```rust
pub mod assembly;
pub mod background_tasks;
pub mod builders;
pub mod config;
pub mod file_transfer_lifecycle;
pub mod init;
mod network_policy;
pub mod non_gui_runtime;
pub mod space_setup;
pub mod task_registry;
pub mod tracing;
```

**禁止动作（Pitfall 1 / 3 / 6 防御）：**
- ❌ 禁止把 `relay_policy_to_iroh_config` 标 `pub`（必须 `pub(crate)` — 不对外暴露）
- ❌ 禁止在 lib.rs 里 `pub use network_policy::*;`（保持模块私有）
- ❌ 禁止合并两个 truth-table 测试（Pitfall 1 双向独立断言）
- ❌ 禁止引用 `uc_observability` 或 `general.telemetry_enabled`（Pitfall 6）
- ❌ 禁止把函数名简化为 `to_config` / `build_cfg`（语义自明的命名是 review 时一眼识别"翻译点"的关键）
- ❌ 禁止给 `IrohNodeConfig` 加额外字段（其它字段如未来 `lan_only_strict_mode` 不在 Phase 94 范围）
  </action>

  <verify>
    <automated>cargo check -p uc-bootstrap 2>&amp;1 | tail -10</automated>
    <automated>cargo test -p uc-bootstrap --lib network_policy::tests -- --nocapture 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-bootstrap` 退出码 0
    - `cargo test -p uc-bootstrap --lib network_policy::tests` 3 个测试全过
    - `rg -n 'pub\(crate\) fn relay_policy_to_iroh_config' src-tauri/crates/uc-bootstrap/src/network_policy.rs` 命中 1 处
    - `rg -n '^pub fn relay_policy_to_iroh_config|^pub use.*relay_policy' src-tauri/crates/uc-bootstrap/` 返回 0（不对外暴露）
    - `rg -n '^mod network_policy;' src-tauri/crates/uc-bootstrap/src/lib.rs` 命中 1 行
    - `rg -n '^pub mod network_policy' src-tauri/crates/uc-bootstrap/src/lib.rs` 返回 0
    - `rg -n 'disable_relays.*!.*allow_relay_fallback|!\s*allow_relay_fallback' src-tauri/ --type rust` 命中**仅** `network_policy.rs:相关行`（取反唯一点）
    - `rg -n 'telemetry|otlp|OTLP' src-tauri/crates/uc-bootstrap/src/network_policy.rs` 返回 0（Pitfall 6）
  </acceptance_criteria>

  <done>
network_policy.rs helper + 3 truth-table 测试就位；lib.rs 私有模块声明；全工程仅本文件含 `disable_relays = !allow_relay_fallback`；无 OTLP 联动。
  </done>
</task>

<task type="auto">
  <name>Task 2: 改造 builders.rs:178 与 non_gui_runtime.rs:280 两个 iroh bind 装配点 — 读 settings → 调 helper → tracing::info! → 喂给 build_space_setup_assembly</name>
  <files>
    src-tauri/crates/uc-bootstrap/src/builders.rs
    src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs
  </files>

  <read_first>
    1. `src-tauri/crates/uc-bootstrap/src/builders.rs:160-200`（line 178 是改造目标 — `IrohNodeConfig::default()` 直传）
    2. `src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs:270-295`（line 280 是改造目标）
    3. `src-tauri/crates/uc-bootstrap/src/assembly.rs:855-870`（确认 `wired.deps.settings` 字段位置和类型）
    4. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` D-B1 / D-B2 / D-B3：D-B1 选项 A 现状决策（NotFound 已被 infra 层吃掉，builders 层 Err 一律硬失败）+ D-B3 启动日志规范
    5. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §8 / §9（builders / non_gui_runtime 改造模式）+ Pattern F（tracing instrument + info! 启动日志）
    6. `src-tauri/crates/uc-infra/src/settings/repository.rs:163-182`（确认 `FileSettingsRepository::load` 内部对 NotFound 兜底，所以 D-B1 分流退化为 Err 硬失败）
    7. Task 1 已新建的 `network_policy::relay_policy_to_iroh_config`
  </read_first>

  <action>
**Step 1：改造 `src-tauri/crates/uc-bootstrap/src/builders.rs:178`。**

定位上下文（line 169-180）：
```rust
    // Build the iroh-stack assembly on the caller's runtime. Must NOT spin up
    // a throwaway current-thread rt here: `Endpoint::bind` spawns magicsock /
    // ...（既有注释保留）
    let space_setup_assembly = build_space_setup_assembly(&wired, IrohNodeConfig::default())
        .await
        .map_err(|e| anyhow::anyhow!("Slice 1+ assembly build failed: {e}"))?;
```

改为（在 `let space_setup_assembly = ...` 之前插入 settings 读取 + helper 调用 + tracing::info!）：
```rust
    // Build the iroh-stack assembly on the caller's runtime. Must NOT spin up
    // a throwaway current-thread rt here: `Endpoint::bind` spawns magicsock /
    // ...（既有注释保留）

    // Phase 94 NETSET-03：从 settings 读取 LAN-only Mode 偏好后翻译为
    // `IrohNodeConfig`。`SettingsPort::load` 当前错误返回类型 `anyhow::Result`
    // 不区分 NotFound vs Parse；`FileSettingsRepository::load`
    // (`repository.rs:166-168`) 已对 NotFound 兜底返回 `Settings::default()`
    // (即 `allow_relay_fallback: true`)。故此处只需对剩余 Parse/IO 错误硬失败
    // —— LAN-only 信任锚点不容许脏 settings 撒谎（D-B1 选项 B 现状决策 — 见
    // 094-CONTEXT.md `<deferred>` 已记录后续 phase 实施 `SettingsLoadError`
    // 偿还此隐式契约）。
    let settings = wired
        .deps
        .settings
        .load()
        .await
        .map_err(|err| anyhow::anyhow!("settings load failed at startup: {err}"))?;
    let allow_relay_fallback = settings.network.allow_relay_fallback;

    // 【checker BLOCKER 4 — 单一取反点铁律】
    // `disable_relays` 的值**只能**通过 `relay_policy_to_iroh_config` 取得，
    // **不**在此处内联写 `let disable_relays = !allow_relay_fallback;`（这会让
    // 取反点泄漏到第二处，违反 Pattern A）。下方 tracing::info! 字段值从
    // `iroh_config.disable_relays` 读取。
    let iroh_config = crate::network_policy::relay_policy_to_iroh_config(
        allow_relay_fallback,
        None, // production 不 override rendezvous，使用默认 RENDEZVOUS_BASE_URL
    );

    // D-B3：方便 support 排障 — 字段名固定为 `allow_relay_fallback` /
    // `disable_relays`（与代码一致）。**不**在 OTLP 加 attribute（Pitfall 6）。
    // 【checker BLOCKER 4 / W1】tracing 字段值通过 `iroh_config.disable_relays`
    // 读取，保证唯一取反点位于 network_policy.rs。
    tracing::info!(
        target: "settings.network",
        allow_relay_fallback,
        disable_relays = iroh_config.disable_relays,
        "applying network.allow_relay_fallback={} → disable_relays={}",
        allow_relay_fallback,
        iroh_config.disable_relays,
    );

    let space_setup_assembly = build_space_setup_assembly(&wired, iroh_config)
        .await
        .map_err(|e| anyhow::anyhow!("Slice 1+ assembly build failed: {e}"))?;
```

**Step 2：改造 `src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs:280`。**

定位上下文（line 277-282）：
```rust
pub async fn build_cli_app_runtime(
    log_profile: Option<uc_observability::LogProfile>,
) -> anyhow::Result<CliAppRuntime> {
    let (config, wired) = crate::builders::build_slice1_cli_context(log_profile)?;
    let storage_paths = get_storage_paths(&config)?;
    let assembly = build_space_setup_assembly(&wired, IrohNodeConfig::default())
        .await
        .map_err(|err| anyhow::anyhow!("failed to bind iroh endpoint: {err}"))?;
```

改为（在 `let assembly = ...` 之前插入 settings 读取 + helper 调用 + tracing::info!；模式与 builders.rs 完全一致）：
```rust
pub async fn build_cli_app_runtime(
    log_profile: Option<uc_observability::LogProfile>,
) -> anyhow::Result<CliAppRuntime> {
    let (config, wired) = crate::builders::build_slice1_cli_context(log_profile)?;
    let storage_paths = get_storage_paths(&config)?;

    // Phase 94 NETSET-03：与 builders.rs 同模式（D-B1 选项 B 现状决策 — 见
    // 094-CONTEXT.md `<deferred>` 后续 phase 实施 `SettingsLoadError` 偿还）。
    let settings = wired
        .deps
        .settings
        .load()
        .await
        .map_err(|err| anyhow::anyhow!("settings load failed at startup: {err}"))?;
    let allow_relay_fallback = settings.network.allow_relay_fallback;

    // 【checker BLOCKER 4 — 单一取反点铁律】见 builders.rs 同处注释。
    // 不在此处内联 `let disable_relays = !allow_relay_fallback;`。
    let iroh_config = crate::network_policy::relay_policy_to_iroh_config(
        allow_relay_fallback,
        None,
    );

    tracing::info!(
        target: "settings.network",
        allow_relay_fallback,
        disable_relays = iroh_config.disable_relays,
        "applying network.allow_relay_fallback={} → disable_relays={}",
        allow_relay_fallback,
        iroh_config.disable_relays,
    );

    let assembly = build_space_setup_assembly(&wired, iroh_config)
        .await
        .map_err(|err| anyhow::anyhow!("failed to bind iroh endpoint: {err}"))?;
```

**禁止动作（Pitfall 1 / 3 / 6 防御）：**
- ❌ 【**checker BLOCKER 4 — 铁律**】禁止在 `builders.rs` / `non_gui_runtime.rs`（或全工程除 `network_policy.rs` 外任何位置）内联写 `let disable_relays = !allow_relay_fallback;` 或 `disable_relays: !settings.network.allow_relay_fallback` —— 这会让取反点泄漏到第二处，违反 Pattern A 单一取反点铁律
- ❌ 【**checker BLOCKER 4**】tracing::info! 字段值**只能**从 `iroh_config.disable_relays` 读取，**不**绕过 helper 自己计算
- ❌ 禁止跳过 `tracing::info!` 启动日志（D-B3 是 ROADMAP success criterion #1 的支援证据 — "启动日志可见 disable_relays = true"）
- ❌ 禁止读 `settings.general.telemetry_enabled` 或与 OTLP exporter 联动（Pitfall 6）
- ❌ 禁止保留旧 `IrohNodeConfig::default()` 调用（除非另有 fallback 路径需要 — 本 plan 全替换为 `iroh_config`）
- ❌ 禁止改 `build_space_setup_assembly` 的签名（保持"装配体不决策"，space_setup.rs 不动）
- ❌ 禁止把 NotFound 单独分流为 `Settings::default()`（D-B1 选项 B：infra 层已兜底，builders 层只看到 Parse/IO 硬错误）
- ❌ 禁止把 `tracing::info!` target 改为 `network.policy` 或其它 — 严格 `settings.network` 与 PR review grep 一致
  </action>

  <verify>
    <automated>cargo check -p uc-bootstrap 2>&amp;1 | tail -10</automated>
    <automated>cargo build -p uc-bootstrap 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-bootstrap` 退出码 0
    - `cargo build -p uc-bootstrap` 退出码 0
    - `rg -n 'relay_policy_to_iroh_config' src-tauri/crates/uc-bootstrap/src/builders.rs` 命中 1 行
    - `rg -n 'relay_policy_to_iroh_config' src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs` 命中 1 行
    - `rg -n 'IrohNodeConfig::default\(\)' src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs` 返回 0（默认配置不再直传）
    - `rg -n 'wired.deps.settings.load\(\)' src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs` 命中 2 行
    - `rg -n 'tracing::info!.*settings.network|target:\s*"settings\.network"' src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs` 命中 2 行
    - **【checker BLOCKER 4】** `rg -n 'disable_relays = iroh_config\.disable_relays' src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs` 命中 2 行（tracing 字段值取自 helper 输出而非内联取反）
    - **【checker BLOCKER 4】** `rg -n 'disable_relays\s*=\s*!|disable_relays:\s*!' src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs` 返回 **0**（两文件内联取反零容忍）
    - **【checker WARNING 1】** `rg -n 'applying network\.allow_relay_fallback=' src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs` 命中 2 行（启动日志格式一致）
    - `rg -n -v '^//|^\s*//' src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs | rg -c '!.*settings.network.allow_relay_fallback' || true`：注释行过滤后，两文件无内联反向写法（取反走 helper）
    - **【checker BLOCKER 4 — 全工程铁律】** `rg -n -v '^\s*//' src-tauri/crates/ --type rust | rg 'disable_relays\s*=\s*!|disable_relays:\s*!' | wc -l` == **1**（注释行过滤后**仅** `network_policy.rs` 中"disable_relays: !allow_relay_fallback"那一处命中；其它命中视为违规）
  </acceptance_criteria>

  <done>
两个 iroh bind 装配点已改造，settings 读取 → 取反 helper → tracing::info! → IrohNodeConfig 注入路径打通。NETSET-03 success criterion #1 / #4 在工程结构上落地（手工验收路径见 ROADMAP）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.json → uc-core::Settings → bootstrap | 已在 plan 01 / infra repository 处理；本 plan builders 层只接 `anyhow::Result` |
| `relay_policy_to_iroh_config` → `IrohNodeConfig` → `IrohNodeBuilder::bind` | 启动期一次性翻译，bind 时 RelayMode 冻结（Pitfall 3 — 由 plan 06 OnceCell 守护重建尝试） |
| code review | 反向命名 + 唯一取反点 + grep 守护是结构性防御 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094.05-01 | Tampering（语义颠倒） | 反向命名导致 `disable_relays = allow_relay_fallback`（缺 `!`）或多重取反 | mitigate | 全工程**唯一**取反点（grep 守护铁律） + truth-table 单元测试双向独立断言（Pitfall 1） + tracing::info! 启动日志 audit |
| T-094.05-02 | Denial of Service | settings.json 损坏/被篡改导致 daemon 启动崩 | mitigate | D-B1 选项 A：infra 层 NotFound 兜底为 default；Parse/IO 错误硬失败拒绝启动（避免脏 settings 让 LAN-only 信任锚点撒谎） |
| T-094.05-03 | Elevation of Privilege | OTLP 联动 LAN-only 暗自关闭遥测 | mitigate | `network_policy.rs` 不引用 `general.telemetry_enabled`（Pitfall 6 — grep PR review 必查） |
| T-094.05-04 | Tampering（运行时热切换） | 开发者尝试 `endpoint.close() + 重 bind` 实现"立即生效" | mitigate | plan 06 `IrohNodeBuilder::bind` 加 OnceCell guard（结构性阻断 Pitfall 3） |
| T-094.05-05 | Information Disclosure | 启动日志暴露 settings 字段 | accept | 字段值（bool）非 PII；audit 用途；现有 `node.rs:399-404` 已有同等等级日志 |
| T-094.05-06 | Repudiation | 用户改 settings 后无 audit | mitigate | `tracing::info!(target: "settings.network", ...)` 启动日志记录 effective 值 |
| T-094.05-07 | Spoofing | helper 命名误导（如 `to_config`）让 reviewer 没看出是翻译点 | mitigate | 函数名 `relay_policy_to_iroh_config` 语义自明 + 模块名 `network_policy` 一眼可识别 |
| T-094.05-08 | Tampering（D-B1 误判） | NotFound 路径未兜底导致首次启动 daemon 拒启 | mitigate | infra 层 `FileSettingsRepository::load` 已兜底为 `Settings::default()`（grep `repository.rs:166-168` 验证）；builders 层 `.load()` Err 实际只是 Parse/IO，硬失败可接受。**【checker BLOCKER 3】** D-B1 / D-B2 选项 B 决议为有意识接受隐式契约（违反 uc-infra/AGENTS.md §4.2），CONTEXT.md `<deferred>` 已记录后续 phase 必须偿还（实施 `SettingsLoadError` discriminated enum） |

无 `high` 严重等级威胁（plan 06 的 OnceCell 是 Pitfall 3 的结构性兜底）。
</threat_model>

<verification>
- `cargo check -p uc-bootstrap` + `cargo build -p uc-bootstrap` 全过
- `cargo test -p uc-bootstrap --lib network_policy::tests` 3 个 truth-table 测试全过
- grep 守护（**关键铁律**）：
  - 全工程 `rg 'disable_relays\s*=\s*!|disable_relays:\s*!' src-tauri/ --type rust`（去注释后）**仅** `network_policy.rs` 一处命中
  - `rg 'telemetry|otlp|OTLP' src-tauri/crates/uc-bootstrap/src/network_policy.rs` 0 条
  - `rg 'IrohNodeConfig::default\(\)' src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/non_gui_runtime.rs` 0 条
  - `rg 'tracing::info!\s*\(\s*target:\s*"settings.network"' src-tauri/crates/uc-bootstrap/src/` 命中 2 行
- 单元测试覆盖 `(true,false), (false,true), rendezvous_override` 三组
- 与 plan 06 协同：本 plan **不**直接验证 bind 后 endpoint 候选地址内容（那是 plan 06 integration test 的范围）
</verification>

<success_criteria>
- [ ] `network_policy.rs` 新建，提供 `pub(crate) fn relay_policy_to_iroh_config`，**唯一**取反点
- [ ] 3 个 truth-table 单元测试覆盖正向 / 反向 / rendezvous override
- [ ] `lib.rs` 加 `mod network_policy;`（私有声明，不 pub mod）
- [ ] `builders.rs:178` 装配点改造完成：read settings → helper → tracing::info!（disable_relays = iroh_config.disable_relays）→ IrohNodeConfig 注入
- [ ] `non_gui_runtime.rs:280` 装配点同模式改造
- [ ] `tracing::info!(target: "settings.network", ...)` 启动日志（D-B3 字段名规范）
- [ ] **【checker BLOCKER 4 — 铁律】** 全工程仅 `network_policy.rs` 含 `disable_relays = !` 取反；`builders.rs` / `non_gui_runtime.rs` 0 命中（grep 双重守护）
- [ ] **【checker BLOCKER 4 — 铁律】** tracing::info! 字段值通过 `iroh_config.disable_relays` 读取（grep 命中 2 处 `disable_relays = iroh_config.disable_relays`）
- [ ] **【checker WARNING 1】** 两文件 `applying network.allow_relay_fallback=` 启动日志命中 2 处
- [ ] `cargo check -p uc-bootstrap` 全过
- [ ] **决策 D-A1 / D-B1（修订后选项 B）/ D-B3 / Pitfall 1 / Pitfall 6 防御已写入 plan must_haves.truths**
- [ ] **【checker BLOCKER 3】** D-B1 / D-B2 选项 B 决议（接受现状 + 后续偿还）已写入 plan must_haves.truths 且 CONTEXT.md `<deferred>` 已增 follow-up 条目
</success_criteria>

<output>
After completion, create `.planning/phases/094-backend-network-allow-relay-fallback/094-05-SUMMARY.md` with:
- 实际改动的 LOC（network_policy.rs 全新建 + lib.rs 1 行 + builders.rs / non_gui_runtime.rs 各一段）
- 3 个 truth-table 测试 pass 输出
- D-B1 选项 A 现状决策的最终验证：grep 全工程 `wired.deps.settings.load()` 错误处理路径，确认 NotFound 不在分流分支
- D-B3 启动日志格式定稿：实际代码用 `target: "settings.network"`，字段名 `allow_relay_fallback` / `disable_relays`
- Pitfall 1 grep 守护结果（去注释后唯一一处取反）
- Pitfall 6 grep 守护结果（network_policy.rs 不含 OTLP 引用）
- 给 plan 06（uc-infra integration test + OnceCell 守护）的"启动期 IrohNodeConfig 已正确翻译"信号
- 留待 ROADMAP success criterion #1 手工验收的范围（添加 `network.allow_relay_fallback: false` → 重启 → 启动日志 + Endpoint::addr().addrs 不含 Relay）
</output>
