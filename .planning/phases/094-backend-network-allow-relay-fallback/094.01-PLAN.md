---
phase: 094-backend-network-allow-relay-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-core/src/settings/model.rs
  - src-tauri/crates/uc-core/src/settings/defaults.rs
autonomous: true
requirements:
  - NETSET-01
  - NETSET-02
tags:
  - uc-core
  - settings
  - network
  - lan-only

must_haves:
  truths:
    - "缺 `network` 段的旧 settings.json 反序列化后 `settings.network.allow_relay_fallback == true`（手写 Default + #[serde(default)] 双重兜底，Pitfall 2 防御）"
    - "新版默认 settings 含 `network.allow_relay_fallback: true`（兼容存量用户跨网段同步）"
    - "schema_version 数值不变（保持 1，不触发 migration codepath；空 migrations vec 保持空）"
    - "全工程除 `uc-bootstrap/src/network_policy.rs`（取反点）+ `uc-infra/src/network/iroh/node.rs:153-162`（infra 字段定义）+ 测试文件外，无 `disable_relays = !` 类反向写法（Pitfall 1 防御铁律）"
    - "`network` 字段不与 `general.telemetry_enabled` 联动（Pitfall 6 防御 - 字段独立流动）"
    - "决策 D-B1（修订后选项 B）：Phase 94 不实施 D-B1 + D-B2 的 port 错误细分；NotFound → default 的语义实质由 `uc-infra::FileSettingsRepository::load` (`repository.rs:166-168`) 的 NotFound 兜底**作为 SettingsPort 实现的隐式契约一部分**承担；`SettingsPort::load` 当前错误返回类型 `anyhow::Result<Settings>` 保持不变。CONTEXT.md `<deferred>` 已记录后续 phase 必须偿还（实施 `SettingsLoadError` discriminated enum）。本 plan 不改 port 签名"
  artifacts:
    - path: "src-tauri/crates/uc-core/src/settings/model.rs"
      provides: "`NetworkSettings { allow_relay_fallback: bool }` 结构定义 + `default_allow_relay_fallback() -> bool` helper + `Settings.network` 顶层字段挂载"
      contains: "pub struct NetworkSettings"
    - path: "src-tauri/crates/uc-core/src/settings/defaults.rs"
      provides: "手写 `impl Default for NetworkSettings` （禁止 #[derive(Default)]）+ `Settings::default()` 字段列表追加 `network`"
      contains: "impl Default for NetworkSettings"
  key_links:
    - from: "src-tauri/crates/uc-core/src/settings/model.rs"
      to: "src-tauri/crates/uc-core/src/settings/defaults.rs"
      via: "Default impl 引用 + Settings::default() 字段挂载"
      pattern: "impl Default for NetworkSettings"
    - from: "src-tauri/crates/uc-core/src/settings/model.rs `Settings.network`"
      to: "缺 `network` 段的旧 JSON"
      via: "serde 反序列化 fallback"
      pattern: "#\\[serde\\(default\\)\\][\\s]+pub network: NetworkSettings"
---

<objective>
在 `uc-core::settings` 模块中新增 `NetworkSettings` 值对象与 `Settings.network` 字段，以承载用户的"是否允许 iroh 公网中继回落"业务偏好。

Purpose: 这是 Phase 94 整个依赖图的源头 —— 没有这个类型，application 层的 view/patch、daemon-contract 的 DTO、bootstrap 的取反 helper 全部无处可挂。NETSET-01 / NETSET-02 的"用户可读写持久化字段 + 缺段时回填默认 true"硬约束在本 plan 完整落地。

Output:
- `NetworkSettings` 结构体（`Debug, Clone, Serialize, Deserialize, PartialEq, Eq`，禁止 `Default` derive）
- 字段级 default helper `default_allow_relay_fallback() -> bool { true }`
- 手写 `impl Default for NetworkSettings`（`defaults.rs`，含 3 行警示注释）
- `Settings.network: NetworkSettings` 顶层字段（`#[serde(default)]`）
- `Settings::default()` 字段列表追加 `network`
- schema_version **不动**（保持 1，新增字段全部走 serde default 兜底）
- 老 settings.json 反序列化向后兼容单元测试（缺 `network` 段断言 `== true`）
- truth-table 单元测试（`NetworkSettings::default().allow_relay_fallback == true`、自定义构造 false 断言保留语义）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

# 受影响代码
@src-tauri/crates/uc-core/src/settings/model.rs
@src-tauri/crates/uc-core/src/settings/defaults.rs

<interfaces>
<!-- 现有 settings 子结构 + 默认值模式（同模式镜像即可） -->
<!-- 来源：src-tauri/crates/uc-core/src/settings/model.rs:166-203 + defaults.rs:205-262 -->

现有可类比子结构（`FileSyncSettings`，model.rs:166-174）：
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct FileSyncSettings {
    pub file_sync_enabled: bool,
    pub small_file_threshold: u64,
    pub max_file_size: u64,
    pub file_cache_quota_per_device: u64,
    pub file_retention_hours: u32,
    pub file_auto_cleanup: bool,
}
```

现有 default helper（model.rs:29-31）：
```rust
fn default_telemetry_enabled() -> bool {
    true
}
```

现有 Settings 顶层挂载（model.rs:177-203，注意 201-202 是占位注释）：
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Settings {
    #[serde(default = "current_schema_version")]
    pub schema_version: u32,
    // ... 已有字段省略 ...
    #[serde(default)]
    pub file_sync: FileSyncSettings,
    // #[serde(default)]
    // pub network: NetworkSettings,    ← 占位注释（取消注释 + 真实定义在新 struct 之后）
}
```

现有手写 Default 模式（defaults.rs:205-225 + 251-262）：
```rust
impl Default for FileSyncSettings {
    fn default() -> Self {
        Self {
            file_sync_enabled: true,
            // ...
        }
    }
}

impl Default for Settings {
    fn default() -> Self {
        Self {
            schema_version: CURRENT_SCHEMA_VERSION,
            // ...
            file_sync: FileSyncSettings::default(),
            // ← 新增 network: NetworkSettings::default(),
        }
    }
}
```

`SettingsPort` 现有签名（src-tauri/crates/uc-core/src/ports/settings.rs，**本 plan 不动**）：
```rust
#[async_trait]
pub trait SettingsPort: Send + Sync {
    async fn load(&self) -> anyhow::Result<Settings>;
    async fn save(&self, settings: &Settings) -> anyhow::Result<()>;
}
```
（D-B1 决策修订为选项 B — 接受现状不动 port 错误类型；后续 phase 偿还，见 CONTEXT.md `<deferred>`）

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 uc-core 新增 NetworkSettings 类型 + Settings.network 字段（含字段级 default helper）</name>
  <files>src-tauri/crates/uc-core/src/settings/model.rs</files>

  <read_first>
    1. `src-tauri/crates/uc-core/src/settings/model.rs`（必读完整文件，重点 1-7、29-31、166-203 的占位注释行）
    2. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` 段 D-A1 / D-B1（取反点位置 + Settings 加载错误分流策略）
    3. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §1（uc-core/src/settings/model.rs 模式映射）+ §跨切面 Pattern B（serde(default) + 手写 Default）
    4. `.planning/research/PITFALLS.md` Pitfall 2（默认值倒置防御 — `bool::default() == false` 极度危险，必须显式手写 + serde(default = "fn") 双兜底）
    5. `.planning/research/STACK.md` §2.2（`network` namespace 扩展模式 — schema_version **不**升、`#[serde(default)]` 兜底）
  </read_first>

  <behavior>
    - Test 1: 默认构造 `NetworkSettings::default().allow_relay_fallback == true`
    - Test 2: `default_allow_relay_fallback()` 字面量返回 `true`
    - Test 3: 缺 `network` 段的旧 JSON 反序列化为 `Settings` 后 `settings.network.allow_relay_fallback == true`（核心向后兼容断言）
    - Test 4: schema_version 不动 — `serde_json::from_str("{}").unwrap()` 后 `settings.schema_version == 1`
    - Test 5: 显式 JSON `{"network":{"allow_relay_fallback": false}, "schema_version": 1}` 反序列化后字段断言 `== false`（保留语义不取反）
  </behavior>

  <action>
在 `src-tauri/crates/uc-core/src/settings/model.rs` 中执行以下精确改动：

**改动点 1（占位注释下方约 line 175，紧贴 `FileSyncSettings` 之后）：** 新增 `NetworkSettings` 结构体定义。完整代码：

```rust
// ======================================================================
// NetworkSettings —— LAN-only Mode（v0.7.0）的后端持久化字段
//
// 反向命名规则（Pitfall 1 防御 — 见 .planning/research/PITFALLS.md §Pitfall 1）：
// - UI = "LAN-only Mode = ON"
// - 后端 = `allow_relay_fallback = false`
// - infra (iroh) = `IrohNodeConfig.disable_relays = true`
//
// 三层语义两次反转。**全工程只允许在 `uc-bootstrap/src/network_policy.rs`
// 唯一一处取反**；DTO ↔ View ↔ core 三层只搬运 `allow_relay_fallback`
// 业务正向语义，不取反。
// ======================================================================

/// 网络相关设置（LAN-only Mode 字段族）。
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct NetworkSettings {
    /// 是否允许 iroh 在直连失败时回落到公网中继。
    /// `true`（默认）= 允许 fallback，跨网段设备仍可通过 relay 同步；
    /// `false` = LAN-only，禁用 relay，跨网段设备会失联。
    /// 业务正向语义：UI "LAN-only Mode = ON" → 此字段 = `false`。
    #[serde(default = "default_allow_relay_fallback")]
    pub allow_relay_fallback: bool,
}

// 默认 true = 允许 fallback。
// 改成 false 会让所有跨网段老用户突然离线，属于 breaking change。
// 修改默认值前请先 grep `LAN-only Mode` 文档与 changelog。
fn default_allow_relay_fallback() -> bool {
    true
}
```

**改动点 2（line 199-203，将占位注释替换为真实字段）：** 把 `Settings` 结构体中的占位注释：
```rust
    #[serde(default)]
    pub file_sync: FileSyncSettings,
    // #[serde(default)]
    // pub network: NetworkSettings,
```
改为：
```rust
    #[serde(default)]
    pub file_sync: FileSyncSettings,

    #[serde(default)]
    pub network: NetworkSettings,
```

**禁止动作**（Pitfall 1 / 2 / 6 防御）：
- ❌ 禁止给 `NetworkSettings` 写 `#[derive(Default)]`（`bool::default() == false` 极度危险，老用户跨网段同步集体崩盘）
- ❌ 禁止重命名字段为 `lan_only` / `disable_relay` / `LAN-only` 等反向语义（DTO 层、TS 层全部以 `allow_relay_fallback` 流动，反向命名只允许在 `uc-bootstrap/src/network_policy.rs` 一处）
- ❌ 禁止把 `network.allow_relay_fallback` 与 `general.telemetry_enabled` 联动（OTLP 独立由 `general.telemetry_enabled` 控制，Pitfall 6）
- ❌ 禁止 bump `CURRENT_SCHEMA_VERSION`（保持 `1`；`#[serde(default)]` 已覆盖向后兼容；bumping 反而触发不必要 migration codepath）
- ❌ 禁止在 `model.rs` 直接写 `impl Default for NetworkSettings`（按既有惯例，所有 default impl 集中放在 `defaults.rs`，见下一 task）

**注释规范：** 上述代码块的中文注释（含警示三行）逐字保留，其它实现细节（trait derive 顺序）按现有 `FileSyncSettings`（model.rs:166-174）模式镜像。
  </action>

  <verify>
    <automated>cargo check -p uc-core 2>&amp;1 | tail -20</automated>
    <automated>cargo test -p uc-core --lib settings::model 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-core` 退出码 0
    - `rg -n 'pub struct NetworkSettings' src-tauri/crates/uc-core/src/settings/model.rs` 返回 1 行
    - `rg -n 'fn default_allow_relay_fallback' src-tauri/crates/uc-core/src/settings/model.rs` 返回 1 行
    - `rg -n '#\[derive\(.*Default.*\)\].*\nstruct NetworkSettings' src-tauri/crates/uc-core/src/settings/model.rs --multiline` 返回 0 行（**不允许 derive Default**）
    - `rg -n 'pub network: NetworkSettings' src-tauri/crates/uc-core/src/settings/model.rs` 返回 1 行
    - `rg -n 'pub const CURRENT_SCHEMA_VERSION: u32 = 1;' src-tauri/crates/uc-core/src/settings/model.rs` 返回 1 行（确认 schema_version 未升）
    - `rg -n '// pub network: NetworkSettings' src-tauri/crates/uc-core/src/settings/model.rs` 返回 0 行（占位注释已被替换）
  </acceptance_criteria>

  <done>
NetworkSettings 类型 + default helper + Settings.network 字段已就位，schema_version 不变，占位注释已替换为真实字段。`cargo check -p uc-core` 通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 defaults.rs 手写 impl Default for NetworkSettings + Settings::default 追加 network 字段 + 向后兼容反序列化测试</name>
  <files>
    src-tauri/crates/uc-core/src/settings/defaults.rs
  </files>

  <read_first>
    1. `src-tauri/crates/uc-core/src/settings/defaults.rs`（必读完整文件，重点 205-262 行 `FileSyncSettings::default` 与 `Settings::default` 模式）
    2. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §2（`defaults.rs` 模式映射）+ §跨切面 Pattern B
    3. `.planning/research/PITFALLS.md` Pitfall 2（默认值倒置 — 警示三行注释逐字照抄；老 JSON 反序列化测试断言 `== true`）
    4. Task 1 已新增的 `NetworkSettings` 结构体（model.rs）
  </read_first>

  <behavior>
    - Test 1: `NetworkSettings::default().allow_relay_fallback == true`（手写 Default 行为正确）
    - Test 2: `Settings::default().network.allow_relay_fallback == true`（顶层装配正确）
    - Test 3: `Settings::default().schema_version == 1`（schema_version 装配未变）
    - Test 4: `serde_json::from_str::<Settings>("{}").unwrap().network.allow_relay_fallback == true`（缺 network 段的最简 JSON 兜底）
    - Test 5: 老 v0.6.x 风格 JSON（含其它字段但缺 `network` 段）反序列化后 `settings.network.allow_relay_fallback == true` —— 这是 NETSET-02 success criterion #2 的硬约束
    - Test 6: 显式 false JSON 反序列化后保留 false 语义（不取反）
  </behavior>

  <action>
在 `src-tauri/crates/uc-core/src/settings/defaults.rs` 中执行以下精确改动：

**改动点 1（在 line 225 之后，紧贴 `impl Default for FileSyncSettings` 之后插入）：** 新增手写 `impl Default for NetworkSettings`，警示注释三行**逐字照抄** Pitfall 2 + CONTEXT.md `<specifics>` 第 4 条：

```rust
impl Default for NetworkSettings {
    /// Returns default `NetworkSettings` allowing iroh to fall back to public
    /// relays when direct connectivity fails (existing v0.6.x behavior).
    ///
    /// Defaults:
    /// - `allow_relay_fallback`: true
    ///
    // 默认 true = 允许 fallback。
    // 改成 false 会让所有跨网段老用户突然离线，属于 breaking change。
    // 修改默认值前请先 grep `LAN-only Mode` 文档与 changelog。
    fn default() -> Self {
        Self {
            allow_relay_fallback: true,
        }
    }
}
```

**改动点 2（line 251-262，`impl Default for Settings`）：** 在 `file_sync: FileSyncSettings::default(),` 后追加一行 `network: NetworkSettings::default(),`。最终块如下：

```rust
impl Default for Settings {
    /// Constructs a Settings instance populated with the current schema version
    /// and sensible nested defaults.
    fn default() -> Self {
        Self {
            schema_version: CURRENT_SCHEMA_VERSION,
            general: GeneralSettings::default(),
            sync: SyncSettings::default(),
            retention_policy: RetentionPolicy::default(),
            security: SecuritySettings::default(),
            pairing: PairingSettings::default(),
            keyboard_shortcuts: HashMap::new(),
            file_sync: FileSyncSettings::default(),
            network: NetworkSettings::default(),
        }
    }
}
```

**改动点 3（在 `defaults.rs` 文件末尾，紧贴 `impl Default for Settings {}` 之后）：** 新增 `#[cfg(test)] mod tests` 块，覆盖 Pitfall 2 防御与向后兼容断言。完整代码：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::settings::model::{NetworkSettings, Settings, CURRENT_SCHEMA_VERSION};

    /// Pitfall 2 防御：默认值必须为 true（允许 fallback），保护老用户
    /// 跨网段同步行为。改 false = breaking change。
    #[test]
    fn network_settings_default_allows_relay_fallback() {
        let n = NetworkSettings::default();
        assert!(
            n.allow_relay_fallback,
            "NetworkSettings::default().allow_relay_fallback MUST be true (Pitfall 2)"
        );
    }

    /// Pitfall 2 防御：顶层 Settings::default 把 network 装配进去，
    /// 字段值与子结构 default 保持一致。
    #[test]
    fn settings_default_includes_network_with_fallback_allowed() {
        let s = Settings::default();
        assert!(s.network.allow_relay_fallback);
        assert_eq!(s.schema_version, CURRENT_SCHEMA_VERSION);
        assert_eq!(s.schema_version, 1, "schema_version MUST stay 1 (no migration)");
    }

    /// NETSET-02 success criterion #2：缺 `network` 段的旧 settings.json
    /// 反序列化必须回填默认值 true。
    #[test]
    fn old_settings_json_without_network_section_falls_back_to_default() {
        // 模拟 v0.6.x 时代写出的 settings.json 片段（无 network 字段）
        let json = r#"{}"#;
        let s: Settings = serde_json::from_str(json).expect("parse minimal settings");
        assert!(
            s.network.allow_relay_fallback,
            "missing network section MUST default to true"
        );
        assert_eq!(s.schema_version, 1);
    }

    /// 显式 false 的 JSON 必须保留 false 语义（确认未误取反）。
    #[test]
    fn explicit_allow_relay_fallback_false_is_preserved() {
        let json = r#"{ "network": { "allow_relay_fallback": false } }"#;
        let s: Settings = serde_json::from_str(json).expect("parse explicit false");
        assert!(!s.network.allow_relay_fallback);
    }

    /// 反向断言：显式 true 也保留 true 语义。
    #[test]
    fn explicit_allow_relay_fallback_true_is_preserved() {
        let json = r#"{ "network": { "allow_relay_fallback": true } }"#;
        let s: Settings = serde_json::from_str(json).expect("parse explicit true");
        assert!(s.network.allow_relay_fallback);
    }
}
```

**依赖项确认：** `serde_json` 在 `uc-core/Cargo.toml` 的 `[dev-dependencies]` 中需可用 —— 若不存在则该 task 必须先在 `uc-core/Cargo.toml` 的 `[dev-dependencies]` 段加 `serde_json = { workspace = true }`（沿用 workspace 风格；如 workspace 未声明 serde_json，则用 `serde_json = "1"`）。注意：在执行测试前先运行 `cargo metadata -p uc-core --format-version 1 | grep serde_json` 验证。

**禁止动作：**
- ❌ 禁止使用 `#[derive(Default)]` 替代手写（Pitfall 2 防御）
- ❌ 禁止删除三行警示注释 —— 它是 PR review 的"红线", reviewer 在 grep 该注释时必须能找到
- ❌ 禁止改 `schema_version` 装配为其他常量
  </action>

  <verify>
    <automated>cargo test -p uc-core --lib settings::defaults::tests -- --nocapture 2>&amp;1 | tail -30</automated>
    <automated>cargo check -p uc-core 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-core` 退出码 0
    - `cargo test -p uc-core --lib settings::defaults::tests` 5 个测试全部通过：
      - `network_settings_default_allows_relay_fallback`
      - `settings_default_includes_network_with_fallback_allowed`
      - `old_settings_json_without_network_section_falls_back_to_default`
      - `explicit_allow_relay_fallback_false_is_preserved`
      - `explicit_allow_relay_fallback_true_is_preserved`
    - `rg -n 'impl Default for NetworkSettings' src-tauri/crates/uc-core/src/settings/defaults.rs` 返回 1 行
    - `rg -n 'network: NetworkSettings::default\(\),' src-tauri/crates/uc-core/src/settings/defaults.rs` 返回 1 行
    - `rg -n '默认 true = 允许 fallback' src-tauri/crates/uc-core/src/settings/defaults.rs` 返回 1 行（警示注释存在）
    - `rg -v '^//|^\s*//' src-tauri/crates/uc-core/src/settings/defaults.rs | grep -c 'derive(Default)' || true`：注释行过滤后无 derive Default for NetworkSettings
  </acceptance_criteria>

  <done>
`NetworkSettings::default()` 与 `Settings::default()` 装配正确，5 个反序列化/默认值单元测试全部通过。Pitfall 2 防御：警示三行注释就位、`#[derive(Default)]` 未被使用。NETSET-02 success criterion #2"老 JSON 反序列化字段断言 == true"已自动覆盖。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.json 文件 → uc-core::Settings | 已在磁盘存在的 JSON 内容反序列化为内存对象，跨"持久化层 → 领域层"边界 |
| code review 边界 | 默认值是不变量级约束，PR 改动 default 是结构性 breaking change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094.01-01 | Tampering | settings.json 反序列化 → `NetworkSettings` | mitigate | `#[serde(default = "default_allow_relay_fallback")]` 字段级 + `#[serde(default)]` 顶层 + 手写 `impl Default` 三重兜底；显式 false 的篡改不会破坏向后兼容（仅按用户原意 LAN-only=ON），显式 true 同理 |
| T-094.01-02 | Information Disclosure | 字段名暴露 LAN-only 内部反向语义 | accept | 字段名 `allow_relay_fallback` 是业务正向语义，与 OS keychain / 密钥物料无关；仅是网络策略偏好，无 PII |
| T-094.01-03 | Denial of Service | `#[derive(Default)]` 误用导致 `bool::default() == false` 致使老用户跨网段同步全断 | mitigate | 强制手写 `impl Default for NetworkSettings`（Pitfall 2 防御）+ 单元测试 `network_settings_default_allows_relay_fallback` 自动断言 + 警示三行注释让 reviewer 一眼看到 |
| T-094.01-04 | Repudiation | 用户改 default 后无 audit trail | accept | 默认值 = breaking change 由 git 提交记录留痕；plan 05 在 daemon 启动时打 `tracing::info!` 记录 effective 值 |
| T-094.01-05 | Spoofing | 反向命名让伪造方向的 PR 不被编译器捕捉 | mitigate | grep 守护（plan 05 的 PR review 必查）+ truth-table 单元测试（plan 05 的 `network_policy.rs`）+ 本 plan 的注释明示反向规则 |
| T-094.01-06 | Elevation of Privilege | OTLP 联动 LAN-only 暗自关闭遥测 | mitigate | 警示注释 + Pitfall 6 防御（`network` 模块禁止引用 `general.telemetry_enabled`）；本 plan 不会引入此联动 |

无 `high` 严重等级威胁；现有缓解措施已全覆盖。
</threat_model>

<verification>
- `cargo check -p uc-core` 通过
- `cargo test -p uc-core --lib settings` 全部通过（含本 plan 5 个新增测试）
- `cargo build -p uc-core --release` 不引入新依赖（仅复用 serde / serde_with 既有派生）
- grep 守护：
  - `rg -n 'pub struct NetworkSettings' src-tauri/` 仅命中 `uc-core/src/settings/model.rs`（一处定义）
  - `rg -n 'allow_relay_fallback' src-tauri/crates/uc-core/` 命中 `model.rs`、`defaults.rs`（含测试）共 ≤ 5 处，无 `!allow_relay_fallback` 反向写法
  - `rg -n '#\[derive\(.*Default.*\)\]\s*\npub struct NetworkSettings' src-tauri/ --multiline` 无任何匹配
</verification>

<success_criteria>
- [ ] `NetworkSettings { allow_relay_fallback: bool }` 类型定义在 `uc-core/src/settings/model.rs`
- [ ] `Settings.network` 顶层字段挂载（`#[serde(default)]`），占位注释已替换
- [ ] `default_allow_relay_fallback() -> bool { true }` helper 存在，字段级 `#[serde(default = "...")]` 引用
- [ ] **`#[derive(Default)]` 在 `NetworkSettings` 上 NOT 出现**（Pitfall 2 防御铁律）
- [ ] 手写 `impl Default for NetworkSettings` 在 `defaults.rs` 存在，含警示三行注释（`grep '默认 true = 允许 fallback'` 命中）
- [ ] `Settings::default()` 在 `defaults.rs` 字段列表追加 `network: NetworkSettings::default()`
- [ ] `CURRENT_SCHEMA_VERSION = 1` 不变（确认无 migration codepath 触发）
- [ ] 5 个新单元测试全部通过：默认值断言 + Settings 装配断言 + 缺 network 段反序列化断言 + 显式 true/false 保留语义双向断言
- [ ] `cargo check -p uc-core` 退出码 0
- [ ] 全工程除 `model.rs` / `defaults.rs` / 测试外无 `allow_relay_fallback` 引用（`rg` 校验）
- [ ] **D-A1 / D-B1（选项 A）/ Pitfall 1 / Pitfall 2 / Pitfall 6 防御决策已写入 plan must_haves.truths**
</success_criteria>

<output>
After completion, create `.planning/phases/094-backend-network-allow-relay-fallback/094-01-SUMMARY.md` with:
- 实际改动的 LOC（model.rs / defaults.rs 各两段）
- 5 个新单元测试的 pass 输出（截取关键行）
- 是否存在任何对 schema_version、`#[derive(Default)]`、警示注释的偏离（应为 0）
- 留给下游（plan 02 application、plan 03 daemon-contract、plan 05 bootstrap）的"NetworkSettings 已就位"信号
- 任何在执行中发现的现状偏离 PATTERNS.md 的情况（如 default helper 命名、注释位置）
</output>
