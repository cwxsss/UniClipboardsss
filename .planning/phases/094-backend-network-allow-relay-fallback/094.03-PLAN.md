---
phase: 094-backend-network-allow-relay-fallback
plan: 03
type: execute
wave: 2
depends_on:
  - "094-01"
files_modified:
  - src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs
  - src-tauri/crates/uc-webserver/src/api/openapi.rs
autonomous: true
requirements:
  - NETSET-01
  - NETSET-02
tags:
  - uc-daemon-contract
  - dto
  - openapi
  - lan-only

must_haves:
  truths:
    - "`NetworkSettingsDto { allow_relay_fallback: bool }` + `NetworkSettingsPatchDto { allow_relay_fallback: Option<bool> }` DTO 镜像在 daemon-contract 层（wire 字段 `allowRelayFallback` 走 camelCase 自动转换 — Pattern D）"
    - "`SettingsDto.network: NetworkSettingsDto` + `SettingsPatchDto.network: Option<NetworkSettingsPatchDto>` 顶层挂载"
    - "`From<core::NetworkSettings> for NetworkSettingsDto` 自动派生映射 + `SettingsDto::from(core::Settings)` 末段补 `network` 映射"
    - "`UpdateSettingsResponse` DTO 加 `restart_required: bool` 字段（D-D1 / D-D3 — wire 契约扩展，camelCase wire = `restartRequired`）"
    - "OpenAPI schema 注册新 DTO（`NetworkSettingsDto` 显式加入 `components::schemas`）；`UpdateSettingsResponse` 已在列表，字段变更自动反映"
    - "DTO 字段名 `allow_relay_fallback` 与 core / view 一致（Pitfall 1 防御 — 不在 DTO 层重命名为 `lan_only` 等反向语义）"
    - "`UpdateSettingsResponse.success` / `data` / `ts` 现有字段保留（不破坏现有 wire 契约）；`restart_required` 是新增字段（向后兼容旧客户端可忽略）"
    - "**【checker BLOCKER 5】** `SettingsPatchDto` 与 `NetworkSettingsPatchDto` 加 `derive(Default)`，让 plan 04 测试 baseline 用 `..Default::default()` 简化（patch 全 None = 无操作语义无歧义）；`NetworkSettingsDto`（非 patch）+ `UpdateSettingsResponse` 仍禁 `Default` derive（前者 bool 默认值反向危险，后者 restart_required 默认 false 误导）"
    - "**【checker WARNING 3】** PUT body `{}` 反序列化为 `SettingsPatchDto` 全字段 None，与 Phase 94 之前 wire 行为一致（NETSET-02 #2 向后兼容硬约束）"
  artifacts:
    - path: "src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs"
      provides: "NetworkSettingsDto / NetworkSettingsPatchDto / SettingsDto.network / SettingsPatchDto.network / From<core::NetworkSettings> / UpdateSettingsResponse.restart_required"
      contains: "pub struct NetworkSettingsDto"
    - path: "src-tauri/crates/uc-webserver/src/api/openapi.rs"
      provides: "OpenAPI schemas 注册含 NetworkSettingsDto"
      contains: "NetworkSettingsDto"
  key_links:
    - from: "uc-daemon-contract::SettingsDto.network"
      to: "uc-core::Settings.network (plan 01)"
      via: "From<core::NetworkSettings> for NetworkSettingsDto"
      pattern: "impl From<core::NetworkSettings> for NetworkSettingsDto"
    - from: "uc-daemon-contract::UpdateSettingsResponse.restart_required"
      to: "前端 daemon client (Phase 95 消费)"
      via: "PUT /settings response wire 契约"
      pattern: "pub restart_required: bool"
    - from: "uc-webserver::ApiDoc components::schemas"
      to: "NetworkSettingsDto"
      via: "OpenAPI 注册列表"
      pattern: "NetworkSettingsDto,"
---

<objective>
在 `uc-daemon-contract::api::dto::settings` 层新增 `NetworkSettingsDto` / `NetworkSettingsPatchDto` DTO 镜像，挂载到 `SettingsDto` / `SettingsPatchDto` 顶层；新增 `From<core::NetworkSettings> for NetworkSettingsDto` 与 `SettingsDto` 末段映射；并把 `UpdateSettingsResponse` 加 `restart_required: bool` 字段（D-D1 / D-D3 wire 契约扩展）。OpenAPI `components::schemas` 同步注册 `NetworkSettingsDto`。

Purpose: daemon-contract 是 daemon HTTP 协议的 wire 真相源，前端 TS 类型直接 import；不在此处补 DTO，前端 PUT `/settings` 传 `network` 段时 422 或字段被忽略，无法验证 NETSET-02 success criterion #2。`UpdateSettingsResponse.restart_required` 是 Pitfall 3 结构性防御 —— 调用方显式承担"还没真正生效"的事实，Phase 94 暴露 wire 契约，Phase 95 前端消费。

Output:
- `NetworkSettingsDto` 结构体（`Debug, Clone, Serialize, Deserialize, PartialEq, Eq, ToSchema` + `#[serde(rename_all = "camelCase")]`）
- `NetworkSettingsPatchDto` 结构体（同 derive，字段 `Option<bool>`）
- `SettingsDto.network: NetworkSettingsDto` + `SettingsPatchDto.network: Option<NetworkSettingsPatchDto>`
- `impl From<core::NetworkSettings> for NetworkSettingsDto`
- `From<core::Settings> for SettingsDto` 末段补 `network: value.network.into(),`
- `UpdateSettingsResponse.restart_required: bool` 字段
- OpenAPI `components::schemas` 列表追加 `NetworkSettingsDto`
- DTO 单元测试：camelCase 序列化 + 反序列化 + 缺字段默认值（兜底）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md

# 受影响代码
@src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs
@src-tauri/crates/uc-webserver/src/api/openapi.rs

<interfaces>
<!-- 现有 DTO 镜像 + UpdateSettingsResponse + From 映射 -->
<!-- 来源：src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs -->

现有 UpdateSettingsResponse（line 17-23）：
```rust
#[derive(Debug, Serialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct UpdateSettingsResponse {
    pub success: bool,
    pub data: SettingsDto,
    pub ts: i64,
}
```

现有 FileSyncSettingsDto（line 186-195，镜像模板）：
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct FileSyncSettingsDto {
    pub file_sync_enabled: bool,
    // ...
}
```

现有 FileSyncSettingsPatchDto（line 288-297）：
```rust
#[derive(Debug, Clone, Serialize, Deserialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct FileSyncSettingsPatchDto {
    pub file_sync_enabled: Option<bool>,
    // ...
}
```

现有 SettingsDto 顶层（line 197-208）：
```rust
#[derive(Debug, Clone, Serialize, Deserialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct SettingsDto {
    pub schema_version: u32,
    // ... 已有字段省略 ...
    pub file_sync: FileSyncSettingsDto,
}
```

现有 SettingsPatchDto 顶层（line 304-314）：
```rust
#[derive(Debug, Clone, Serialize, Deserialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct SettingsPatchDto {
    pub general: Option<GeneralSettingsPatchDto>,
    // ... 其它字段省略 ...
    pub file_sync: Option<FileSyncSettingsPatchDto>,
}
```

现有 From<core::FileSyncSettings>（line 458-468）：
```rust
impl From<core::FileSyncSettings> for FileSyncSettingsDto {
    fn from(value: core::FileSyncSettings) -> Self {
        Self {
            file_sync_enabled: value.file_sync_enabled,
            // ...
            file_auto_cleanup: value.file_auto_cleanup,
        }
    }
}
```

现有 SettingsDto 末段映射（line 536-553）：
```rust
impl From<core::Settings> for SettingsDto {
    fn from(value: core::Settings) -> Self {
        Self {
            schema_version: value.schema_version,
            // ...
            file_sync: value.file_sync.into(),
        }
    }
}
```

现有 OpenAPI schemas 列表（uc-webserver/src/api/openapi.rs:122-141，FileSyncSettingsDto 在 line 138）：
```rust
        schemas(
            // Common
            ContentTypesDto,
            // ...
            GetSettingsResponse,
            UpdateSettingsResponse,
            SettingsDto,
            // ...
            FileSyncSettingsDto,
            ShortcutKeyDto,
            // ...
        )
```

现有 OpenAPI import 列表（line 28-33）：
```rust
use crate::api::dto::settings::{
    ContentTypesDto, FileSyncSettingsDto, GeneralSettingsDto, GetSettingsResponse,
    PairingSettingsDto, RetentionPolicyDto, RetentionRuleDto, RuleEvaluationDto,
    SecuritySettingsDto, SettingsDto, ShortcutKeyDto, SyncFrequencyDto, SyncSettingsDto, ThemeDto,
    UpdateChannelDto, UpdateSettingsResponse,
};
```

注意：webserver 通过 `crate::api::dto::settings` 间接 re-export `uc_daemon_contract::api::dto::settings::*`（见 `uc-webserver/src/api/dto/mod.rs`）。daemon-contract 加新类型，webserver 自动可见。

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 daemon-contract DTO 文件新增 NetworkSettingsDto / NetworkSettingsPatchDto + 顶层挂载 + From 映射 + UpdateSettingsResponse.restart_required</name>
  <files>src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs</files>

  <read_first>
    1. `src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs`（必读完整文件，重点 line 17-23 UpdateSettingsResponse、186-195 FileSyncSettingsDto、288-297 FileSyncSettingsPatchDto、197-208 SettingsDto、217-227 GeneralSettingsPatchDto、304-314 SettingsPatchDto、458-468 From<core::FileSyncSettings>、536-553 SettingsDto::from(core::Settings)）
    2. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` D-D1 / D-D3（restart_required wire 契约 + OpenAPI schema 同步）
    3. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §5（DTO 模式映射）+ Pattern D（DTO `serde(rename_all = "camelCase")`）
    4. `.planning/research/PITFALLS.md` Pitfall 1（DTO 字段名 = core 字段名 = `allow_relay_fallback`，不允许重命名）+ Pitfall 3（`restart_required` 让调用方显式承担"还没真正生效"）
    5. `src-tauri/crates/uc-core/src/settings/model.rs`（plan 01 产出的 `NetworkSettings`）
    6. **【checker BLOCKER 5 引入必读】** 当前 `SettingsPatchDto` 定义（line 304-314）+ `GeneralSettingsPatchDto` 字段列表（line 217-227）—— 测试 baseline 用 `..Default::default()` 简化，本 task 同步给 `SettingsPatchDto` + `NetworkSettingsPatchDto` 加 `Default` derive；执行前先 grep `#\[derive\(.*Default.*\)\]` `src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs` 看哪些兄弟 *PatchDto 已有 `Default` derive，沿用一致策略
    7. **【checker WARNING 3 引入必读】** `FileSyncSettingsPatchDto` 现状 serde 注解（line 288-297）—— 验证 `NetworkSettingsPatchDto` 的 serde 行为与之一致；不引入额外 `#[serde(default)]` / `#[serde(skip_serializing_if = ...)]`，保持向后兼容（PUT body `{}` 行为与 Phase 94 之前一致）
  </read_first>

  <behavior>
    - Test 1: `NetworkSettingsDto { allow_relay_fallback: true }.serialize` 出 JSON `{"allowRelayFallback": true}`（camelCase wire）
    - Test 2: JSON `{"allowRelayFallback": false}` 反序列化为 `NetworkSettingsDto { allow_relay_fallback: false }`
    - Test 3: `From<core::NetworkSettings> for NetworkSettingsDto` 透明搬运（不取反）
    - Test 4: `SettingsDto::from(core::Settings::default())` 含 `network.allow_relay_fallback == true`（与 plan 01 默认值一致）
    - Test 5: `UpdateSettingsResponse { success: true, data: ..., ts: 0, restart_required: true }.serialize` 含 `restartRequired: true`（camelCase wire）
    - Test 6: 反序列化 `NetworkSettingsPatchDto` `{"allowRelayFallback": null}` 得到 `NetworkSettingsPatchDto { allow_relay_fallback: None }`（嵌套 None 走 Option 原生反序列化；旧 daemon-contract 模式没有 `#[serde(default)]`，沿用既有 sibling 行为）
    - Test 7（**checker BLOCKER 5 新增**）: `SettingsPatchDto::default()` 全字段 None，`NetworkSettingsPatchDto::default().allow_relay_fallback == None`（derive(Default) 行为正确，下游 plan 04 测试用 `..Default::default()` 不会引入隐藏赋值）
    - Test 8（**checker WARNING 3 新增 — 向后兼容硬断言**）: 反序列化 PUT body `{}` 为 `SettingsPatchDto`，所有顶层字段 == None（包括 `network`）；行为与 Phase 94 之前一致 —— 不引入新强制字段
  </behavior>

  <action>
在 `src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs` 中执行以下精确改动：

**改动点 1（line 23 闭合 `}` 之后，在 UpdateSettingsResponse 块下追加 `restart_required` 字段）：** 当前块：
```rust
#[derive(Debug, Serialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct UpdateSettingsResponse {
    pub success: bool,
    pub data: SettingsDto,
    pub ts: i64,
}
```
改为：
```rust
#[derive(Debug, Serialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct UpdateSettingsResponse {
    pub success: bool,
    pub data: SettingsDto,
    pub ts: i64,
    /// 表示本次 patch 涉及需要重启 daemon 才能生效的字段（目前仅 `network.*`）。
    /// 该字段由 webserver handler 内联计算（plan 04），不依赖 application facade
    /// 公共签名变更（D-D1 / Pitfall 3 防御 — 调用方显式承担"还没真正生效"）。
    pub restart_required: bool,
}
```

**改动点 2（line 195 之后，FileSyncSettingsDto 之后）：** 新增 `NetworkSettingsDto`：
```rust
/// LAN-only Mode（v0.7.0）DTO 镜像。
///
/// 反向命名规则（Pitfall 1）：业务正向语义 `allow_relay_fallback`，
/// 不在此层重命名为 `lan_only` 或类似镜像。wire 字段 = `allowRelayFallback`
/// （camelCase 自动转换）。取反唯一发生在 `uc-bootstrap/src/network_policy.rs`。
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct NetworkSettingsDto {
    pub allow_relay_fallback: bool,
}
```

**改动点 3（line 207 之后，SettingsDto 块内 `pub file_sync: FileSyncSettingsDto,` 之后追加一行）：**
```rust
    pub file_sync: FileSyncSettingsDto,
    pub network: NetworkSettingsDto,
}
```

**改动点 4（line 297 之后，FileSyncSettingsPatchDto 之后）：** 新增 `NetworkSettingsPatchDto`，含 `Default` derive（**checker BLOCKER 5** — 让 plan 04 测试 baseline 可写 `..Default::default()`）：
```rust
/// LAN-only Mode 字段 patch DTO 镜像 — `null` = 不修改。
#[derive(Debug, Clone, Default, Serialize, Deserialize, ToSchema)]
#[serde(rename_all = "camelCase")]
pub struct NetworkSettingsPatchDto {
    pub allow_relay_fallback: Option<bool>,
}
```

**改动点 5（line 304-314，`SettingsPatchDto`）：分为两步同时改 derive 和加字段。**

**5a：给 `SettingsPatchDto` 顶层加 `Default` derive（checker BLOCKER 5）。** 当前 derive 行（line 304）：
```rust
#[derive(Debug, Clone, Serialize, Deserialize, ToSchema)]
```
改为：
```rust
#[derive(Debug, Clone, Default, Serialize, Deserialize, ToSchema)]
```

**为什么 SettingsPatchDto 可以 derive(Default)：** 子字段全部是 `Option<...>`，`Option::default() == None`；语义即"全 None patch = 无操作 patch"，与 `SettingsPatch`（uc-application）的 `derive(Default)` 现状（models.rs:217）一致；与"core::NetworkSettings 禁用 derive(Default)"不冲突 —— 后者禁用是因为字段语义反转危险（bool::default==false → 老用户离线），而 patch 全 None 永远代表"无操作"，无歧义。

**5b：在 `SettingsPatchDto` 块内 `pub file_sync: Option<FileSyncSettingsPatchDto>,` 之后追加一行（原改动点 5）：**
```rust
    pub file_sync: Option<FileSyncSettingsPatchDto>,
    pub network: Option<NetworkSettingsPatchDto>,
}
```

**5c（依赖性 grep — 防止 break 现有调用方）：** 给 `SettingsPatchDto` 加 `Default` derive 不会破坏现有 `SettingsPatchDto { ... }` literal 构造方式（field-by-field 构造仍合法）；但允许新代码用 `..Default::default()` 简化。grep `rg -n 'SettingsPatchDto\s*\{' src-tauri/ --type rust` 确认现有调用方不受影响。

**改动点 6（line 468 之后，紧跟 `impl From<core::FileSyncSettings> for FileSyncSettingsDto` 之后）：** 新增 From 映射：
```rust
impl From<core::NetworkSettings> for NetworkSettingsDto {
    fn from(value: core::NetworkSettings) -> Self {
        Self {
            allow_relay_fallback: value.allow_relay_fallback,
        }
    }
}
```

**改动点 7（line 550 附近，`From<core::Settings> for SettingsDto` 块内 `file_sync: value.file_sync.into(),` 之后追加一行）：**
```rust
            file_sync: value.file_sync.into(),
            network: value.network.into(),
        }
    }
}
```

**改动点 8（在文件末尾追加 `#[cfg(test)] mod tests` 块；如已存在 tests 模块则在末尾追加测试函数）：** 完整代码：
```rust
#[cfg(test)]
mod network_dto_tests {
    use super::*;

    #[test]
    fn dto_serializes_camel_case_wire() {
        let dto = NetworkSettingsDto { allow_relay_fallback: true };
        let json = serde_json::to_string(&dto).expect("serialize");
        assert_eq!(json, r#"{"allowRelayFallback":true}"#);
    }

    #[test]
    fn dto_deserializes_camel_case_wire() {
        let json = r#"{"allowRelayFallback":false}"#;
        let dto: NetworkSettingsDto = serde_json::from_str(json).expect("deserialize");
        assert!(!dto.allow_relay_fallback);
    }

    #[test]
    fn from_core_passes_through_business_semantics() {
        let core_value = core::NetworkSettings { allow_relay_fallback: false };
        let dto: NetworkSettingsDto = core_value.into();
        assert!(!dto.allow_relay_fallback, "DTO MUST NOT invert semantics (Pitfall 1)");
    }

    #[test]
    fn settings_dto_default_includes_network() {
        let core_settings = core::Settings::default();
        let dto: SettingsDto = core_settings.into();
        assert!(dto.network.allow_relay_fallback, "Settings::default network MUST be true");
    }

    #[test]
    fn update_settings_response_serializes_restart_required_camel_case() {
        let resp = UpdateSettingsResponse {
            success: true,
            data: SettingsDto::from(core::Settings::default()),
            ts: 0,
            restart_required: true,
        };
        let json = serde_json::to_string(&resp).expect("serialize");
        assert!(
            json.contains(r#""restartRequired":true"#),
            "wire field MUST be camelCase: got {json}"
        );
    }

    #[test]
    fn patch_dto_with_null_field_means_none() {
        let json = r#"{"allowRelayFallback":null}"#;
        let dto: NetworkSettingsPatchDto = serde_json::from_str(json).expect("deserialize");
        assert!(dto.allow_relay_fallback.is_none());
    }

    #[test]
    fn patch_dto_with_explicit_false() {
        let json = r#"{"allowRelayFallback":false}"#;
        let dto: NetworkSettingsPatchDto = serde_json::from_str(json).expect("deserialize");
        assert_eq!(dto.allow_relay_fallback, Some(false));
    }

    /// checker BLOCKER 5：`SettingsPatchDto::default()` 全字段 None，
    /// 让下游 plan 04 测试用 `..Default::default()` 简化 baseline 构造。
    #[test]
    fn settings_patch_dto_default_is_all_none() {
        let dto = SettingsPatchDto::default();
        assert!(dto.general.is_none());
        assert!(dto.sync.is_none());
        assert!(dto.retention_policy.is_none());
        assert!(dto.security.is_none());
        assert!(dto.pairing.is_none());
        assert!(dto.keyboard_shortcuts.is_none());
        assert!(dto.file_sync.is_none());
        assert!(dto.network.is_none());

        let net_patch = NetworkSettingsPatchDto::default();
        assert!(net_patch.allow_relay_fallback.is_none());
    }

    /// checker WARNING 3：向后兼容硬断言 —— PUT body `{}` 反序列化所有
    /// 顶层字段全 None；与 Phase 94 之前一致，没有引入新强制字段。
    #[test]
    fn settings_patch_dto_deserializes_empty_object_to_all_none() {
        let json = r#"{}"#;
        let dto: SettingsPatchDto = serde_json::from_str(json).expect("deserialize empty body");
        assert!(dto.general.is_none());
        assert!(dto.network.is_none());
        assert!(dto.file_sync.is_none());
    }
}
```

**禁止动作（Pitfall 1 / 3 / 6 防御）：**
- ❌ 禁止把 DTO 字段重命名为 `lan_only` / `disable_relay` / `LANOnly` 等反向语义
- ❌ 禁止把 `restart_required` 用 `Option<bool>` 包裹（必须是 `bool` — 总是返回明确 true/false 信号；`Option` 会让客户端二次猜测）
- ❌ 禁止给 `UpdateSettingsResponse` 加 `Default` derive（生成默认 `restart_required = false` 危险）—— 注意：`SettingsPatchDto` 与 `NetworkSettingsPatchDto` 的 `Default` derive 是 patch 专用语义（全 None = 无操作）允许，与 `UpdateSettingsResponse` 的禁用不冲突
- ❌ 禁止在 DTO 层引用 `general.telemetry_enabled` 与 `network.*` 联动（Pitfall 6）
- ❌ 禁止在 `restart_required` 计算逻辑（plan 04 webserver）流入 daemon-contract DTO 文件（DTO 是纯结构，逻辑不进 contract crate）
- ❌ 禁止给 `NetworkSettingsDto`（非 patch）加 `Default` derive（与 `core::NetworkSettings` 同理 — `bool::default()==false` 危险）
  </action>

  <verify>
    <automated>cargo check -p uc-daemon-contract 2>&amp;1 | tail -10</automated>
    <automated>cargo test -p uc-daemon-contract --lib api::dto::settings::network_dto_tests -- --nocapture 2>&amp;1 | tail -30</automated>
    <automated>cargo check -p uc-webserver 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-daemon-contract` 退出码 0
    - `cargo check -p uc-webserver` 退出码 0（沿用 re-export 路径，确认未 break 现有 import）
    - 9 个新测试全部通过（含 checker BLOCKER 5 / WARNING 3 引入的 `settings_patch_dto_default_is_all_none` + `settings_patch_dto_deserializes_empty_object_to_all_none`）
    - `rg -n 'pub struct NetworkSettingsDto' src-tauri/crates/uc-daemon-contract/` 命中 1 处
    - `rg -n 'pub struct NetworkSettingsPatchDto' src-tauri/crates/uc-daemon-contract/` 命中 1 处
    - `rg -n 'pub network: NetworkSettingsDto' src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs` 命中 1 处
    - `rg -n 'pub network: Option<NetworkSettingsPatchDto>' src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs` 命中 1 处
    - `rg -n 'pub restart_required: bool' src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs` 命中 1 处
    - `rg -n 'impl From<core::NetworkSettings> for NetworkSettingsDto' src-tauri/crates/uc-daemon-contract/` 命中 1 处
    - `rg -n 'network: value.network.into\(\)' src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs` 命中 1 处
    - `rg -n '!\s*value.network.allow_relay_fallback' src-tauri/crates/uc-daemon-contract/` 返回 0（无反向写法）
    - **【checker BLOCKER 5】** `rg -n '#\[derive\(.*Default.*\)\]' src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs | rg -c 'Default'` ≥ 2（`SettingsPatchDto` + `NetworkSettingsPatchDto` 各 1）
    - **【checker BLOCKER 5】** `rg -n '#\[derive\(.*Default.*\)\][\s\n]+pub struct NetworkSettingsDto' src-tauri/crates/uc-daemon-contract/ --multiline` 返回 0（`NetworkSettingsDto` 仍禁 `Default` derive）
    - **【checker BLOCKER 5】** `rg -n 'derive\(Debug, Default\)|derive\(.*Default.*\)' src-tauri/crates/uc-daemon-contract/src/api/dto/settings.rs | rg 'UpdateSettingsResponse'` 返回 0（`UpdateSettingsResponse` 禁 `Default`）
  </acceptance_criteria>

  <done>
DTO 镜像 + UpdateSettingsResponse.restart_required + From 映射 + 7 个 wire/序列化测试全部到位。daemon-contract 编译通过，webserver re-export 路径未 break。
  </done>
</task>

<task type="auto">
  <name>Task 2: OpenAPI schemas 列表注册 NetworkSettingsDto（D-D3 同步更新）</name>
  <files>src-tauri/crates/uc-webserver/src/api/openapi.rs</files>

  <read_first>
    1. `src-tauri/crates/uc-webserver/src/api/openapi.rs`（必读完整文件 — 仅 185 行；重点 line 28-33 import 与 line 122-141 schemas 注册）
    2. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` D-D3（OpenAPI schema 同步更新）
    3. `.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md` §7（OpenAPI 注册模式）
    4. Task 1 已新增的 `NetworkSettingsDto`
  </read_first>

  <action>
在 `src-tauri/crates/uc-webserver/src/api/openapi.rs` 中执行两处改动：

**改动点 1（line 28-33，import 列表）：** 在 alphabetic 顺序中追加 `NetworkSettingsDto`。当前：
```rust
use crate::api::dto::settings::{
    ContentTypesDto, FileSyncSettingsDto, GeneralSettingsDto, GetSettingsResponse,
    PairingSettingsDto, RetentionPolicyDto, RetentionRuleDto, RuleEvaluationDto,
    SecuritySettingsDto, SettingsDto, ShortcutKeyDto, SyncFrequencyDto, SyncSettingsDto, ThemeDto,
    UpdateChannelDto, UpdateSettingsResponse,
};
```
改为：
```rust
use crate::api::dto::settings::{
    ContentTypesDto, FileSyncSettingsDto, GeneralSettingsDto, GetSettingsResponse,
    NetworkSettingsDto, PairingSettingsDto, RetentionPolicyDto, RetentionRuleDto,
    RuleEvaluationDto, SecuritySettingsDto, SettingsDto, ShortcutKeyDto, SyncFrequencyDto,
    SyncSettingsDto, ThemeDto, UpdateChannelDto, UpdateSettingsResponse,
};
```

**改动点 2（line 138 附近，components::schemas 列表）：** 在 `FileSyncSettingsDto,` 之后追加 `NetworkSettingsDto,`。当前块（line 122-141）：
```rust
            // Common
            ContentTypesDto,
            ApiErrorResponse,
            GetLocalDeviceInfoResponse,
            LocalDeviceInfoDto,
            GetSettingsResponse,
            UpdateSettingsResponse,
            SettingsDto,
            GeneralSettingsDto,
            SyncSettingsDto,
            SyncFrequencyDto,
            RetentionPolicyDto,
            RetentionRuleDto,
            RuleEvaluationDto,
            SecuritySettingsDto,
            PairingSettingsDto,
            FileSyncSettingsDto,
            ShortcutKeyDto,
            ThemeDto,
            UpdateChannelDto,
```
改为（在 `FileSyncSettingsDto,` 之后插入一行 `NetworkSettingsDto,`）：
```rust
            // Common
            ContentTypesDto,
            ApiErrorResponse,
            GetLocalDeviceInfoResponse,
            LocalDeviceInfoDto,
            GetSettingsResponse,
            UpdateSettingsResponse,
            SettingsDto,
            GeneralSettingsDto,
            SyncSettingsDto,
            SyncFrequencyDto,
            RetentionPolicyDto,
            RetentionRuleDto,
            RuleEvaluationDto,
            SecuritySettingsDto,
            PairingSettingsDto,
            FileSyncSettingsDto,
            NetworkSettingsDto,
            ShortcutKeyDto,
            ThemeDto,
            UpdateChannelDto,
```

注意：`UpdateSettingsResponse` 已在列表（line 128）。其字段变更（新增 `restart_required: bool`）会通过 `ToSchema` derive 自动反映到生成的 OpenAPI 文档（无需在 schemas 列表里再加）。

**禁止动作：**
- ❌ 禁止把 `NetworkSettingsPatchDto` 也加进 schemas 列表（注意：现有列表里 `FileSyncSettingsPatchDto` 也未单独注册 —— Patch DTO 通过 `SettingsPatchDto` 的字段递归 derive 进入 OpenAPI 文档，无需重复）
- ❌ 禁止改 `paths(...)` 块（settings 路由本身未变）
  </action>

  <verify>
    <automated>cargo check -p uc-webserver 2>&amp;1 | tail -10</automated>
    <automated>cargo build -p uc-webserver 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-webserver` 退出码 0
    - `cargo build -p uc-webserver` 退出码 0（OpenAPI schemas 列表不可有重复或缺失符号）
    - `rg -n 'NetworkSettingsDto' src-tauri/crates/uc-webserver/src/api/openapi.rs` 命中 2 处（import + schemas）
    - `rg -n '^use crate::api::dto::settings' src-tauri/crates/uc-webserver/src/api/openapi.rs` 命中 1 处（且块内含 NetworkSettingsDto）
  </acceptance_criteria>

  <done>
OpenAPI schemas 列表注册 `NetworkSettingsDto`，import 同步更新；`UpdateSettingsResponse.restart_required` 通过 `ToSchema` derive 自动出现在生成的 OpenAPI 文档。webserver 编译通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP wire（前端 / 第三方客户端）→ daemon-contract::SettingsPatchDto | 不可信入参跨"传输层 → daemon"边界；通过 `serde + axum::Json` 反序列化 |
| OpenAPI schema → 第三方 SDK 生成器 | wire 契约改动会透传到使用 OpenAPI 生成 client 的下游（Phase 95 前端 + 第三方） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094.03-01 | Tampering | 恶意客户端 PUT 含 `network.allowRelayFallback: false` | mitigate | serde-strict typing + 现有 `/settings` 路径上的 session JWT 中间件（`uc-webserver` SecurityAddon，openapi.rs:50-72）；客户端必须先认证 |
| T-094.03-02 | Tampering | 恶意客户端 PUT 含恶意字段名（拼写错误 / 未知字段） | accept | serde 默认忽略未知字段（与 sibling DTO 一致）；不会污染合法字段；schema 层无 `deny_unknown_fields` 是项目既有约定 |
| T-094.03-03 | Information Disclosure | wire 字段 `allowRelayFallback` 暴露 LAN-only 内部状态 | accept | 业务正向语义无 PII；前端已知；OpenAPI 文档公开是预期 |
| T-094.03-04 | Spoofing | 第三方 client 伪造 `restartRequired: true` 响应 | accept | `restart_required` 只在 daemon 出方向 wire；进方向 wire 是 SettingsPatchDto，无该字段；客户端无法伪造 daemon 响应（HTTPS / mutual TLS / IPC 信任由 webserver 现有架构保证） |
| T-094.03-05 | Denial of Service | 大型/嵌套 patch 构造导致 serde 栈溢出 | accept | DTO 字段固定数量，无递归字段；与 sibling FileSyncSettingsPatchDto 风险等级一致 |
| T-094.03-06 | Elevation of Privilege | OpenAPI schema 注册缺失导致前端类型生成错误 | mitigate | Task 2 的 `cargo build -p uc-webserver` 自动验证 utoipa 派生的 ToSchema 完整性 |

无 `high` 严重等级威胁。
</threat_model>

<verification>
- `cargo check -p uc-daemon-contract` + `cargo build -p uc-webserver` 全过
- `cargo test -p uc-daemon-contract --lib api::dto::settings` 全部通过（含本 plan 7 个新测试）
- grep 守护：
  - `rg -n 'pub struct NetworkSettings(Dto|PatchDto)' src-tauri/crates/uc-daemon-contract/` 共 2 处
  - `rg -n '!.*value\.network\.allow_relay_fallback|!.*allow_relay_fallback' src-tauri/crates/uc-daemon-contract/` 返回 0（无反向写法）
  - `rg -n 'NetworkSettingsDto' src-tauri/crates/uc-webserver/src/api/openapi.rs` 命中 2（import + schemas）
- OpenAPI 文档变化预期：增 `NetworkSettingsDto` schema、`SettingsDto.network` 字段、`SettingsPatchDto.network` 字段、`UpdateSettingsResponse.restartRequired` 字段（5 处增量）
</verification>

<success_criteria>
- [ ] `NetworkSettingsDto` + `NetworkSettingsPatchDto` DTO 结构体已镜像，`#[serde(rename_all = "camelCase")]` 派生正确
- [ ] `SettingsDto.network` + `SettingsPatchDto.network` 顶层挂载
- [ ] `From<core::NetworkSettings> for NetworkSettingsDto` 透明搬运（不取反）
- [ ] `From<core::Settings> for SettingsDto` 末段补 `network: value.network.into()`
- [ ] `UpdateSettingsResponse.restart_required: bool` 字段已加（D-D1 / D-D3 wire 契约扩展）
- [ ] OpenAPI `components::schemas` 列表追加 `NetworkSettingsDto`
- [ ] 9 个 DTO 测试全部通过（含 checker 引入的 `Default` 行为 + PUT `{}` 向后兼容）
- [ ] **【checker BLOCKER 5】** `SettingsPatchDto` + `NetworkSettingsPatchDto` 已加 `derive(Default)`；`NetworkSettingsDto` + `UpdateSettingsResponse` 未加（grep 守护通过）
- [ ] **【checker WARNING 3】** PUT body `{}` 行为与 Phase 94 之前一致（grep + 反序列化 unit test 双重确认）
- [ ] `cargo check -p uc-daemon-contract` + `cargo build -p uc-webserver` 全过
- [ ] **决策 D-D1 / D-D3 实施已写入 must_haves.truths**
</success_criteria>

<output>
After completion, create `.planning/phases/094-backend-network-allow-relay-fallback/094-03-SUMMARY.md` with:
- 实际改动的 LOC（settings.rs DTO 文件 8 处插入 + openapi.rs 2 处插入）
- 7 个 DTO 测试 pass 输出（截取关键行）
- 给下游 plan 04（webserver mapping + handler）的"DTO + From + restart_required wire 已就位"信号
- 检查 wire 契约的向后兼容性：旧客户端 PUT 不带 `network` 段是否仍 200（这是 plan 04 集成测试覆盖的硬约束，本 plan 通过单元测试 `apply_settings_patch` no-op 间接验证）
- Pitfall 1 自查：grep 全工程 `disable_relays|allow_relay_fallback` 无反向写法（除 plan 05 与测试外）
</output>
