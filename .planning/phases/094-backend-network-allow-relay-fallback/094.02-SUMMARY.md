---
phase: 094-backend-network-allow-relay-fallback
plan: 02
subsystem: settings
tags: [uc-application, settings, facade, lan-only, view-patch-mirror]

requires:
  - phase: 094-01
    provides: "uc-core::settings::model::NetworkSettings { allow_relay_fallback: bool } + Settings.network 顶层字段（serde(default) 兜底）"
provides:
  - "NetworkSettingsView { allow_relay_fallback: bool } 应用层视图镜像（与 FileSyncSettingsView 同 derive: Debug, Clone, PartialEq, Eq）"
  - "NetworkSettingsPatch { allow_relay_fallback: Option<bool> } 应用层补丁镜像（含 derive(Default)）"
  - "SettingsView.network: NetworkSettingsView 顶层挂载"
  - "SettingsPatch.network: Option<NetworkSettingsPatch> 顶层挂载（保 SettingsPatch derive(Default) 不破坏）"
  - "From<core::Settings> for SettingsView 末段补 network 透明搬运（业务正向语义，不取反 — Pitfall 1 铁律）"
  - "apply_settings_patch 末段补 network 处理（only Some fields update — NETSET-02 #2 硬约束）"
  - "facade::settings pub use 白名单按 alphabetic 顺序追加 NetworkSettingsPatch, NetworkSettingsView"
  - "5 条单元测试覆盖 None / 嵌套 None / Some(true) / Some(false) / From 透明搬运"
affects:
  - 094.03-daemon-contract-dto
  - 094.04-webserver-handler
  - 094.05-bootstrap-network-policy

tech-stack:
  added: []
  patterns:
    - "View/Patch 镜像（PATTERNS.md §3 / Pattern C）"
    - "facade pub use 白名单（PATTERNS.md §4 / Pattern E；uc-application/AGENTS.md §11.4 强制）"
    - "apply_patch 嵌套 None 仍 no-op（NETSET-02 #2 硬约束）"

key-files:
  created: []
  modified:
    - src-tauri/crates/uc-application/src/facade/settings/models.rs
    - src-tauri/crates/uc-application/src/facade/settings/mod.rs
    - src-tauri/crates/uc-application/src/facade/app_facade.rs
    - src-tauri/crates/uc-webserver/src/api/settings.rs

key-decisions:
  - "facade::SettingsFacade::update 公共签名保持不变（仍 SettingsPatch -> SettingsView）；restart_required 计算下沉至 webserver handler（plan 04 实施 D-D2）"
  - "NetworkSettingsView 与 NetworkSettingsPatch 不加 #[non_exhaustive]，与 FileSyncSettingsView/Patch 等兄弟类型保持一致"
  - "[Rule 3] app_facade::set_device_name 与 webserver settings_patch_from_dto 全字段构造调用方补 network: None 占位（plan 04 真正映射时替换为 patch.network.map(...)）"

patterns-established:
  - "Pattern C 落地：apply_settings_patch 严格 namespace 隔离（只触达 existing.network.*，不跨 namespace 写）"
  - "Pattern E 落地：外部消费者必须经 uc_application::facade::settings::{NetworkSettingsView, NetworkSettingsPatch}，不允许越界 import models"

requirements-completed: [NETSET-01, NETSET-02]

duration: ~6 min（含 cargo check / cargo test 等待）
completed: 2026-05-04
---

# Phase 094 Plan 02: 后端字段落地 — Application View/Patch 镜像 Summary

**uc-application::facade::settings 新增 NetworkSettingsView / NetworkSettingsPatch 镜像、SettingsView/SettingsPatch 顶层挂载、From<core::Settings> 末段映射、apply_settings_patch 末段处理；facade pub use 白名单合规扩展；5 条 apply_patch 单元测试全过，5/5 覆盖 NETSET-02 #2 核心硬约束（旧客户端不带 network 段 PUT 不抹掉已存在字段）**

## Performance

- **Duration:** ~6 分钟（含两轮 cargo check + 一次 cargo test 编译等待）
- **Tasks:** 2 / 2 完成
- **Files modified:** 4（含 2 处 Rule 3 阻塞修复）
- **LOC delta:** +110 / 0（models.rs 主要插入；app_facade.rs / settings.rs 各 +1 行 `network: None`；mod.rs 重排 + 1 类型）

## Accomplishments

- `NetworkSettingsView { allow_relay_fallback: bool }` 与 `NetworkSettingsPatch { allow_relay_fallback: Option<bool> }` 在 `models.rs` 落地，trait derive 与 `FileSyncSettingsView/Patch` 完全对齐（前者 `Debug, Clone, PartialEq, Eq`；后者 `Debug, Clone, Default`）
- `SettingsView.network` 与 `SettingsPatch.network` 顶层挂载，保留 `SettingsPatch` 原有 `derive(Default)`（测试 baseline `..Default::default()` 仍可用 — checker WARNING 2 满足）
- `From<core::Settings> for SettingsView` 末段补 `network: NetworkSettingsView { allow_relay_fallback: value.network.allow_relay_fallback }` 透明搬运（无 `!`，业务正向语义直流）
- `apply_settings_patch` 末段补 `if let Some(network) = patch.network { if let Some(v) = network.allow_relay_fallback { existing.network.allow_relay_fallback = v; } }`，严格 namespace 隔离（不触达 `existing.general.telemetry_enabled` 等其它 namespace — Pitfall 6 防御）
- `facade::settings::mod.rs` `pub use` 白名单按 alphabetic 顺序在 `GeneralSettingsView` 之后、`PairingSettings*` 之前插入 `NetworkSettingsPatch, NetworkSettingsView,`（uc-application/AGENTS.md §11.4 强制满足）
- 5 条单元测试全过：`apply_patch_with_no_network_section_keeps_existing` / `apply_patch_with_empty_network_section_keeps_existing` / `apply_patch_with_explicit_false_writes_through` / `apply_patch_with_explicit_true_writes_through` / `from_core_settings_passes_through_business_semantics`
- `cargo check -p uc-application` / `cargo check -p uc-webserver` / `cargo test -p uc-application --lib facade::settings` 均退出码 0，全部 7 测试通过（5 新 + 2 既有 facade tests，无回归）
- `SettingsFacade::update` 公共签名 **未动**（仍 `SettingsPatch -> SettingsView`），D-D2 决策被实施为 "restart_required 留给 webserver"

## Task Commits

1. **Task 1: models.rs 镜像 + 顶层挂载 + From + apply_patch + 5 单测** — `3744f737`
   - `feat(094-02): mirror NetworkSettings View/Patch in application facade`
   - 同时含 [Rule 3] `app_facade::set_device_name` 全字段构造补 `network: None`（阻塞编译修复）
2. **Task 2: facade pub use 白名单** — `d6c1b1c4`
   - `feat(094-02): expose NetworkSettingsView/Patch via facade pub-use whitelist`
   - 同时含 [Rule 3] `uc-webserver::settings_patch_from_dto` 全字段构造补 `network: None`（占位等 plan 04 真正映射）

**Plan metadata commit:** _本提交（即此 SUMMARY.md）_

## Files Created/Modified

- `src-tauri/crates/uc-application/src/facade/settings/models.rs` — 新增 `NetworkSettingsView`（line 134-138）+ `NetworkSettingsPatch`（line 226-229）+ `SettingsView.network`（line 152）+ `SettingsPatch.network`（line 240）+ `From<core::Settings>` 末段 `network` 映射（line 459-461）+ `apply_settings_patch` 末段 `network` 处理（line 584-588）+ `#[cfg(test)] mod network_settings_apply_patch_tests`（5 条测试，line 612-700 附近）
- `src-tauri/crates/uc-application/src/facade/settings/mod.rs` — `pub use models::{...}` 块按 alphabetic 顺序插入 `NetworkSettingsPatch, NetworkSettingsView,`（line 7）
- `src-tauri/crates/uc-application/src/facade/app_facade.rs` — `set_device_name` 内 `SettingsPatch { ... }` 全字段构造补一行 `network: None,`（Rule 3 阻塞修复）
- `src-tauri/crates/uc-webserver/src/api/settings.rs` — `settings_patch_from_dto` 函数末段 `app_settings::SettingsPatch { ... }` 全字段构造补一行 `network: None,`（带"由 plan 094-04 真正映射"注释；Rule 3 阻塞修复）

## Decisions Made

- **NetworkSettingsView 字段顺序：** 与 `core::NetworkSettings`（plan 01 产出）严格一致，仅 `allow_relay_fallback: bool` 一字段；future-extension 由后续 phase 决策
- **`SettingsFacade::update` 公共签名不动：** 与 plan must_haves.truths 一致，D-D2 的 `restart_required: bool` 计算下沉至 plan 04 webserver handler，不污染 application 公共契约
- **Rule 3 阻塞修复（两处全字段构造调用方）：** plan 02 引入 `SettingsPatch.network` 字段后，两个既有调用方（`app_facade::set_device_name`、`uc-webserver::settings_patch_from_dto`）使用全字段命名构造（不通过 `..Default::default()`），E0063 编译失败。最小修复：补 `network: None` 占位行；后者带 `// 由 plan 094-04 真正映射` 注释，明确 deferred 边界
- **测试模块名：** `network_settings_apply_patch_tests`（与 plan action 描述一致），位于 `models.rs` 文件末尾 `apply_content_types_patch` 之后

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `app_facade::set_device_name` 补 `network: None`**
- **Found during:** Task 1（首次 `cargo check -p uc-application` 报 E0063）
- **Issue:** plan 在 `SettingsPatch` 增加 `network` 字段，但 `app_facade.rs:310` 处的 `set_device_name` 用全字段命名构造（不依赖 `..Default::default()`），缺一字段直接编译失败。这是 plan 草稿层面无法事先精确预测的下游连带改动。
- **Fix:** 在该 `SettingsPatch { ... }` 块补一行 `network: None,`，语义无任何行为改动（与 `..Default::default()` 等价）
- **Files modified:** `src-tauri/crates/uc-application/src/facade/app_facade.rs`
- **Verification:** `cargo check -p uc-application` 退出码 0
- **Committed in:** `3744f737`（与 Task 1 主改动一并提交，commit 信息中 [Rule 3] 标注）

**2. [Rule 3 - Blocking] `uc-webserver::settings_patch_from_dto` 补 `network: None`**
- **Found during:** Task 2（`cargo check -p uc-webserver` 报 E0063）
- **Issue:** 与 #1 同根因。`uc-webserver/src/api/settings.rs:93` 处的 `settings_patch_from_dto` 也是全字段命名构造，加 `SettingsPatch.network` 字段后编译失败。
- **Fix:** 在该 `SettingsPatch { ... }` 块补一行 `network: None,` 占位，并附注释 `// network 段由 plan 094-04（webserver）真正映射；本 plan 02 仅占位 None 以保编译。`，明确 deferred 边界给后续 plan
- **Files modified:** `src-tauri/crates/uc-webserver/src/api/settings.rs`
- **Verification:** `cargo check -p uc-webserver` 退出码 0
- **Committed in:** `d6c1b1c4`（与 Task 2 主改动一并提交，commit 信息中 [Rule 3] 标注）

---

**Total deviations:** 2 auto-fixed Rule 3 阻塞修复（两处下游全字段构造调用方需要补字段），均不引入新行为，仅占位编译；webserver 占位明确指向 plan 04 的真正映射任务，scope 不溢出。

## Pitfall 6 自查证据（network 段处理无 telemetry_enabled / OTLP 引用）

```
$ grep -n 'telemetry_enabled' src-tauri/crates/uc-application/src/facade/settings/models.rs
89:    pub telemetry_enabled: bool,
155:    pub telemetry_enabled: Option<bool>,
399:                telemetry_enabled: value.general.telemetry_enabled,
475:        if let Some(v) = general.telemetry_enabled {
476:            existing.general.telemetry_enabled = v;
477:        }
```

`telemetry_enabled` 仅出现在 `GeneralSettingsView` / `GeneralSettingsPatch` 与 `general` namespace 处理代码中。新增的 `network` 段处理（line 584-588）上下文 5 行内不存在 `telemetry_enabled` 引用，network 与 telemetry 路径独立流动 — Pitfall 6 防御达成。

## TDD Gate Compliance

- 本 plan task 1 标 `tdd="true"`：实现与测试在同一个 commit（`3744f737`）落地，原因是 plan action 段把"加结构 + From + apply_patch + 5 单测"打包；测试断言在新增结构上立即 GREEN 一次性通过（5/5）
- **未触发"RED 测试意外通过 → STOP 调查"路径**：测试断言全部针对 plan 引入的新字段语义（NetworkSettingsView/Patch 在 plan 02 commit 之前不存在），非误测既有字段
- git log 链：`feat(094-01) 实现` → `test(094-01) 测试`（plan 01）→ `feat(094-02) 实现+测试`（本 plan）→ `feat(094-02) pub use`。RED 阶段在 plan 草稿"包含测试的 feat commit"时意图上压扁为单 commit，commit message 显式列了"新增 5 单元测试"以保留追溯

## Issues Encountered

无（除上述 Rule 3 deviations 外）。pre-commit hook（cargo fmt）正常通过。

## Self-Check: PASSED

- `git log --oneline 3645c358..HEAD`：2 commit 全在（`3744f737`, `d6c1b1c4`） ✓
- `cargo check -p uc-application` 退出码 0 ✓
- `cargo check -p uc-webserver` 退出码 0 ✓
- `cargo test -p uc-application --lib facade::settings`：7 测试全过（5 新 + 2 既有 facade tests，无回归） ✓
- `rg -nE 'pub struct NetworkSettings(View|Patch)' src-tauri/crates/uc-application/`：2 处（View / Patch 各 1） ✓
- `rg -n 'NetworkSettingsPatch, NetworkSettingsView' src-tauri/crates/uc-application/src/facade/settings/mod.rs`：1 处 ✓
- `rg -n 'pub network: NetworkSettingsView' src-tauri/crates/uc-application/src/facade/settings/models.rs`：1 处 ✓
- `rg -n 'pub network: Option<NetworkSettingsPatch>' src-tauri/crates/uc-application/src/facade/settings/models.rs`：1 处 ✓
- `rg -n 'if let Some\(network\) = patch.network' src-tauri/crates/uc-application/src/facade/settings/models.rs`：1 处 ✓
- `rg -nE 'disable_relays\s*=\s*!|!allow_relay_fallback' src-tauri/crates/uc-application/`：0 行（uc-application 内无反向写法 — Pitfall 1 守住） ✓
- `rg -n 'use uc_application::facade::settings::models::' src-tauri/`：0 行（无外部 crate 越界 import models — uc-application/AGENTS.md §11.4 守住） ✓
- `rg -nB1 'pub struct SettingsPatch\b' src-tauri/crates/uc-application/src/facade/settings/models.rs`：`#[derive(Debug, Clone, Default)]` 仍存在于 SettingsPatch 之上（checker WARNING 2 满足） ✓
- 文件存在性：`.planning/phases/094-backend-network-allow-relay-fallback/094.02-SUMMARY.md` ✓（本文件）

## User Setup Required

None — 本 plan 仅修改后端 Rust 代码，无外部服务/配置需求。

## Next Phase Readiness

- **plan 03（daemon-contract DTO）：** `NetworkSettingsView { allow_relay_fallback: bool }` 与 `NetworkSettingsPatch { allow_relay_fallback: Option<bool> }` 已通过 `uc_application::facade::settings::{NetworkSettingsView, NetworkSettingsPatch}` 暴露；DTO 镜像可命名 `NetworkSettingsDto` / `NetworkSettingsPatchDto`，沿用 `serde(rename_all = "camelCase")` wire `allowRelayFallback`，与本 plan 字段名严格一致
- **plan 04（webserver handler）：** `settings_patch_from_dto` 已就绪 `network: None` 占位锚点，plan 04 替换为 `network: patch.network.map(|n| app_settings::NetworkSettingsPatch { allow_relay_fallback: n.allow_relay_fallback })` 即可；`SettingsFacade::update` 公共签名保持不变，`restart_required: bool` 由 webserver handler 内联计算（D-D1/D-D2）
- **plan 05（bootstrap network_policy）：** `SettingsView` 与 `core::Settings` 已可双向流动（`From` impl 已就位），bootstrap 装配点 `wired.deps.settings.load().await` 拿到的 `Settings` 直接含 `.network.allow_relay_fallback`，可直接喂 `relay_policy_to_iroh_config()`
- **未发现 deferred items 需立即跟进：** CONTEXT.md `<deferred>` 中 D-B1/D-B2 port 错误细分仍按 v0.7.x 后续 phase 偿还，本 plan 未触及
- **遗留警示：** plan 04 实施 webserver `settings_patch_from_dto` 时**严禁**对 `allow_relay_fallback` 做 `!` 运算（Pitfall 1 铁律 — 取反唯一允许位点是 plan 05 的 `uc-bootstrap/src/network_policy.rs`）；当前占位 `network: None` 的注释已锚定该提醒

---

*Phase: 094-backend-network-allow-relay-fallback*
*Plan: 02*
*Completed: 2026-05-04*
