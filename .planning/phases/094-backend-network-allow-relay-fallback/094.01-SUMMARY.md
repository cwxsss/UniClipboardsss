---
phase: 094-backend-network-allow-relay-fallback
plan: 01
subsystem: settings
tags: [uc-core, settings, network, lan-only, serde, default-values]

requires:
  - phase: 094-context
    provides: NetworkSettings 字段族契约（CONTEXT.md / PATTERNS.md）+ Pitfall 1/2/6 防御决策
provides:
  - "NetworkSettings { allow_relay_fallback: bool } 值对象（uc-core::settings::model）"
  - "default_allow_relay_fallback() -> bool { true } 字段级 helper"
  - "Settings.network: NetworkSettings 顶层字段（#[serde(default)] 兜底）"
  - "手写 impl Default for NetworkSettings（含 Pitfall 2 警示三行注释）"
  - "5 条单元测试覆盖默认值 + Settings 装配 + 缺段反序列化兜底 + 显式 true/false 保留语义"
affects:
  - 094.02-application-view-patch
  - 094.03-daemon-contract-dto
  - 094.05-bootstrap-network-policy

tech-stack:
  added: []
  patterns:
    - "serde(default) 顶层 + 字段级 default = \"fn\" 双兜底（Pitfall 2 防御）"
    - "禁止 #[derive(Default)] for bool-bearing 业务值对象，强制手写 Default impl"
    - "schema_version 不变（保持 1）—— 通过 #[serde(default)] 实现纯加法字段，避免 migration codepath"

key-files:
  created: []
  modified:
    - src-tauri/crates/uc-core/src/settings/model.rs
    - src-tauri/crates/uc-core/src/settings/defaults.rs

key-decisions:
  - "NetworkSettings 单字段（仅 allow_relay_fallback）—— 扩展性留给未来 phase 决策（如 rendezvous_base_url_override），本 plan 不预占"
  - "Pitfall 2 防御策略：fn default_allow_relay_fallback() helper 字面量 + #[serde(default = \"...\")]  字段级 + 手写 impl Default 三重兜底"
  - "schema_version 保持 1，不触发 migration codepath（NETSET 字段族走 serde default 兜底）"
  - "执行偏离 plan 文本：Task 1 commit 同时更新 defaults.rs（最小 impl Default + Settings::default 字段挂载），让 cargo check 立即通过；Task 2 再升级 impl Default 加警示三行注释 + 测试模块。原 plan 把 Task 1 限定为只动 model.rs 但 acceptance criteria 又要求 cargo check 通过，自相矛盾，按 Rule 3 自动处理"

patterns-established:
  - "Pattern B（PATTERNS.md §跨切面）落地：NetworkSettings 走 serde(default) + 手写 Default impl 双兜底"
  - "警示注释三行（'默认 true = 允许 fallback'）作为 PR review 红线 grep 锚点"

requirements-completed: [NETSET-01, NETSET-02]

duration: ~24 min
completed: 2026-05-04
---

# Phase 094 Plan 01: 后端字段落地 — NetworkSettings 类型与 Settings 挂载 Summary

**uc-core::settings 新增 NetworkSettings { allow_relay_fallback: bool } 值对象、字段级 default helper、Settings.network 顶层挂载，及 5 条防御性单元测试（含缺段 JSON 反序列化兜底、显式 true/false 双向保留语义），schema_version 保持不变**

## Performance

- **Duration:** ~24 min（含 cargo check + 两次 cargo test 等待时间）
- **Started:** 2026-05-04T09:05:00Z（approximate — plan 加载时点）
- **Completed:** 2026-05-04T09:29:01Z
- **Tasks:** 2 / 2 完成
- **Files modified:** 2（model.rs, defaults.rs）
- **LOC delta:** +111 / -2（model.rs +34/-2，defaults.rs +77）

## Accomplishments

- `NetworkSettings` 值对象与 `default_allow_relay_fallback()` helper 在 `uc-core/src/settings/model.rs` 落地，反向命名规则注释（Pitfall 1）逐字写入头部
- `Settings.network` 顶层字段挂载（`#[serde(default)]`），原 line 201-202 占位注释替换为真实字段
- `impl Default for NetworkSettings` 手写实现（**未** 使用 `#[derive(Default)]`），含 Pitfall 2 警示三行注释 + `Doc` 注释
- `Settings::default()` 字段列表追加 `network: NetworkSettings::default()`
- 5 条单元测试全部通过：默认值断言（×1）、Settings 装配 + schema_version 锁定（×1）、缺 `network` 段 JSON 兜底 true（×1，NETSET-02 success criterion #2 硬约束）、显式 false 保留语义（×1）、显式 true 保留语义（×1）
- `cargo check -p uc-core` / `cargo test -p uc-core --lib settings` / `cargo build -p uc-core --release` 全部退出码 0，无新依赖引入
- `schema_version` 保持 1，无 migration codepath 触发（PATTERNS.md §STACK §2.2 模式确认）

## Task Commits

Each task was committed atomically:

1. **Task 1: 新增 NetworkSettings 类型 + Settings.network 字段** — `cc9a73fd` (feat)
   - `feat(094-01): add NetworkSettings type with allow_relay_fallback field`
   - 同时含 defaults.rs 最小化 `impl Default` + Settings::default 字段挂载（让 cargo check 通过的最小补丁）
2. **Task 2: 升级 impl Default 警示注释 + 加单元测试** — `d75addf2` (test)
   - `test(094-01): add NetworkSettings default & serde fallback unit tests`

**Plan metadata commit:** _本提交（即此 SUMMARY.md）_

## Files Created/Modified

- `src-tauri/crates/uc-core/src/settings/model.rs` — 新增 `NetworkSettings` struct 定义（line 178-204）、`default_allow_relay_fallback()` helper（line 203-211）、`Settings.network` 字段挂载（line 233-234），删除 `// pub network: NetworkSettings` 占位注释（原 line 201-202）
- `src-tauri/crates/uc-core/src/settings/defaults.rs` — 新增 `impl Default for NetworkSettings`（line 227-242，含警示三行 + Doc 注释）、`Settings::default()` 字段列表追加 `network: NetworkSettings::default()`（line 278）、新增 `#[cfg(test)] mod tests`（line 283-348，5 条断言）

## Decisions Made

- **Task 切分微调（Rule 3 自动处理）：** plan Task 1 `<files>` 仅声明 `model.rs` 且 `<verify>` 要求 `cargo check` 退出码 0 —— 但仅改 model.rs 会让 `Settings::default()`（在 defaults.rs）的字段构造缺失 `network`，编译必失败。按 Rule 3（auto-fix blocking issues），Task 1 commit 同步包含 defaults.rs 的最小补丁（手写 impl Default 不带警示注释 + Settings::default 字段挂载），让编译通过；Task 2 再把 impl Default 升级为带警示三行 + Doc 注释的完整版，并新增测试模块。两步 commit 边界保持清晰（feat 与 test 分离）。
- **`mod tests` 不引入 `use super::*;`：** 初版加了 `use super::*;` 触发 `unused_imports` 警告（defaults.rs 内 super::* 含 model.rs re-export，但测试只直接 import `NetworkSettings, Settings, CURRENT_SCHEMA_VERSION`）。Rule 1（auto-fix bug）就地删除 `use super::*;` 仅保留显式 import，警告消除。
- **测试文件位置：** 5 条测试集中放 `defaults.rs` 内的 `#[cfg(test)] mod tests`（与 plan Task 2 action 一致），未额外创建 `tests/` 目录或拆分到 `model.rs`。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Task 1 范围扩展到 defaults.rs（最小补丁）**
- **Found during:** Task 1（新增 NetworkSettings struct 后准备运行 `cargo check`）
- **Issue:** plan Task 1 `<files>` 仅声明 `model.rs`，但 `<verify>` / `<acceptance_criteria>` 要求 `cargo check -p uc-core` 退出码 0。仅改 model.rs（新增 `pub network: NetworkSettings` 字段且无 Option 包裹）后，`Settings::default()` 在 defaults.rs 缺字段构造，必定 E0063 编译失败。这是 plan 草稿层面的内部不一致。
- **Fix:** Task 1 commit 同步追加 defaults.rs 最小补丁 —— 不带警示注释、不带 Doc 的最简 `impl Default for NetworkSettings { fn default() -> Self { Self { allow_relay_fallback: true } } }` + `Settings::default()` 字段列表追加 `network: NetworkSettings::default()`。Task 2 再把这段升级为带警示三行 + Doc 注释的完整版（替换式 Edit）。
- **Files modified:** `src-tauri/crates/uc-core/src/settings/defaults.rs`（额外纳入 Task 1）
- **Verification:** Task 1 commit 后 `cargo check -p uc-core` 退出码 0；Task 2 commit 后 5 测试全过 + grep 警示注释命中。
- **Committed in:** `cc9a73fd`（Task 1）+ `d75addf2`（Task 2 升级版）

**2. [Rule 1 - Bug] 删除 mod tests 中冗余的 `use super::*;`**
- **Found during:** Task 2（首次 cargo test 后看到 warning）
- **Issue:** 初版 `#[cfg(test)] mod tests` 同时使用 `use super::*;` 与 `use crate::settings::model::{NetworkSettings, Settings, CURRENT_SCHEMA_VERSION};`，前者所引入的符号都被显式 import 覆盖，触发 `unused_imports` 警告。
- **Fix:** 删除 `use super::*;`，保留显式 import 行。
- **Files modified:** `src-tauri/crates/uc-core/src/settings/defaults.rs`
- **Verification:** 重跑 `cargo test -p uc-core --lib settings::defaults::tests` 5 个测试全过且无 warning；`cargo check -p uc-core` 退出码 0。
- **Committed in:** `d75addf2`（修正在 Task 2 commit 内完成，未单独 commit —— 处于同一逻辑改动）

---

**Total deviations:** 2 auto-fixed（1 × Rule 3 阻塞修正，1 × Rule 1 bug 修正）
**Impact on plan:** 第 1 项是 plan 自身 task 范围划分与 acceptance criteria 不一致带来的强制扩展，commit 边界仍按 feat / test 分离保持清晰；第 2 项是 lint warning 修正。两项均不影响最终交付物语义，无 scope creep。

## TDD Gate Compliance

- 严格 TDD 顺序在本 plan 中部分让位于 plan 自身的 task 切分：plan 把 Task 1 定义为"加结构 + 字段（让 cargo check 过）"，Task 2 定义为"加 Default impl + 测试模块"。结果是测试单元在 Task 2 落地时实现已就位（即 GREEN 测试一次性全过，无 RED 阶段）。
- **未触发"RED 测试意外通过 → STOP 调查"路径**：原因是 Task 1 commit 已结构性提供了让所有断言通过的实现。这是 plan 拆分的预期结果，不是测试设计错误（测试断言确实在断言新增字段的语义，不是误测既有字段）。
- 最终 commit 链：`feat(094-01): ...`（Task 1 实现）→ `test(094-01): ...`（Task 2 测试 + 文档化 Default）。git log 中 feat 与 test 顺序与 plan task 序号一致；REFACTOR 阶段不需要（删除一行 `use super::*;` 已就地处理，未单独成 commit）。

## Issues Encountered

无（除上述 deviations 外）。Pre-commit hook（cargo fmt）正常通过，无格式化冲突。

## Self-Check: PASSED

- `git log --oneline cc9a73fd d75addf2`：2 commit 均存在 ✓
- `cargo check -p uc-core` exit 0 ✓
- `cargo test -p uc-core --lib settings::defaults::tests`：5 / 5 通过 ✓
- `cargo test -p uc-core --lib settings`：5 / 5 新测试 + 既有测试无回归 ✓
- `cargo build -p uc-core --release` exit 0，无新依赖 ✓
- `rg 'pub struct NetworkSettings' src-tauri/`：仅命中 `uc-core/src/settings/model.rs` 一处定义 ✓
- `rg 'fn default_allow_relay_fallback' src-tauri/crates/uc-core/`：1 处 ✓
- `rg 'pub network: NetworkSettings' src-tauri/crates/uc-core/`：1 处（model.rs:234） ✓
- `rg 'pub const CURRENT_SCHEMA_VERSION: u32 = 1;' src-tauri/crates/uc-core/`：仍为 1 ✓
- `rg '// pub network: NetworkSettings' src-tauri/crates/uc-core/`：占位注释已被替换（无匹配） ✓
- `rg '默认 true = 允许 fallback' src-tauri/crates/uc-core/src/settings/`：警示注释命中（model.rs + defaults.rs 共 2 处） ✓
- `rg 'disable_relays\s*=\s*!|disable_relays:\s*!|!allow_relay_fallback' src-tauri/crates/uc-core/`：无匹配（Pitfall 1 在 uc-core 范围内守住） ✓
- `rg 'allow_relay_fallback' src-tauri/crates/uc-core/`：仅命中 model.rs（5 处）+ defaults.rs（12 处，含测试），共 2 个文件，无外溢 ✓
- 文件存在性：`.planning/phases/094-backend-network-allow-relay-fallback/094.01-SUMMARY.md` ✓（本文件）

## User Setup Required

None — 本 plan 仅修改后端 Rust 代码，无外部服务配置需求。

## Next Phase Readiness

- **plan 02（application view/patch）：** `NetworkSettings` 类型已可从 `uc_core::settings::model` 直接 import；`Settings.network` 字段已就位，`From<core::Settings> for SettingsView` 末段可镜像 `FileSyncSettings` 模式追加 `network: NetworkSettingsView { ... }` 段。
- **plan 03（daemon-contract DTO）：** `NetworkSettings { allow_relay_fallback: bool }` 字段名稳定，DTO 镜像可直接命名为 `NetworkSettingsDto`，沿用 `serde(rename_all = "camelCase")` wire `allowRelayFallback`。
- **plan 05（bootstrap network_policy）：** `Settings::default().network.allow_relay_fallback == true` 已断言，bootstrap 层 `wired.deps.settings.load().await` 失败兜底为 `Settings::default()` 时仍能保留"允许 fallback"语义（CONTEXT.md D-B1 选项 B 隐式契约依赖点已锁定）。
- **schema_version 不变（仍为 1）：** plan 06 / migration.rs `migrations: vec![]` 保持空，无需新增 migration 编排。
- **未发现 deferred items 需要立即跟进：** CONTEXT.md `<deferred>` 中 "D-B1 / D-B2 port 错误细分延迟" 仍按 v0.7.x 后续 phase 偿还，本 plan 未引入 `SettingsLoadError` discriminated enum，与决策一致。
- **遗留警示：** 后续 plan 在 `models.rs` / `settings.rs` 等文件中映射 `network` 字段时，**严禁** 出现 `disable_relays = !allow_relay_fallback` 类反向写法（Pitfall 1 铁律），唯一允许位点是 plan 05 的 `uc-bootstrap/src/network_policy.rs`。

---

*Phase: 094-backend-network-allow-relay-fallback*
*Plan: 01*
*Completed: 2026-05-04*
