---
phase: 094-backend-network-allow-relay-fallback
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src-tauri/crates/uc-infra/src/network/iroh/node.rs
  - src-tauri/crates/uc-infra/tests/lan_only_relay_mode.rs
autonomous: true
requirements:
  - NETSET-03
tags:
  - uc-infra
  - iroh
  - lan-only
  - integration-test

must_haves:
  truths:
    - "**【checker BLOCKER 2 决议】** `IrohNodeBuilder::bind` 的 OnceCell 守护采用**分支 B**（`#[cfg(not(test))]` 条件编译）—— 不需要 grep 验证：CONTEXT.md `<canonical_refs>` 已锁定 ≥6 处同 binary `bind` 调用（uc-infra/src/network/iroh/node.rs:775/797/804/851/912/968 + uc-bootstrap/tests/slice2_phase2_clipboard_e2e.rs:371），分支 A 必然 break 现有测试"
    - "**【checker BLOCKER 2 — 双契约 production/test 分离】** OnceCell 守护在 production build (`#[cfg(not(test))]`) 生效；test build 下 elided（不参与编译）。Production daemon 单 entrypoint 由 `uc-bootstrap` 单调用站点保证 single-bind 不变性 —— 这是**两个独立契约**而非一个，必须显式记录"
    - "**【checker BLOCKER 2 — CI 盲点声明】** Production 守护无法用 `#[test]` 覆盖（cfg-elided）。这是该防御的**固有 CI 盲点**，必须显式记录在 must_haves.truths（不能藏在 STRIDE T-094.06-06）。补偿：(a) `uc-bootstrap` 单 entrypoint 由 §4 builders.rs 装配体系保证 single-bind；(b) 任何 PR 加二次 bind 调用站点 → 在 production build 立刻 panic（user 报错 + 启动日志可见）"
    - "新增 `uc-infra/tests/lan_only_relay_mode.rs` integration test 覆盖 D-C1 两组用例：(a) `RelayMode::Disabled` bind 后 `endpoint.addr().addrs` 不含 `TransportAddr::Relay(_)` 项（强不等式 — Tier B 自动断言）；(b) `RelayMode::Default` bind 后只断 'no panic'（不强断 'must contain Relay' — CI 环境与 Relay mesh 连通性不可靠 — D-C1 / PATTERNS.md §11 critical finding 3）"
    - "测试沿用 `uc-infra/tests/iroh_presence_probe.rs:17-29` loopback bind helper 模式 + 直接用 iroh API（`Endpoint::builder().relay_mode(...).bind()`）— 不复用 production-flavored `IrohNodeConfig` 减少测试 stub"
    - "OnceCell 守护**只**针对 `IrohNodeBuilder::bind`（uc-infra 内部实现细节），不漏到 core/app 层；`uc-infra/AGENTS.md §4.2`：技术细节向下收敛（pub static 禁止）"
    - "**【checker WARNING 6】** 测试 binary panic 由 tokio runtime 局部化为单测 fail（不污染其它测试）；production daemon main task panic 触发进程级失败（这是预期行为 — Pitfall 3 阻断目标）"
  artifacts:
    - path: "src-tauri/crates/uc-infra/src/network/iroh/node.rs"
      provides: "IrohNodeBuilder::bind 加 OnceCell 单进程守护（Pitfall 3 结构性防御）"
      contains: "BIND_LOCK"
    - path: "src-tauri/crates/uc-infra/tests/lan_only_relay_mode.rs"
      provides: "Tier B 自动化 — bind 后 addr().addrs 含/不含 Relay 候选断言（D-C1 两组用例）"
      contains: "relay_disabled_publishes_no_relay_addrs"
  key_links:
    - from: "uc-infra::IrohNodeBuilder::bind"
      to: "OnceCell BIND_LOCK"
      via: "进程级 single-shot bind 守护"
      pattern: "BIND_LOCK\\.set"
    - from: "tests/lan_only_relay_mode.rs"
      to: "iroh::Endpoint::builder().relay_mode().bind()"
      via: "Tier B 自动断言 RelayMode → addr().addrs 行为"
      pattern: "TransportAddr::Relay"
---

<objective>
在 `uc-infra::network::iroh::node.rs` 给 `IrohNodeBuilder::bind` 添加 OnceCell 单进程守护，结构性阻断 Pitfall 3"运行时热切换"诱惑（任何尝试同进程二次 bind 都 panic）；并新增 `uc-infra/tests/lan_only_relay_mode.rs` integration test，自动验证 RelayMode 翻译路径在 endpoint bind 后 `addr().addrs` 候选集是否含 Relay 项（Tier B 自动化覆盖 D-C1）。

Purpose: NETSET-03 的"iroh endpoint 以 RelayMode::Disabled bind"是行为契约 — 单测只能断"配置传递正确"，必须有 integration test 断"endpoint bind 后真实候选集行为"（Pitfall 8）。同时 OnceCell 是 Pitfall 3 结构性防御 —— 任何 PR 试图加运行时热切换会立刻 panic 让测试 fail，编译期 + 运行时双重护栏。

Output:
- `node.rs` 加 `static BIND_LOCK: OnceLock<()>` + `IrohNodeBuilder::bind` 入口断言守护
- `tests/lan_only_relay_mode.rs`（新建）—— 2 个 integration test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md
@.planning/phases/094-backend-network-allow-relay-fallback/094-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md

# 受影响代码
@src-tauri/crates/uc-infra/src/network/iroh/node.rs
@src-tauri/crates/uc-infra/tests/iroh_presence_probe.rs

<interfaces>
<!-- 现有 IrohNodeBuilder::bind + iroh test fixture -->

`IrohNodeBuilder::bind`（`uc-infra/src/network/iroh/node.rs:362-411`）：
```rust
#[instrument(skip_all)]
pub async fn bind(
    identity_store: &IrohIdentityStore,
    config: IrohNodeConfig,
) -> Result<Self, IrohNodeError> {
    let secret = identity_store.ensure_secret_key()?;
    let relay_mode = if config.disable_relays {
        RelayMode::Disabled
    } else {
        RelayMode::Default
    };
    let endpoint = Endpoint::builder(presets::N0)
        .secret_key(secret)
        .alpns(vec![PAIRING_ALPN.to_vec()])
        .relay_mode(relay_mode)
        // ... 其它配置 ...
        .bind()
        .await
        .map_err(|err| IrohNodeError::Bind(err.to_string()))?;
    // ...
}
```

现有 loopback bind helper（`uc-infra/tests/iroh_presence_probe.rs:17-29`，沿用模式）：
```rust
async fn bind_endpoint() -> Endpoint {
    Endpoint::builder(iroh::endpoint::presets::N0)
        .alpns(vec![PROBE_ALPN.to_vec()])
        .relay_mode(RelayMode::Disabled)
        .bind()
        .await
        .expect("bind endpoint")
}
```

现有 wait helper（`iroh_presence_probe.rs:34-42`）：
```rust
async fn wait_for_direct_addrs(endpoint: &Endpoint) {
    for _ in 0..100 {
        if !endpoint.addr().addrs.is_empty() {
            return;
        }
        tokio::time::sleep(Duration::from_millis(10)).await;
    }
    panic!("endpoint never published direct addresses");
}
```

`uc-bootstrap/tests/slice*_e2e.rs` 三个 integration test 当前用 `IrohNodeConfig { disable_relays: true, .. }`（loopback only）；OnceCell 守护后必须串行（cargo test 默认并行但每个 #[test] 在独立进程中 — Rust 测试默认每个 binary 一个进程；同 binary 内多 #[test] 共享进程，同 binary 多次调 IrohNodeBuilder::bind 会触发 OnceCell panic）。

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IrohNodeBuilder::bind 加 OnceCell 单进程守护（Pitfall 3 结构性防御）</name>
  <files>src-tauri/crates/uc-infra/src/network/iroh/node.rs</files>

  <read_first>
    1. `src-tauri/crates/uc-infra/src/network/iroh/node.rs:140-411`（必读 IrohNodeConfig + IrohNodeBuilder::bind 完整结构）
    2. `src-tauri/crates/uc-infra/src/network/iroh/node.rs:775,797,804,851,912,968`（**【checker BLOCKER 2 锚点】** uc-infra 自身测试 binary 内已有 ≥6 处 `IrohNodeBuilder::bind` 调用 — 这是分支 B 的硬证据）
    3. `src-tauri/crates/uc-bootstrap/tests/slice2_phase2_clipboard_e2e.rs:371`（**【checker BLOCKER 2 锚点】** uc-bootstrap e2e 同 binary 多次 bind 调用 — 第二个分支 B 证据）
    4. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<code_context>` 关于 `OnceCell / tokio::sync::OnceCell` 段（Rust 标准实现已在 lockfile，间接通过 once_cell crate；无新依赖）
    5. `.planning/research/PITFALLS.md` Pitfall 3（运行时热切换诱惑 — `IrohNodeBuilder::bind` 强制 OnceCell 只能跑一次）
  </read_first>

  <action>
**【checker BLOCKER 2】** 由于已在 `<read_first>` 锚定的 grep 结果显示 ≥6 处同 binary `IrohNodeBuilder::bind` 调用（uc-infra/src/network/iroh/node.rs:775/797/804/851/912/968 + uc-bootstrap/tests/slice2_phase2_clipboard_e2e.rs:371），本 task **直接采用分支 B**（`#[cfg(not(test))]` 条件守护），删除"executor 必须 grep 决策"的伪选择。

**Step 1：在 `node.rs` 顶部 import 段添加 `use std::sync::OnceLock;`**（如已 import 则跳过；项目 stdlib 即可，无新依赖）。

**Step 2：在 `IrohNodeBuilder` 类型定义上方添加条件守护 static**：
```rust
/// Pitfall 3 结构性防御：进程级单次 bind 守护（**production-only**）。
///
/// `iroh::Endpoint::builder().relay_mode(...).bind()` 完成后 `RelayMode` 被冻结
/// 为 endpoint 的 bind-time 常量；任何 PR 试图实现"运行时热切换 LAN-only Mode"
/// 必须经过 `endpoint.close() + 重新 IrohNodeBuilder::bind`，第二次 `set` 会 panic
/// 让 production daemon 启动失败 / panic 进程级可见。
///
/// 双契约（**checker BLOCKER 2**）：
/// 1. **Production build (`cfg(not(test))`)** — OnceCell 守护激活，进程级 single-shot；
/// 2. **Test build (`cfg(test)`)** — 守护 elided（不编译），允许同 binary 内多次 bind
///    支持现有 ≥9 处测试 binding 调用（包括 uc-bootstrap pairing e2e 的 sponsor+joiner 双 endpoint）。
///
/// 跨契约的 single-bind 保证由 `uc-bootstrap` 单 entrypoint（`builders.rs:178` /
/// `non_gui_runtime.rs:280` — 详见 plan 05）承担。**这是固有 CI 盲点：单元测试不能
/// 覆盖 production 守护**；任何修改本守护的 PR 必须用手工 production-build 验证。
///
/// 见：`.planning/research/PITFALLS.md` §Pitfall 3 + 094-PLAN-06 must_haves.truths。
#[cfg(not(test))]
static BIND_LOCK: OnceLock<()> = OnceLock::new();
```

**Step 3：在 `IrohNodeBuilder::bind` 方法体最起始（`let secret = ...` 之前）添加条件守护**：
```rust
    pub async fn bind(
        identity_store: &IrohIdentityStore,
        config: IrohNodeConfig,
    ) -> Result<Self, IrohNodeError> {
        // Pitfall 3 防御（production-only — checker BLOCKER 2 双契约）：
        // 单进程只允许 bind 一次。第二次调用 panic 阻断任何"运行时重建
        // endpoint"路径，迫使运行时热切换走独立 phase 立项。
        // test 配置下 elided —— uc-infra/uc-bootstrap 测试 binary 内 ≥9 处 bind
        // 调用必须正常工作。
        #[cfg(not(test))]
        BIND_LOCK
            .set(())
            .expect("IrohNodeBuilder::bind called more than once in the same process — runtime hot-swap of LAN-only Mode is explicitly out of scope (Phase 94 / Pitfall 3); see .planning/research/PITFALLS.md");

        let secret = identity_store.ensure_secret_key()?;
        // ... 其余代码不动
```

**注意（双契约权衡）：**
- production daemon 单 entrypoint，OnceCell 守护对真实代码路径生效
- test 配置下守护被 elided，e2e 测试可继续多 endpoint loopback —— 不破坏现有 ≥9 处 binding 调用（含 uc-infra `node::tests` 模块 + uc-bootstrap pairing/clipboard e2e）
- "运行时热切换"提议 PR 在 production build 上立刻 panic，启动期 user 可见

**禁止动作：**
- ❌ 禁止用 `Mutex<bool>`/`AtomicBool` 替代 OnceCell（前者允许 reset，OnceCell 是 single-shot 不可逆）
- ❌ 禁止把 BIND_LOCK 暴露为 `pub static`（uc-infra/AGENTS.md §4.2 — 技术细节向下收敛）
- ❌ 禁止给 `IrohNodeBuilder` 加 `pub fn reset_bind_lock_for_testing()` 后门（违背 Pitfall 3 防御原则）
- ❌ 禁止用 `tokio::sync::OnceCell`（异步版）— `OnceLock` (std) 同步语义足够，且不会引入额外依赖
- ❌ 禁止把守护改成无 `#[cfg(not(test))]` 条件（会 break 9+ 处测试 binding 调用 — 已锁定证据）
- ❌ 禁止把测试 binary 改造成"测试一次 bind"（不允许 — 现有 ≥9 处 binding 是必要的，pairing e2e 必须双 endpoint loopback）
  </action>

  <verify>
    <automated>cargo check -p uc-infra 2>&amp;1 | tail -10</automated>
    <automated>cargo build -p uc-infra 2>&amp;1 | tail -10</automated>
    <!-- 【checker BLOCKER 2】实际会触发 9+ 处 bind 的 binary 必须显式跑过 -->
    <automated>cargo test -p uc-infra --lib network::iroh::node 2>&amp;1 | tail -20</automated>
    <automated>cargo test -p uc-bootstrap --test slice2_phase2_clipboard_e2e 2>&amp;1 | tail -20</automated>
    <automated>cargo test -p uc-bootstrap --test slice1_handshake_e2e 2>&amp;1 | tail -20</automated>
    <automated>cargo test -p uc-bootstrap --test slice2_phase1_presence_e2e 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check -p uc-infra` 退出码 0
    - `cargo build -p uc-infra` 退出码 0
    - **【checker BLOCKER 2】** `cargo test -p uc-infra --lib network::iroh::node` 退出码 0（uc-infra 模块内置 4+ 个 bind 测试在分支 B 下必须正常 elide 守护）
    - **【checker BLOCKER 2】** `cargo test -p uc-bootstrap --test slice2_phase2_clipboard_e2e` 退出码 0（这是包含 line 371 binding 的 e2e binary，必须不被守护 break）
    - **【checker BLOCKER 2】** `cargo test -p uc-bootstrap --test slice1_handshake_e2e` 退出码 0
    - **【checker BLOCKER 2】** `cargo test -p uc-bootstrap --test slice2_phase1_presence_e2e` 退出码 0
    - `rg -n 'BIND_LOCK|OnceLock' src-tauri/crates/uc-infra/src/network/iroh/node.rs` 命中 ≥ 2 行（static 声明 + bind() 守护）
    - **【checker BLOCKER 2】** `rg -n '#\[cfg\(not\(test\)\)\]' src-tauri/crates/uc-infra/src/network/iroh/node.rs | rg -c 'BIND_LOCK|cfg\(not'` ≥ 2（条件守护成对出现：static 声明 + bind() 入口）
    - `rg -n '^pub static BIND_LOCK|pub fn reset_bind_lock' src-tauri/crates/uc-infra/` 返回 0 行（不对外暴露）
    - **【checker BLOCKER 2】** commit message 显式说明：选分支 B；理由 = `<read_first>` 锚定的 9+ 处测试 binary bind 调用；CI 盲点已记录在 must_haves
  </acceptance_criteria>

  <done>
`IrohNodeBuilder::bind` OnceCell 守护就位（A 或 B 分支）；现有 e2e 测试未被 break；Pitfall 3 结构性防御生效（production 构建任何二次 bind 即 panic）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新建 uc-infra/tests/lan_only_relay_mode.rs integration test（Tier B — D-C1 两组用例）</name>
  <files>src-tauri/crates/uc-infra/tests/lan_only_relay_mode.rs</files>

  <read_first>
    1. `src-tauri/crates/uc-infra/tests/iroh_presence_probe.rs`（必读完整文件，重点 line 1-42 — bind helper + wait_for_direct_addrs 模式）
    2. `src-tauri/crates/uc-infra/Cargo.toml`（确认 `iroh` 在 dev-dependencies / workspace dependencies；`tokio` test-util 也在）
    3. `.planning/phases/094-backend-network-allow-relay-fallback/094-CONTEXT.md` `<decisions>` D-C1（两组用例覆盖 Disabled / Default）+ PATTERNS.md §11 critical finding 3（反向用例 "no panic" 即可，不强断 must contain Relay）
    4. `.planning/research/PITFALLS.md` Pitfall 8（Tier A/B/C 测试分层 — 本 plan 是 Tier B endpoint 状态断言）
  </read_first>

  <behavior>
    - Test 1（D-C1 用例 a — 强不等式）：`Endpoint::builder().relay_mode(RelayMode::Disabled).bind().await` 后 wait_for_addrs，然后 `endpoint.addr().addrs.iter().any(|a| matches!(a, TransportAddr::Relay(_)))` 必须 == false
    - Test 2（D-C1 用例 b — 弱不等式）：`Endpoint::builder().relay_mode(RelayMode::Default).bind().await` 必须不 panic、不抛错，bind 成功；不强断 Relay 候选**必须**存在（CI 环境与 Relay mesh 连通性不可靠 — D-C1 / PATTERNS.md §11 critical finding 3）
  </behavior>

  <action>
新建 `src-tauri/crates/uc-infra/tests/lan_only_relay_mode.rs`，完整代码：

```rust
//! LAN-only Mode bind-time 行为断言（Tier B 自动化覆盖）：
//!
//! 验证 `IrohNodeConfig.disable_relays` 通过 iroh
//! `Endpoint::builder().relay_mode(...).bind()` 路径正确翻译为
//! `RelayMode::Disabled` / `RelayMode::Default`，效果体现在
//! `endpoint.addr().addrs` 是否含 `TransportAddr::Relay(_)` 项。
//!
//! ## 测试分层（Pitfall 8）
//!
//! - **Tier A（unit）**：`uc-bootstrap/src/network_policy.rs` truth-table 单测
//!   覆盖配置传递（plan 05 task 1）
//! - **Tier B（integration — 本文件）**：bind 后断 endpoint 的候选地址中是否
//!   含 Relay 项；强不等式覆盖 Disabled，弱不等式覆盖 Default
//! - **Tier C（manual）**：跨平台 Wireshark / tcpdump 抓包验证（D-C1 锁定为
//!   手工流程，不在本 phase 自动化）
//!
//! ## 反向用例约束（D-C1 / PATTERNS.md §11 critical finding 3）
//!
//! `RelayMode::Default` 不一定立刻发布 Relay 地址（取决于 iroh 与公网 relay
//! mesh 的连通性，CI 环境可能没有公网或被 firewall 限制），所以反向断言
//! 只断"bind 不 panic"这一条**弱**不等式；具体 Relay 候选行为留给 Tier C
//! 抓包验证。
//!
//! 见：`.planning/research/PITFALLS.md` Pitfall 8 + 094-CONTEXT.md D-C1。

use std::time::Duration;

use iroh::{Endpoint, RelayMode, TransportAddr};

const TEST_ALPN: &[u8] = b"uniclipboard/lan-only-test/0";

/// loopback bind helper —— 与 `iroh_presence_probe.rs:17-29` 同模式。
async fn bind_with_relay_mode(mode: RelayMode) -> Endpoint {
    Endpoint::builder(iroh::endpoint::presets::N0)
        .alpns(vec![TEST_ALPN.to_vec()])
        .relay_mode(mode)
        .bind()
        .await
        .expect("bind endpoint")
}

/// 等候 endpoint 发布候选地址（loopback 通常 < 100ms；与 sibling fixture 同
/// 重试策略）。**不 panic** —— 即使没有任何候选也允许后续断言；本 helper 只
/// 是给 magicsock 一点时间枚举接口。
async fn wait_for_addrs(endpoint: &Endpoint) {
    for _ in 0..100 {
        if !endpoint.addr().addrs.is_empty() {
            return;
        }
        tokio::time::sleep(Duration::from_millis(10)).await;
    }
}

/// D-C1 用例 (a) — 强不等式：`RelayMode::Disabled` bind 后候选地址中
/// **不应**含 `TransportAddr::Relay` 项。这是 LAN-only Mode 在 endpoint 层面
/// 的可观察事实（与 ROADMAP success criterion #1 对齐）。
#[tokio::test]
async fn relay_disabled_publishes_no_relay_addrs() {
    let endpoint = bind_with_relay_mode(RelayMode::Disabled).await;
    wait_for_addrs(&endpoint).await;

    let addrs = endpoint.addr().addrs.clone();
    let has_relay = addrs.iter().any(|a| matches!(a, TransportAddr::Relay(_)));

    assert!(
        !has_relay,
        "RelayMode::Disabled MUST NOT publish Relay addrs; got: {:?}",
        addrs
    );

    endpoint.close().await;
}

/// D-C1 用例 (b) — 弱不等式：`RelayMode::Default` bind 不应 panic / 抛错。
/// 不强断"必须含 Relay 候选"（CI 环境 / Relay mesh 不可靠）；这是 PATTERNS.md
/// §11 critical finding 3 与 D-C1 共同确认的 Tier B 边界。
///
/// 真正"Relay 路径活跃"的验证留给 Tier C 手工抓包（D-C1）。
#[tokio::test]
async fn relay_default_binds_without_panic() {
    let endpoint = bind_with_relay_mode(RelayMode::Default).await;
    wait_for_addrs(&endpoint).await;

    // 【checker WARNING 4】显式探一下 addr().addrs 字段以确认 endpoint 处于
    // 可读状态（不 panic 即可），但**不**对内容做断言 —— 这就是 Tier B 弱不
    // 等式的设计：bind 成功 + endpoint 状态可读 = 通过；具体 Relay/Direct
    // 候选行为留给 Tier C 抓包（D-C1）。
    let _addrs = endpoint.addr().addrs;

    endpoint.close().await;
}
```

**Cargo.toml 检查：** 确认 `iroh` 与 `tokio` 在 `uc-infra/Cargo.toml` 的 `[dev-dependencies]` 中可用 —— 若 grep 不到则**先复制**现有 `tests/iroh_presence_probe.rs` 用法相同的 dev-deps 配置。预期不需要新增 dep（`uc-infra` 本身就依赖 iroh）。

**禁止动作：**
- ❌ 禁止复用 `IrohNodeConfig { disable_relays: true, .. }` 形式的 production-flavored config（直接用 iroh API 减少 stub）
- ❌ 禁止断"`RelayMode::Default` MUST contain Relay"（CI 不可靠 — D-C1 / PATTERNS.md §11 critical finding 3）
- ❌ 禁止设置全局 mock relay server（Tier C 是手工抓包覆盖）
- ❌ 禁止把测试名改为 "test_lan_only_toggle"/"test_relay_mode" 等不描述行为的命名
  </action>

  <verify>
    <automated>cargo build -p uc-infra --tests 2>&amp;1 | tail -10</automated>
    <automated>cargo test -p uc-infra --test lan_only_relay_mode -- --nocapture 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `cargo build -p uc-infra --tests` 退出码 0
    - `cargo test -p uc-infra --test lan_only_relay_mode` 2 个测试均过：
      - `relay_disabled_publishes_no_relay_addrs`
      - `relay_default_binds_without_panic`
    - 文件 `src-tauri/crates/uc-infra/tests/lan_only_relay_mode.rs` 存在
    - `rg -n 'TransportAddr::Relay' src-tauri/crates/uc-infra/tests/lan_only_relay_mode.rs` 命中 1 处
    - `rg -n 'IrohNodeConfig' src-tauri/crates/uc-infra/tests/lan_only_relay_mode.rs` 返回 0（直接用 iroh API，不复用 production stub）
    - `rg -n 'must contain.*Relay|MUST contain Relay|relay_default.*assert' src-tauri/crates/uc-infra/tests/lan_only_relay_mode.rs | rg -v 'MUST NOT'` 返回 0（反向用例不强断"必须含 Relay"）
  </acceptance_criteria>

  <done>
Tier B integration test 就位，2 case 自动断言 Disabled / Default 行为。NETSET-03 success criterion #1（强不等式 — disable=true 时 endpoint 不发布 Relay 候选）已自动覆盖。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `IrohNodeBuilder::bind` 入口 → 进程级 OnceCell | 进程内任何重 bind 尝试跨"应用代码 → 进程单例守护"边界 |
| iroh 公开 API → integration test | Tier B 通过 iroh 的稳定公共 API（`Endpoint::addr().addrs`）做白盒断言 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094.06-01 | Tampering（运行时热切换 PR） | 开发者尝试加 `endpoint.close() + IrohNodeBuilder::bind` 第二次 | mitigate | OnceCell BIND_LOCK 第二次 set 立即 panic（Pitfall 3 结构性防御） |
| T-094.06-02 | Denial of Service | OnceCell 与现有 e2e 测试冲突致 CI 全红 | mitigate | grep 验证现有 bind 调用模式；如同 binary 多次 bind 走 `#[cfg(not(test))]` 分支 B；production 守护不变 |
| T-094.06-03 | Tampering（绕过守护） | PR 加 `pub fn reset_bind_lock_for_testing()` 后门 | mitigate | 显式禁止 + grep 守护 `rg 'reset_bind_lock' src-tauri/` 返回 0 |
| T-094.06-04 | Information Disclosure | bind panic message 泄露内部状态 | accept | panic message 提示 "runtime hot-swap... explicitly out of scope" — 是引导信号而非泄露 |
| T-094.06-05 | Spoofing（测试假装通过） | integration test 用 `assert!(true)` 等假断言 | mitigate | `relay_disabled_publishes_no_relay_addrs` 是强不等式（has_relay == false 才 pass）；CI fail 即捕获回归 |
| T-094.06-06 | Repudiation（CI 通过但 production 失效） | OnceCell 在 test 配置下被 elided 导致测试盲点 | accept | **【checker BLOCKER 2】** 此 CI 盲点已**显式上浮**到 plan must_haves.truths（不再仅藏在此 STRIDE 表），让任何 reviewer 在审 plan 时第一眼看到。补偿措施：(1) `uc-bootstrap` 单 entrypoint 由 `builders.rs:178` / `non_gui_runtime.rs:280`（plan 05）单一调用站点保证 production single-bind；(2) 任何二次 bind 修改 PR 在 production build 立刻 panic（启动期 user 可见） |
| T-094.06-07 | Elevation of Privilege | uc-infra 暴露 BIND_LOCK 给上层 | mitigate | `static` 私有声明（无 `pub`）— uc-infra/AGENTS.md §4.2 技术细节向下收敛 |

无 `high` 严重等级威胁。
</threat_model>

<verification>
- `cargo check -p uc-infra` + `cargo build -p uc-infra --tests` 全过
- `cargo test -p uc-infra --test lan_only_relay_mode` 2 个测试全过（< 5s 完成）
- 现有 e2e 测试至少 2 个未被 break：
  - `cargo test -p uc-bootstrap --test slice1_handshake_e2e`
  - `cargo test -p uc-bootstrap --test slice2_phase1_presence_e2e`
- grep 守护：
  - `rg -n 'BIND_LOCK' src-tauri/crates/uc-infra/src/` 命中 ≥ 2（声明 + 守护）
  - `rg -n 'pub static BIND_LOCK|pub fn reset_bind_lock' src-tauri/` 返回 0（私有 + 无后门）
  - `rg -n 'IrohNodeBuilder::bind' src-tauri/ --type rust` 命中数应 ≤ 现有数 + 1（本 plan 不新增其它 bind 调用站点）
</verification>

<success_criteria>
- [ ] **【checker BLOCKER 2】** `IrohNodeBuilder::bind` 入口加 OnceCell BIND_LOCK 守护，**直接选分支 B（`#[cfg(not(test))]`）** —— 锚定证据：uc-infra 测试 binary ≥6 处 + uc-bootstrap clipboard e2e line 371 调用
- [ ] **【checker BLOCKER 2 — 双契约】** Production build 守护激活（user 可见）；test build 守护 elided（不破坏 9+ 处测试 binding）
- [ ] **【checker BLOCKER 2 — CI 盲点】** must_haves.truths 显式记录"production 守护无法用 #[test] 覆盖（cfg-elided）"
- [ ] BIND_LOCK 是 `static`（不对外暴露）、不依赖 `tokio::sync::OnceCell`（用 `std::sync::OnceLock`）
- [ ] panic message 包含 "Pitfall 3" 或 "runtime hot-swap" 或类似引导
- [ ] 新建 `uc-infra/tests/lan_only_relay_mode.rs`，2 case 覆盖 D-C1（Disabled 强不等式 + Default 弱不等式 — "no panic"）
- [ ] **【checker WARNING 4】** Default 用例显式 `let _addrs = endpoint.addr().addrs;` + 设计意图注释
- [ ] integration test 直接用 iroh API（不复用 IrohNodeConfig）
- [ ] **【checker BLOCKER 2】** 4 个 cargo test command 全过：`uc-infra --lib network::iroh::node` + `uc-bootstrap --test slice2_phase2_clipboard_e2e` + 另外 2 个 slice e2e
- [ ] commit message 说明分支 B 选择理由（基于已知 grep 锚点）
- [ ] `cargo check -p uc-infra` 全过
- [ ] **决策 D-C1 / Pitfall 3 / Pitfall 8 防御实施已写入 plan must_haves.truths**
</success_criteria>

<output>
After completion, create `.planning/phases/094-backend-network-allow-relay-fallback/094-06-SUMMARY.md` with:
- 实际改动的 LOC（node.rs 加 ~10 行 + tests/lan_only_relay_mode.rs 全新建 ~80 行）
- 选择了分支 A 还是 B 的最终决定（基于 grep 现有 e2e bind 调用次数）
- 现有 slice*_e2e.rs 测试 pass 矩阵（哪些过 / 哪些 break / 修复方式）
- 2 个 lan_only_relay_mode.rs 测试 pass 输出
- D-C1 用例 (b) "no panic" 反向用例的 rationale 写入 plan：CI 不可靠 / Tier C 手工
- Pitfall 3 / Pitfall 8 防御自查：grep `pub static BIND_LOCK|pub fn reset_bind_lock` 全工程返回 0
- 给整 phase 验收的"endpoint bind 时 RelayMode::Disabled → addr().addrs 不含 Relay"自动断言信号
</output>
